# 角色需求

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `organize` 依 schema 填寫；檔案存在不代表這一層已經有東西。
> `organize` 整理大綱時同步維護；要單獨設計某個角色走 `character`。


（依 `結構定義/角色需求.schema.md` 填寫。每個主角一個 section。）

## （主角名）
- 期盼（動力）：
- 想要（誤導）：
- 落差（阻礙）：
- 需要（圓滿）：
- 預期弧線：盲目 → 挫折 → 醒悟 → 滿足

## 下游回饋記錄
| 日期 | 來源 | 對象角色 | 發現 | 建議回寫 | 狀態 |
|------|------|----------|------|----------|------|
