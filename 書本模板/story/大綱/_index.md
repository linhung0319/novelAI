# 大綱索引（局部下沉）

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `organize` 局部模式依 `結構定義/大綱.schema.md` 填寫；檔案存在不代表已經有下沉的 arc。
> **一列只寫 `arcNN：名稱 —— 狀態`**（`outline-lint` 第 3 項守視圖 ≡ 資料夾＋列序遞增，單列行長由成長哨兵的 400 管）。沿革、逐題發落紀錄、回填條件的歷史一律走 `story/參照/裁決流.md`，不進列。
> **已併入的退役源住 `_已併入/`**（`git mv`，見 schema「收斂規則」）——它們的列**留在本檔**、狀態改成「已併入」。

- arc01：＜arc 名稱＞ —— 狀態：暫定，待粗層鎖定
