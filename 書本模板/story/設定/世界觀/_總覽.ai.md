# 世界觀總覽（衍生 rollup）

> ⚠️ **尚未產出**——本檔是 AI管·rollup 衍生檔，由 `develop`／`worldbuild` 從本資料夾所有 `<主題>.md` 源檔重生，重生後 `tools/derived_sync stamp _總覽.ai.md` 封章。**作者不手改**。
> **怎麼新增設定**：在本資料夾（`story/設定/世界觀/`）新增一個 `<主題>.md`（魔法體系／組織／時間結構…；仙俠即功法/招式），自由手寫；skill 會生成同名 `<主題>.ai.md`（限制與代價／影響力／自洽）並重生本總覽。
> 完整格式見 `結構定義/世界觀.schema.md`。尚未封章時 `derived-sync check` 會報 `unstamped`、`world-lint` 會跳過本檔的內容比對——正常。
> **下面三節只放指標，不放內容**（2026-07-27 起）：矛盾住 `story/參照/待裁決.md`、硬約束住 `story/物件/<名>.md`、裁決理由與已駁回方案住 `story/參照/裁決流.md`。

## 一句話定位

## 核心規則索引
| 主題 | 主從 | 影響力 | 帶升格哨兵 | 檔 |
|------|------|--------|-----------|-----|

（`主題`／`檔` 兩欄由 `world-lint` 驗 ≡ 本資料夾的源檔；後三欄是 AI 重生時重讀各檔 `## 影響力` 的判斷，**無機械核對者**。）

## 背景維度盤點
| 維度 | 內容（指向源檔的節名，不複述散文） | 狀態 |
|------|--------------------------------|------|
| 時間 |  | 空白 |
| 空間 |  | 空白 |
| 活動 |  | 空白 |
| 地點 |  | 空白 |
| 組織 |  | 空白 |
| 社會 |  | 空白 |
| 自身 |  | 空白 |

（封閉七維，`world-lint` 驗。空白不必硬填，**但那一列要在**、狀態寫「空白」。）

## 待確認／潛在矛盾
- 0 筆·見 `story/參照/待裁決.md`（N 取自 `decision-project` 的覆蓋率行，**0 也印**）

## 升格哨兵彙總
- 0 條硬約束·見 `story/物件/`（逐條只寫〈約束名〉→ 哪支物件檔，不複述內容）

## 素材出處
- （`raw/` 檔名清單＋web 出處註記。**輪次裁決紀錄與已駁回方案走 `story/參照/裁決流.md`**）
