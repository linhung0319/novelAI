# 風格

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `develop` 陪你拍板後填寫；檔案存在不代表這一層已經有東西。
> **這是「源」檔（人管，無格式要求）**：自由寫「這本書該用什麼腔調寫」。**腔調・時代文化色・句式偏好・修辭傾向的散文全部住這裡**——衍生檔 `風格.ai.md` 只有 front-matter 五欄（禁用詞表／稱謂系統／可用句式／語域／基調參照），不重述本檔的散文。
> 全書單一檔起步；多世界並存時可日後拆 `風格/主世界.md`、`風格/異界.md`。

（依 `結構定義/風格.schema.md` 源檔部分填寫。想到什麼寫什麼：）

- 基調（承接 `00-摘要.md`）：
- 腔調／敘述態度：
- 時代・文化色（哪些詞會破壞氛圍、該禁用；**「某類詞在某語域下才禁」這種要語意判斷的判準寫在這裡**，不進衍生檔的 `禁用詞表`——那張表只放可字面掃的詞）：
- 稱謂習慣（人物怎麼互稱）：
- 句式偏好（常態段 vs 高潮段）：
- 修辭傾向（文學向才需要；純網文向可略）：

## 語感範例參照（供 `write` 對腔，選填）

（從 `raw/` 挑兩三段最能代表本書聲音的場景摘下來，各標明對應哪個 `可用句式` 檔位。
 **挑哪幾段本身是創作決定，所以住源檔**——留在衍生檔會讓那支檔有兩個源，而
 `derived-sync stamp` 的 digest 只吃得到 `風格.md` 一個，raw 改了 `check` 報 fresh
 且報得沒錯。見 `風格.schema.md`「三個搬移動作」。）
