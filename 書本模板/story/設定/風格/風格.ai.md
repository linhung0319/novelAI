# 風格（衍生）

> ⚠️ **尚未產出**——本檔是 AI管·衍生檔，由 `develop` 從源 `風格.md` 重生，重生後 `tools/derived_sync stamp 風格.ai.md` 封章。**作者不手改**；改腔調去改源 `風格.md` 再重生。
> **本檔只有 front-matter，沒有本體**（2026-07-27 功能 07）：五個機讀欄 `禁用詞表`／`稱謂系統`／`可用句式`／`語域`／`基調參照`，**不開任何 `##` 節**——腔調・時代文化色・句式節奏・修辭天花板的散文住源 `風格.md`，在這裡再寫一遍只會變成同義改寫的漂移面（實測 1.02×、8-gram 重疊 30%，而那份漂移沒有任何人在守）。`derived-sync validate` 的節枚舉對本檔是**空 tuple**：開節就報。
> 五欄的值**可不可核對**由 `style-lint --book <書>` 守（六項），`develop` 落檔前必跑；`可用句式` 被報「無顆粒度 token／無數字／全落最粗一級」時**停下回問作者、不得封章**。完整格式見 `結構定義/風格.schema.md`。
> 尚未封章時 `derived-sync check` 會報 `unstamped`——正常。
