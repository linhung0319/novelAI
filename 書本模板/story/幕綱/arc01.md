# arc01

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `beat-sheet` 依 schema 填寫；檔案存在不代表這一層已經有東西。
> 大綱就緒後才拆幕。
> （`beat-lint` 認得上面這個「尚未產出」標記並跳過本檔，覆蓋率行會印「跳過 N 支骨架」。）


（依 `結構定義/幕綱.schema.md` 填寫。**分區是硬規定**：承諾區 → 各幕 → 伏筆狀態表（選用）→ 檔尾設計註。
每幕一個 `## 幕NNN · 幕名` section，含固定八欄位；連結語法見 schema「連結規範」。範例：）

## 本 arc 承諾

- 節奏檔位：
- 統一母題：（一行，選填）
- 必須發生：
  - （各指向幕號）
- 不得發生：
  - （排除線＝負向承諾，與正向同等拘束力。`fact-project --for-beat` 會一起讀給 `write`）

## 幕001 · （幕名）
- 角色：
- 時空：
- 行動：
- 衝突：
- 結果：
- 前因：（首幕可為「—」；否則依 schema「連結規範」填指向前因幕的連結）
- 伏筆：（無則「—」；否則依 schema「連結規範」填埋／收標記）
- 結構階段：

## 本 arc 伏筆狀態

（選用。埋設點與回收點不在同一個 arc 時才需要，見 schema「本 arc 伏筆狀態」。
這張表是幕內標記的**視圖**，四欄一律只寫伏筆名純文字。）

| 伏筆 | 埋設幕 | 收回 | 備註 |
|------|--------|------|------|

---

## 設計註（下游不抄·暫存區）

（本輪還沒搬走的「為什麼」：母題論證／判例／本輪拍板記錄／降回閥推導。
**落檔時搬進 `story/參照/裁決流.md`**，見 schema「設計註要搬走」——
那支檔不在的話 `beat-lint` 會報，別讓箭頭指向空氣。）
