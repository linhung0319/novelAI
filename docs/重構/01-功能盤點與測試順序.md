# 重構盤點：功能清單與測試順序

> **這是什麼**：把系統拆成 15 個**功能**（不是 15 支檔），逐一用 `結構定義/設計原則.md` 的六問＋原則 A–G 檢測，產出診斷報告；作者拍板後才重構。
> **怎麼跑**：每個功能開一個新 session，貼 `03-每輪-prompt.md` 的 prompt。每個功能兩輪：**診斷輪**（只提案不動手）→ 作者拍板 → **重構輪**（動手）。
> **為什麼一功能一 session**：context 集中，且診斷報告本身就是跨 session 的交接物。

---

## 進度表

狀態：`未開始` → `已診斷`（報告已產出，待作者拍板） → `已拍板` → `已重構`

| # | 功能 | 狀態 | 報告 |
|---|------|------|------|
| 01 | 狀態物件軸（事實流：狀態／錨／約束／伏筆／水下） | **已重構** | [功能報告/01-狀態物件軸.md](功能報告/01-狀態物件軸.md) |
| 02 | 幕綱 | **已重構** | [功能報告/02-幕綱.md](功能報告/02-幕綱.md) |
| 03 | 正文章節 | **已重構** | [功能報告/03-正文章節.md](功能報告/03-正文章節.md) |
| 04 | 裁決與回饋 | **已重構** | [功能報告/04-裁決與回饋.md](功能報告/04-裁決與回饋.md) |
| 05 | 設定層·世界觀 | **已重構** | [功能報告/05-設定層-世界觀.md](功能報告/05-設定層-世界觀.md) |
| 06 | 設定層·角色 | **已重構** | [功能報告/06-設定層-角色.md](功能報告/06-設定層-角色.md) |
| 07 | 設定層·風格 | **已重構** | [功能報告/07-設定層-風格.md](功能報告/07-設定層-風格.md) |
| 08 | 摘要 | **已重構** | [功能報告/08-摘要.md](功能報告/08-摘要.md) |
| 09 | 大綱 | **已重構** | [功能報告/09-大綱.md](功能報告/09-大綱.md) |
| 10 | 就緒儀表 | **已重構** | [功能報告/10-就緒儀表.md](功能報告/10-就緒儀表.md) |
| 11 | 結構對應表 | 未開始 | |
| 12 | rollup 索引（`_index` / `_總覽`） | 未開始 | |
| 13 | raw 分揀 | 未開始 | |
| 14 | 檢查器與工具鏈 | 未開始 | |
| 15 | skill 的 context 取用契約 | 未開始 | |

---

## 順序的依據

不是隨意排的，是**依賴驅動**：

- **01 先跑**，因為原則 G 全部落在它身上，而且它一旦定案，會連帶決定 04（裁決／約束落點）、02 的伏筆軌、03 的錨、以及 `🧊 水下` 標記——**一個功能定案，四個功能跟著定案**。
- **02、03 次之**，它們是 01 的兩條時間軌（計畫軌／已成軌）的來源，且是目前已知違反最嚴重的兩處。
- **05–09 是設定與劇情層**，多數已經符合 A（前向：散文源＋衍生），預期是快速驗證而非大改。
- **10–12 是綜合視圖**，必須等前面的投影輸出定案才知道它們該不該存在。
- **14 倒數第二**：檢查器要等格式定案才設計得出來。**先寫檢查器＝把還會變的格式釘死。**
- **15 最後**：它是前面所有東西的消費端。

---

## 各功能的現行落點與已知疑點

> 「已知疑點」是 2026-07-26／27 對話中已經確認的事，寫在這裡是為了**讓新 session 不必重新發現**。它們是線索，不是結論——診斷輪仍要自己驗證。

> 下面標「**（← 01 診斷輪順手記下）**」的是跑功能 01 時撞到、但屬本功能的疑點，未經本功能自己驗證。

### 01 · 狀態物件軸　**已重構（2026-07-27）**

**現行落點**：`結構定義/物件.schema.md` ＋ `story/物件/<名>.md`（物件定義／約束／揭示層級）／`結構定義/事實流.schema.md` ＋ `chapters/chNNNN.ai.md` 的 `## 本章事實`（狀態／錨）／幕綱裡的 `埋|收[[伏筆:x]]`／`tools/fact_projection/`（`fact-project`／`fact-lint`／**`object-lint`**／`fact-refs`）／`tools/foreshadow_project/`

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/01-狀態物件軸.md](功能報告/01-狀態物件軸.md) §八「重構實況」。

**刻意留給後續的**（不是遺漏）：
- **G3 的「投影按型別 fold」**：物件檔的 `型別` 欄目前裝的是**種類**（封閉七型），不是折法（集合／純量／區間／多態）。把 delta 的 6 維改成型別參數要等**有真實物件檔**之後再做，否則是憑空設計型別系統 → 本功能的下一輪。
- **G5 兩條時間軌 diff**：依賴 02 的幕綱衍生層。
- **完整的 C3**：物件檔名這一半有 registry 了（檔名天生唯一），**幕綱的伏筆名那一半仍是自由造字** → 02。目前的補償是 `object-lint` 的近似名偵測。
- **物化快照**：量過了、未到痛點（fold 259 筆／輸出 4,624 字元）。觸發門檻與駁回理由寫進 `02-待用構想.md` 已駁回清單。
- ~~**`.co.md` 這個檔類**~~ **04 已裁（2026-07-27）**：廢除，裁決流正名 `story/參照/裁決流.md`＋`decision-lint`（依新增的 A4）。
- ~~**書級方針**裝不進物件檔~~ **04 已裁（2026-07-27）**：新增**第八型 `方針`** ＋ 保留檔名 `story/物件/全書.md`（雙向綁定，`object-lint` 守），由 `fact-project --for-beat` 的 `### 全書方針` 節無條件印出（0 條也印）。**「要第八型＝停下來問作者」這條規則第一次被執行。** 注意 `方針` 沒有狀態，G3 的 fold 要單獨處理它。

### 02 · 幕綱　**已重構（2026-07-27）**

**現行落點**：`結構定義/幕綱.schema.md`／`.claude/skills/beat-sheet`、`beat-test`／`story/幕綱/arcNN.md`、`_index.md`／`tools/beat_metrics/`（**`beat-lint`** 格式閘門＋`beat-metrics` 漂移統計）／解析它的另三支工具：`tools/foreshadow_project/scan.py`、`tools/fact_projection/beats.py`、`tools/settings_select/select.py`

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/02-幕綱.md](功能報告/02-幕綱.md) §八「重構實況」。

**核心判斷**：幕綱**維持作者直改的強格式 `.md`**（抉擇 1 A，已駁回「拆成散文源＋衍生機讀層」的 C4 路線——`[[幕NNN]]` 是作者重排劇情要直接操作的結構，不是描述的副產品），改為兌現「強格式的前提是有檢查器」（B1／E1）。

**刻意留給後續的**（不是遺漏）：
- **`_index.md` 的 arc 概覽膨脹**（11 行單行、最長 1,949 字，索引檔在複述全書）：與 §10 就緒儀表、§12 rollup 是**同一個 D1／D3 判斷**（綜合視圖該是投影不是檔），三者一起裁 → 10／12。
- **spine 四份實作收斂成一份**：本輪只統一失效行為（全部硬報錯，修掉 `beat_metrics` 那份靜默退化的假陰性）；要不要維持「工具間零相依」交 14。
- ~~**`.co.md` 依 A1 應廢除**~~ **04 已裁（2026-07-27）**：`.co.md` 這個檔類廢除，正名 `story/參照/裁決流.md`，格式由新的 `decision-lint` 守（依新增的 `設計原則.md` A4）。`beat-lint` 的 `DECISION_LOG` 兩種命名都吃，黃金檔只變 1 行（檔名）。**04 抉擇 3 選 A（理由維持集中），所以「`beat-lint` 的設計註目的地檢查會失去標的」那個風險沒有發生。**
- **`幕尾鉤` 第九欄**：實測 79/108 幕在用而 schema 只定義八欄。`beat-lint` 只印提示，本輪不裁它該不該升格 → 本功能下一輪。
- **arc09／arc10 的 14 個 `## ⚠️` 未定義分區**、arc01–arc10 缺承諾區：`beat-lint` 已報，但**不遷移病例書**。
- **排除線在 `story/大綱/arcNN.md` 也有一份**（arc09/10/11 各 12 條編號），與 arc11 幕綱承諾區的 14 條重複 → 大綱層（06）。**09 已處置大綱那一側**：`## 明確排除／壓著睡的線` 是 schema 枚舉外的節，`outline-lint` 第 1 項會報，schema 明訂它的家是幕綱的 `## 本 arc 承諾`（射程天然綁 arc）。
- **（← 09 重構輪追加）節奏檔位是「兩層各一份」，08 的交接寫得不完整。** 08 抉擇 2 A 刪掉摘要那一份時記的是「這一項的唯一落點就是幕綱那個沒有守衛的檔頭」——**實測不對**：`大綱/arcNN.md` **11/11 支都寫了節奏檔位**（幕綱側 arc02／arc03 沒寫，與 08 的盤點一致）。所以刪掉摘要那份之後是**兩份**，而大綱那份是「大綱層宣告」、幕綱那份是「拆幕時兌現」——它們可能不是複本而是**兩個層級的同名欄位**。**兩層都零 lint，且沒有任何東西比對它們。** 09 **刻意不裁**（它跨兩個功能，在 09 裁等於替 02 拍板；`大綱.schema.md` 的節枚舉也沒有替它開一格）→ 02 的下一輪。
- **（← 09 重構輪追加）`幕綱/arc10.md` 有一節做對了，值得升成通則**：`## ⚠️ 其餘照舊守死的硬約束（**不複述、只指路**·`大綱/arc10.md` 排除線①–⑫）`——788 字元，指路而不抄。對照 `幕綱/arc11.md` 的 `## 本 arc 承諾`（1,351 字元，與大綱側排除線 8-gram 重疊 7.1%／10.5%，是抄了一部分又改了寫法）。**「不複述、只指路」是跨層引用該有的形狀**，02 立的排除線落點判準（抉擇 4 B：留在 arc 檔、靠查詢層合流）少了這一句。**09 已在自己這一側把它機械化**（`outline-lint` 第 11 項：同卷其餘檔提到「卷級方向」的那一行必須帶 `大綱/arcNN.md` 路徑指標）——**那個判準形狀可以直接搬到幕綱側**。
- **G5 兩軌 diff 只覆蓋有標記的那半**：抉擇 2 B 決定不逼「刻意不立 token」的續行補標記，代價是計畫軌有一部分程式看不到。`beat-lint` 覆蓋率行誠實印出「其中 N 列的伏筆名全書無標記」。

### 03 · 正文章節　**已重構（2026-07-27）**

**現行落點**：`結構定義/章節.schema.md`／`.claude/skills/write`、`write-test`、`revise`／`chapters/chNNNN.md`、`chNNNN.ai.md`、`chapters/_index.ai.md`／`tools/beat_metrics/`（**`ch-lint`** 正文層格式閘門，與 `beat-lint` 同套件共用幕號 registry）／`tools/prose_metrics/`（漂移統計＋章密度參考行）／`tools/derived_sync/` 的成長哨兵

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/03-正文章節.md](功能報告/03-正文章節.md) §八「重構實況」。

**核心判斷**：正文層**維持**「一章一檔的散文源＋內嵌 `<!-- 幕NNN -->` 錨點」（C2 已定案，「一幕一檔」已駁回），改為兌現 C2 那句「更細的定位只能內嵌，**且必須配檢查器**」——`<!-- 幕NNN -->` 從「全 repo 唯一零工具解析的機讀標記」變成有閘門。

**刻意留給後續的**（不是遺漏）：
- **`chapters/_index.ai.md` 的形態**（抉擇 2 C）：它是成長型產物（196→781 B/列，4.0×；500 章外推 ≈381 KB）而**沒有投影入口**，依 D3 可能該是 `ch-project` 的輸出而不是一支檔。與 §10 就緒儀表、§12 rollup 是**同一個判斷**，三者一起裁 → 10／12。本輪只做「清掉備註欄三類內容＋納入哨兵＋在 `共同約定.md` 八的查詢入口表**登記成欠債**」。
- **`POV`／`風格`／`狀態`「重生前後未變」無人守**（抉擇 4 A 的已知缺口）：那是跨時間斷言，系統不保存「上一版」。`ch-lint` 只守得住「存在＋枚舉＋與 `_index` 視圖一致」，其餘靠 `write`／`revise` 帶舊值的紀律。**這與 V12（`revise` 的「修訂前後錨點集合必須相同」）是同一個結構性缺口** → 14。
- **`章末狀態快照` 殘骸**：一世之尊 `_index.ai.md` 還有那一節，`derived-sync validate` 持續報。病例書不遷移。
- **事實層覆蓋率仍是 0**：93 章 `## 本章事實` 0 章使用，全書 259 筆仍在 legacy `story/參照/狀態事件流.md`。01 的新格式在真實語料上**沒有被驗證過一次**——第一本走新格式的書會是它的第一次實測。
- **`prose_metrics` 有 6 支測試長期 fail**（依賴 repo 裡沒有的語料：`芯片巫師/chapters`、`Data/` 參照語料）。**本輪未動，也不是本輪造成的**（已用 `git stash` 對照確認）→ 14。
- ~~**（← 07 診斷輪順手記下·03）`chNNNN.ai.md` 的 `風格` 欄零目的地存在性檢查**~~ **07 已補（2026-07-27）**：`ch-lint` 第 11 項（`風格` 欄指向的檔存在，聚合成一行，覆蓋率行加印「93 個 `風格` 欄指向的檔（0 個不存在）」）。它是 E1「目的地承諾」推論的**第 4 個實例**（前三：幕綱設計註→裁決流、`derived-sync` 的 `missing_destinations`、`decision-lint` 兩處）。實測一世之尊 **93/93 通過**，`ch-lint` 問題數維持 3、黃金檔只差覆蓋率那一行。

**原則本身的問題 2 條**（見報告 §六，本輪未改 `設計原則.md`）：D3 漏了「選取」這一類用途，在 `_index.ai.md` 上無法決定任何事；A3 第 2 條路（人核可後封章）在混欄的衍生檔上不成立。**E2 的嚴重度尺缺一格「警報疲勞」**（守衛全報於是沒人看），本輪又量到第二個案例 → 14。

### 04 · 裁決與回饋　**已重構（2026-07-27）**

**現行落點**：`結構定義/待裁決.schema.md` ＋ `story/參照/待裁決.md`（AI 的觀察，四欄無狀態欄）／`結構定義/裁決流.schema.md` ＋ `story/參照/裁決流.md`（人拍板的理由，append-only；**`.co.md` 已廢除**）／`story/物件/全書.md`（書級方針，型別 `方針`）／`tools/decision_projection/`（`decision-project` ＋ **`decision-lint`**）／`tools/derived_sync/` 的 blockquote 禁令與目的地存在性

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/04-裁決與回饋.md](功能報告/04-裁決與回饋.md) §八「重構實況」。

**核心判斷**：三筆內容（AI 的**觀察**／人拍板的**理由**／**書級方針**）從「同居一格、同居一檔、無處安置」拆成三個各有檢查器的落點。副檔名 `.co.md` **廢除**——依新增的 **A4**，檔的第二個位元（程式會不會解析它）由**有沒有檢查器**承擔，補上 `decision-lint` 之後裁決流與幕綱、物件檔同格。

**本輪改了 `設計原則.md` 兩條**（前三輪都只記不改）：新增 **A4**；**F2 第三列**判準由「跟著它保護的那條規則」改成**「跟著它的射程」**（四格）。理由是那兩條在本功能上**無法決定任何事**，而本檔末節明訂那種條文該改寫而不是加註解。

**刻意留給後續的**（不是遺漏）：
- **T5（回饋列離開 ⇄ 裁決軸 append 的對帳）改寫不成** → 14。兩個方向都試過且都不成立（見報告 §八）。**不假裝守住 V1**，改交出兩個近似：`decision-lint` 的「有待裁決列卻沒有裁決流」＋覆蓋率行並排印「待裁決 N 列／裁決 M 列」（`f22082f` 的病徵就是 N 在掉而 M 恆 0）。與 03 記的兩條同類。
- **F2 的核心顧慮只補掉一半** → 14。「集中 log 不會自然被載入」這一點，本輪只補了**書級方針**那一半（`fact-project --for-beat` 無條件印 `### 全書方針`）；**裁決軸那一半沒補**，仍靠「有人記得跑 `decision-project`」。作者拍板不做——要讓 `fact_projection` 順帶吐裁決，得在零相依政策下複製一份裁決流解析，而且會把「理由」塞回 `write` 的 context（正是本功能診斷出的病）。
- **兩支新閘門都還沒被真實語料驗過**：0 本書有裁決軸兩支檔；blockquote 守衛唯一的真實語料是刻意不遷移的病例書。
- **`方針` 型別的性質與其餘七型不同**（它沒有狀態）。G3 的「投影按型別 fold」若日後實作，它要單獨處理。
- **一世之尊不遷移**：validate 24→31、哨兵 46→51，其中「目的地不存在 3 行」正是本輪要的（在此之前它報的是「檔太大」，看不出那些補救建議**照做會失敗**）。

### 05 · 設定層·世界觀　**已重構（2026-07-27）**

**現行落點**：`結構定義/世界觀.schema.md`／`.claude/skills/worldbuild`＋`develop`（世界觀寫入路徑）／`story/設定/世界觀/`／`tools/derived_sync`（**新增 `world-lint`** ＋`long_lines` 的兩個新目標集）／`tools/settings_select`（覆蓋率行）

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/05-設定層-世界觀.md](功能報告/05-設定層-世界觀.md) §八「重構實況」。

**核心判斷**：這一軸的病不是「方向錯」而是**衍生檔變成三種無家內容的收容所**，而三個家（01 的物件軸、04 的裁決／待裁決軸）2026-07-27 都已蓋好——**缺的一直只是「誰去搬、什麼時候搬」**。所以本輪的主要產出是把三個搬移動作寫成**帶觸發者的 schema 條文**，而不是新機制。同時依 **A4** 刪掉四個「機讀格式＋零解析器＋零消費者＋已漂移」的 front-matter 欄。

**本輪改了 `設計原則.md` 三條**（報告 §六 提案、作者拍板全改）：**A3 加第 0 條**（「這筆內容的射程指向別的落點嗎？指向就搬，別在這支檔裡調和」——舊的三條路在 `_總覽.ai.md` 上全部走不通）；**D2 補覆蓋率判準**（「有選擇器」不是布林值，要能回答命中幾筆、依據是什麼）；**E1 涵蓋消費者承諾**（凡 schema 寫「供 X 盯」，X 那一側要指得出讀它的那一行）。

**刻意留給後續的**（不是遺漏）：
- **選擇器沒修，只量了**（抉擇 2 D）。世界觀那一側比對的是**檔名字串**，實測 4 個主題有 3 個裸提及率為 0——命中的是幕綱散文裡的 `見 \`X.ai.md\`` 註腳。**02 清掉那些註腳的那一天，這 3 筆會靜默消失**。本輪只補覆蓋率行讓數字可見；**選項 C（改讀 `_總覽` 索引由 LLM 挑）仍在檯面上**，它依賴 `_總覽` 真的縮小。→ 05 的下一輪或 15。
- **遷移路徑步驟 6（6 支 skill 的 wildcard 收斂成 `settings-select`）刻意不做** → 15。理由：它依賴延後的抉擇 2，而在現況下收斂 `beat-test`／`character`（兩支目前零選取）會讓它們**當場少讀三份核心規則**——把一個「讀太多」的問題換成「讀不到」。
- **`_總覽.ai.md` 的形態隨 10／12 一起裁**（抉擇 4 A）。本輪只把它變正常（搬走 80% 寄居內容）。**別在 10／12 重新提案「它與就緒儀表是同一個問題」**——本輪已依「hash 機制的有無」否掉那個歸類。
- **`江湖勢力.md` 觸頂沒拆**（抉擇 3 B）：拆的粒度是創作判斷，而沒有任何活書需要這個決定。程序（九步，含「舊檔名的引用怎麼處理」）已寫進 schema，**但它一次都沒被跑過**。
- **`伏筆` 歸一只到「提示」不到「門檻」**：`world-lint` 報查無的名字，但不擋——`物件.schema.md` 明訂沒有物件檔的 ID 是合法的。完整的 C3（抽取器只能命中既有 ID）仍等有書真的用物件檔。
- **行長門檻是 n=4 的暫定值**：800／600 取自一世之尊四支世界觀檔的分佈空隙，schema 已標明第二本書有設定層時重取。
- **一世之尊不遷移**：哨兵 51→53（新增的 2 筆正是本輪要的——在此之前 1,155 與 1,072 字元的單行完全隱形）、`world-lint` 首跑報 5 筆。

### 06 · 角色　**已重構（2026-07-27）** ／ 07 · 風格　**已重構（2026-07-27）**

**現行落點**：`結構定義/角色.schema.md`、`風格.schema.md`／`.claude/skills/character`、`develop`（＋07 的 reader：`write`／`write-test`／`revise`）／`story/設定/{角色,風格}/`／`tools/settings_select/`、`tools/derived_sync/`（**`char-lint`**＋**`style-lint`**）／`tools/beat_metrics/` 的 `ch-lint`（`風格` 欄目的地）

**06 的診斷報告**：[功能報告/06-設定層-角色.md](功能報告/06-設定層-角色.md)（§八 有重構實況）。**核心判斷（推翻下面第一條「預期大致符合原則」的預判）**：方向確實對（A 前向），但這是目前量到最壞的一軸——**枚舉外內容佔角色衍生檔節內容的 72.2%**（24/24 支全中），**12/24 支的四個必填節全是佔位字串**（84 處「（源檔未載，待跑 `character` 補）」）而 `check`／`validate`／`settings-select` 三支守衛全部回報正常。根因在上游：`derived_sync/core.py` 從 `rglob("*.ai.md")` 出發，「源有衍生無」在定義上掃不到，於是系統手建 13 支空殼檔去餵掃描器。

**07 的診斷報告**：[功能報告/07-設定層-風格.md](功能報告/07-設定層-風格.md)。**核心判斷**：方向對（A1／A2 乾淨，`check` 從未失效），病集中在**五個 front-matter 欄是本軸唯一產物，而那個「機讀核對基準」是空的**——`可用句式` 4/4 檔位違反 schema 自己列的三條 ❌、0/4 宣告顆粒度、0/4 給數字，已封章、`check` 報 fresh、五輪重生零守衛。**與 05／06 不是同一個病**：那兩軸是「零解析器＋零消費者」故刪欄；風格五欄**消費者存在且指得出行號**，只是消費者是 **LLM 不是程式**——所以報告 §六 提案改寫 **A4**（位元由「程式會不會解析」改成「有沒有人逐欄取值·程式或 LLM 都算」）。另量到本體是源檔的**同長度改寫**（1.02×、8-gram 重疊 30%），無家內容佔全檔 **42.8%**。

**07 七項抉擇已全數拍板（2026-07-27）**：新增 `style-lint`（6 項＋覆蓋率行，落 `derived_sync`）／`可用句式` 用**結構判準**當閘門（`分句|句|段` token ＋阿拉伯數字 ＋不得全檔位同級）且 `develop` 落檔前必跑／`禁用詞表` 收斂成純字面詞、含括號的 entry 報／禁用詞正文掃描機械化（只印覆蓋率與候選）／語感範例搬回源檔／`ch-lint` 補 `風格` 欄的目的地存在性／多檔 rollup 標成「零實作承諾」不實作／**`風格.ai.md` 廢除本體四節、只留 front-matter**（`DERIVED_SECTIONS["風格"]` ＝空 tuple，四支 SKILL.md 改讀「源散文＋衍生五欄」）。**同輪改 `設計原則.md` 兩條**：A4 的位元由「程式會不會解析」改成「**有沒有人逐欄取值·程式或 LLM 都算**」（照舊條文字面套用會刪掉本軸唯一的產物——不是無法決定，是決定錯）、E1 新增第四條推論（宣稱某欄「供 X 核對」時要一併定義「什麼樣的值算可核對」且機械可判）。

**07 重構輪做了什麼（2026-07-27）**：七項抉擇全部落地——新增 **`style-lint`**（六項＋覆蓋率行＋正文掃描，落 `derived_sync`）／`風格.ai.md` **廢除本體只留 front-matter 五欄**（`DERIVED_SECTIONS["風格"]` ＝**空 tuple**，四支 SKILL.md 改讀「源散文＋衍生五欄」，形狀照抄角色軸的 `read_paths`）／`禁用詞表` 收斂成純字面詞（含括號的 entry 報）／禁用詞正文掃描機械化（只印覆蓋率與候選）／語感範例搬回源檔＋三個**帶觸發者**的搬移條文／`ch-lint` 補 `風格` 欄的目的地存在性／多檔 rollup 標成「零實作承諾」不實作。**同輪改 `設計原則.md` 兩條**：**A4 的位元**由「程式會不會解析」改成「**有沒有人逐欄取值·程式或 LLM 都算**」（＋「LLM 取值者比程式取值者更需要 lint」），**E1 新增第四條推論**（宣稱某欄「供 X 核對」時要一併定義「什麼樣的值算可核對」且機械可判）。實測：`validate` `fm_only` 2→1、`風格.ai.md` 報 6 個枚舉外節、哨兵 53 維持、`style-lint` 首跑 5 筆、`ch-lint` 新項 93/93 通過。
> **規劃時沒料到、實作時實測出來的一格**：§三 寫的判準「值裡有沒有 `分句|句|段` token」照字面實作是**假陰性**——`短句快節奏…`／`短促碎句動詞驅動` 都含「句」字，4/4 讀感形容詞裡有 2 個被判成「已宣告句層」，**閘門在自己身上重演了 E2 最後一格**（首跑只報 4 筆）。收緊成位置判準（token 要在**值的開頭或 `碎在` 之後**，那是 schema 兩個 ✅ 範例一直在用的正規形）後首跑 5 筆。**另外 schema 自己的範例過不了自己的閘門**（`碎在句（一句一分句）` 是中文數字），改範例而非放寬判準。

**06 重構輪做了什麼（2026-07-27）**：六個抉擇全部落地——新增 **`char-lint`**（八項＋覆蓋率行）、front-matter 依 A4 刪三欄（`角色`／`弧線類型`／`影響力`）、形態拆成「因為大」與「因為有秘密」兩條獨立規則、`血刀頭陀` 那格改成「**先搬後拆**」並對齊哨兵 hint、`一行需求` 改內容判準（不定專用數字門檻）、角色欄集合名 token 只報 ≥3 次。**`設計原則.md` 同輪加兩條推論**（E1「凡宣稱每一支 X 都有一支 Y，就要從 X 側掃」／E2「覆蓋率行要能回答命中的裡面幾筆是空的」），後者的直接應用是 `settings-select` 覆蓋率行加印空殼數（實測 arc02：「角色命中 7 筆，其中 **4 筆是空殼**」）。**規劃時另外查出一個報告沒寫的 blocker**：目錄形態在 `derived_sync` 裡**完全是壞的**（`check` 報 `orphan`、`stamp` raise、改切面不會讓 rollup stale），`角色.schema.md` 那條「rollup digest 對目錄形態取切面集合」是**零實作的承諾**——同輪補上實作與六個測試。

**已知疑點**：
- ~~這兩個**預期大致符合原則**（A 前向：散文源＋衍生），是驗證而非大改。~~ **06 已推翻（見上）**；07 尚未診斷，但它與角色共用 `validate.py` 的缺口（見下方兩條），別再假設它是快速驗證。
- ~~**（← 05 診斷輪順手記下·06）`血刀頭陀.md` 32,650 B 觸源檔門檻…**~~ **06 已處置（不遷移病例書，改的是規則）**：schema 加「先搬後拆」程序、成長哨兵的單檔 hint 先問「是不是有寄居內容」；`_index.ai.md` 的 12 行超長由 `char-lint` 的內容判準接手（實測 10 列含 `🧊`／章號）。**「衍生比源大」已用 05 的那把尺量過**：源 135,435 B → 衍生 182,815 B（**1.35×**），成長倍率源 2.37× vs 衍生 2.43×，**逐支比較衍生較快的 17/22**；最決定性的是 **9/22 支的源檔一次都沒改過而衍生仍長了 1.30–2.08×**——那些增量在定義上不可能來自源，而 `check` 對它們全報 fresh 且報得沒錯（digest 覆蓋的是輸入，不是輸出的內容）。原文如下——
- **（← 05 診斷輪順手記下·06）`血刀頭陀.md` 32,650 B 觸源檔門檻、`血刀頭陀.ai.md` 32,033 B 帶 5 個枚舉外的節**（「反派備註」「水下層分界規則」…）；`角色/_index.ai.md` 12 行超過 400 字（最長 1,071）。**05 診斷出的「衍生比源大」在角色軸也成立**（32,033 vs 32,650，已幾乎相等）——值得用同一把尺（`git log --follow` 逐版成長倍率）量一次：05 實測 `江湖勢力.ai.md` 八次重生長 18.2×、源只長 9.2×，**衍生的成長倍率是源的兩倍**。
- **（← 05 診斷輪順手記下·06）`character/SKILL.md:41` 零 `settings-select`、三處 wildcard**，是本 repo 讀設定層最重的一支（51,225 字元）。而它的職責（判新角色是否違反世界觀核心規則）其實只需要核心規則索引。**05 刻意沒收斂它**（見上「刻意留給後續的」）。
- ~~**（← 05 診斷輪順手記下·07）`風格.ai.md` 12,247 B 剛超 `DERIVED_BYTES`**，且它是 `validate` 的「2 支只驗 front-matter」之一（節枚舉是空頭承諾）。另：它最長單行 602 字元、離 800 門檻只有 25%。~~ **07 已處置（2026-07-27）**：節枚舉補上（**空 tuple**，`fm_only` 2→1）；體積與長行的**根因是本體四節＋42.8% 的無家內容**，本輪把本體整個廢除（實測它是源檔的同長度改寫 1.02×、8-gram 重疊 30%），新書的 `風格.ai.md` 約 1 KB。**一世之尊不遷移**，所以那兩筆哨兵還在（53 維持）。
- ~~但 `.ai.md` 的 `## 待裁決回饋` 問題落在這三個身上（見 04），且要等 04 定案。~~ **04 已裁（2026-07-27）**：該節從 `validate.py` 的節枚舉**移除**（角色／角色\_index／世界觀／世界觀\_總覽／章節五處），回饋改住 `story/參照/待裁決.md`。既有書那些節現在會被 `derived-sync validate` 報成枚舉外——**那是預期的**（同移除 `## 🧊 水下` 時）。
- ~~**（← 04 重構輪追加）`風格` 與 `摘要` 根本不在 `validate.py:DERIVED_SECTIONS` 裡**~~ **07 已補一半（2026-07-27）**：`風格` 登記成**空 tuple**（＝最嚴的枚舉：不得有任何 `##` 節），一世之尊 `fm_only` **2→1**、`enumerated` 124→125。**剩下的那一支是 `摘要` → 08。**
  > **實作時實測到的陷阱，08 補摘要枚舉時會再遇到一次**：`enum_for()` 回空 tuple 時，`if allowed:` 會把它當成「沒有枚舉」——那支檔會被算進 `fm_only`、stray 檢查整個跳過，**而輸出看起來完全正常**。判斷要用 `is not None`（`validate.py` 那兩行有註解，`test_validate.py` 有兩面測試）。
- `settings-select` 的粒度是「哪幾支檔」不是「檔裡哪一段」，所以設定層 `.ai.md` **沒有行內切片工具**，依 D2 不該享有「可以很大」的豁免（`sentinel.py` 的 `DERIVED_BYTES=12000` 已經在管，但量的是 bytes 不是查詢成本）。
- 角色的「水下」切面是存取控制，走檔名（`角色/<名>/水下.md`）——這是 C1 的正例，確認它仍成立即可。**（01 重構輪確認仍成立**，且它現在是揭示層級的**更強一級**：物件檔的 `揭示層級` 只是提醒，`水下.md` 是 `write` 拿不到。）
- **（← 01 診斷輪順手記下）`derived-sync check` 實測：8 支角色 `.ai.md` 有枚舉外的節、6 支世界觀／風格 `.ai.md` 超過 12,000 B**（最大 44,978 B）。其中**屬 01 的那 70 行已經有家了**（枚舉外節的「硬事實」「下游硬約束」「反派備註」→ 錨進章 delta、約束進物件檔），**但一世之尊刻意不遷移，所以那些節還在**；世界觀那幾支的成因 05 已診斷並改掉 schema（見 05），角色／風格那幾支是 06／07 的事。
- **（← 01 重構輪追加）枚舉已經縮小過三次**：`角色.ai.md` 移除 `## 🧊 水下` 節（01）、`世界觀.ai.md` 移除 front-matter 鍵 `🧊水下:`（01）、**世界觀 `.ai.md` 再移除 `影響力`／`主從`／`維度`／`升格哨兵` 四欄且 `## 自洽 / 升格哨兵` 改名 `## 自洽`（05）**。既有書那些節／欄現在會被 `derived-sync validate`／`world-lint` 報——**那是預期的**。
  > **06／07 要用 05 的那把尺自問一次**：角色與風格 `.ai.md` 的 front-matter 欄（`角色`／`型別`／…）**有沒有解析者、有沒有消費者**？05 實測世界觀那五欄是「零解析器、零消費者、schema 卻宣稱『供 X 盯』」，而 `validate.py` 的 `REQUIRED_KEYS` 只驗 `generated-*` 兩個鍵——**那個縫對所有產物都成立，不只世界觀**。判準見 `設計原則.md` A4 與同輪新增的 E1 消費者推論。
  > **06 已量並處置（2026-07-27）**：七欄×24 支＝**162 個欄位實例，程式讀取 0 個**。刪 `角色`／`弧線類型`／`影響力`，留 `定位`／`所屬arc`／`暫定`／`伏筆` 並由 `char-lint` **真的讀**（`角色.schema.md` 有逐欄對照表）。`弧線類型` 是這個縫最純的形態——**連假的消費者都沒宣稱過**（世界觀的 `升格哨兵` 至少寫了「供 beat-test/sync 盯」）。**07 照樣要跑這一問。**

### 08 · 摘要 ／ 09 · 大綱

**現行落點**：`結構定義/摘要.schema.md`、`大綱.schema.md`／`.claude/skills/organize`、`outline-test`、`expand`／`story/00-摘要.md`＋`.ai.md`／`story/01-大綱.md`、`story/大綱/arcNN.md`（08 另有：`tools/beat_metrics/scan.py:179` 的 `load_pov`＝唯一的程式解析器／`tools/derived_sync/style_lint.py:442`＝`基調` 欄的比對對象）

**08 的診斷報告（2026-07-27 診斷，2026-07-28 全數拍板）**：[功能報告/08-摘要.md](功能報告/08-摘要.md)。**核心判斷**：方向對（A1／A2 乾淨、`check` 從未失效、源→衍生分權清楚），病在**守衛覆蓋率是各軸最低**——`validate` 的 `fm_only` 剩下的那唯一一支就是它（節枚舉是空頭承諾，於是 04 從五處移除的 `## 待裁決回饋` 在這裡還活著，818 字元／19.2%，因為 `classify()` 不認得 `story/00-摘要.ai.md`）；**四支成長哨兵目標集 0/4 含它**（源 15,656 B＝門檻 62.6%、衍生 10,530 B＝門檻 **87.8%**，全靜音，這是「四份手寫路徑清單」的第六次）；**schema 的 8 條檢查點 0 條有實作**，其中「人稱只能填系統支援值·不得直接封章」是封閉二值枚舉、機械完全可判，而唯一的書填了非支援值並封章寫了 93 章。front-matter 11 鍵中 **8 鍵被逐欄取值（2 個是程式取值者：`beat_metrics.load_pov` 讀 `視角結構`、`style-lint` 第 6 項讀 `基調`）而整支檔零 lint**；`基調主從`／`終局` 是 05 四欄／06 三欄的第三個實例（`終局` 更狠——**schema 從沒定義過它**，534 字元，而 `validate` 只驗缺不驗多）。**唯一有真實傷害路徑的縫**：`風格.md:7` 的基調句與 `00-摘要.md:43-44` 正規化後**逐字相同、零守衛**，而 07 把 `write`／`revise` 改成「源散文＋衍生五欄兩邊都讀」之後，舊基調會直接進正文（`style-lint` 第 6 項只守衍生↔衍生那條邊）。內容分佈：源 **51.4% 是裁決理由與已駁回**（10 處駁回語彙）、16.7% 是「臨場拍板」而其中一大半寫著「已定案／已鎖定」。成長軸是**裁決輪次**（源 10.6×／衍生 15.5×，而 `共同約定.md:290` 明文把摘要列進「本來就是單檔且有界」）。**八項抉擇 2026-07-28 全數拍板**（詳見報告 §五，重構輪照那裡做）：① 裁決理由走 **C**（**只搬被捨棄的方案＋為什麼捨棄**去裁決流，已採用的結論留源檔、理由壓成一句；覆蓋率行印駁回語彙處數當提示）／② `節奏檔位` **刪欄**，下游改指幕綱檔頭，「檔頭該不該有 lint」記進 09／③ `終局` **刪欄**＋追加進已駁回清單（「為零消費者的宣告欄補 lint」第三次，新變體＝**連 schema 都沒宣告過的自生欄**）／④ 基調**砍到三份**（`風格.md` 的複本改成指標），`summary-lint` 第 5 項降級成兩方比對／⑤ 人稱閘門**實作、不做 opt-out**（一世之尊預期常駐一筆）／⑥「臨場拍板」節先做**歸位**（帶「已定案／已鎖定」字樣的條目回承重節），剩下的走 `raw/`、**不走 `待裁決.md`**／⑦ 新開 `summary-lint` 落 `derived_sync`，「front-matter 不得有 schema 外的鍵」交 14／⑧ 改 `共同約定.md:320` 的措辭，明寫「判**達標**」（已駁回）與「比對兩份抄本**一致**」（合法）的分界。**§六 的 `設計原則.md` A4 改寫也已拍板**：補第三條路——被逐欄取值的若是**源檔**，正解是把那幾欄的權威移到衍生層並在那裡補 lint，**不要給自由源補 lint**，也不要假裝「改成整檔讀」解決了問題（現行 A4 的兩條路在摘要源檔上**都指向現況**）。這不是新規則，是把 06／07 已經走過的路寫出來，**不影響 04–07 的既有結論**。

**08 重構輪實際做了什麼（2026-07-27／28）**：八項抉擇＋§六 的 A4 改寫全數落地。

1. **新增 `summary-lint`**（`tools/derived_sync/summary_lint.py`，八項＋覆蓋率行＋兩個「只印不擋」的源檔提示）：源側存在性／front-matter 八鍵／殘留欄／**人稱落檔閘門**（封閉二值枚舉，`develop`·`organize` 落檔前必跑、命中就停下回問作者）／`POV` 可被 `beat_metrics.load_pov` 的同一把尺解析／**`基調` ≡ 源基調節首句**／幕號·arc 引用不懸空／**基調宣告的單位＋阿拉伯數字**（摘要或源 `風格.md` 兩邊至少一邊有）。CLI 掛 `derived-sync summary-lint` ＋獨立入口 `summary-lint`。
2. **`validate` 認得 `story/00-摘要.ai.md`**＋`DERIVED_SECTIONS["摘要"]`＝三節：一世之尊 `fm_only` **1→0**、`enumerated` 125→126、問題 32→**33**（多報的那一筆正是 04 從五處移除卻在這裡活著的 `## 待裁決回饋`）。**`fm_only` 從此該恆為 0**，那一格改當「有沒有新產物又沒登記枚舉」的哨兵。
3. **成長哨兵三支目標集補上摘要兩支檔**（第六次補同一個根因，**根因不在本輪修** → 14）：源套 `SOURCE_BYTES`、衍生套 `DERIVED_BYTES`＋節枚舉、行長**沿用設定層的 800／600 不新造第五級**。一世之尊哨兵 53→**54**（新增的那一筆是衍生檔的枚舉外節；行長 0 筆——實測 626／594 都只差門檻一點點）。
4. **依抉擇刪三個欄**：`基調主從`（零取值者）／`終局`（零取值者 **＋ schema 從沒定義過它**，534 字元自生欄）／`節奏檔位`（**權威已外移**到幕綱檔頭，這一份覆蓋 1/11 arc）。`summary-lint` 第 2 項是殘留守衛。
5. **基調從四份砍到三份**（抉擇 4）：`風格.schema.md` 的基調節改成**指標而非複本**，`summary-lint` 第 5 項降級成兩方比對。砍完之後**沒有任何一條邊是「源↔源、零守衛、逐字相同」**——那是本輪唯一有真實傷害路徑的縫。
6. **12 支 SKILL.md 的讀檔步驟改成「源散文＋衍生六個語意欄」**，10 處「`00-摘要.md`「XX」欄」的寫法全部改掉（另順手改 `章節.schema.md`／`技巧知識庫/_index.md` 3 列／`網文與文學取捨.md` 2 處的同型寫法）。
7. **`設計原則.md` A4 補第三條路**（源檔被逐欄取值時：權威移到衍生層並在那裡補 lint，源檔維持自由散文）——**不是新規則，是把 06／07 走過的路寫出來**，且**不影響 04–07 的既有結論**。
8. **`共同約定.md` 六處對齊**：八 的查詢入口表補摘要一列／閘門清單新增 `summary-lint`／覆蓋率行那段補摘要那一串／`fm_only` 那句改成「1→0，該恆為 0」／哨兵那段記第六次漏檔／**九 依抉擇 8 改寫措辭**（明寫「判**達標**」＝已駁回 vs「比對兩份抄本**一致**」＝合法的分界，**2026-07-22 那四個理由一個字都不改**）／「不受此限的」拿掉摘要與風格的無條件豁免。
9. **達標線範例句一律改阿拉伯數字**（`每章至少 1 次…`）：判準不認中文數字（認了「短句為主、一律放慢」也會過關），照 07 的先例**改範例、不放寬判準**——動到 `摘要.schema.md`／`風格.schema.md`／`共同約定.md` 九／`氛圍與筆調.md`／`develop`·`write-test`。
10. **三筆駁回進 `02-待用構想.md`**：為零消費者的宣告欄補 lint（第三次，新變體＝schema 未宣告的自生欄）／保留 `風格.md` 的基調複本（B 與 C 兩案）／把「臨場拍板」搬進 `待裁決.md`。

**一世之尊不遷移**（病例書），所以它在新守衛上常駐：`validate` +1（`待裁決回饋`）、哨兵 +1、`summary-lint` **3 筆**（三個殘留欄聚合成一行／`人稱: 第一人稱貼身`／基調兩邊都沒有達標線）。**基調兩方比對首跑印「相符」——那正是它該印的**：它守的是未來的漂移，不是現在的傷。

**09 的診斷報告（2026-07-28 診斷，待作者拍板）**：[功能報告/09-大綱.md](功能報告/09-大綱.md)。**核心判斷**：方向對（A1 乾淨——大綱確實沒有 inbound 重算規則），病在**它是全系統唯一一個被 `共同約定.md:24／:91／:96` 明文列進「強格式源·**一支 lint**·嚴格（有 lint）」那一格、而 lint 不存在的產物**（`CLAUDE.md` 分類公理同段；**這是 04 在同一張表上抓過 `.co.md` 之後、同一句假宣稱的第二個主詞**）。實測 **0 支工具解析大綱**（grep 命中全是 `.venv` 裡 pygments 的 Gherkin 關鍵字表），而 4 支 SKILL.md 逐節取值（`beat-sheet:36`「含選用結構公式、結局、題旨」／`outline-test:22-23` 兩條自稱「機械」／`expand:49`／`organize:29`），**`大綱.schema.md` 的 7 條檢查點 0 條有實作**。後果量得出一條乾淨的曲線：**`本段全文` 佔全檔 54.0%（arc01）→ 11.1%（arc11）**，同期 **schema 枚舉外的節 0%→64%**，11 個 arc、5 天、零警報。最大的一塊是 `## 本 arc 伏筆狀態`（6 支檔、**28,941 字元＝arcNN 檔的 28.6%**）——它是 `foreshadow-project` 輸出的**手抄第三份**（幕綱那份有 `beat-lint`），兩側 8-gram 重疊從 arc06 的 55.8% 掉到 arc11 的 **8.4%**，而 `foreshadow-project` 印「15 條，其中 **0 條為可疑點**」exit 0。**四支成長哨兵目標集 0/4 含 `story/大綱/`**（第七次「四份手寫路徑清單漏檔」，這次漏的是**一整個產物軸**、repo 體積第三大、302,669 B），實測 **6/12 支超過 `SOURCE_BYTES`**（最大 44,307＝1.77×）全部靜音，同一次 `check` 卻報了 32,650 B 的 `血刀頭陀.md`。另量到三筆：**`01-大綱.md` 與未併入的 arc09/10/11 並存，而 `organize/SKILL.md:52` 明文寫「不會有這種狀況」**（造成它的正是 `organize` 自己，3 個 arc、0 警報；`beat-sheet:36-37` 的路由在此狀態下指向一個字都沒寫卷三情節的檔，**沒出事的唯一原因是有人在 `01-大綱.md` 檔頭手寫了一句指標**）／**「已併入」是有損壓縮且無驗收**（arc05–08 只有 **2.7–4.7%** 的 8-gram 進了 `01-大綱.md`，而那 8 支檔已被檔頭一句話宣告非權威）／**摘要「結局方向」↔ 大綱「全書終局落點」8-gram 重疊 45.4%、源↔源、零守衛**（08 交下來未量的那一筆，見下）。**八項抉擇 2026-07-28 全數拍板**（詳見報告 §五，重構輪照那裡做）：① `outline-lint` 落 **`tools/beat_metrics/`**（第三個指令，共用 `structure.parse_book` 的幕號 registry＝零複製）／② `本 arc 伏筆狀態` **收窄成三欄意圖表**（`伏筆｜本 arc 打算怎麼動｜為什麼`），禁 `幕NNN`／`chNNNN`，既成事實改由 **`foreshadow-project --arc`**（新增過濾）回答／③ 併入的最後一步改成 **`git mv` 進 `story/大綱/_已併入/`**，且**搬檔前要先印保真率交作者確認**／④ **並存合法化**（`01-大綱.md` 管已收斂的卷、scoped 管未收斂的卷）＋ **`beat-sheet` 路由反轉**（先找 `arcNN.md`）／⑤ 結局／題旨**切乾淨**（「結局是什麼」回指摘要，「怎麼走到那裡」留大綱），不加欄位比對／⑥ 卷級方向**升格成 `## 卷級方向（本卷權威）` 節** ＋ **`設計原則.md` F2 補第五格「綁單一卷」**／⑦ `大綱/_index.md` 形態隨 10／12 裁，本輪只納入哨兵＋視圖檢查＋登記成欠債／⑧ 門檻**全部沿用現成值**（`SOURCE_BYTES` 25,000 落在 21,836→33,544 的空隙；行長沿用設定層源的 600，聚合輸出）。**§六 兩條原則改寫也已拍板**：新增 **A5「退役源」**（一支源檔的身分只能被另一支檔的存在撤銷、且撤銷要從檔案系統看得出來，撤銷前要有驗收步驟回答「內容去哪了」）／**F2 補第五格**；**D2 的成本函數本輪不改**，第五票記進 14。**§六 提三條原則問題**：**A 章沒有一條講「一支源檔被宣告失效之後是什麼」**（「已併入」在 A1–A4 上無法決定任何事，提案 **A5「退役源」**：身分只能被另一支檔的存在撤銷、且撤銷要從檔案系統看得出來）／**F2 四格漏了「綁單一卷」**（`arc09.md` 檔頭〈卷三方向〉5,079 字元自稱「權威長版」，四格全落不進去，於是寄生 blockquote 並自我複製兩份）／**D2 第五票，且是第一個「選擇器完全健康而成本仍線性」的案例**（選擇器是檔名、覆蓋率 100%，成長發生在**被選中的那一份裡面**：4,848→44,307 B）。

**09 重構輪實際做了什麼（2026-07-28）**：八項抉擇＋§六 兩條原則改寫全數落地。

1. **新增 `outline-lint`**（`tools/beat_metrics/src/beat_metrics/outline.py`，**第三個指令**，抉擇 1 B：與 `beat-lint`／`ch-lint` **共用 `structure.parse_book` 的幕號 registry ＝零複製**）：十一項＋覆蓋率行。節枚舉（一支檔一行）／檔頭狀態二選一且與目錄一致／`_index` 視圖 ≡ 資料夾＋列序遞增／`幕NNN`·`chNNNN` 引用不懸空／`[[伏筆:x]]` 命中 registry／`結局：` 非空非模糊＋`題旨：` 因果句型／`本段收束與鉤子` 三格／同一 arc 不得兩邊都有內容／檔內路徑目的地存在／`本 arc 伏筆狀態` 三欄且禁既成事實 token／`卷級方向（卷X·本卷權威）` 的卷 token 唯一與指路紀律。33 支單元測試 ＋ 1 支黃金檔回歸。
2. **抉擇 2 B**：`## 本 arc 伏筆狀態` 升格成 schema 定義的節、**收窄成三欄意圖表**，既成事實一律問 `foreshadow-project --arc`（**該過濾本來就存在**，`entering()`，不必新增——見報告 §八）。
3. **抉擇 3 A ＋ A5**：併入的最後一步改成 `git mv` 進 `story/大綱/_已併入/`，**搬檔之前先印「本次併入 N 支、8-gram 保真率 X%」交作者確認**；`_已併入/` 不受成長哨兵管、不得帶仍在生效的 `## 卷級方向`。
4. **抉擇 4 A**：並存**合法化**（按卷分工），`organize/SKILL.md` 的禁令改寫；**`beat-sheet` 路由反轉**（先找 `大綱/arcNN.md`）——同一處寫出卷級方向的載入步驟（F2 第五格的「怎麼被載入」是承諾不是期望）。
5. **抉擇 5 A**：`結局與題旨` **切乾淨**（「結局是什麼」回指 `00-摘要.md`、本節只寫怎麼走到那裡），**不加欄位比對**（沒有兩份可比了），改由覆蓋率行印「是否仍帶結局複本」（只印不擋，形狀照抄 `summary-lint`）。
6. **抉擇 6 B ＋ F2 第五格**：新增 `## 卷級方向（卷X·本卷權威）` 節。**卷 token 寫進節名是實作時加的**——卷界在系統裡沒有機讀宣告，照抉擇字面實作驗不出「同一卷」（E1：驗不到就不宣稱）。
7. **抉擇 7 A**：`大綱/_index.md` 只做四件事（納入 `ROLLUP_LINE_CHARS` ／`outline-lint` 驗視圖＋列序／schema 明訂一列寫什麼／`共同約定.md` 八 登記成「尚無投影入口」的欠債），形態隨 10／12 一起裁。
8. **抉擇 8 A ＋ 成長哨兵**：`oversized_sources` 與 `long_lines` 補上整個大綱軸（**第七次「四份手寫路徑清單漏檔」**，這次漏的是**一整個產物軸**、repo 體積第三大 302,669 B）。門檻**全部沿用現成值**：`SOURCE_BYTES` 25,000（空隙 21,836→33,468）／行長 600（空隙 409→723）／`_index` 400，樣本 n=11，旁註取值依據。7 支新測試。
9. **`設計原則.md` 兩條**：新增 **A5「退役源」**（撤銷只能由另一支檔的存在完成、要從檔案系統看得出來、撤銷前要驗收「內容去哪了」）／**F2 補第五格「綁單一卷」**。**兩條都註明不影響 01–08 的既有結論。**
10. **跨檔對齊**：`共同約定.md` 六處（零 的三處假宣稱、八 的查詢入口表兩列、閘門清單、覆蓋率行那一段、哨兵那一段）／`CLAUDE.md` 分類公理／`02-待用構想.md` **追加三筆駁回**（抉擇 2 C／3 C／5 B）＋第五節記 `outline-lint` 已落實／`書本模板` 兩支（`01-大綱.md` 骨架 ＋ 新增 `story/大綱/_index.md` 骨架）。

**一世之尊不遷移**（病例書），所以它在新守衛上常駐：`outline-lint` **16 筆問題 ＋ 1 行資訊**（6 支檔的四欄伏筆狀態表＋既成事實 token ＝12／2 支檔的枚舉外節 ＝2／registry 查無 1／索引列序 1；資訊行＝8 支已併入卻仍住在 `大綱/`）、成長哨兵 **54 → 68**（源檔肥大 6／行長 7＋索引 1）。**那些數字正是本輪要的**——在此之前這一軸的 302,669 B 對整個工具鏈是不存在的。乾淨的那一半也釘進了黃金檔：**164 筆幕引用 ＋ 106 筆章引用懸空 0、83 個檔內路徑引用 0 個不存在、索引 11 列 ≡ 資料夾 11 支**。

**已知疑點**（08 已逐條驗證，見報告）：
- ~~**（← 04 重構輪追加）`摘要` 不在 `validate.py:DERIVED_SECTIONS` 裡**~~ **✅ 08 已補**（三節枚舉，`fm_only` 1→0）。
- ~~**（← 07 重構輪追加）基調字串住在四個地方，07 只補了守衛、沒裁該砍到剩幾份**~~ **✅ 08 抉擇 4 A 已裁：砍到三份**（`風格.md` 的複製句改成指標），`summary-lint` 第 5 項補上源↔衍生那條邊。**一世之尊不遷移**，所以它那一句複本留著當活體證據——覆蓋率行印「`風格.md` 是否仍帶基調複本：**是**」。
- ~~**（← 07 重構輪追加）`基調主從` 要跑 05／06／07 的那一問**~~ **✅ 08 已答：零解析器、零消費者 → 刪欄**（同 05 四欄／06 三欄）。半形括號的病徵隨欄一起消失。
- ~~**（← 07 重構輪追加）`書本模板/story/00-摘要.ai.md` 的骨架頭註仍寫「…＋待裁決回饋」**~~ **✅ 08 已清**（連帶把列出的鍵改成留下來的八鍵、源骨架的「＋節奏檔位」標題一併改掉）。
- ~~`共同約定.md` 九「宣告型約束」落在摘要上~~ **✅ 08 已接上實作**：`summary-lint` 第 7 項驗「單位 token ＋阿拉伯數字落在**同一行**」，**兩邊至少一邊有**（摘要基調節或源 `風格.md`）。**與 E1 沒有合併**——九 管「宣告怎麼寫才能被核對」、E1 管「每條格式承諾要指名檢查器」，本輪讓九 的三件事第一次有機械前哨，那證明兩者是互補不是重複。
- ~~大綱**沒有任何工具解析**（已 grep 確認），是乾淨的散文源——確認它不需要衍生層即可（**09 的題目**）。~~ **09 已答，且前提要修正**：「沒有任何工具解析」為真，但「**乾淨的散文源**」不成立——`共同約定.md:24` 把它列在**強格式源**那一格（宣稱有 lint），4 支 SKILL.md 逐節取值，且它自己長出了表格（`本 arc 伏筆狀態`）、編號排除線與 164＋106 筆 `幕NNN`／`chNNNN` 引用。**不需要衍生層是對的（09 維持 02 對幕綱的判斷：強格式 `.md` ＋ 補 lint），但「不需要守衛」是錯的。**
- ~~**（← 08 重構輪追加·→ 09）幕綱檔頭的節奏檔位零格式、零 lint**：…**這一項的唯一落點就是那個沒有守衛的檔頭**~~ **09 已驗，08 的交接不完整**：刪掉摘要那一份之後是**兩份不是一份**——`大綱/arcNN.md` **11/11 支都寫了節奏檔位**（幕綱側 arc02／arc03 沒寫，與 08 的盤點一致）。大綱那份是「大綱層宣告」、幕綱那份是「拆幕時兌現」，可能不是複本而是兩個層級的同名欄位；**兩層都零 lint，且沒有任何東西比對它們**。→ 02 的下一輪或 09 的重構輪。
- ~~**（← 08 重構輪追加·→ 09）源「結局方向」3,054 字元與大綱層的重疊程度本輪沒量**~~ **✅ 09 已量：8-gram 重疊 45.4%**（`00-摘要.md`「結局方向」3,049 字元 ↔ `01-大綱.md`「全書終局落點」1,828 字元），而大綱那一節自己寫著「**措辭同源 `00-摘要.md`「結局方向」**」。**兩邊都是源檔、零守衛**——所以 08 的收束句「砍完之後沒有任何一條邊是『源↔源、零守衛、逐字相同』」**只對基調成立**。與基調那筆的差別：這一條不是逐字複製而是 45.4% 的改寫（大綱那 1,828 字元有 55% 是摘要沒有的走法），所以處置不是「刪一節」而是**切乾淨**（09 抉擇 5）。


### 10 · 就緒儀表　**已重構（2026-07-28）** ／ 11 · 結構對應表

**10 的現行落點**：`結構定義/就緒.schema.md` ＋ `story/參照/就緒.md`（A1 源檔＝作者的成熟度判斷與就緒清單）／`tools/derived_sync`（**新增 `readiness` 投影 ＋ `readiness-lint` 閘門**，`_reference_sources` 體積哨兵）／`tools/beat_metrics`（`beat-lint`／`ch-lint` 的測試紀錄欄）／10 支 SKILL.md
**11 的現行落點**：`結構定義/結構.schema.md`／`story/參照/結構.ai.md`／`tools/derived_sync` 的 `_is_declarative`（**10 之後只剩它一支**）

**10 這一輪做了什麼**：見 [功能報告/10-就緒儀表.md](功能報告/10-就緒儀表.md) §八「重構實況」。八項抉擇全部落地——**`就緒儀表.ai.md` 廢除、拆成「小源檔 ＋ 投影」**（不可重生的兩筆留 `story/參照/就緒.md`，1,350 B、五 token 封閉枚舉、無備註欄、恰兩節；可重生的四筆走 `readiness` 三節、0 也印）／**新增 `readiness-lint` 七項**／**測試執行紀錄有家了**（`幕綱/arcNN.md` 檔頭宣告行＋`chNNNN.ai.md` 的 `write-test` 欄，`beat-lint`／`ch-lint` 守形狀、**缺席合法**，同輪接上 `write:81`／`readiness`／`beat-test`·`write-test` 收尾四個消費者與生產者）／**分流條文帶觸發者**寫進新 schema（搬檔前先印保真率）／**`story/參照/` 納入體積哨兵**／**`DECLARATIVE_STEMS` 縮成 `{"結構"}`**。**同輪改 `設計原則.md` 三條**：新增 **A6「無源衍生 ＝ 一個必須解掉的衝突，不是一種檔」**（與 A3 同形反向，兩條合起來填滿 `(會不會被覆蓋)×(人能不能改)` 的 2×2 表）／**D3 補可執行推論**（凡把一支檔改成投影，先逐格回答「這一格的機械來源是哪一支檔的哪一欄」）／**E2 補第七種形態「豁免的射程比它的理由大」**——**三條都註明不影響 01–09 的既有結論**。
實測：`readiness` 對一世之尊印 **20 格＝4 有前哨·4 未接·12 無機械來源**、清單 **2 完整·2 部分·2 未接**、局部下沉 11 個 arc（**與診斷輪手算的 306 字元投影逐格相同**）；成長哨兵 **68 → 70**、`missing_destinations` 62 → **64**；`check`／`validate`／`outline-lint` 數字**全部不變**，`beat-lint`／`ch-lint` 問題數不變、各多一格覆蓋率（`0/11`／`0/93`）；測試 **207＋125 全綠**。

> **⚠️ 這一輪的「不遷移」與前八輪性質不同，代價要明白記著**：一世之尊**沒有 `story/參照/裁決流.md`**，而它全部的拍板史（**68,776 字元＝該檔 53.8%**）住在 `就緒儀表.md` 裡。前八輪「不遷移」的代價都只是「哨兵上常駐幾筆」；**這一輪的代價是一份無備份、無守衛、無 append-only 保證的決定史**。它現在是**可見的**（哨兵 +1＝292,591 B 是門檻的 11.7×、`readiness-lint` 2 筆、`missing_destinations` 64 筆都指向同一件事），**但可見不等於安全**——要真的保住它，只有走報告 §五 抉擇 8 的選項 C（例外遷移日誌那一半）。已駁回選項 B（`git mv` 成 `.ai.md`）：那只換來三項豁免，格式守衛仍是零。

**10 的診斷報告（2026-07-28 診斷，同日全數拍板）**：[功能報告/10-就緒儀表.md](功能報告/10-就緒儀表.md)。**核心判斷**：`就緒儀表` 不是一支肥大的儀表板，**它是這本書事實上的裁決流，穿著儀表板的名字**——30 個帶日期的輪次條目、**68,776 字元（佔全檔 53.8%）**，而 `裁決流.schema.md` 早就把「`就緒儀表.ai.md` 的表格 cell（實測最長單一 cell 逾 10,000 字）」明文列為它要取代的**三個寄生落點之一**（家在 04 就蓋好了，缺的一直只是「誰去搬、什麼時候搬」，同 05）。實測 **292,591 B ＝ 全書最大的一支檔**（佔全書 2,589,923 B 的 11.3%，大於次大的 `狀態事件流.md` 111,178 與最大的幕綱檔 86,734），成長 **1,149 → 292,591 B ＝ 254.7×／6 天／45 個版本**（成長軸是 skill 輪次，比摘要 10.6×／風格 20.04× 快一個數量級）。**「壞了永遠不會發現」佔比是九個功能裡最高的**——七種壞法有六種落在最後一格，最狠的一種是**結構性的**：舊名 `就緒儀表.md` → `check_book` 從 `rglob("*.ai.md")` 出發**掃不到**（實測對 `story/參照/` 回 0 支）；新名 `就緒儀表.ai.md` → `_is_declarative` 在 `validate_file` 早退**三次**（front-matter 必填鍵／裁決 blockquote／節枚舉），**沒有任何命名能讓它被驗**，而 `共同約定.md:106` 與 schema:5 都寫著「工具兩種命名都吃」——**那句只對 `long_lines` 成立**。另量到：三支體積哨兵目標集 **0/3 含 `story/參照/`**（第八次「四份手寫路徑清單漏檔」，這次漏的是全書最大的一支檔）／狀態格 **20 格 7 格違反 schema 的四值枚舉、最長 3,647 字元**，而 `long_lines` 量的是**行**不是**格**（832 字元的那一行含 107／92 字元的違枚舉格，不報）／「⚠️N 筆待裁決」**26 處字樣 vs 實際 0 筆**（04 量到 14 處，**病徵翻倍**）／`beat-sheet` 被 schema 列為更新者而全文 **0 次**提及它、實際會寫它的 `character`／`expand`／`worldbuild` 不在名單上／`sync` 五處提及全是「讀」與「更新」，**零比對步驟**（schema:4「深層語意過期走 sync 語意比對」指不出行號）。**最決定性的一個數字**：三節「現況」投影出來是 **306 字元 vs 128,371 字元 ＝ 419× 壓縮**，而輸入 100% 現成（`ch-lint` 第 8 項已在做同一個重算）——**但 `02-待用構想.md` §八 的假說只對了一半**：全檔與其他檔的 8-gram 重疊合計僅 ≈10%，**90% 是沒有別的家的獨有內容**，先砍檔就是刪掉一本書的決定史（A5）。**八項抉擇 2026-07-28 全數拍板**（詳見報告 §五，重構輪照那裡做）：① 檔的形態（建議 B ＝「小源檔 `story/參照/就緒.md` ＋ `readiness` 投影」，已擋下 A ＝「全部改成投影」，那會逼人把作者判斷編成機械規則）／② 68,776 字元怎麼搬（建議 B ＝ 分流，實測 53.2% 是工具當場算得出來或已過期的東西）／③ 測試執行紀錄住哪（三份手抄零比對）／④ 狀態格枚舉還是刪／⑤ 就緒清單改成「機械前哨 ＋ 作者確認」（實測 **4/6 條的機械閘門 05–09 已經寫好了，而六個 `[x]` 從沒跟它們對過一次**，其中兩條的前哨現在各報一筆）／⑥ `readiness` 落哪個套件（**它會是 repo 第一個跨工具聚合器 ＝ 零相依政策第一次被逼到牆角**）／⑦ 體積哨兵補 `story/參照/`／⑧ 一世之尊要不要例外遷移（**本輪的不遷移代價與前八輪不同**：那本書沒有 `裁決流.md`，不遷移＝一份無備份、無守衛、無 append-only 保證的決定史）。**§六 三條原則改寫也已拍板**（D3 補推論／新增 **A6**／E2 補第七格）：**D3 沒說「投影的輸入不存在時怎麼辦」**（它的條文自己拿就緒儀表當例子，而六筆裡兩筆不可重生——照字面執行是**決定錯**不是無法決定，會逼出「一份會漂移的機讀規則」，即 05／06／07 三次被駁回的形狀；提案補推論「凡把一支檔改成投影，先逐格回答『這一格的機械來源是哪一支檔的哪一欄』」，與 A5 同形）／**A 章對「宣告式綜合檔」無法決定任何事**（A1 說它是源該叫 `.md`，系統卻改名 `.ai.md` 並開特例；提案 **A6「無源衍生＝一個必須解掉的衝突，不是一種檔」**，與 A3 同形反向，兩條合起來才填滿 `(會不會被覆蓋)×(人能不能改)` 的 2×2 表；**`結構.ai.md` 是它的第二個實例 → 11 直接套**）／**E2 尺的第七種形態「豁免的射程比它的理由大」**（`_is_declarative` 因為一個 hash 理由關掉三項檢查，其中兩項與 hash 無關；提案「豁免要寫成豁免哪一項檢查，不能豁免整支檔」）。

**已知疑點**（10 已逐條驗證，見報告）：
- ~~兩者帶 `.ai.md` 但**無單一源、不能 hash、不能 stamp**，是「假衍生檔」。過期只靠 `sync` 的語意比對。~~ **✅ 10 已驗，兩半都要修正**：① 「假衍生檔」這個說法**在 A 章裡沒有依據**——A1 說沒有 inbound 重算規則的就是**源檔**，所以 10 提案 **A6**（見上）把它從一種檔改成一個要解掉的衝突；② 「過期只靠 `sync` 的語意比對」是**空頭承諾**——`sync/SKILL.md` 五處提及全是「讀」與「更新」，**沒有任何一步是比對儀表寫的與實際一不一致**（E1 的 05 推論：指不出行號）。
- ~~依 D3（理解全局 → 投影輸出），**就緒儀表可能根本不該是一支檔**，而是一個投影工具的輸出。若成立，`_is_declarative` 這個特例可以刪掉。~~ **✅ 10 已驗，假說只對一半**：三節「現況」投影出來 **306 字元 vs 128,371 ＝ 419× 壓縮**、輸入 100% 現成（**這一半成立**）；但全檔與其他檔的 8-gram 重疊合計僅 **≈10%**，**90% 是沒有別的家的獨有內容**（30 個輪次條目／68,776 字元），所以**「該是投影」不等於「該砍掉」**——先給日誌一個家（`裁決流.md`，04 已蓋好），再談形態。**`_is_declarative` 那一半 10 只縮掉「就緒儀表」，`結構` 留給 11。**
- **（← 04 重構輪追加）「⚠️N 筆待裁決回饋」的被動偵測規則實測 14 處字樣 vs 實際 0 筆。** **10 複量：現在是 26 處字樣 vs 實際 0 筆（`待裁決.md` 不存在），病徵翻倍**——04 改了規則（N 從 `decision-project` 讀）而**沒有配守衛**，那是「改了規則、沒有人驗證照做」的實測案例。 04 已把規則改成「**N 從 `decision-project` 的覆蓋率行讀出來，不要用人數的**」（`就緒儀表.schema.md` 與 `共同約定.md` 四已改），但**沒有動這支檔該不該存在**——那是 10 的題目。**另：一世之尊的書級通則原文住在 `就緒儀表.md`**（`角色/_index.ai.md:60` 明文指過去）。04 已經給書級方針一個家（`story/物件/全書.md`，型別 `方針`），**所以 10 若決定砍掉就緒儀表，那條通則有地方去了**——但一世之尊不遷移，它現在還在原地。
- **（← 07 診斷輪順手記下）同一筆駁回紀錄住在兩個地方、零比對**：`就緒儀表.md:145` 有一筆完整的四理由駁回（不加 `現代梗配額` 機讀欄），**同一筆的另一份**在 `風格.ai.md:60` 的 602 字元 blockquote 裡。依 F2 它的射程是全書設定契約 → `story/參照/裁決流.md`。**07 沒搬**（一世之尊是病例書），但 `derived-sync validate` 現在把 `.ai.md` 那一份算進「29 支檔有裁決日誌 blockquote」。
- **（← 07 診斷輪順手記下）`就緒儀表.md:147` 逐字記錄「刻意未動 `風格.ai.md` front-matter 五欄」**——那正是「基準沒跟著源檔更新」被寫下來卻沒人接手的痕跡（源檔那一輪新增了整節〈沉段的喜劇怎麼落〉，五個「機讀核對基準」一欄未動）。**07 新增的 `style-lint` 只驗值的形狀，驗不出「該更新而沒更新」**——那是跨時間斷言，同 14 記的那一族。
- **（← 01 診斷輪順手記下）`就緒儀表.md` 有 3 行超過 2,000 字（最長 10,038 字單行）**，並且其中 **26 處提到 🧊**——一支綜合檔在複述別條軸的狀態。那 26 處現在指向一個已廢除的落點（揭示層級改住 `story/物件/<名>.md`），這正是 D1「散文檔不該回答『全書關於 X 的一切』」的實例。

- ~~**（← 08 重構輪追加）本輪沒查就緒儀表對摘要的引用。**~~ **✅ 10 已查：沒有第二份**（就緒儀表 ↔ `00-摘要.md` 共同 8-gram 僅 295 個＝佔摘要 5.3%）。08 抉擇 6 的分流**不會製造孤兒**。
- ~~**（← 09 診斷輪順手記下·→ 10）就緒儀表提到「大綱」117 次**~~ **✅ 10 已查：確實有第三份，而且它與 `outline-lint` 直接矛盾**。含「大綱」的行 117 行、「已併入」10 次；`outline-lint` 的資訊行報「8 支標了『已併入』卻仍住在 `大綱/`」，而儀表寫的是「已併入」。09 已把大綱層那兩份收乾淨，**儀表這一份是第三份**（與 `幕綱/_index.md` 共同 8-gram 2,524／24.5%、與 `大綱/_index.md` 1,926／26.5%——**三份沿革在複述同一件事** → 12）。
- **（← 10 診斷輪追加·→ 11）`參照/結構.md` 58,546 B、2 行超過 2,000 字（最長 2,563），與就緒儀表共同 8-gram 3,907（佔結構自身 19.1%）**——兩支「宣告式綜合檔」在複述同一件事。**11 診斷時直接套 10 提的 A6**（無源衍生＝要解掉的衝突），別重新推導；`DECLARATIVE_STEMS` 在 10 之後只剩 `{"結構"}`，11 決定它的去留就決定整個特例能不能刪。
- **（← 09 重構輪追加·→ 11）`參照/結構.md` 帶著卷級方向的第二份**（與 `大綱/arc09.md` 檔頭 8-gram 重疊 **321 個**，`大綱/_index.md` 另有 239 個）。09 已給卷級方向一個有守衛的家（`## 卷級方向（卷X·本卷權威）`＋`outline-lint` 第 11 項的「同卷其餘檔只准指路」），**但那一項只掃大綱層的檔**——`參照/結構.md` 不在它的射程裡。11 要決定那一份是刪、是改成指標、還是另有職責。
- **（← 09 診斷輪順手記下·→ 11）5 支 skill 指名的檔名與書裡實際的檔名不同**：`outline-test/SKILL.md:14,42` 等處讀的是 `結構.ai.md`，而一世之尊書裡叫 `結構.md`（既有書相容規則吃得下，但**沒有任何東西驗證過這件事**）。`outline-lint` 第 9 項只驗大綱檔內指名的路徑，驗不到 SKILL.md 那一側。
  > **10 重構輪把這件事的前提改了**：「工具兩種命名都吃」這句話已從 `共同約定.md` 撤掉——實測它**只對 `long_lines` 成立**（`check`／`validate` 的入口是 `rglob("*.ai.md")`）。所以 11 面對 `結構.md` 時**沒有那條相容承諾可以靠**。
- **（← 10 重構輪追加·→ 11）`結構.ai.md` 現在是 `DECLARATIVE_STEMS` 裡唯一的一支，而 10 已經把判準寫成原則了**：**直接套 A6**（給重算規則／承認是源＋配 lint／拆成小源檔＋投影，三條路依序試），並套 **D3 的新推論**（逐格回答「這一格的機械來源是哪一支檔的哪一欄」）。10 走的是第三條路，**11 不必重新推導那個選擇，只要問結構表的每一格機械來源是什麼**。實測體積：`結構.md` **58,455 B ＝ `SOURCE_BYTES` 的 2.3×**（10 已納入哨兵，現在每次 `check` 都會報）。

### 12 · rollup 索引

**現行落點**：`_index.ai.md`／`_總覽.ai.md`／`tools/derived_sync/core.py` 的 rollup digest

**已知疑點**：
- rollup 的源＝同層所有源檔的 `檔名:內容hash` 排序後再 hash，機制本身健全。
- 真問題是 D1：rollup 是不是在回答「全書關於 X 的一切」？若是，它該是投影不是檔（同 10）。
- **（← 03 重構輪追加）本功能現在有兩個實測案例，病徵一致：索引檔的最後一欄變成無家內容的落腳處。** ① `chapters/_index.ai.md`：50,782 B、最長行 2,235 字元、備註欄 196→781 B/列（4.0×）、500 章外推 ≈381 KB；② `幕綱/_index.md` 的 arc 概覽（02 記的，11 行單行、最長 1,949 字）。**03 已經把 ① 的三類寄生內容清掉、納入哨兵、並在 `共同約定.md` 八登記成「尚無投影入口」的欠債**，但**刻意不裁它該不該存在**（抉擇 2 C）——那要與 10、12 一起判。診斷 12 時這兩支是主要語料。
- **（← 04 重構輪追加）第三個實測案例：舊回饋表的 `狀態` 欄。** schema 只允許一個值（`待裁決`），實測 7 列**平均 171 字元、最長 440、佔整列 61%**——人的裁決被塞進 AI 那一筆的最後一格。**三個案例的共同結構：一個窄欄位 ＋ 沒有別的地方可放 ＝ 欄位溢位。** 04 的處置不是「把枚舉守起來」而是**把那一格拿掉**（新的 `待裁決.md` 四欄無狀態欄），並確保它要裝的東西有家（理由→裁決流）。**這可能該升格成一條原則**：「schema 規定某欄只能是枚舉值時，必須同時指出非枚舉內容該去哪」——12 或 14 決定。`共同約定.md` 零 已先寫進「登記表一律不設狀態欄」。
- **（← 03 重構輪追加）如果 12 決定「rollup 該是投影」**，`chapters/_index.ai.md` 的替代品是 `ch-project`：章序完全可由 93 支 `chNNNN.ai.md` 的 `對應幕`／`所屬arc` 重算（`ch-lint` 第 8 項已經在做這個重算來比對視圖，等於**投影器的核心已經寫好了**）。要改的是 `write`／`write-test` 現在靠它反查 arc→chNNNN 的那兩處。
- **（← 05 診斷輪順手記下）第四個實測案例，但形態不同：`_總覽.ai.md` 是「整節寄生」而非「欄位溢位」。** 前三個（`chapters/_index.ai.md` 備註欄、`幕綱/_index.md` arc 概覽、舊回饋表 `狀態` 欄）溢出的單位是 **cell**；這一支溢出的單位是 **section**——`素材出處` 佔全檔 51%、`升格哨兵彙總` 29%，合計 80%。共同結構仍是「沒有別的地方可放」，但**12 若要立通則，得涵蓋這兩種粒度**。
- **（← 05 重構輪追加）`_總覽.ai.md` 不該與 10 的就緒儀表歸成同一類，判準是 hash 機制的有無。** 它**有**單一源（資料夾 digest）、能 stamp、`_is_declarative` 不適用它——所以它不是「假衍生檔」問題的實例。05 抉擇 4 A 據此**只把它變正常**（搬走 80% 寄居內容、三節改成指標）而不裁形態。**12 若要重新提案「它也該是投影」，要先推翻這個判準，不是重述形狀相似。**
- **（← 07 重構輪追加）第三支零實作的 rollup 承諾：`風格/_總覽.ai.md`**（前兩支：角色目錄形態 digest 已由 06 補、`_總覽` 的 digest 射程由 05 診斷）。**0 本書拆過多檔**，所以那段規格從未被任何工具或語料驗證過。07 **刻意不實作**（實作等於憑空設計），只在 `風格.schema.md` 那段旁**明標「零實作的行為承諾，第一本拆多檔的書要先驗」**＋補上 `ch-lint` 的目的地存在性（那條路徑一旦有人走，全書每章的 `風格` 欄會同時落空）。**12 若要盤 rollup，這是第三支語料。**
- **（← 05 重構輪追加）它同時是「hash 守錯對象」的唯一實測案例**：`derived-sync check` 對它報 fresh 是**正確的**（四支源檔的 digest 真的沒變），錯的是那個 digest **不覆蓋 80% 的檔內容**。rollup 的 digest 只吃「同層源檔集合」，所以 rollup 裡任何不是從源檔算出來的東西都在 hash 的射程外——**這對每一支 rollup 都成立**，是 12 的結構性問題不是 05 的個案。

- **（← 09 重構輪追加）第六個實測案例，而且形態是新的：`大綱/_index.md` 是唯一一支不帶 `.ai.md` 的 rollup。** 它有「視圖 ≡ 資料夾」的形狀，卻**沒有 hash、不能 stamp、`_is_declarative` 也不適用**（那個特例只認 `參照/` 底下兩支）。前五個案例（`chapters/_index.ai.md` 備註欄／`幕綱/_index.md` arc 概覽／舊回饋表 `狀態` 欄／`_總覽.ai.md` 整節寄生／`00-摘要.ai.md` 的 `終局` key 溢位）**全部是 `.ai.md`**。**05 立的判準（用 hash 機制的有無分類）在這一支上會把它分到「假衍生檔」那一堆去，而它連 `.ai.md` 都不是**——12 立通則時這是第六支語料，而且它會逼那把尺再細一格。數字：1,411 → 19,959 B（**14.1×**／5 天）而 arc 數只長 2.75×，11 列平均 823 字元、最長 1,828、6/11 超過 400，列序逆序。**09 只做了四件事**（納入 400 ／`outline-lint` 驗視圖＋列序／schema 明訂一列寫什麼／登記成欠債），**刻意不裁形態**。
- **（← 09 重構輪追加）`大綱/_index.md` 與 `幕綱/_index.md` 是同一支檔的兩層**（同一組 arc 編號、同一段沿革、同一種膨脹）。02 記的「`幕綱/_index.md` arc 概覽 11 行單行、最長 1,949」與 09 量的「11 列平均 823、最長 1,828」數字幾乎一樣——**兩份索引在複述同一件事**。12 裁形態時要一起裁。
- **（← 08 重構輪追加）第五種粒度：key 溢位。** 12 現在記了三個「欄位溢位」＋一個「整節寄生」，08 補上第五種——**根本不在 schema 裡的欄**：`00-摘要.ai.md` 的 `終局` 是某一輪重生自己長出來的 534 字元單行（該檔最長的 front-matter 行），活過至少 4 個版本，而 `validate` 的 `REQUIRED_KEYS` **只驗缺、不驗多**。12 若要立通則（「schema 規定某欄只能是枚舉值時，必須同時指出非枚舉內容該去哪」），得涵蓋這一格。

- **（← 10 診斷輪追加）第七個實測案例，而且它是「枚舉欄溢位」最極端的樣本 ＋ 一個前六個案例沒有的新事實：門檻量錯了單位。** `就緒儀表` 的產物軸表 **20 個狀態格中 7 格違反 schema 的四值枚舉**（空白／草擬／就緒／已鎖定），**最長 3,647 字元、溢位合計 8,450 字元**，備註欄佔（狀態格＋備註）的 **72.0%**、三列的備註欄超過 8,000 字元。**新事實**：`long_lines` 量的是**行**，病在**格**——`主線＋題旨＋基調` 那一行 832 字元（< 2,000 **不報**）而它含 107／92 字元的違枚舉狀態格。12 若要立通則（「schema 規定某欄只能是枚舉值時，必須同時指出非枚舉內容該去哪」），**要一併涵蓋「守衛的量測單位與病徵的單位不一致」這一格**——前六個案例（`chapters/_index.ai.md` 備註欄／`幕綱/_index.md` arc 概覽／舊回饋表 `狀態` 欄／`_總覽.ai.md` 整節寄生／`00-摘要.ai.md` 的 `終局` key 溢位／`大綱/_index.md`）都沒有暴露這一點。
- **（← 10 診斷輪追加）三份沿革在複述同一件事**：`就緒儀表` ↔ `幕綱/_index.md` 共同 8-gram **2,524**（佔後者 24.5%）、↔ `大綱/_index.md` **1,926**（26.5%）。09 已記「`大綱/_index.md` 與 `幕綱/_index.md` 是同一支檔的兩層」，**加上儀表是三層**。12 裁形態時三支一起裁。
- **（← 10 重構輪追加）10 已裁，而它立的判準對 12 是現成的。** 就緒儀表拆成「**小源檔 ＋ 投影**」——判準是 `設計原則.md` D3 同輪新增的推論：**逐格回答「這一格的機械來源是哪一支檔的哪一欄」，答不出來的那幾格才是真正的源**。實測 20 格裡 **12 格無機械來源**（它們留在源檔）、8 格有（4 格接得到、4 格跨套件未接）。**12 面對 `chapters/_index.ai.md`／`大綱/_index.md`／`幕綱/_index.md` 時可以直接跑同一個問題**——而 rollup 那三支的答案很可能是「每一格都答得出來」（`ch-lint` 第 8 項已經在重算章序來比對視圖），那就是「整支改成投影」與就緒儀表不同的地方。**別再提案「rollup 與就緒儀表是同一個問題」**：05 已用 hash 機制的有無否掉一次，10 的結論則是「**形狀相似但可重生的比例不同**」。
- **（← 10 重構輪追加）三份沿革現在剩兩份**：儀表那一份連同容器一起廢除（內容分流進 `裁決流.md` 或刪）。`大綱/_index.md` ↔ `幕綱/_index.md` 那兩份仍在，**12 裁形態時是兩份不是三份**。

### 13 · raw 分揀

**現行落點**：`.claude/skills/develop`／各書 `raw/`

**已知疑點**：
- F2 已定案：**未定的靈感 → `raw/`**。所以 raw 的定位是清楚的，重點是確認 `develop` 收尾有沒有履行 F1（明講這輪拍板了什麼、寫進哪支檔）。

### 14 · 檢查器與工具鏈

**現行落點**：`tools/derived_sync`（check／validate／stamp／sentinel）／`tools/fact_projection`（lint／refs）／`tools/decision_projection`／`tools/foreshadow_project`／`tools/beat_metrics`／`tools/prose_metrics`／`tools/settings_select`

**已知疑點**：
- E1 盤點：把每一條 schema 宣稱的格式，對上「誰守、何時跑」，列出無人守的。（~~幕綱全部機讀語法、`_index.md` spine~~ 已由 02 的 `beat-lint` 接手；~~正文幕錨點~~ 已由 03 的 `ch-lint` 接手；~~`裁決流`~~ 已由 04 的 `decision-lint` 接手。）**目前已知無人守的只剩「跨時間斷言」那一類**（見下）。
- B1：檢查器要分成「守人破結構」與「守 AI 破克制」兩類，現行 `validate` 與 `fact-lint` 已隱含這個分工，該顯性化。
- 成長哨兵量的是 bytes，依 D2 該改量**查詢成本**。
- 工具間刻意零相依（`dependencies = []`），代價是 `parse_spine` 有三份實作。重構時要決定這個取捨是否維持。**（01 重構輪後：spine 4 份、伏筆 regex 3 份，共 6 份複製。）**
- **（← 01 診斷輪順手記下）訊號強度與後果不成比例**：`fact-project` 對舊格式書只印一行 stderr 就整本切換行為模式，而後果是 206 個檢查全關。**01 已修掉這個特例**（世代改逐行判），但同一個病徵值得在 14 全面盤一次：**還有哪些「一支檔存在與否改變整本書行為」的開關？**
- **（← 01 重構輪追加）E1 盤點的「無人守」清單少了三項**：事實行的幕號落錯章、物件檔的格式、揭示層級語法（皆已由 `fact-lint`／`object-lint` 接手）。**新增一條跨工具通則要在 14 推廣**：`設計原則.md` E2 現在要求每個檢查器都能回答「我在這本書上檢查了幾筆」——`fact-lint`／`object-lint`／`foreshadow-project`／`beat-lint`（02）／`ch-lint`（03）／**`derived-sync validate`／`decision-project`／`decision-lint`（04 重構輪）**已兌現，**其餘還沒**（`beat-metrics`／`prose-metrics`／`settings-select`）。
- **（← 01 重構輪追加）成長哨兵的 `APPEND_LOG_STEMS` 移除了 `約束`／`約束.co`**（落點廢除）。哨兵量 bytes 而非查詢成本這件事沒動，仍是 14 的題目。
- **（← 03 重構輪追加）「這本書有哪些受管檔」該有一份**：成長哨兵四支函式的目標集**各自硬寫路徑**（`sentinel.py` 的 `beat_sheet_density`／`oversized_sources`／`_derived_targets`／`long_lines`），03 之前 `chapters/` 只被其中一支（`bloated_fact_lines`）掃到——**不是刻意豁免，是四份手寫清單漏了一個資料夾**，後果是 `derived-sync check` 對 50,782 B 的檔印「0 個需處理」。03 已補上 `chapters/`，但**根因（四份清單）沒動**。
- **（← 03 重構輪追加）E2 的嚴重度尺缺一格：「守衛全報，於是沒人看」（警報疲勞）**。兩個實測案例：① `prose-metrics` 的行文節奏四項絕對門檻在一世之尊 **11/11 個 arc 全中**（41 條可疑點＝全書 62 條的 66%）；② `ch-lint` 的三條格式收斂在病例書上共 **120 支檔／列**命中——後者已用「聚合成一行」處理（駁回理由寫進 `02-待用構想.md`），前者仍待 14 決定輸出策略。**它與 E2 最後兩格的機制不同**（不是假陰性），值得在 14 決定要不要補這一格。
- **（← 04 重構輪追加）目的地存在性檢查現在有兩份實作**（`beat_metrics/lint.py` 與 `derived_sync/sentinel.py:missing_destinations`），零相依政策下的**第 7 份複製**（spine 4、伏筆 regex 3）。04 的那份是把 02 的形狀推廣到 `derived-sync check` 的 hint（實測一世之尊：43 筆→裁決流、30 筆→待裁決、31 筆→物件目錄，全部指向不存在的檔）。
- **（← 04 重構輪追加）E2 的嚴重度尺缺的那一格有第三種形態：「報得沒錯但照做會失敗」。** 前兩個案例是「報得太多」（`prose-metrics` 11/11 全中）與「報得太碎」（`ch-lint` 120 條）；這個是**建議本身正確、但它指的目的地不存在**（實測 37→43 行）。三者都導致人不看，機制不同。04 已補上守衛，但**這一格要不要正式進 E2 的尺**由 14 決定。
- **（← 04 重構輪追加）F2「集中 log 不會自然被載入」只補掉一半** → 要不要讓 `fact-project --for-beat` 順帶吐出生效中的裁決。它牽動兩個零相依套件（`fact_projection` 得複製一份裁決流解析），而且會把「理由」塞回 `write` 的 context（正是 04 診斷出的病）。04 作者拍板不做，**誠實記錄為未補**。
- **（← 03 重構輪追加，04 追加第三條）「跨時間斷言」這一類承諾在現行架構裡原則上不可檢查**，而 schema 寫了三條：`revise` 的「修訂前後錨點集合必須相同」（03 已改寫成無狀態不變式）、`chNNNN.ai.md` 的「`POV`／`風格`／`狀態` 重生前後未變」（03 只守得住一半）、**04 的「回饋列離開 ⇄ 裁決軸 append 是同一個原子動作」＋「裁決流 append-only」**（兩個方向都試過改寫成無狀態不變式、都不成立，改交出兩個近似：`decision-lint` 的「有待裁決列卻沒有裁決流」＋覆蓋率行並排印「待裁決 N 列／裁決 M 列」）。系統沒有「上一版」的概念（git 歷史不是系統的一部分）。**14 該決定：要嘛給衍生層一個「上一版」，要嘛把這一類承諾一律改寫成無狀態不變式。**
- **（← 05 重構輪追加）`long_lines` 的目標集是第五份手寫路徑清單漏檔的案例**：03 補了 `chapters/`，05 補了 `story/設定/<kind>/` 的**非 rollup** 檔（衍生與源）。**證據這兩次都不是刻意豁免**：同一支 `sentinel.py` 裡的 `unsliceable_derived` 早就在掃設定層的衍生檔**大小**，只有行長那一份漏了它；於是 1,155 字元的單行（rollup 門檻的 2.9×）與同資料夾的 `_總覽.ai.md`（一直在報）待遇不同，差別只在檔名有沒有以 `_` 開頭。**根因（四份手寫清單）仍然沒動**，這是它第二次被同一個機制咬到。
- **（← 05 重構輪追加）第 8 份同類複製**：`world-lint` 要的 `[[伏筆:x]]` regex ＋掃幕綱、以及「rollup 視圖 ≡ 各檔 front-matter」的形狀（同 `ch-lint` 第 6 項），在零相依政策下在 `derived_sync` 又複製了一份（spine 4、伏筆 regex 3→4、目的地存在性 2、rollup 視圖比對 2）。**05 刻意選了「同套件新增指令」而非新開 `tools/world_lint/`**（理由：世界觀 `.ai.md` 的格式真相已經在 `validate.py`，拆出去等於一支檔兩個擁有者），但那只省掉 front-matter 解析那一半。
- **（← 05 重構輪追加）E2 的嚴重度尺缺的那一格有第四種形態：「守衛守錯對象」。** 前三種是報得太多（`prose-metrics` 11/11 全中）／報得太碎（`ch-lint` 120 條）／目的地不存在（43 行指向空氣）。這一種是：`derived-sync check` 對 `_總覽.ai.md` 報 fresh **完全正確**——它守的 hash 真的沒變，錯的是那個 hash **與檔的內容不重疊**（80% 不在 digest 的輸入裡）。**它比前三種更危險，因為它連「有東西沒被守」的暗示都不給**：前三種至少有輸出可看，這一種的輸出是 `[ok] fresh`。要不要正式進 E2 的尺由 14 決定。
- **（← 06 重構輪追加）第 9／10 份同類複製，而且「三份 rollup 視圖比對」是最值得先收斂的一組。** `char-lint` 帶進兩份：`[[伏筆:x]]` regex ＋掃幕綱（**第 5 份**）、「rollup 視圖 ≡ 各檔 front-matter」（**第 3 份**）。**三份 rollup 比對的差別只有欄名**——`ch-lint` 第 6 項對 `chapters/_index.ai.md` 比六欄、`world-lint` 第 4 項對 `_總覽.ai.md` 比兩欄、`char-lint` 第 5 項對 `角色/_index.ai.md` 比列名＋三欄；`_section_body`／`_table_rows`／`_front_matter` 三個 helper 在 `world_lint.py` 與 `char_lint.py` 幾乎逐字相同。**一個「rollup ≡ 資料夾＋各檔 front-matter」的參數化 helper（欄名對照表當參數）會一次收掉三份。** 另加**第 2 份佔位偵測**：`char-lint` 第 1 項與 `settings_select.is_hollow` 各一份（E2 新推論要求覆蓋率行印空殼數，而 selector 不能 import lint）。
- **（← 06 重構輪追加）E2 嚴重度尺的第五種形態：「三個守衛各自正確，合起來仍指向空的東西」。** 前四種是報得太多／報得太碎／目的地不存在／守錯對象。這一種**沒有任何一支守衛做錯**——`check` 該報 fresh、`validate` 該只驗結構、`settings-select` 該只管選取；病在**沒有人負責問「選出來的東西有沒有內容」**。**06 已把它升成 `設計原則.md` E2 的可執行推論**（覆蓋率行要能回答「命中的裡面幾筆是空的」），並在 `char-lint` 與 `settings-select` 兩處實作。要不要連同第四種形態一起正式寫進 E2 的表由 14 決定。
- **（← 06 重構輪追加）「零實作的格式承諾」值得在 14 做一次全 repo 清查。** 實測：`角色.schema.md` 寫著「rollup 的 digest 對目錄形態取其下所有切面檔的集合」，而 `derived_sync` 從來沒有實作過——目錄形態的 `<名>.ai.md` 會被 `check` 報成 `orphan`、`stamp` 直接 raise、改切面不會讓 rollup stale。**它在 schema 裡形式完全合法，而且 0/24 實測所以三輪盤點都沒撞到。** 這與 E1 的「消費者承諾」（欄位有沒有人讀）、「目的地承諾」（箭頭指向的檔在不在）是同一族的第三種：**行為承諾（schema 宣稱工具會這樣做，而工具沒有）**。查法是機械的——schema 裡每一句描述工具行為的話，去那支工具找對應的那一行。
- **（← 07 重構輪追加）第 11／12 份同類複製**：`style-lint` 帶進第 **4** 份 front-matter 解析（`validate`／`world_lint`／`char_lint` 各一）與第 **4** 份跨產物欄位比對（`基調參照` ≡ `00-摘要.ai.md` 的 `基調`，形狀同 `world-lint` 第 4 項／`char-lint` 第 5 項／`ch-lint` 第 6 項）。06 記的「一個參數化的 rollup≡資料夾 helper 會一次收掉三份」現在是**四份**（第四份的兩邊不是 rollup 與資料夾，而是**兩支不同產物的 front-matter 欄**——收斂時要涵蓋這個變體）。
- **（← 07 重構輪追加）E2 尺的第六種形態：「輸出正確而基準不存在」。** 前五種是報得太多／報得太碎／目的地不存在／守錯對象／三個守衛各自正確但合起來指向空的東西。這一種是 `prose-metrics` 對 11 個 arc 印四項輔助量共 **44 個數字**、並在腳註寫「供對照 `可用句式` 宣告用」，而**那個宣告是空的**——**印了完全正確、完全無法使用的數字**。07 已補上基準那一側的閘門（`style-lint` 第 4 項），但**新書才吃得到**；要不要正式進 E2 的表由 14 決定。
- **（← 07 重構輪追加）`風格` 曾是唯一「有 `SETTINGS_KINDS` 身分卻沒有 `DERIVED_SECTIONS` 條目」的產物**（`validate.py:39-69` 兩份清單各自維護、零一致性檢查——同「四份手寫路徑清單」的病，第七次）。07 補上 `風格` 之後**只剩 `摘要`**，但**根因（兩份清單沒有一致性檢查）沒動**：`SETTINGS_KINDS` 多一種產物時，沒有任何東西會提醒你去補枚舉。**一支「每個 `SETTINGS_KINDS` 都要有 `DERIVED_SECTIONS` 條目」的檢查是 3 行**，形狀同 06 立的 E1 推論（從 X 側掃）。
- **（← 07 重構輪追加）`設計原則.md` §六 C 記的成長軸本輪刻意不改原則**：D2 的資格條款是「有選擇器**且**查詢成本不隨全書長度線性成長」，而 `風格.ai.md` 實測 5 次重生從 611 B 長到 12,247 B（**20.04×**）、成長軸是**裁決輪次**不是章數——照 D2 字面它合格。這與 05 的 `_總覽.ai.md`（寄居內容）、06 的角色衍生檔（9/22 支源檔一次沒改而衍生長了 1.30–2.08×）是**同一個軸的三個實例**。作者拍板本輪不改原則（三個實例的直接病因都是無家內容寄居，而 A3 第 0 條已經在管），**但 D2 若在 14 重新表述成本函數，這個軸要進去**：「不隨書長成長」不等於「有界」。
- **（← 03 重構輪追加）`prose_metrics` 有 6 支測試長期 fail**（`test_rhythm.py` ×5、`test_exposition.py` ×1），原因是它們依賴 repo 裡沒有的語料：`芯片巫師/chapters`（該書只有 `raw/`）與 `Data/` 參照語料。**03 已用 `git stash` 對照確認不是本輪造成的。** 它們 fail 而不是 skip 是對的（E2：會自己 skip 的測試就是報平安的守衛），但一個永遠紅的測試套件與關掉的測試套件效果相同——同上一條的警報疲勞。

- **（← 08 重構輪追加）「四份手寫路徑清單漏檔」的第六次，而且 `validate.classify()` 是第七次同根因**：`sentinel.py` 四支目標集 **0/4** 含 `story/00-摘要.*`（前五次：03 補 `chapters/`、05 補設定層衍生與源）。**這次漏的不是資料夾，是 `story/` 根目錄下的兩支檔**；而 `classify()` 的路徑判斷（只認 `chapters/` 與 `story/設定/<kind>/`）是**同一個根因在另一支檔裡**——它讓摘要衍生檔整段開發期只驗 front-matter，而 04 從五處移除的 `## 待裁決回饋` 就在裡面活著。08 各補各的、**根因不修**：統一的「這本書有哪些受管檔」清單仍在這裡等。
- **（← 08 重構輪追加）`validate` 只驗 front-matter「缺」不驗「多」**，這對每一支 `.ai.md` 都成立。**`終局` 是第一個實測證據**（534 字元、schema 未定義、零消費者、活過至少 4 個版本）。08 抉擇 7 把「front-matter 不得有該產物 schema 外的鍵」**交給這裡**：它是全產物通用的，在摘要那一輪做等於替別的功能拍板，而且它會製造**第三份**與 `DERIVED_SECTIONS`／`SETTINGS_KINDS` 平行的清單。
- **（← 08 重構輪追加）複製份數更新**：`summary-lint` 帶進**第 5 份 front-matter 解析**（`validate`／`world_lint`／`char_lint`／`style_lint` 各一）、**第 5 份跨產物欄位比對**、**第 5 份目的地存在性檢查**（幕號·arc 引用），以及 `beat_metrics.scan._POV_RE` 的第 2 份。⚠️ **第 5 份跨產物比對的形狀與前四份都不同**：它比的是**源檔散文 ↔ 衍生欄**（前四份都是欄↔欄或欄↔資料夾），所以 07 記的「一個參數化 helper 會一次收掉四份」**收不掉這一份**。
- **（← 09 重構輪追加）「四份手寫路徑清單漏檔」的第七次，而且這次漏的是一整個產物軸。** `sentinel.py` 四支目標集 **0/4** 含 `story/大綱/`——不是資料夾（03 的 `chapters/`）、也不是根目錄下的兩支檔（08 的摘要），是 **repo 裡體積第三大的軸**（302,669 B，僅次於幕綱與正文）。實測 6/12 支超過 25,000 B（最大 44,307＝**1.77×**）、7 支有超過 600 字元的單行（最長 1,324）**全部靜音**，而同一次 `check` 卻報了 32,650 B 的 `血刀頭陀.md`。**09 各補各的、根因不修**：前七次補完之後 `story/` 底下只剩 `參照/`（部分）在名單外；統一的「這本書有哪些受管檔」清單仍在這裡等。
- **（← 09 重構輪追加）第 6 份「rollup 視圖 ≡ 資料夾」比對 ＋ 第 6 份目的地存在性 ＋ 第 6 份伏筆 regex。** 前五份 rollup 比對：`ch-lint` 第 6 項／`world-lint` 第 4 項／`char-lint` 第 5 項／`style-lint` 第 6 項／`summary-lint` 第 5 項。**09 這一份的兩邊又是新組合**：**Markdown 清單列 ↔ 資料夾**（不是表格列、不是 front-matter、也不是欄↔欄）——06 記的「一個參數化 helper 會一次收掉三份」現在是**六份，而每加一份就多一種兩邊的形狀**，收斂時要涵蓋這個變體。**第 7 份幕號 registry 解析因為抉擇 1 B 而省下來了**（`outline-lint` 與 `beat-lint`／`ch-lint` 同套件、共用 `structure.parse_book`）——那正是選 B 的唯一理由。
- **（← 09 重構輪追加）D2 的第五票，而且是第一個「選擇器完全健康而成本仍線性」的案例。** 前四票（05 `_總覽.ai.md`／06 角色衍生檔／07 `風格.ai.md` 20.04×／08 摘要 10.6×）的病因都能歸到「無家內容寄居」或「沒有選擇器」。**大綱不同**：選擇器是**檔名**（C1 的正例）、覆蓋率 **100%**、命中依據是 arc 編號而不是散文裡的字串——**它是全系統最健康的選擇器之一**，而查詢成本仍從 4,848 B 長到 44,307 B（9.1×／11 arc），因為**成長發生在被選中的那一份裡面**（後面的 arc 天生要背前面所有 arc 的伏筆帳、排除線與卷級方向）。它的成本不隨**全書**長度成長（讀 arc03 永遠只讀 arc03），卻隨 **arc 序號**線性成長。**作者拍板 09 不改 D2**，但這一票是最乾淨的一份證據（選擇器這個變數在這裡被控制住了）：**D2 現在量的是「一次查詢讀幾支檔」，而該量的是「一次查詢讀幾個位元組」。**
- **（← 08 重構輪追加）摘要是全系統讀寫比最懸殊的產物**（12 支 skill 中 10 支純讀、2 支寫），也是**唯一每支 skill 都無條件整檔讀的源檔**。它是 D2 成長軸問題的**第四個實例**（前三：05 `_總覽.ai.md`、06 角色衍生檔、07 `風格.ai.md` 20.04×），而且是**最乾淨的一份證據**——它沒有 rollup、沒有 arc 分檔、沒有任何「因為書變長所以它變長」的機制，純粹靠拍板輪次長了 10.6×。**D2 若重新表述成本函數，這是第四票。**

- **（← 10 診斷輪追加）「四份手寫路徑清單漏檔」的第八次，而且這次漏的是全書最大的一支檔。** 三支體積哨兵目標集（`beat_sheet_density`／`oversized_sources`／`unsliceable_derived`）**0/3 含 `story/參照/`**，於是 `就緒儀表.md` **292,591 B**（`DERIVED_BYTES` 的 **24.4×**、`SOURCE_BYTES` 的 11.7×、模板骨架的 249×）**完全靜音**——它比次大的 `狀態事件流.md`（111,178）大 2.6 倍，佔全書 2,589,923 B 的 **11.3%**。`long_lines` 是唯一掃到它的一支（只量行長）。前七次：03 `chapters/`、05 設定層衍生與源、08 摘要兩支、09 整個大綱軸。**補完之後 `story/` 底下只剩 `參照/` 的一部分在名單外，而那正是本輪這一支。**
  > **10 重構輪已補**（`sentinel._reference_sources`：`story/參照/*.md` 排除 `APPEND_LOG_STEMS`，門檻沿用 `SOURCE_BYTES` 不新造）。實測哨兵 68 → **70**（＋`就緒儀表.md` 292,591 B、＋`結構.md` 58,455 B）。**這是第八次各補各的，根因（每支函式各自手寫路徑）仍在這裡等**；補完之後 `story/` 底下已無已知的漏網目標。
- **（← 10 診斷輪追加）E2 尺的第七種形態：「豁免的射程比它的理由大」。** 前六種是報得太多／報得太碎／目的地不存在／守錯對象／三個守衛各自正確但合起來指向空的東西／輸出正確而基準不存在。這一種是：`_is_declarative` 因為**一個理由**（無單一源可 hash）而在 `validate_file` 早退 **三次**（front-matter 必填鍵 `:232`／裁決 blockquote `:226`／節枚舉與 `fm_only` 計數 `:249`），**其中兩項與 hash 完全無關**。它與「守錯對象」不同——那一支是守衛守的東西不覆蓋病灶，這一支是**守衛被明確關掉了，而關它的人只想關掉三分之一**。**可執行推論（提案）**：**每一個豁免要寫出它豁免的是哪一項檢查，不能豁免「這支檔」**——`_is_declarative` 該長成 `skip_hash(p)`。
  > **10 重構輪已把這一格與推論寫進 `設計原則.md` E2**（第七種形態）。**實作沒做**：本輪只把 `DECLARATIVE_STEMS` 縮成 `{"結構"}`（`就緒儀表` 已廢除），predicate 的形狀原封不動——改它牽動 11 還沒裁的 `結構.ai.md`。**14 動手時，它只剩一支檔要考慮。**
- **（← 10 診斷輪追加）「兩種命名都吃」是半真的相容承諾，而半真比沒有更糟。** `共同約定.md:106` 與 `就緒儀表.schema.md:5` 都寫「既有書仍用舊名亦可，工具兩種命名都吃」——實測**只對 `long_lines` 成立**（它用 `_base_stem` ＋ `glob("*.md")`）。`check_book`／`validate` 的入口是 `rglob("*.ai.md")`，**掃不到舊名**（實測 `check_book(一世之尊)` 對 `story/參照/` 回 **0 支**，而 `validate` 的覆蓋率行印「**0 支宣告式**」——那個 0 讀起來像「這本書沒有這種檔」）。**14 該把「相容承諾」也納入 E1 的清查**：schema 說「工具都吃」時，要指得出每一支工具吃它的那一行。
  > **10 重構輪把那句話撤掉了**（`共同約定.md` 二 現在明說它只對 `long_lines` 成立），但**只撤了就緒儀表那一半**——`結構` 那一支既有書仍可能叫 `結構.md`，那個狀態現在是**已登記的欠債**（→ 11）。清查本身仍在這裡等。
- **（← 10 診斷輪追加）`readiness` 會是 repo 裡第一個跨工具聚合器，零相依政策第一次被逼到牆角。** 10 抉擇 1 B 的投影要讀七支工具的結果，其中 `outline-lint`／`beat-lint`／`ch-lint` 在 `beat_metrics`、`decision-project` 在 `decision_projection`、其餘在 `derived_sync`。零相依（`dependencies = []`）下只有兩條路：**subprocess 呼叫別的 `uv run`**，或**再複製一份**。前十輪的複製都是「一個 helper 的第 N 份」，這一個是**整支工具的輸出**——性質不同，14 該一起裁。
  > **10 重構輪的處置是「不接、但把欠債印出來」**（抉擇 6 A）：同套件的四支 lint ＋ `check_book` 直接 import；**跨套件那四支印「未接」＋該跑的指令**，不 subprocess、不複製。實測一世之尊：**20 格中 4 格未接 ＋ 就緒清單 6 條中 2 條未接 ＋ 待裁決那一行未接**。**那些「未接」是刻意印出來的儀表**——14 裁零相依政策時，它是第一份具體的價碼。另：`readiness._chapter_arcs` 是 `所屬arc` 的**第 2 份解析**（第 1 份在 `ch-lint`）。
- **（← 10 診斷輪追加）「零留痕的守衛」等於沒有守衛，而系統原則上量不到它。** `write/SKILL.md:81` 的就緒提醒（non-blocking）是**對話行為、不落檔**，所以「它在 93 章裡實際擋下過幾次」在原則上無法回答——10 抉擇 3 C 因此無法評估代價。這與已記的「跨時間斷言」同一族：系統沒有「上一版」，也沒有「上一次執行」。
- **（← 10 診斷輪追加）`sync/SKILL.md` 有兩個步驟 7**（「彙報形態走樣（成長哨兵待辦）」與「更新就緒源檔」），編號重複。**10 重構輪改了那一步的內容、刻意沒改編號**（那是 14 的清查標的，不是就緒軸的事）。

### 15 · skill 的 context 取用契約

**現行落點**：`共同約定.md` 八／各 `SKILL.md` 的讀檔步驟

**已知疑點**：
- 硬規則「不得整檔讀成長型產物」已存在，但要重新對照 D1／D2 檢查每個入口是否還成立。
- 前面 14 個功能改完後，這裡的查詢入口表要整份重寫。
- **（← 05 診斷輪順手記下）設定層有兩套互不相通的入口。** 12 支 skill 裡 6 支仍有 `設定/世界觀/*.ai.md` wildcard，其中 `beat-test`／`character` **零 `settings-select`**（等於整軸全撈：32,965／51,225 字元）。而 5 支 SKILL.md（`organize`／`beat-sheet`／`expand`／`character`／`develop`）宣稱「先讀 `_總覽.ai.md` 索引挑實體」——**`settings-select` 明文跳過 `_` 開頭的檔**（`select.py`），所以查詢入口表登記的入口與 SKILL.md 宣稱的入口是兩套。05 已把這件事寫進 `共同約定.md` 八 那一列。
- **（← 05 重構輪追加）遷移路徑步驟 6（wildcard 收斂成 `settings-select`）05 刻意不做，理由要記住**：現況下世界觀 4 個主題有 3 個**只靠幕綱散文裡的檔名註腳**被選中（裸提及率 0），所以把 `beat-test`／`character` 從 wildcard 改成 `settings-select`，會把「讀太多」換成「**當場少讀三份核心規則**」。收斂的前置條件是先修選擇器（05 抉擇 2 的 C 案），而那個依賴 `_總覽` 真的縮小。**新的覆蓋率行（`settings-select` 一律印「世界觀命中 N 筆，其中 M 筆僅因檔名被引用」）就是判斷時機的儀表。**
- **（← 06 重構輪追加）角色軸的 wildcard 收斂也刻意不做，但前置條件不同。** `character/SKILL.md` 的三處 wildcard 實測讀 **353,297 B**（源 135,435＋衍生 182,815＋rollup 35,047），是本 repo 讀設定層最重的一處。角色這一側的**選擇器比世界觀健康**（比對的是幕綱角色欄這個正式宣告，不是散文裡的檔名註腳），所以收斂的前置條件**不是「修選擇器」而是「先修空殼」**：在 12/24 支還是佔位時收斂，`character` 會從「讀 353 KB 含 12 支空殼」變成「讀 7.9 支含 4 支空殼」——**空殼比例不變而總量下降，看起來像改善**。**先修內容，再收入口。** 判斷時機的儀表是 06 新增的那一格：`settings-select` 印「角色命中 N 筆，**其中 M 筆是空殼**」，M 歸零才談收斂。
- **（← 10 重構輪追加）就緒那一列已經補進查詢入口表了，形狀與風格軸同型。** 它在 10 之前**根本不在那張表裡**——292,591 B、6 支 skill 整檔讀、零投影、零欠債登記，而**全系統最壞的讀取比在這裡**：`write/SKILL.md` 步驟 2 每寫一章讀 292,591 B 取 ≈50 字元（**≈5,850:1**），已執行 93 次。10 之後那一列寫的是**「源 `story/參照/就緒.md` 整檔讀（< 1,500 B）＋ `readiness` 投影」**（同 07 為風格立的「兩邊都讀」）。**15 重寫入口表時照抄即可，不必重新設計。**
  > 它同時是「宣稱的入口與實際的入口不一致」那一族的第三個（前兩個：05 的設定層、09 的大綱層），**而它比前兩個更糟——它根本不在表裡**。三個都已各自處置，15 要問的是**還有沒有第四個**（查法是機械的：grep 12 支 SKILL.md 讀的每一支書內檔，對照那張表）。
- **（← 07 重構輪追加）風格軸對 15 的貢獻不是「收斂 wildcard」而是「縮小被無條件載入的那一份」。** 它**沒有選擇器也不該有**（單檔），而 `write`／`write-test`／`revise` 每次落筆／審稿／修訂都整檔載入 12,247 B、其中 42.8% 是無家內容。07 抉擇 5 B 把衍生檔縮到只剩 front-matter（新書約 1 KB，**−92%**），代價是**四支 SKILL.md 改成兩邊都讀**（源散文＋衍生五欄，形狀照抄 `settings_select.Entity.read_paths`）。**15 重寫查詢入口表時，風格那一列要寫成兩支檔、不是一支。**
- **（← 09 重構輪追加）大綱層是查詢入口表上第二個「宣稱的入口與實際的入口不一致」的軸**（第一個是 05 記的設定層）：`共同約定.md` 八 在 09 之前**根本沒有大綱那一列**，而零 的表格把它登記成「逐行／逐欄解析＋一支 lint」——實際上零工具、6 支 skill 全部整檔讀。**09 已補上那一列**（含 `01-大綱.md`＋`arcNN.md` 與 `_index.md` 兩列），但 15 重寫入口表時要注意兩件事：① 大綱層的選擇器是**健康的**（檔名 `arcNN`，覆蓋率 100%），所以這裡的題目不是「收斂 wildcard」而是**「被選中的那一份自己在長」**（D2 第五票，見 14）；② `大綱/_index.md` 是**已登記的欠債**（尚無投影入口），形態隨 10／12 裁。
  > 那個「兩邊都讀」現在是設定層**第二個**這樣的軸（角色是第一個）。若 15 要立通則，判準是：**衍生檔只放源做不到的事**（角色＝需求四象限分析、風格＝五個機讀欄），**其餘一律留在源檔、由下游一起讀**——07 就是拿這把尺量出「風格本體是同長度改寫」而廢掉它的。
  > 角色側殘留的兩個選擇器缺口（**只量不修**）：2/24 從未被任何幕的角色欄選中（`無嗔` 是反派、`真德`——兩者 `mentioned_only` 都有報）、集合名 `同伴組`×14 展不開（`char-lint` 第 8 項現在會報，**已駁回建對照表**）。
- **（← 10 診斷輪追加）就緒儀表是查詢入口表上第三個「宣稱的入口與實際的入口不一致」的軸，而且比前兩個更糟——它根本不在那張表裡。** 前兩個是 05 的設定層與 09 的大綱層（至少被登記過）。**292,591 B ＝ 全書最大的一支檔、6 支 skill 整檔讀、零投影、零選擇器、連「已登記的欠債」都不是**。**最壞的讀取比在這裡**：`write/SKILL.md:81` 每寫一章要查「該 arc 的幕綱欄」＝**讀 292,591 B 取 ≈50 字元（≈5,850:1）**，已執行 93 次。15 重寫入口表時，這一列若照 10 抉擇 1 B 就要寫成「**一支 <1,500 B 的源 ＋ 一個 `readiness` 投影指令**」，形狀同 07 為風格立的「兩邊都讀」。

---

## 兩個貫穿全程的檢查

跑每個功能時順手記，最後彙總：

1. **E2 的最後一格**——這個功能有沒有「壞了永遠不會發現」的東西？有的話單獨列出，那是最高優先。
2. **數字**——這個功能在 `一世之尊/` 上的量是多少？（幾份、每份幾行、一次查詢讀幾筆、成長率）沒量過的就去量。
