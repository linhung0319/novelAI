# 重構盤點：功能清單與測試順序

> **這是什麼**：把系統拆成 15 個**功能**（不是 15 支檔），逐一用 `結構定義/設計原則.md` 的六問＋原則 A–G 檢測，產出診斷報告；作者拍板後才重構。
> **怎麼跑**：每個功能開一個新 session，貼 `03-每輪-prompt.md` 的 prompt。每個功能兩輪：**診斷輪**（只提案不動手）→ 作者拍板 → **重構輪**（動手）。
> **為什麼一功能一 session**：context 集中，且診斷報告本身就是跨 session 的交接物。

---

## 進度表

狀態：`未開始` → `已診斷`（報告已產出，待作者拍板） → `已拍板` → `已重構`

| # | 功能 | 狀態 | 報告 |
|---|------|------|------|
| 01 | 狀態物件軸（事實流：狀態／錨／約束／伏筆／水下） | **已重構** | [功能報告/01-狀態物件軸.md](功能報告/01-狀態物件軸.md) |
| 02 | 幕綱 | **已重構** | [功能報告/02-幕綱.md](功能報告/02-幕綱.md) |
| 03 | 正文章節 | **已重構** | [功能報告/03-正文章節.md](功能報告/03-正文章節.md) |
| 04 | 裁決與回饋 | **已重構** | [功能報告/04-裁決與回饋.md](功能報告/04-裁決與回饋.md) |
| 05 | 設定層·世界觀 | **已重構** | [功能報告/05-設定層-世界觀.md](功能報告/05-設定層-世界觀.md) |
| 06 | 設定層·角色 | **已重構** | [功能報告/06-設定層-角色.md](功能報告/06-設定層-角色.md) |
| 07 | 設定層·風格 | 未開始 | |
| 08 | 摘要 | 未開始 | |
| 09 | 大綱 | 未開始 | |
| 10 | 就緒儀表 | 未開始 | |
| 11 | 結構對應表 | 未開始 | |
| 12 | rollup 索引（`_index` / `_總覽`） | 未開始 | |
| 13 | raw 分揀 | 未開始 | |
| 14 | 檢查器與工具鏈 | 未開始 | |
| 15 | skill 的 context 取用契約 | 未開始 | |

---

## 順序的依據

不是隨意排的，是**依賴驅動**：

- **01 先跑**，因為原則 G 全部落在它身上，而且它一旦定案，會連帶決定 04（裁決／約束落點）、02 的伏筆軌、03 的錨、以及 `🧊 水下` 標記——**一個功能定案，四個功能跟著定案**。
- **02、03 次之**，它們是 01 的兩條時間軌（計畫軌／已成軌）的來源，且是目前已知違反最嚴重的兩處。
- **05–09 是設定與劇情層**，多數已經符合 A（前向：散文源＋衍生），預期是快速驗證而非大改。
- **10–12 是綜合視圖**，必須等前面的投影輸出定案才知道它們該不該存在。
- **14 倒數第二**：檢查器要等格式定案才設計得出來。**先寫檢查器＝把還會變的格式釘死。**
- **15 最後**：它是前面所有東西的消費端。

---

## 各功能的現行落點與已知疑點

> 「已知疑點」是 2026-07-26／27 對話中已經確認的事，寫在這裡是為了**讓新 session 不必重新發現**。它們是線索，不是結論——診斷輪仍要自己驗證。

> 下面標「**（← 01 診斷輪順手記下）**」的是跑功能 01 時撞到、但屬本功能的疑點，未經本功能自己驗證。

### 01 · 狀態物件軸　**已重構（2026-07-27）**

**現行落點**：`結構定義/物件.schema.md` ＋ `story/物件/<名>.md`（物件定義／約束／揭示層級）／`結構定義/事實流.schema.md` ＋ `chapters/chNNNN.ai.md` 的 `## 本章事實`（狀態／錨）／幕綱裡的 `埋|收[[伏筆:x]]`／`tools/fact_projection/`（`fact-project`／`fact-lint`／**`object-lint`**／`fact-refs`）／`tools/foreshadow_project/`

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/01-狀態物件軸.md](功能報告/01-狀態物件軸.md) §八「重構實況」。

**刻意留給後續的**（不是遺漏）：
- **G3 的「投影按型別 fold」**：物件檔的 `型別` 欄目前裝的是**種類**（封閉七型），不是折法（集合／純量／區間／多態）。把 delta 的 6 維改成型別參數要等**有真實物件檔**之後再做，否則是憑空設計型別系統 → 本功能的下一輪。
- **G5 兩條時間軌 diff**：依賴 02 的幕綱衍生層。
- **完整的 C3**：物件檔名這一半有 registry 了（檔名天生唯一），**幕綱的伏筆名那一半仍是自由造字** → 02。目前的補償是 `object-lint` 的近似名偵測。
- **物化快照**：量過了、未到痛點（fold 259 筆／輸出 4,624 字元）。觸發門檻與駁回理由寫進 `02-待用構想.md` 已駁回清單。
- ~~**`.co.md` 這個檔類**~~ **04 已裁（2026-07-27）**：廢除，裁決流正名 `story/參照/裁決流.md`＋`decision-lint`（依新增的 A4）。
- ~~**書級方針**裝不進物件檔~~ **04 已裁（2026-07-27）**：新增**第八型 `方針`** ＋ 保留檔名 `story/物件/全書.md`（雙向綁定，`object-lint` 守），由 `fact-project --for-beat` 的 `### 全書方針` 節無條件印出（0 條也印）。**「要第八型＝停下來問作者」這條規則第一次被執行。** 注意 `方針` 沒有狀態，G3 的 fold 要單獨處理它。

### 02 · 幕綱　**已重構（2026-07-27）**

**現行落點**：`結構定義/幕綱.schema.md`／`.claude/skills/beat-sheet`、`beat-test`／`story/幕綱/arcNN.md`、`_index.md`／`tools/beat_metrics/`（**`beat-lint`** 格式閘門＋`beat-metrics` 漂移統計）／解析它的另三支工具：`tools/foreshadow_project/scan.py`、`tools/fact_projection/beats.py`、`tools/settings_select/select.py`

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/02-幕綱.md](功能報告/02-幕綱.md) §八「重構實況」。

**核心判斷**：幕綱**維持作者直改的強格式 `.md`**（抉擇 1 A，已駁回「拆成散文源＋衍生機讀層」的 C4 路線——`[[幕NNN]]` 是作者重排劇情要直接操作的結構，不是描述的副產品），改為兌現「強格式的前提是有檢查器」（B1／E1）。

**刻意留給後續的**（不是遺漏）：
- **`_index.md` 的 arc 概覽膨脹**（11 行單行、最長 1,949 字，索引檔在複述全書）：與 §10 就緒儀表、§12 rollup 是**同一個 D1／D3 判斷**（綜合視圖該是投影不是檔），三者一起裁 → 10／12。
- **spine 四份實作收斂成一份**：本輪只統一失效行為（全部硬報錯，修掉 `beat_metrics` 那份靜默退化的假陰性）；要不要維持「工具間零相依」交 14。
- ~~**`.co.md` 依 A1 應廢除**~~ **04 已裁（2026-07-27）**：`.co.md` 這個檔類廢除，正名 `story/參照/裁決流.md`，格式由新的 `decision-lint` 守（依新增的 `設計原則.md` A4）。`beat-lint` 的 `DECISION_LOG` 兩種命名都吃，黃金檔只變 1 行（檔名）。**04 抉擇 3 選 A（理由維持集中），所以「`beat-lint` 的設計註目的地檢查會失去標的」那個風險沒有發生。**
- **`幕尾鉤` 第九欄**：實測 79/108 幕在用而 schema 只定義八欄。`beat-lint` 只印提示，本輪不裁它該不該升格 → 本功能下一輪。
- **arc09／arc10 的 14 個 `## ⚠️` 未定義分區**、arc01–arc10 缺承諾區：`beat-lint` 已報，但**不遷移病例書**。
- **排除線在 `story/大綱/arcNN.md` 也有一份**（arc09/10/11 各 12 條編號），與 arc11 幕綱承諾區的 14 條重複 → 大綱層（06）。
- **G5 兩軌 diff 只覆蓋有標記的那半**：抉擇 2 B 決定不逼「刻意不立 token」的續行補標記，代價是計畫軌有一部分程式看不到。`beat-lint` 覆蓋率行誠實印出「其中 N 列的伏筆名全書無標記」。

### 03 · 正文章節　**已重構（2026-07-27）**

**現行落點**：`結構定義/章節.schema.md`／`.claude/skills/write`、`write-test`、`revise`／`chapters/chNNNN.md`、`chNNNN.ai.md`、`chapters/_index.ai.md`／`tools/beat_metrics/`（**`ch-lint`** 正文層格式閘門，與 `beat-lint` 同套件共用幕號 registry）／`tools/prose_metrics/`（漂移統計＋章密度參考行）／`tools/derived_sync/` 的成長哨兵

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/03-正文章節.md](功能報告/03-正文章節.md) §八「重構實況」。

**核心判斷**：正文層**維持**「一章一檔的散文源＋內嵌 `<!-- 幕NNN -->` 錨點」（C2 已定案，「一幕一檔」已駁回），改為兌現 C2 那句「更細的定位只能內嵌，**且必須配檢查器**」——`<!-- 幕NNN -->` 從「全 repo 唯一零工具解析的機讀標記」變成有閘門。

**刻意留給後續的**（不是遺漏）：
- **`chapters/_index.ai.md` 的形態**（抉擇 2 C）：它是成長型產物（196→781 B/列，4.0×；500 章外推 ≈381 KB）而**沒有投影入口**，依 D3 可能該是 `ch-project` 的輸出而不是一支檔。與 §10 就緒儀表、§12 rollup 是**同一個判斷**，三者一起裁 → 10／12。本輪只做「清掉備註欄三類內容＋納入哨兵＋在 `共同約定.md` 八的查詢入口表**登記成欠債**」。
- **`POV`／`風格`／`狀態`「重生前後未變」無人守**（抉擇 4 A 的已知缺口）：那是跨時間斷言，系統不保存「上一版」。`ch-lint` 只守得住「存在＋枚舉＋與 `_index` 視圖一致」，其餘靠 `write`／`revise` 帶舊值的紀律。**這與 V12（`revise` 的「修訂前後錨點集合必須相同」）是同一個結構性缺口** → 14。
- **`章末狀態快照` 殘骸**：一世之尊 `_index.ai.md` 還有那一節，`derived-sync validate` 持續報。病例書不遷移。
- **事實層覆蓋率仍是 0**：93 章 `## 本章事實` 0 章使用，全書 259 筆仍在 legacy `story/參照/狀態事件流.md`。01 的新格式在真實語料上**沒有被驗證過一次**——第一本走新格式的書會是它的第一次實測。
- **`prose_metrics` 有 6 支測試長期 fail**（依賴 repo 裡沒有的語料：`芯片巫師/chapters`、`Data/` 參照語料）。**本輪未動，也不是本輪造成的**（已用 `git stash` 對照確認）→ 14。

**原則本身的問題 2 條**（見報告 §六，本輪未改 `設計原則.md`）：D3 漏了「選取」這一類用途，在 `_index.ai.md` 上無法決定任何事；A3 第 2 條路（人核可後封章）在混欄的衍生檔上不成立。**E2 的嚴重度尺缺一格「警報疲勞」**（守衛全報於是沒人看），本輪又量到第二個案例 → 14。

### 04 · 裁決與回饋　**已重構（2026-07-27）**

**現行落點**：`結構定義/待裁決.schema.md` ＋ `story/參照/待裁決.md`（AI 的觀察，四欄無狀態欄）／`結構定義/裁決流.schema.md` ＋ `story/參照/裁決流.md`（人拍板的理由，append-only；**`.co.md` 已廢除**）／`story/物件/全書.md`（書級方針，型別 `方針`）／`tools/decision_projection/`（`decision-project` ＋ **`decision-lint`**）／`tools/derived_sync/` 的 blockquote 禁令與目的地存在性

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/04-裁決與回饋.md](功能報告/04-裁決與回饋.md) §八「重構實況」。

**核心判斷**：三筆內容（AI 的**觀察**／人拍板的**理由**／**書級方針**）從「同居一格、同居一檔、無處安置」拆成三個各有檢查器的落點。副檔名 `.co.md` **廢除**——依新增的 **A4**，檔的第二個位元（程式會不會解析它）由**有沒有檢查器**承擔，補上 `decision-lint` 之後裁決流與幕綱、物件檔同格。

**本輪改了 `設計原則.md` 兩條**（前三輪都只記不改）：新增 **A4**；**F2 第三列**判準由「跟著它保護的那條規則」改成**「跟著它的射程」**（四格）。理由是那兩條在本功能上**無法決定任何事**，而本檔末節明訂那種條文該改寫而不是加註解。

**刻意留給後續的**（不是遺漏）：
- **T5（回饋列離開 ⇄ 裁決軸 append 的對帳）改寫不成** → 14。兩個方向都試過且都不成立（見報告 §八）。**不假裝守住 V1**，改交出兩個近似：`decision-lint` 的「有待裁決列卻沒有裁決流」＋覆蓋率行並排印「待裁決 N 列／裁決 M 列」（`f22082f` 的病徵就是 N 在掉而 M 恆 0）。與 03 記的兩條同類。
- **F2 的核心顧慮只補掉一半** → 14。「集中 log 不會自然被載入」這一點，本輪只補了**書級方針**那一半（`fact-project --for-beat` 無條件印 `### 全書方針`）；**裁決軸那一半沒補**，仍靠「有人記得跑 `decision-project`」。作者拍板不做——要讓 `fact_projection` 順帶吐裁決，得在零相依政策下複製一份裁決流解析，而且會把「理由」塞回 `write` 的 context（正是本功能診斷出的病）。
- **兩支新閘門都還沒被真實語料驗過**：0 本書有裁決軸兩支檔；blockquote 守衛唯一的真實語料是刻意不遷移的病例書。
- **`方針` 型別的性質與其餘七型不同**（它沒有狀態）。G3 的「投影按型別 fold」若日後實作，它要單獨處理。
- **一世之尊不遷移**：validate 24→31、哨兵 46→51，其中「目的地不存在 3 行」正是本輪要的（在此之前它報的是「檔太大」，看不出那些補救建議**照做會失敗**）。

### 05 · 設定層·世界觀　**已重構（2026-07-27）**

**現行落點**：`結構定義/世界觀.schema.md`／`.claude/skills/worldbuild`＋`develop`（世界觀寫入路徑）／`story/設定/世界觀/`／`tools/derived_sync`（**新增 `world-lint`** ＋`long_lines` 的兩個新目標集）／`tools/settings_select`（覆蓋率行）

**這一輪實際做了什麼、與提案有何出入**：見 [功能報告/05-設定層-世界觀.md](功能報告/05-設定層-世界觀.md) §八「重構實況」。

**核心判斷**：這一軸的病不是「方向錯」而是**衍生檔變成三種無家內容的收容所**，而三個家（01 的物件軸、04 的裁決／待裁決軸）2026-07-27 都已蓋好——**缺的一直只是「誰去搬、什麼時候搬」**。所以本輪的主要產出是把三個搬移動作寫成**帶觸發者的 schema 條文**，而不是新機制。同時依 **A4** 刪掉四個「機讀格式＋零解析器＋零消費者＋已漂移」的 front-matter 欄。

**本輪改了 `設計原則.md` 三條**（報告 §六 提案、作者拍板全改）：**A3 加第 0 條**（「這筆內容的射程指向別的落點嗎？指向就搬，別在這支檔裡調和」——舊的三條路在 `_總覽.ai.md` 上全部走不通）；**D2 補覆蓋率判準**（「有選擇器」不是布林值，要能回答命中幾筆、依據是什麼）；**E1 涵蓋消費者承諾**（凡 schema 寫「供 X 盯」，X 那一側要指得出讀它的那一行）。

**刻意留給後續的**（不是遺漏）：
- **選擇器沒修，只量了**（抉擇 2 D）。世界觀那一側比對的是**檔名字串**，實測 4 個主題有 3 個裸提及率為 0——命中的是幕綱散文裡的 `見 \`X.ai.md\`` 註腳。**02 清掉那些註腳的那一天，這 3 筆會靜默消失**。本輪只補覆蓋率行讓數字可見；**選項 C（改讀 `_總覽` 索引由 LLM 挑）仍在檯面上**，它依賴 `_總覽` 真的縮小。→ 05 的下一輪或 15。
- **遷移路徑步驟 6（6 支 skill 的 wildcard 收斂成 `settings-select`）刻意不做** → 15。理由：它依賴延後的抉擇 2，而在現況下收斂 `beat-test`／`character`（兩支目前零選取）會讓它們**當場少讀三份核心規則**——把一個「讀太多」的問題換成「讀不到」。
- **`_總覽.ai.md` 的形態隨 10／12 一起裁**（抉擇 4 A）。本輪只把它變正常（搬走 80% 寄居內容）。**別在 10／12 重新提案「它與就緒儀表是同一個問題」**——本輪已依「hash 機制的有無」否掉那個歸類。
- **`江湖勢力.md` 觸頂沒拆**（抉擇 3 B）：拆的粒度是創作判斷，而沒有任何活書需要這個決定。程序（九步，含「舊檔名的引用怎麼處理」）已寫進 schema，**但它一次都沒被跑過**。
- **`伏筆` 歸一只到「提示」不到「門檻」**：`world-lint` 報查無的名字，但不擋——`物件.schema.md` 明訂沒有物件檔的 ID 是合法的。完整的 C3（抽取器只能命中既有 ID）仍等有書真的用物件檔。
- **行長門檻是 n=4 的暫定值**：800／600 取自一世之尊四支世界觀檔的分佈空隙，schema 已標明第二本書有設定層時重取。
- **一世之尊不遷移**：哨兵 51→53（新增的 2 筆正是本輪要的——在此之前 1,155 與 1,072 字元的單行完全隱形）、`world-lint` 首跑報 5 筆。

### 06 · 角色　**已重構（2026-07-27）** ／ 07 · 風格

**現行落點**：`結構定義/角色.schema.md`、`風格.schema.md`／`.claude/skills/character`、`develop`／`story/設定/{角色,風格}/`／`tools/settings_select/`、`tools/derived_sync/`

**06 的診斷報告**：[功能報告/06-設定層-角色.md](功能報告/06-設定層-角色.md)（§八 有重構實況）。**核心判斷（推翻下面第一條「預期大致符合原則」的預判）**：方向確實對（A 前向），但這是目前量到最壞的一軸——**枚舉外內容佔角色衍生檔節內容的 72.2%**（24/24 支全中），**12/24 支的四個必填節全是佔位字串**（84 處「（源檔未載，待跑 `character` 補）」）而 `check`／`validate`／`settings-select` 三支守衛全部回報正常。根因在上游：`derived_sync/core.py` 從 `rglob("*.ai.md")` 出發，「源有衍生無」在定義上掃不到，於是系統手建 13 支空殼檔去餵掃描器。

**06 重構輪做了什麼（2026-07-27）**：六個抉擇全部落地——新增 **`char-lint`**（八項＋覆蓋率行）、front-matter 依 A4 刪三欄（`角色`／`弧線類型`／`影響力`）、形態拆成「因為大」與「因為有秘密」兩條獨立規則、`血刀頭陀` 那格改成「**先搬後拆**」並對齊哨兵 hint、`一行需求` 改內容判準（不定專用數字門檻）、角色欄集合名 token 只報 ≥3 次。**`設計原則.md` 同輪加兩條推論**（E1「凡宣稱每一支 X 都有一支 Y，就要從 X 側掃」／E2「覆蓋率行要能回答命中的裡面幾筆是空的」），後者的直接應用是 `settings-select` 覆蓋率行加印空殼數（實測 arc02：「角色命中 7 筆，其中 **4 筆是空殼**」）。**規劃時另外查出一個報告沒寫的 blocker**：目錄形態在 `derived_sync` 裡**完全是壞的**（`check` 報 `orphan`、`stamp` raise、改切面不會讓 rollup stale），`角色.schema.md` 那條「rollup digest 對目錄形態取切面集合」是**零實作的承諾**——同輪補上實作與六個測試。

**已知疑點**：
- ~~這兩個**預期大致符合原則**（A 前向：散文源＋衍生），是驗證而非大改。~~ **06 已推翻（見上）**；07 尚未診斷，但它與角色共用 `validate.py` 的缺口（見下方兩條），別再假設它是快速驗證。
- ~~**（← 05 診斷輪順手記下·06）`血刀頭陀.md` 32,650 B 觸源檔門檻…**~~ **06 已處置（不遷移病例書，改的是規則）**：schema 加「先搬後拆」程序、成長哨兵的單檔 hint 先問「是不是有寄居內容」；`_index.ai.md` 的 12 行超長由 `char-lint` 的內容判準接手（實測 10 列含 `🧊`／章號）。**「衍生比源大」已用 05 的那把尺量過**：源 135,435 B → 衍生 182,815 B（**1.35×**），成長倍率源 2.37× vs 衍生 2.43×，**逐支比較衍生較快的 17/22**；最決定性的是 **9/22 支的源檔一次都沒改過而衍生仍長了 1.30–2.08×**——那些增量在定義上不可能來自源，而 `check` 對它們全報 fresh 且報得沒錯（digest 覆蓋的是輸入，不是輸出的內容）。原文如下——
- **（← 05 診斷輪順手記下·06）`血刀頭陀.md` 32,650 B 觸源檔門檻、`血刀頭陀.ai.md` 32,033 B 帶 5 個枚舉外的節**（「反派備註」「水下層分界規則」…）；`角色/_index.ai.md` 12 行超過 400 字（最長 1,071）。**05 診斷出的「衍生比源大」在角色軸也成立**（32,033 vs 32,650，已幾乎相等）——值得用同一把尺（`git log --follow` 逐版成長倍率）量一次：05 實測 `江湖勢力.ai.md` 八次重生長 18.2×、源只長 9.2×，**衍生的成長倍率是源的兩倍**。
- **（← 05 診斷輪順手記下·06）`character/SKILL.md:41` 零 `settings-select`、三處 wildcard**，是本 repo 讀設定層最重的一支（51,225 字元）。而它的職責（判新角色是否違反世界觀核心規則）其實只需要核心規則索引。**05 刻意沒收斂它**（見上「刻意留給後續的」）。
- **（← 05 診斷輪順手記下·07）`風格.ai.md` 12,247 B 剛超 `DERIVED_BYTES`**，且它是 `validate` 的「2 支只驗 front-matter」之一（節枚舉是空頭承諾，01 盤點時已記）。**另：它最長單行 602 字元**——05 新增的設定層行長門檻是 800，所以它現在剛好在門檻下，**但它離門檻只有 25%**。
- ~~但 `.ai.md` 的 `## 待裁決回饋` 問題落在這三個身上（見 04），且要等 04 定案。~~ **04 已裁（2026-07-27）**：該節從 `validate.py` 的節枚舉**移除**（角色／角色\_index／世界觀／世界觀\_總覽／章節五處），回饋改住 `story/參照/待裁決.md`。既有書那些節現在會被 `derived-sync validate` 報成枚舉外——**那是預期的**（同移除 `## 🧊 水下` 時）。
- **（← 04 重構輪追加）`風格` 與 `摘要` 根本不在 `validate.py:DERIVED_SECTIONS` 裡**：`風格.schema.md` 與 `摘要.schema.md` 都定義了節，而 `enum_for()` 對它們回 `None` → **只查 front-matter，節枚舉是空頭承諾**（E1）。04 新增的覆蓋率行現在會把這件事印出來（實測一世之尊「124 支套節枚舉／**2 支只驗 front-matter**」），但**沒有補上枚舉**——那是 07 與 08 的題目。
- `settings-select` 的粒度是「哪幾支檔」不是「檔裡哪一段」，所以設定層 `.ai.md` **沒有行內切片工具**，依 D2 不該享有「可以很大」的豁免（`sentinel.py` 的 `DERIVED_BYTES=12000` 已經在管，但量的是 bytes 不是查詢成本）。
- 角色的「水下」切面是存取控制，走檔名（`角色/<名>/水下.md`）——這是 C1 的正例，確認它仍成立即可。**（01 重構輪確認仍成立**，且它現在是揭示層級的**更強一級**：物件檔的 `揭示層級` 只是提醒，`水下.md` 是 `write` 拿不到。）
- **（← 01 診斷輪順手記下）`derived-sync check` 實測：8 支角色 `.ai.md` 有枚舉外的節、6 支世界觀／風格 `.ai.md` 超過 12,000 B**（最大 44,978 B）。其中**屬 01 的那 70 行已經有家了**（枚舉外節的「硬事實」「下游硬約束」「反派備註」→ 錨進章 delta、約束進物件檔），**但一世之尊刻意不遷移，所以那些節還在**；世界觀那幾支的成因 05 已診斷並改掉 schema（見 05），角色／風格那幾支是 06／07 的事。
- **（← 01 重構輪追加）枚舉已經縮小過三次**：`角色.ai.md` 移除 `## 🧊 水下` 節（01）、`世界觀.ai.md` 移除 front-matter 鍵 `🧊水下:`（01）、**世界觀 `.ai.md` 再移除 `影響力`／`主從`／`維度`／`升格哨兵` 四欄且 `## 自洽 / 升格哨兵` 改名 `## 自洽`（05）**。既有書那些節／欄現在會被 `derived-sync validate`／`world-lint` 報——**那是預期的**。
  > **06／07 要用 05 的那把尺自問一次**：角色與風格 `.ai.md` 的 front-matter 欄（`角色`／`型別`／…）**有沒有解析者、有沒有消費者**？05 實測世界觀那五欄是「零解析器、零消費者、schema 卻宣稱『供 X 盯』」，而 `validate.py` 的 `REQUIRED_KEYS` 只驗 `generated-*` 兩個鍵——**那個縫對所有產物都成立，不只世界觀**。判準見 `設計原則.md` A4 與同輪新增的 E1 消費者推論。
  > **06 已量並處置（2026-07-27）**：七欄×24 支＝**162 個欄位實例，程式讀取 0 個**。刪 `角色`／`弧線類型`／`影響力`，留 `定位`／`所屬arc`／`暫定`／`伏筆` 並由 `char-lint` **真的讀**（`角色.schema.md` 有逐欄對照表）。`弧線類型` 是這個縫最純的形態——**連假的消費者都沒宣稱過**（世界觀的 `升格哨兵` 至少寫了「供 beat-test/sync 盯」）。**07 照樣要跑這一問。**

### 08 · 摘要 ／ 09 · 大綱

**現行落點**：`結構定義/摘要.schema.md`、`大綱.schema.md`／`.claude/skills/organize`、`outline-test`、`expand`／`story/00-摘要.md`＋`.ai.md`／`story/01-大綱.md`、`story/大綱/arcNN.md`

**已知疑點**：
- **（← 04 重構輪追加）`摘要` 不在 `validate.py:DERIVED_SECTIONS` 裡**：`摘要.schema.md:77` 定義了節，而 `enum_for()` 對它回 `None` → **只查 front-matter，節枚舉是空頭承諾**（E1，同 07 的 `風格`）。04 的覆蓋率行會印「M 支只驗 front-matter」把它暴露出來，但**沒有補枚舉**。另：04 已把 `## 待裁決回饋` 從摘要 schema 的範例區移除（回饋改住 `story/參照/待裁決.md`）。
- 大綱**沒有任何工具解析**（已 grep 確認），是乾淨的散文源——確認它不需要衍生層即可。
- `共同約定.md` 九「宣告型約束」（基調／取向定位／節奏檔位／配額）落在摘要上：一條宣告要成立必須連帶寫出「單位／達標線／誰核對」。這條與 E1 是同一件事的兩個說法，檢查是否該合併。

### 10 · 就緒儀表 ／ 11 · 結構對應表

**現行落點**：`結構定義/就緒儀表.schema.md`、`結構.schema.md`／`story/參照/就緒儀表.ai.md`、`結構.ai.md`／`tools/derived_sync` 的 `_is_declarative`

**已知疑點**：
- 兩者帶 `.ai.md` 但**無單一源、不能 hash、不能 stamp**，是「假衍生檔」。過期只靠 `sync` 的語意比對。
- 依 D3（理解全局 → 投影輸出），**就緒儀表可能根本不該是一支檔**，而是一個投影工具的輸出。若成立，`_is_declarative` 這個特例可以刪掉。
- **（← 04 重構輪追加）「⚠️N 筆待裁決回饋」的被動偵測規則實測 14 處字樣 vs 實際 0 筆。** 04 已把規則改成「**N 從 `decision-project` 的覆蓋率行讀出來，不要用人數的**」（`就緒儀表.schema.md` 與 `共同約定.md` 四已改），但**沒有動這支檔該不該存在**——那是 10 的題目。**另：一世之尊的書級通則原文住在 `就緒儀表.md`**（`角色/_index.ai.md:60` 明文指過去）。04 已經給書級方針一個家（`story/物件/全書.md`，型別 `方針`），**所以 10 若決定砍掉就緒儀表，那條通則有地方去了**——但一世之尊不遷移，它現在還在原地。
- **（← 01 診斷輪順手記下）`就緒儀表.md` 有 3 行超過 2,000 字（最長 10,038 字單行）**，並且其中 **26 處提到 🧊**——一支綜合檔在複述別條軸的狀態。那 26 處現在指向一個已廢除的落點（揭示層級改住 `story/物件/<名>.md`），這正是 D1「散文檔不該回答『全書關於 X 的一切』」的實例。

### 12 · rollup 索引

**現行落點**：`_index.ai.md`／`_總覽.ai.md`／`tools/derived_sync/core.py` 的 rollup digest

**已知疑點**：
- rollup 的源＝同層所有源檔的 `檔名:內容hash` 排序後再 hash，機制本身健全。
- 真問題是 D1：rollup 是不是在回答「全書關於 X 的一切」？若是，它該是投影不是檔（同 10）。
- **（← 03 重構輪追加）本功能現在有兩個實測案例，病徵一致：索引檔的最後一欄變成無家內容的落腳處。** ① `chapters/_index.ai.md`：50,782 B、最長行 2,235 字元、備註欄 196→781 B/列（4.0×）、500 章外推 ≈381 KB；② `幕綱/_index.md` 的 arc 概覽（02 記的，11 行單行、最長 1,949 字）。**03 已經把 ① 的三類寄生內容清掉、納入哨兵、並在 `共同約定.md` 八登記成「尚無投影入口」的欠債**，但**刻意不裁它該不該存在**（抉擇 2 C）——那要與 10、12 一起判。診斷 12 時這兩支是主要語料。
- **（← 04 重構輪追加）第三個實測案例：舊回饋表的 `狀態` 欄。** schema 只允許一個值（`待裁決`），實測 7 列**平均 171 字元、最長 440、佔整列 61%**——人的裁決被塞進 AI 那一筆的最後一格。**三個案例的共同結構：一個窄欄位 ＋ 沒有別的地方可放 ＝ 欄位溢位。** 04 的處置不是「把枚舉守起來」而是**把那一格拿掉**（新的 `待裁決.md` 四欄無狀態欄），並確保它要裝的東西有家（理由→裁決流）。**這可能該升格成一條原則**：「schema 規定某欄只能是枚舉值時，必須同時指出非枚舉內容該去哪」——12 或 14 決定。`共同約定.md` 零 已先寫進「登記表一律不設狀態欄」。
- **（← 03 重構輪追加）如果 12 決定「rollup 該是投影」**，`chapters/_index.ai.md` 的替代品是 `ch-project`：章序完全可由 93 支 `chNNNN.ai.md` 的 `對應幕`／`所屬arc` 重算（`ch-lint` 第 8 項已經在做這個重算來比對視圖，等於**投影器的核心已經寫好了**）。要改的是 `write`／`write-test` 現在靠它反查 arc→chNNNN 的那兩處。
- **（← 05 診斷輪順手記下）第四個實測案例，但形態不同：`_總覽.ai.md` 是「整節寄生」而非「欄位溢位」。** 前三個（`chapters/_index.ai.md` 備註欄、`幕綱/_index.md` arc 概覽、舊回饋表 `狀態` 欄）溢出的單位是 **cell**；這一支溢出的單位是 **section**——`素材出處` 佔全檔 51%、`升格哨兵彙總` 29%，合計 80%。共同結構仍是「沒有別的地方可放」，但**12 若要立通則，得涵蓋這兩種粒度**。
- **（← 05 重構輪追加）`_總覽.ai.md` 不該與 10 的就緒儀表歸成同一類，判準是 hash 機制的有無。** 它**有**單一源（資料夾 digest）、能 stamp、`_is_declarative` 不適用它——所以它不是「假衍生檔」問題的實例。05 抉擇 4 A 據此**只把它變正常**（搬走 80% 寄居內容、三節改成指標）而不裁形態。**12 若要重新提案「它也該是投影」，要先推翻這個判準，不是重述形狀相似。**
- **（← 05 重構輪追加）它同時是「hash 守錯對象」的唯一實測案例**：`derived-sync check` 對它報 fresh 是**正確的**（四支源檔的 digest 真的沒變），錯的是那個 digest **不覆蓋 80% 的檔內容**。rollup 的 digest 只吃「同層源檔集合」，所以 rollup 裡任何不是從源檔算出來的東西都在 hash 的射程外——**這對每一支 rollup 都成立**，是 12 的結構性問題不是 05 的個案。

### 13 · raw 分揀

**現行落點**：`.claude/skills/develop`／各書 `raw/`

**已知疑點**：
- F2 已定案：**未定的靈感 → `raw/`**。所以 raw 的定位是清楚的，重點是確認 `develop` 收尾有沒有履行 F1（明講這輪拍板了什麼、寫進哪支檔）。

### 14 · 檢查器與工具鏈

**現行落點**：`tools/derived_sync`（check／validate／stamp／sentinel）／`tools/fact_projection`（lint／refs）／`tools/decision_projection`／`tools/foreshadow_project`／`tools/beat_metrics`／`tools/prose_metrics`／`tools/settings_select`

**已知疑點**：
- E1 盤點：把每一條 schema 宣稱的格式，對上「誰守、何時跑」，列出無人守的。（~~幕綱全部機讀語法、`_index.md` spine~~ 已由 02 的 `beat-lint` 接手；~~正文幕錨點~~ 已由 03 的 `ch-lint` 接手；~~`裁決流`~~ 已由 04 的 `decision-lint` 接手。）**目前已知無人守的只剩「跨時間斷言」那一類**（見下）。
- B1：檢查器要分成「守人破結構」與「守 AI 破克制」兩類，現行 `validate` 與 `fact-lint` 已隱含這個分工，該顯性化。
- 成長哨兵量的是 bytes，依 D2 該改量**查詢成本**。
- 工具間刻意零相依（`dependencies = []`），代價是 `parse_spine` 有三份實作。重構時要決定這個取捨是否維持。**（01 重構輪後：spine 4 份、伏筆 regex 3 份，共 6 份複製。）**
- **（← 01 診斷輪順手記下）訊號強度與後果不成比例**：`fact-project` 對舊格式書只印一行 stderr 就整本切換行為模式，而後果是 206 個檢查全關。**01 已修掉這個特例**（世代改逐行判），但同一個病徵值得在 14 全面盤一次：**還有哪些「一支檔存在與否改變整本書行為」的開關？**
- **（← 01 重構輪追加）E1 盤點的「無人守」清單少了三項**：事實行的幕號落錯章、物件檔的格式、揭示層級語法（皆已由 `fact-lint`／`object-lint` 接手）。**新增一條跨工具通則要在 14 推廣**：`設計原則.md` E2 現在要求每個檢查器都能回答「我在這本書上檢查了幾筆」——`fact-lint`／`object-lint`／`foreshadow-project`／`beat-lint`（02）／`ch-lint`（03）／**`derived-sync validate`／`decision-project`／`decision-lint`（04 重構輪）**已兌現，**其餘還沒**（`beat-metrics`／`prose-metrics`／`settings-select`）。
- **（← 01 重構輪追加）成長哨兵的 `APPEND_LOG_STEMS` 移除了 `約束`／`約束.co`**（落點廢除）。哨兵量 bytes 而非查詢成本這件事沒動，仍是 14 的題目。
- **（← 03 重構輪追加）「這本書有哪些受管檔」該有一份**：成長哨兵四支函式的目標集**各自硬寫路徑**（`sentinel.py` 的 `beat_sheet_density`／`oversized_sources`／`_derived_targets`／`long_lines`），03 之前 `chapters/` 只被其中一支（`bloated_fact_lines`）掃到——**不是刻意豁免，是四份手寫清單漏了一個資料夾**，後果是 `derived-sync check` 對 50,782 B 的檔印「0 個需處理」。03 已補上 `chapters/`，但**根因（四份清單）沒動**。
- **（← 03 重構輪追加）E2 的嚴重度尺缺一格：「守衛全報，於是沒人看」（警報疲勞）**。兩個實測案例：① `prose-metrics` 的行文節奏四項絕對門檻在一世之尊 **11/11 個 arc 全中**（41 條可疑點＝全書 62 條的 66%）；② `ch-lint` 的三條格式收斂在病例書上共 **120 支檔／列**命中——後者已用「聚合成一行」處理（駁回理由寫進 `02-待用構想.md`），前者仍待 14 決定輸出策略。**它與 E2 最後兩格的機制不同**（不是假陰性），值得在 14 決定要不要補這一格。
- **（← 04 重構輪追加）目的地存在性檢查現在有兩份實作**（`beat_metrics/lint.py` 與 `derived_sync/sentinel.py:missing_destinations`），零相依政策下的**第 7 份複製**（spine 4、伏筆 regex 3）。04 的那份是把 02 的形狀推廣到 `derived-sync check` 的 hint（實測一世之尊：43 筆→裁決流、30 筆→待裁決、31 筆→物件目錄，全部指向不存在的檔）。
- **（← 04 重構輪追加）E2 的嚴重度尺缺的那一格有第三種形態：「報得沒錯但照做會失敗」。** 前兩個案例是「報得太多」（`prose-metrics` 11/11 全中）與「報得太碎」（`ch-lint` 120 條）；這個是**建議本身正確、但它指的目的地不存在**（實測 37→43 行）。三者都導致人不看，機制不同。04 已補上守衛，但**這一格要不要正式進 E2 的尺**由 14 決定。
- **（← 04 重構輪追加）F2「集中 log 不會自然被載入」只補掉一半** → 要不要讓 `fact-project --for-beat` 順帶吐出生效中的裁決。它牽動兩個零相依套件（`fact_projection` 得複製一份裁決流解析），而且會把「理由」塞回 `write` 的 context（正是 04 診斷出的病）。04 作者拍板不做，**誠實記錄為未補**。
- **（← 03 重構輪追加，04 追加第三條）「跨時間斷言」這一類承諾在現行架構裡原則上不可檢查**，而 schema 寫了三條：`revise` 的「修訂前後錨點集合必須相同」（03 已改寫成無狀態不變式）、`chNNNN.ai.md` 的「`POV`／`風格`／`狀態` 重生前後未變」（03 只守得住一半）、**04 的「回饋列離開 ⇄ 裁決軸 append 是同一個原子動作」＋「裁決流 append-only」**（兩個方向都試過改寫成無狀態不變式、都不成立，改交出兩個近似：`decision-lint` 的「有待裁決列卻沒有裁決流」＋覆蓋率行並排印「待裁決 N 列／裁決 M 列」）。系統沒有「上一版」的概念（git 歷史不是系統的一部分）。**14 該決定：要嘛給衍生層一個「上一版」，要嘛把這一類承諾一律改寫成無狀態不變式。**
- **（← 05 重構輪追加）`long_lines` 的目標集是第五份手寫路徑清單漏檔的案例**：03 補了 `chapters/`，05 補了 `story/設定/<kind>/` 的**非 rollup** 檔（衍生與源）。**證據這兩次都不是刻意豁免**：同一支 `sentinel.py` 裡的 `unsliceable_derived` 早就在掃設定層的衍生檔**大小**，只有行長那一份漏了它；於是 1,155 字元的單行（rollup 門檻的 2.9×）與同資料夾的 `_總覽.ai.md`（一直在報）待遇不同，差別只在檔名有沒有以 `_` 開頭。**根因（四份手寫清單）仍然沒動**，這是它第二次被同一個機制咬到。
- **（← 05 重構輪追加）第 8 份同類複製**：`world-lint` 要的 `[[伏筆:x]]` regex ＋掃幕綱、以及「rollup 視圖 ≡ 各檔 front-matter」的形狀（同 `ch-lint` 第 6 項），在零相依政策下在 `derived_sync` 又複製了一份（spine 4、伏筆 regex 3→4、目的地存在性 2、rollup 視圖比對 2）。**05 刻意選了「同套件新增指令」而非新開 `tools/world_lint/`**（理由：世界觀 `.ai.md` 的格式真相已經在 `validate.py`，拆出去等於一支檔兩個擁有者），但那只省掉 front-matter 解析那一半。
- **（← 05 重構輪追加）E2 的嚴重度尺缺的那一格有第四種形態：「守衛守錯對象」。** 前三種是報得太多（`prose-metrics` 11/11 全中）／報得太碎（`ch-lint` 120 條）／目的地不存在（43 行指向空氣）。這一種是：`derived-sync check` 對 `_總覽.ai.md` 報 fresh **完全正確**——它守的 hash 真的沒變，錯的是那個 hash **與檔的內容不重疊**（80% 不在 digest 的輸入裡）。**它比前三種更危險，因為它連「有東西沒被守」的暗示都不給**：前三種至少有輸出可看，這一種的輸出是 `[ok] fresh`。要不要正式進 E2 的尺由 14 決定。
- **（← 06 重構輪追加）第 9／10 份同類複製，而且「三份 rollup 視圖比對」是最值得先收斂的一組。** `char-lint` 帶進兩份：`[[伏筆:x]]` regex ＋掃幕綱（**第 5 份**）、「rollup 視圖 ≡ 各檔 front-matter」（**第 3 份**）。**三份 rollup 比對的差別只有欄名**——`ch-lint` 第 6 項對 `chapters/_index.ai.md` 比六欄、`world-lint` 第 4 項對 `_總覽.ai.md` 比兩欄、`char-lint` 第 5 項對 `角色/_index.ai.md` 比列名＋三欄；`_section_body`／`_table_rows`／`_front_matter` 三個 helper 在 `world_lint.py` 與 `char_lint.py` 幾乎逐字相同。**一個「rollup ≡ 資料夾＋各檔 front-matter」的參數化 helper（欄名對照表當參數）會一次收掉三份。** 另加**第 2 份佔位偵測**：`char-lint` 第 1 項與 `settings_select.is_hollow` 各一份（E2 新推論要求覆蓋率行印空殼數，而 selector 不能 import lint）。
- **（← 06 重構輪追加）E2 嚴重度尺的第五種形態：「三個守衛各自正確，合起來仍指向空的東西」。** 前四種是報得太多／報得太碎／目的地不存在／守錯對象。這一種**沒有任何一支守衛做錯**——`check` 該報 fresh、`validate` 該只驗結構、`settings-select` 該只管選取；病在**沒有人負責問「選出來的東西有沒有內容」**。**06 已把它升成 `設計原則.md` E2 的可執行推論**（覆蓋率行要能回答「命中的裡面幾筆是空的」），並在 `char-lint` 與 `settings-select` 兩處實作。要不要連同第四種形態一起正式寫進 E2 的表由 14 決定。
- **（← 06 重構輪追加）「零實作的格式承諾」值得在 14 做一次全 repo 清查。** 實測：`角色.schema.md` 寫著「rollup 的 digest 對目錄形態取其下所有切面檔的集合」，而 `derived_sync` 從來沒有實作過——目錄形態的 `<名>.ai.md` 會被 `check` 報成 `orphan`、`stamp` 直接 raise、改切面不會讓 rollup stale。**它在 schema 裡形式完全合法，而且 0/24 實測所以三輪盤點都沒撞到。** 這與 E1 的「消費者承諾」（欄位有沒有人讀）、「目的地承諾」（箭頭指向的檔在不在）是同一族的第三種：**行為承諾（schema 宣稱工具會這樣做，而工具沒有）**。查法是機械的——schema 裡每一句描述工具行為的話，去那支工具找對應的那一行。
- **（← 03 重構輪追加）`prose_metrics` 有 6 支測試長期 fail**（`test_rhythm.py` ×5、`test_exposition.py` ×1），原因是它們依賴 repo 裡沒有的語料：`芯片巫師/chapters`（該書只有 `raw/`）與 `Data/` 參照語料。**03 已用 `git stash` 對照確認不是本輪造成的。** 它們 fail 而不是 skip 是對的（E2：會自己 skip 的測試就是報平安的守衛），但一個永遠紅的測試套件與關掉的測試套件效果相同——同上一條的警報疲勞。

### 15 · skill 的 context 取用契約

**現行落點**：`共同約定.md` 八／各 `SKILL.md` 的讀檔步驟

**已知疑點**：
- 硬規則「不得整檔讀成長型產物」已存在，但要重新對照 D1／D2 檢查每個入口是否還成立。
- 前面 14 個功能改完後，這裡的查詢入口表要整份重寫。
- **（← 05 診斷輪順手記下）設定層有兩套互不相通的入口。** 12 支 skill 裡 6 支仍有 `設定/世界觀/*.ai.md` wildcard，其中 `beat-test`／`character` **零 `settings-select`**（等於整軸全撈：32,965／51,225 字元）。而 5 支 SKILL.md（`organize`／`beat-sheet`／`expand`／`character`／`develop`）宣稱「先讀 `_總覽.ai.md` 索引挑實體」——**`settings-select` 明文跳過 `_` 開頭的檔**（`select.py`），所以查詢入口表登記的入口與 SKILL.md 宣稱的入口是兩套。05 已把這件事寫進 `共同約定.md` 八 那一列。
- **（← 05 重構輪追加）遷移路徑步驟 6（wildcard 收斂成 `settings-select`）05 刻意不做，理由要記住**：現況下世界觀 4 個主題有 3 個**只靠幕綱散文裡的檔名註腳**被選中（裸提及率 0），所以把 `beat-test`／`character` 從 wildcard 改成 `settings-select`，會把「讀太多」換成「**當場少讀三份核心規則**」。收斂的前置條件是先修選擇器（05 抉擇 2 的 C 案），而那個依賴 `_總覽` 真的縮小。**新的覆蓋率行（`settings-select` 一律印「世界觀命中 N 筆，其中 M 筆僅因檔名被引用」）就是判斷時機的儀表。**
- **（← 06 重構輪追加）角色軸的 wildcard 收斂也刻意不做，但前置條件不同。** `character/SKILL.md` 的三處 wildcard 實測讀 **353,297 B**（源 135,435＋衍生 182,815＋rollup 35,047），是本 repo 讀設定層最重的一處。角色這一側的**選擇器比世界觀健康**（比對的是幕綱角色欄這個正式宣告，不是散文裡的檔名註腳），所以收斂的前置條件**不是「修選擇器」而是「先修空殼」**：在 12/24 支還是佔位時收斂，`character` 會從「讀 353 KB 含 12 支空殼」變成「讀 7.9 支含 4 支空殼」——**空殼比例不變而總量下降，看起來像改善**。**先修內容，再收入口。** 判斷時機的儀表是 06 新增的那一格：`settings-select` 印「角色命中 N 筆，**其中 M 筆是空殼**」，M 歸零才談收斂。
  > 角色側殘留的兩個選擇器缺口（**只量不修**）：2/24 從未被任何幕的角色欄選中（`無嗔` 是反派、`真德`——兩者 `mentioned_only` 都有報）、集合名 `同伴組`×14 展不開（`char-lint` 第 8 項現在會報，**已駁回建對照表**）。

---

## 兩個貫穿全程的檢查

跑每個功能時順手記，最後彙總：

1. **E2 的最後一格**——這個功能有沒有「壞了永遠不會發現」的東西？有的話單獨列出，那是最高優先。
2. **數字**——這個功能在 `一世之尊/` 上的量是多少？（幾份、每份幾行、一次查詢讀幾筆、成長率）沒量過的就去量。
