# 重構後驗證流程 · 階段順序與判準

> **這是什麼**：15 軸全系統重構（2026-07-27～29）之後的驗證流程。**本檔是這條線的權威計畫**，逐階段的產物落 `docs/驗證/NN-*.md`。
> **形狀對照 `docs/重構/`**：那一輪的計畫是 `01-功能盤點與測試順序.md`、逐輪產物是 `功能報告/01`–`15`。這一輪同形。
> **每個階段一個獨立 session**（context 隔離是流程的一部分，理由見 §五）。

---

## 一、為什麼有這條線

重構的方法論是刻意的：拿 `一世之尊/` 當**病例書**，從它的失效模式反推第一性原則（`結構定義/設計原則.md` 六問＋A–G）。做法是對的，但留下三個結構性風險，而重構自己在報告裡承認了大部分：

1. **legacy 接口可能是死重量。** 為了讓病例書跑得動，`tools/` 留了成組的向後兼容分支（spine 兩落點回退 ×4 份實作、`裁決流.co.md` 舊名 ×4 處、`事實流.md`／`狀態事件流.md` 兩代命名、`結構.md`／`結構.ai.md`、`就緒儀表` 舊名、`prose_metrics._arc_of` 雙格式 fallback）。`共同約定.md:42` 自己診斷過其中一條是**「一句半真的相容承諾」**（查詢吃得動、格式守衛吃不動），而它在 `.co.md` 廢除三天後仍原樣活著。未來的新書全是新格式，這些分支就是純負債。
2. **分階段重構製造了跨階段的暫留。** 12 輪報告都有一段「為與舊版自洽而暫留」，多數標「隨 NN 一起裁」，而有幾筆的下游輪次跑完了卻沒接（`功能報告/15` 明文：`beat-lint`／`outline-lint` 對兩支 `.md` rollup **仍是照舊比對的相容模式**，於是今天還在要求作者去「修好」一支已廢除的檔）。
3. **重構的核心成果幾乎沒有真實語料驗證。** 事實層覆蓋率 **0**（93 章 0 章用 `## 本章事實`）、物件檔全 repo **0 支**、裁決軸兩支檔 **0 本書有**、`_已併入/` 程序一次都沒跑過、`風格` 多檔 rollup **0 本書用過**、**22/39 支測試檔零真實語料**（功能 10／11／12 的**全部**新產物都在那 22 支裡）。最傳神的一格在 `功能報告/11`：`_is_declarative` 的五支測試**在真實語料上跑的是 0 支檔**——「測試是綠的，射程是空的」。

### 兩個驗證目標

- **Q-M 機制覆蓋**：系統有沒有涵蓋創作真正需要的機制？例（作者原話）「一個會隨劇情變化的地點狀態」該有 `story/物件/<名>.md`、大綱該有落點。**缺口的症狀是作者的內容被塞進自由散文或自創節。**
- **Q-R 機制重複**：同一個需求有沒有被系統**存了多次而沒帶來多餘幫助**？這是重構反覆撞到的病形（`節奏檔位` 三份、`基調參照` 第四份拷貝、跨套件 42 份同形實作、五支 rollup 獨有內容 47–78%），但**重構是逐軸看的，沒有人從「作者的一個需求」這個角度橫著數過落點**。

Q-M／Q-R 要靠**實際跑一次「模擬作者用本系統寫一本書」**才找得出來（作者判斷）。那是階段 2。

---

## 二、已拍板的前提（2026-07-30，不要重新討論）

| 決定 | 內容 |
|---|---|
| **legacy 授權** | **清死碼**。量完之後，凡「唯一用戶是 `一世之尊` 或已廢除落點」的分支直接刪；`一世之尊/` 留原地，接受它從此跑不動，黃金檔改成記錄「不再支援」。 |
| **測試材料** | **哈利波特·消失的密室**。AI 熟悉它是**優點**：模擬作者能隨問隨供完整一致的故事，把機制頂到極限。生成力量不準是已知代價，移到階段 5。 |
| **防火牆** | **事後機械稽核**。保留 prompt 規勸，但每個系統 agent 收工後掃它的 transcript，越界即判該 session 污染，**每 session 印一行「檢查了 N 次呼叫，越界 M 次」，0 也印**。 |
| **`gothic_witch/`** | **不是真書，只是實驗素材**，可自由使用／改／刪。 |

---

## 三、階段

### 階段 0 — 修好量測儀器 ✅ 完成（commit `adafdfd`）

產物：**`docs/驗證/00-系統層邊界與儀器.md`**（系統層白名單在那裡，階段 2 的防火牆允許清單直接用它）。

做完的事：射程表 3 格→5 格／`meta-lint` 第 10 項墓碑那格加掃 `情境測試/`／修 `端到端貫穿測試流程.md` 四處過期引用／拆 `EMPTY_BOOK` 硬編碼／清 `tools/state_projection/` 殘骸／修 `tools.yml` 靜默跳過／移除 4 個帶 48 支 pre-refactor SKILL.md 的過期 worktree／`驗證範例` 三處落點。

出場數字：`meta-lint` exit 0、933 passed / 6 registered known-red、`meta_lint` 52→59。

### 階段 1 — legacy 射程帳與清死碼 ✅ 完成（commit `ef3d863` 凍結 ＋ 本輪）

產物：**`docs/驗證/01-legacy射程帳.md`**（帳、四類分帳、逐筆處置與代價都在那裡）

做完的事：1a 凍結 107,176 字元／1b 對 4 本書 × 21 支指令 ＋ pytest 開 coverage／1c 刪四組舊格式讀取路徑（spine 舊落點 ×4〔**＋量到第五份**〕、舊單檔事實流、`裁決流.co.md` ×7 處、`_arc_of` 雙格式）並全部改成墓碑／1d 三筆掛帳全清。

出場數字：`meta-lint` exit 0、**941 passed / 0 failed**（原 933 passed / 6 known-red）、`known-red.toml` 清空、17 份黃金檔重新蓋章（4 份有內容變動）、`一世之尊` 21 支指令 **0 traceback**。

**兩件要帶進後續階段的更正**（詳見產物）：

- **本檔 §三-1b 的四類表分錯了兩格**：`結構.md`／`結構.ai.md`（`structure_project.py:37`）與 `就緒儀表` 舊名（`readiness.py:219`）被列在「舊格式讀取路徑 → 刪」，而它們**只做 `is_file()`／`stat()`、從不讀內容**——那是**乙類墓碑**。照表刪會刪掉那兩支殘留檔唯一的可見性。它們與 `SPINE_FILES` 同名同形，差別只在函式體。
- **本檔 §三-1d 對 `prose_metrics` 的診斷前半對後半錯**：六本 known-good 語料一直都在 `examples/` 且測試一直讀得到（那幾支本來就綠）。**缺的是第七本——對照組 `芯片巫師`，而它是被刪掉的（commit `a19bf0d`），不是放錯地方的。**

**1a. 先做不可逆前的備份（優先，只做一次）**

`功能報告/10` 用 ⚠️ 標註過：`一世之尊/story/參照/就緒儀表.md` 是 **292,591 B 的全書最大單檔**，其中 **68,776 字元是決定史**，躺在一支**無備份、無守衛、無 append-only 保證**的檔裡；`功能報告/12` 另量到五支 rollup 有 **9,766 字元（12.9%）真正不可重生**。清死碼之後這些只能靠手撈。

→ 凍結匯出 `docs/驗證/一世之尊-不可重生內容-凍結.md`。**這是全流程唯一不可逆的一步，先做完再談刪。**

**1b. 量：每一支 legacy 分支的活用戶**（機械，不是判斷題）

1. 對 4 個對象各跑全部 21 支吃 `--book` 的指令並開 coverage：`書本模板/`（乾淨新書）、`驗證範例/`（乾淨 demo）、`harry_potter/`（只有 raw）、`一世之尊/`（病例書）
2. 跑 pytest 的 coverage
3. **只被「一世之尊 ＋ legacy 測試」命中的行 ＝ 死碼候選**

帳的欄位：`分支｜file:line｜類型｜活用戶（哪本書哪一格）｜刪掉會怎樣｜處置`

**「類型」是這份帳的關鍵，四類處置相反**：

| 類型 | 例子 | 處置 |
|---|---|---|
| **舊格式讀取路徑** | spine `_順序.md`→`_index.md` 回退（`beat_metrics/scan.py:165`、`fact_projection/fold.py:184`、`decision_projection/parse.py:273`、`foreshadow_project/project.py:84` **四份**）、`裁決流.co.md` 舊名（4 處）、`事實流.md`／`狀態事件流.md`（`sources.py:68`）、`結構.md`／`結構.ai.md`（`structure_project.py:37`）、`就緒儀表` 舊名（`readiness.py:219`）、`prose_metrics/metrics.py:110` 的 `_arc_of` 雙格式 fallback | **刪** |
| **墓碑守衛（擋復活）** | `ABOLISHED_ROLLUPS`（`derived_sync/core.py:65`）、`meta_lint/repo.py:232` 的 `ABOLISHED`、各 `--emit` 殘留節 | **留**。用途不是讀舊格式，是**擋復活**——`功能報告/15` 發現「復活那五支檔完全合法」，因為 `_is_rollup` 是檔名 pattern 級機制，12 廢除了五個實例、沒廢除機制 |
| **forward-compat，0 用戶** | `風格.schema.md:19,129` 的多世界／多檔 rollup（0 本書用過）、`rollup digest` 機制 | **刪**，但依 **F4** 必須同時指出「非它的內容該去哪」＋目的地要有存在性守衛。填不出來就不准收窄 |
| **人類寬容（不是 legacy）** | 全形／半形冒號都收（4 處）、`→`／`->`、`對應幕` 兩種寫法、排除線標籤粗體與裸標籤 | **留**。判準寫在註解裡且一致：「這一欄是機讀的，但它終究是人會看、偶爾會手改的一行」。**這是對作者寬容，不是對舊檔寬容**（唯一可收的是那 4 份全形/半形處理各自獨立） |

**1c. 刪除的驗收條件（兩條硬的）**

1. **移除一條 legacy 讀取路徑，必須降級成「被回報的問題」，絕不能降級成 traceback。** 例：spine 回退移除後 `spine_path()` 回 `None`，四支工具要報「spine 不在新落點 `_順序.md`」，不是 `AttributeError`。每一支刪除都要有一支測試釘住「`一世之尊` 現在報什麼」。
2. **17 份黃金檔重新蓋章**，記錄新的（更大聲的）紅字輸出。活體證據不會消失，只是換形式：從「工具安靜地容納舊格式」變成「工具大聲說這本書是舊格式」——後者更符合 E2。覆蓋率行仍要印「我檢查了幾筆」，**0 也印**。

**1d. 一併結清的三筆掛帳**

- `共同約定.md:42` 那條**至今仍半真的相容承諾**（`裁決流.co.md` 查詢吃得動、`decision-lint`／`derived-sync` 掃描起點吃不動）。系統自己寫過「一句半真的相容承諾比沒有承諾更糟」。
- `功能報告/15` 交回 12 的那筆：`beat-lint`／`outline-lint` 對兩支 `.md` rollup 仍是「照舊比對」而非存在性檢查。
- **`prose_metrics` 6 支長期紅測試**（`tools/meta_lint/known-red.toml`）。`skipif` 守錯對象：檢查 `芯片巫師/` **資料夾**在不在（在），而測試要的是 `芯片巫師/chapters/`（不在）。`known-red.toml:20` 說真正的處置要連同「三組回歸語料該住哪」一起裁 → 交功能 02，**沒接**。**現在可以接了**：那 6 本對照語料（`一世之尊`／`詭秘之主`／`極道天魔`／`神秀之主`／`從紅月開始`／`我有一個修仙世界`）**全部都在 `examples/` 裡**——測試找不到它們是因為它在錯的地方找。修對之後 6 支變綠且射程非空，`known-red.toml` 清空（`tools.yml` 雙向擋，清單過期也 fail）。

### 階段 2 — 模擬作者跑《消失的密室》，記需求→機制帳

產物：`情境測試/harry_potter/` 的 `session台帳.md`、`SNN-*.md`、`fallback-log.md`、**`需求機制帳.md`**（新）；書內產物落 `harry_potter/`。

**基座是 `情境測試/端到端貫穿測試流程.md`**（§5 續跑協定原封不動照用）。它已經有三角色＋硬防火牆＋續跑協定＋六步執行迴圈＋purity 檢查，並在 `一世之尊` 上跑過 51 個 session。**本階段對它做三項增修**：

1. **第四個角色「觀測者 agent」**（Q-M／Q-R 的帳由它記）
2. **防火牆從規勸升級為 transcript 機械稽核**
3. **session 序列刻意排入橫向 skill**，把重構自承 0 實例的那幾軸壓出實例

**四角色與知識邊界**：

| 角色 | 誰扮 | 能讀 | 能寫 |
|---|---|---|---|
| **導演** | 主 session | 全部 | 只逐字轉述、commit、寫台帳。**不加料**——導演也認得 HP，注入是最容易破壞測試的動作 |
| **系統 agent（受測體）** | 每個管線步驟一個**冷** subagent | `harry_potter/` ＋ **階段 0 定案的系統層白名單** | `harry_potter/` |
| **作者 agent（神諭）** | 每次被問時冷啟 | `harry_potter/raw/` ＋ `情境測試/harry_potter/` ＋（fallback）自身 canon 記憶 | 無 |
| **觀測者 agent（新）** | 每 session 收工後冷啟 | 全部 ＋ 該 session 的 transcript ＋ `git diff` ＋ 12 支閘門輸出 | **只** `情境測試/harry_potter/` |

**HP 不在 `examples/` 裡**（那是 6 本中文網路小說的爬取語料）。所以 P session 要產一份**結構化 canon 參照**寫進 `情境測試/harry_potter/`（該資料夾本來就在防火牆黑名單上）：逐章劇情節拍、角色表、物件與地點的狀態變化序列、reveal 順序。先例：`情境測試/一世之尊/作者腦內-*.md`（4 支）。這樣神諭讀的是檔（`fallback-log.md` 恢復意義）、那份檔在黑名單上（稽核盯得到）、不必處理全文取得問題。

**session 序列**（範圍是參數，一次一個乾淨的階段邊界收 session）：

```
P   產 raw/ ＋ 情境測試/harry_potter/（照 §4 切分原則，刻意留 elicitation 缺口）；複製 書本模板/ init 書架
S1  develop            → 00-摘要、設定/{世界觀,角色,風格}
S2  organize → outline-test → 01-大綱（或局部 arcNN）
S3  worldbuild ＋ character ＋ expand 各一次   ← 橫向能力，不排它們物件軸／裁決流永遠是 0 實例
S4  beat-sheet → beat-test（arc01）
S5  write → write-test → revise（ch0001–）
S6  sync（反向萃取已承諾事實）＋ 收尾盤點
```

**《消失的密室》天然頂到每一軸**——這是它被選中的理由，也是本階段的價值來源：

| 重構自承 0 實例 | 出處 | 消失的密室怎麼壓它 |
|---|---|---|
| 事實層覆蓋率 0（93 章 0 章用 `## 本章事實`） | `功能報告/03` §8.4 | 金妮被附身 → 逐章 `狀態` delta；石化受害者遞增 → 集合運算 |
| 物件檔全 repo **0 支** | `功能報告/01` §8.4 | 里德爾的日記（狀態會變＋有隱藏身分）、密室與被攻擊的城堡（**作者點名的「會隨劇情變化的地點狀態」**） |
| `🧊 水下`／`揭示層級` | `物件.schema.md:83` | 日記的真實身分、金妮是兇手——揭底前 `write` 拿不到 |
| 裁決軸兩支檔 **0 本書有** | `功能報告/04`:544 | 「兇手是誰」「日記怎麼運作」是承重拍板 → `裁決流.md`；誤導線 → `待裁決.md` |
| `readiness` 乾淨書語料 | `功能報告/10`:494 | 從 `書本模板/` 開新書，`就緒.md` 從空白長起來（階段 0 已用 `驗證範例` 補了半格） |
| `_已併入/` 一次都沒跑過 | `功能報告/09`:439 | arc 局部大綱收斂進 `01-大綱.md` 時跑一次 |
| `beat-lint` 在乾淨書上不誤報未驗（「本輪最大的方法論弱點」） | `功能報告/02`:389 | 第一本照新規矩拆幕的書 |

**需求機制帳**（Q-M／Q-R 的量尺）。觀測者每 session 追加，三欄：

`作者說的需求｜系統把它存到哪（落點數 0／1／N）｜證據（file:line、閘門輸出、git diff）`

判定規則，**每一條都有機械症狀，不是讀感**：

- **落點 0 ＝ 機制缺口。** 症狀：閘門報 `夾帶`／自創節／`落點已廢除`；或內容被塞進自由散文；或作者答了但根本沒落檔（`SNN-*.md` 的 elicitation 紀錄有、`git diff` 沒有）。`derived-sync check` 的成長哨兵「事實行肥大／夾帶」是現成偵測器。
- **落點 N ＝ 機制重複**（作者要找的）。症狀：同一個值出現在 ≥2 個落點，**且沒有一個是投影**。逐筆做全書落點計數（`grep` ＋ `fact-refs --entity`／`decision-project --target`／各 `*-lint --emit`）。同形前例可當基準：`節奏檔位` 三份、`基調參照` 第四份、五支 rollup 獨有內容 47–78%。
- **落點 1 ＝ 正常。**

### 階段 3 — 裁決（需要作者）

輸入是階段 1 的射程帳 ＋ 階段 2 的需求機制帳，逐筆拍板。三條約束來自系統自己的原則：

- **新增機制**依 **E1** 指名「一個檢查器 ＋ 一個觸發時機」，且觸發時機要指得出**哪支 SKILL.md 第幾步或哪個自動化**（E1 後半句前十三輪零次執行）。填不出來就不准在 schema 宣稱。
- **收窄任何欄／節**依 **F4** 同時指出「非它的內容該去哪」＋目的地要有存在性守衛。填不出來就不准收窄。
- **消除重複**必須指名 N 份拷貝裡哪一份是正本，其餘改投影或刪除，並補守衛擋它們回來。**不要新開一份平行清單**——「一份會漂移的機讀資料」在 `docs/重構/02-待用構想.md` 已駁回清單裡被駁回過**至少七次**，唯一被接受的一次（`結構公式.md` registry）條件是「封閉、與真相同居一檔、不隨書成長」。

產物：`docs/驗證/03-拍板.md` ＋ append 進對應 `結構定義/*.schema.md` 的「## 沿革」節。已駁回的方案照 **F2** 落檔（讀者是 AI，沒落檔下次一定被重新提案）。

### 階段 4 — 執行與回歸

1. 套用階段 3 的拍板
2. 回歸四層：
   - `pytest`（8 套件）＋ `meta-lint` ＋ `.github/workflows/tools.yml` 全套
   - **17 份黃金檔重新蓋章**（見 1c 條件 2）
   - **重跑 `情境測試/skill覆蓋情境測試.md` 的 18 個情境**——那份檔自己寫著「每次改動 skill／技巧知識庫／結構定義後回頭重跑」
   - **乾淨書零誤報**：12 支閘門對 `harry_potter/` 全綠（`功能報告/02` 自稱「本輪最大方法論弱點」的第一次真正驗收）
3. **把 `harry_potter/` 立成第一份新格式真實語料黃金檔**，補上「22/39 支測試檔零真實語料」

### 階段 5（選配，本輪之後）— 生成力

作者已選 HP，所以「AI 太熟悉、量不準生成力」本輪不補。要補時：從 `examples/` 挑最冷門的一本（`我有一個修仙世界` 14 章最小，或 `從紅月開始`／`神秀之主`），跑短程序列，防火牆同階段 2。**這一階段才需要考慮升級成 worktree 物理沙箱**——生成力是唯一會被污染毀掉的量測。

---

## 五、為什麼分這樣的階段（context 隔離的理由）

- **階段 0／1／3 是唯讀量測與拍板，各自 context 獨立**：階段 1 要吃 coverage 輸出與 21 支指令的實跑結果，量大且與階段 2 的創作內容無關；混在一起會把 legacy 判斷污染成「這本書需不需要」。
- **階段 2 內部必須再隔離**：系統 agent 一旦看到原著或測試 meta，「系統輔助產出」與「AI 背誦原著」就分不開了，整個階段的結論作廢。這是 `端到端貫穿測試流程.md` §2 的地基決定。
- **觀測者與系統 agent 必須分開**：受測體不能自評。觀測者寫的是測試 meta，**永不進書資料夾**（進去＝把測試提示餵給系統 agent，`端到端貫穿測試流程.md` §5 記為「HP 教訓」）。
- **階段 1 必須在階段 2 之前**：階段 2 要用 12 支閘門的輸出當 Q-M／Q-R 的偵測器。閘門裡還有指向已廢除檔的相容分支時，它報的問題數是髒的。
- **階段 1a 必須在 1b／1c 之前**：那是全流程唯一不可逆的一步。

---

## 六、每階段的出場條件

| 階段 | 怎麼確認做完了 |
|---|---|
| 0 ✅ | `meta-lint` 綠且第 10 項覆蓋率行印出 `情境測試/` 的檔數（射程真的擴了，不是宣稱擴了）；`端到端貫穿測試流程.md` 不再出現 `就緒儀表`（除墓碑句） |
| 1a ✅ | 凍結檔存在，字元數 **≥ 78,542**（68,776 決定史 ＋ 9,766 rollup 不可重生），數字對得上 `功能報告/10`／`12` ——**實得 107,176**，四筆 rollup 逐格相符 |
| 1b ✅ | 帳裡每一列的「活用戶」欄都填了，coverage 報告可覆算 |
| 1c ✅ | `pytest` 全綠（**941 passed / 0 failed**）；`known-red.toml` 清空；每支刪除都有一支測試釘住「`一世之尊` 現在報什麼」（17 份黃金檔 ＋ 15 支新測試）；**沒有任何指令對 `一世之尊/` 吐 traceback**（21 支 × 病例書，**0 支**） |
| 2 | `需求機制帳.md` 每列都有機械證據欄；每個 `SNN-*.md` 都有防火牆那一行（M=0 也印）；`harry_potter/` 底下真的長出 `story/物件/*.md`、`story/參照/裁決流.md`、`chNNNN.ai.md` 的 `## 本章事實`——**這三樣目前全 repo 是 0 實例，長出來本身就是驗收** |
| 3 | 每筆拍板都填得出 E1 的（檢查器，觸發時機）與 F4 的（非它的內容去哪，目的地守衛） |
| 4 | 四層回歸全綠；12 支閘門對 `harry_potter/` 問題數 0（覆蓋率行仍印檢查筆數） |

> **1c 最後一格的條件改寫過，理由記在案。** 原文寫「逐支確認 exit ∈ {0,1}」，而 `decision-project --book 一世之尊` 回 **exit 2**——那不是本輪造成的，而且是**對的**：那本書從來沒有 `story/參照/裁決流.md`，exit 2 ＝「這本書還沒有這一層」是 `共同約定.md` 明訂的第三個值，且它照印覆蓋率行。
> **條件的本意是「不能崩」，而 exit 2 不是崩。** 改寫成三件事，並且**做成守衛**（`meta-lint` 第 6 項新增第 4 條契約，21 支 × 病例書實跑）：**不吐 traceback ＋ exit 在契約枚舉 {0,1,2} 內 ＋ 照印覆蓋率行**。
> **這不是放水，有反例為證**：`foreshadow-project` 在刪 spine 回退之後曾回 exit 2 並說「這本書還沒有這一層」，而那本書有 11 支 arc 幕綱檔——**那句話是假的**，已改成 exit 1。**exit 2 的正當性來自「這句話是真的」，不來自「它在枚舉裡」。**

**端到端的一句話驗收**：`meta-lint` ＋ `tools.yml` 全綠，`harry_potter/` 在 12 支閘門下 0 問題，`一世之尊/` 在 21 支指令下 0 traceback 且黃金檔記錄它已不受支援。
