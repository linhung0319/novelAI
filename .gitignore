# 版控設計（2026-07-12 作者拍板）：書資料夾（gothic_witch/ 等）與系統套件
# 一起在本 repo 版控——迭代型創作需要版本歷史，單人使用不需要分離。
# 下方 `/*` 白名單規則保留註解狀態即是此設計；若日後要改回「書獨立版控」，
# 恢復 /* 並逐一放行系統資料夾，書內另行 git init。
#/*

# 系統套件
!/.gitignore
!/README.md
!/novel-writing-system-plan.md
!/結構定義/
!/技巧知識庫/
!/書本模板/
!/Data/
!/docs/

# skill 版控，但 .claude 其餘（本地設定）不版控
!/.claude/
/.claude/*
!/.claude/skills/

# Python（`tools/` 底下的 8 個 uv 套件）——不版控環境與快取產物。
# 2026-07-30 校：原本這一行寫「子專案 C：tools/state_projection」，而那個套件
# 2026-07-27（功能 01）就被 `fact_projection` 取代並刪除了（commit 57352b9）。
# 規則本身對每個套件都適用，所以改成不指名任一套件。
.venv/
__pycache__/
*.pyc
.pytest_cache/
uv.lock

# MCP 除錯產物（playwright console/page 快照·非專案內容·非以檔案形式保留）
.playwright-mcp/

# 網站建置產物（由 GitHub Actions 部署，不版控）
/dist/
