# novelAI — AI 輔助小說寫作系統

這個 repo 是「寫作系統套件」本身，不是某一本書；書資料夾（如 `gothic_witch/`）放在根目錄下，與系統套件一起版控。完整說明見 `README.md`。

## 分類公理（動任何檔前先過這一關）

> **重設計進行中（2026-07-27 起）**：全系統的第一性原則已抽出到 `結構定義/設計原則.md`（六問＋原則 A–G），逐功能遷移中，進度見 `docs/重構/01-功能盤點與測試順序.md`。
> **本節仍是現行系統的操作規則**——執行既有 skill、動既有書內檔，依本節與 `共同約定.md`。**設計新東西、或重構某個功能時，依 `設計原則.md`。** 現行系統多處違反設計原則，那是已知的、正在遷移的，不是 bug 回報。

> **副檔名的設計意圖是「程式怎麼讀這支檔」，不是「誰寫的」。** 人類可讀性是蓋在這之上的 bonus，不是分界線。
> 它扛的是**兩個獨立的位元**，但**只有第一個由副檔名承擔**：
>
> | | 你的手改會不會被覆蓋 | 副檔名 |
> |---|---|---|
> | **源** | ❌ 你的編輯就是新的真相 | `.md` |
> | **衍生** | ✅ 下次重生即覆蓋 | `.ai.md` |
>
> **第二個位元「程式會不會解析它」由「有沒有檢查器」承擔**（`設計原則.md` **A4**，2026-07-27 功能 04 新增）：會被解析的檔必須有一支 lint，沒有 lint 的檔程式只能整檔讀。所以源檔底下再分「自由源」（`raw/`、設定層源、正文）與「**強格式源·作者利器·直改**」（大綱→`outline-lint`、幕綱→`beat-lint`、`story/物件/<名>.md`→`object-lint`、`story/參照/裁決流.md`＋`待裁決.md`→`decision-lint`、`story/參照/就緒.md`→`readiness-lint`）。
> **`.co.md` 這個字位已廢除**（既有書仍用舊名的，工具照吃）——它當初就是為第二個位元開的，而幕綱與物件檔一直用 `.md`＋lint 做同一件事。
>
> 格式保證由 `derived-sync validate`／`fact-lint`／`object-lint`／`beat-lint`／`ch-lint`／`decision-lint`／`world-lint`／`char-lint`／`style-lint`／`summary-lint`／`outline-lint`／`readiness-lint` 在守；**沒有檢查器的保證只是口頭承諾**。
> **`world-lint` 是 2026-07-27 功能 05 新增的**（世界觀軸：front-matter 欄語意／伏筆歸一／`_總覽` 視圖 ≡ 資料夾／幕綱檔名引用不懸空）。同輪依 A4 **刪掉世界觀 `.ai.md` 的四個宣告欄**（`影響力`／`主從`／`維度`／`升格哨兵`）——它們寫成機讀格式而零解析器零消費者、已實測漂移；內容降級成 `## 影響力` 節的散文。同輪新增的推論：**凡 schema 寫「供 X 盯」，X 那一側要指得出讀它的那一行**（`設計原則.md` E1）。
> **`style-lint` 是 2026-07-27 功能 07 新增的**（風格軸六項：front-matter 五欄齊全／`禁用詞表` entry 不得含括號／`稱謂系統` 形狀／**`可用句式` 的顆粒度 token 位置＋阿拉伯數字＋不得全落最粗一級**／`語域` 不得半形括號／`基調參照` ≡ `00-摘要.ai.md` 的 `基調`），另順帶掃正文的字面禁用詞（只印覆蓋率與候選、不做 pass/fail）。同輪依抉擇 5 B **廢除風格 `.ai.md` 的本體四節**（實測是源檔的**同長度改寫**：1.02×、8-gram 重疊 30%，而角色／世界觀的衍生本體是新表述）——衍生檔只留 front-matter 五欄，腔調散文的唯一落點是源 `風格.md`，`write`／`write-test`／`revise`／`develop` **兩邊都讀**（同角色軸的 `read_paths`）；`DERIVED_SECTIONS["風格"]` ＝**空 tuple**（有任何 `##` 節就報）。
> **同輪改寫 A4**：檔的第二個位元由「程式會不會解析它」改成「**有沒有人逐欄／逐行取值，程式或 LLM 都算**」——風格五欄零程式解析器但**消費者存在且指得出行號**，照舊字面套用會刪掉本軸唯一的產物（不是無法決定，是決定錯）。**LLM 取值者比程式取值者更需要 lint**：程式讀壞格式會當場炸，LLM 會安靜地讀錯，而錯的那一格會直接進正文。**這不影響 04／05／06 的既有結論**（那三軸是零取值者）。同輪新增 E1 第四條推論：**凡 schema 宣稱某欄「供 X 核對」，要一併定義「什麼樣的值算可核對」且機械可判**。
> **`char-lint` 是 2026-07-27 功能 06 新增的**（角色軸八項：**每支源檔都有衍生檔·從源側掃**／必填節不得是佔位／front-matter 欄語意／伏筆歸一／`_index` 視圖 ≡ 資料夾＋各檔 front-matter／`一行需求` 內容判準／單檔與目錄形態不得並存／幕綱角色欄的未知 token）。同輪依 A4 **刪掉角色 `.ai.md` 的三個宣告欄**（`角色`／`弧線類型`／`影響力`），留下的四欄每一欄都指得出 `char-lint` 裡讀它的那一行。同輪新增兩條推論（`設計原則.md`）：**凡宣稱「每一支 X 都有一支對應的 Y」，要有一支從 X 側出發的檢查**（E1；否則守衛對「Y 不存在」永遠報乾淨，系統就會被逼著建假檔去餵它）、**覆蓋率行要能回答「命中的筆數裡有幾筆是空的」**（E2；只印命中率＝用命中率冒充可用率）。
> **`outline-lint` 是 2026-07-28 功能 09 新增的**（大綱軸十一項：節枚舉／檔頭狀態二選一且與目錄一致／`_index` 視圖 ≡ 資料夾＋列序／`幕NNN`·`chNNNN` 引用不懸空／伏筆歸一／`結局：` 非空非模糊＋`題旨：` 因果句型／`本段收束與鉤子` 三格／同一 arc 不得兩邊都有內容／目的地存在／`本 arc 伏筆狀態` 三欄且禁既成事實 token／`卷級方向（卷X·本卷權威）` 的卷 token 唯一與指路紀律），**落 `tools/beat_metrics`（第三個指令，與 `beat-lint`／`ch-lint` 共用 `structure.parse_book` 的幕號 registry ＝零複製）**。大綱曾是**這份分類公理自己列在第一位、而 lint 不存在**的那一項——與 04 抓到 `.co.md` 的假宣稱同形，換了主詞活到 09。同輪：`## 本 arc 伏筆狀態` **收窄成三欄意圖表**（既成事實一律問 `foreshadow-project --arc`）／`結局與題旨` **切乾淨**（「結局是什麼」回指 `00-摘要.md`，本節只寫怎麼走到那裡）／`01-大綱.md` 與 scoped 大綱**按卷分工**＋`beat-sheet` **路由反轉**／併入的最後一步改成 `git mv` 進 `story/大綱/_已併入/` 且**搬檔前先印保真率**／成長哨兵補上整個大綱軸（第七次「四份手寫路徑清單漏檔」）。同輪改 `設計原則.md` 兩條：新增 **A5「退役源」**（源檔的身分只能被**另一支檔的存在**撤銷、撤銷要從檔案系統看得出來、撤銷前要驗收「內容去哪了」）、**F2 補第五格「綁單一卷」**。**兩條都不影響 01–08 的既有結論。**

> **`readiness`／`readiness-lint` 是 2026-07-28 功能 10 新增的**——那一輪把 `story/參照/就緒儀表.ai.md` **廢除**：實測它是全書最大的一支檔（**292,591 B、六天 254.7×**），而 **53.8% 是拍板日誌**（家在 04 就蓋好了，缺的只是「誰去搬」），三節「現況」投影出來只有 **306 字元（419× 壓縮）**。最狠的一格是結構性的——它**在任何命名下都拿不到格式守衛**（舊名 `.md` 讓 `check_book` 的 `rglob("*.ai.md")` 掃不到，新名 `.ai.md` 讓 `_is_declarative` 早退三次，而其中兩項與那個豁免的理由無關）。處置是**拆**：不可重生的兩筆（成熟度判斷、就緒清單）留 A1 源檔 `story/參照/就緒.md`（五 token 封閉枚舉、無備註欄、恰兩節，`readiness-lint` 守），可重生的四筆走 `readiness` 投影（三節、0 也印，**跨套件的印「未接」＋該跑的指令**）。同輪：測試執行紀錄落 `幕綱/arcNN.md` 檔頭宣告行與 `chNNNN.ai.md` 的 `write-test` 欄（`beat-lint`／`ch-lint` 守形狀，**缺席合法**），`story/參照/` 納入體積哨兵（**第八次「四份手寫路徑清單漏檔」**，這次漏的是全書最大的一支檔）。同輪改 `設計原則.md` 三條：新增 **A6「無源衍生 ＝ 一個必須解掉的衝突，不是一種檔」**（與 A3 同形反向，兩條合起來才填滿 `(會不會被覆蓋)×(人能不能改)` 的 2×2 表）、**D3 補推論**（凡把一支檔改成投影，先逐格回答「這一格的機械來源是哪一支檔的哪一欄」，答不出來的那幾格是真正的源）、**E2 補第七種形態「豁免的射程比它的理由大」**。**三條都不影響 01–09 的既有結論。**

動任何檔前問三次。權威在 `結構定義/共同約定.md` 零。

**第一問：怎麼被讀 → 該不該限制大小**

| 類別 | 讀取方式 | 檔案大小 | 單行長度 |
|------|---------|---------|---------|
| **自由源**（`raw/`、`<實體>.md`、`00-摘要.md`、`chNNNN.md`） | **必須整檔讀** | **要限制**（靠資料夾骨架引導分檔，檔名即選擇器） | 不限 |
| **強格式源·有 lint**（大綱、幕綱、`story/物件/<名>.md`、`參照/裁決流.md`、`參照/待裁決.md`、`參照/就緒.md`） | 程式按需切片，作者可直改 | 不限 | **要限制** |
| **只由AI與程式產生**（`.ai.md`） | 程式按需切片 | 不限——**但沒有工具能切它，就跟源檔同等受限** | **要限制** |

> **「有投影工具」不豁免行長**：投影的粒度**就是行**，一行不可再切。實測某本書的單一事件行從 76 字長到 194 字（最長 645）、括號註解佔比 36%→52%，長了 11 個 arc 沒有任何哨兵吭聲——因為當時整支檔被豁免了。

**第二問：可不可重生 → 該住 `.ai.md` 還是源檔**

能從別的檔重算出來 → `.ai.md`（走 hash 偵測，源一動就報 stale）；不可重生 → **源檔**（不重生）。**住錯的下場相反且都很糟**：可重生的關進源檔，源改了沒人發現（孤兒事實永遠躺著）；不可重生的放進 `.ai.md`，下次重生直接消失。

> **可執行的判準**：把 `.ai.md` 全刪掉、只拿源檔重跑一次，這條會不會回來？
> 會 → 它是源的**新表述**（源寫「怕水、欠債、嘴硬」，`.ai.md` 展開成四象限＋馬斯洛，字數十倍也仍是同步）。
> 不會 → 它是**新資訊**，得回寫源檔，或進 `story/物件/<名>.md`／`參照/裁決流.md`／`參照/待裁決.md`。
>
> **共創問答問出來的具體事實正是這一類**（源檔沒寫「他喜歡蘋果」，重生一次可能變成梨子）。它最危險，因為看起來像分析、住在分析區、每次重生前都還在，直到某次重生吃掉它而沒有人會發現——`.ai.md` 沒有 diff 會被審。正解是**把結論回寫源檔一句話**，讓 `.ai.md` 重生時從那句話展開。

**第三問：事件流還是登記表 → 不重生的檔底下再分一次**

歷史本身就是內容（判例要能回查）→ **append-only 事件流**（`story/參照/裁決流.md`）；只查「當下有效的那一組」、欄位會被就地改 → **登記表**（`story/物件/<名>.md` 的「## 不得寫成什麼」、`story/參照/待裁決.md`）。判錯的代價是**死行累積**：把登記表做成 log，延長射程就只能再 append 一行同名的去覆蓋，舊行從此不再被讀到卻永遠不會消失。
**登記表一律不設「狀態」欄**——狀態要嘛可由別欄導出，要嘛因為表裡只住一種狀態而恆真；硬設一個窄狀態欄的下場是欄位溢位（實測舊回饋表 7 列平均 171 字元、最長 440、佔整列 61%）。

### 作者能怎麼碰這些檔

**`.ai.md` ＝「別改，改了會被覆蓋」的唯一訊號**（`共同約定.md` 二）；**`.md` 底下「這支檔要不要守語法」則由有沒有 lint 決定，不由副檔名決定**（A4）。**但 `.ai.md` 不是完全不能碰，差別在碰的方式**：它保證格式，所以作者可以在**既有格式內**改一格的內容（改表格某欄、改一句分析）；但**不該在裡面新增結構**（開新節、加新欄）——那是 schema 的事。而且任何手改都只是「**回報一筆修正**」，重生就沒了；要留住它，得回頭改源檔。

`.md` 源檔與作者利器都可以改：「你改就是新的真相」。**強格式源**（`參照/裁決流.md`、`參照/待裁決.md`、`參照/就緒.md`）平常由 AI 寫，但**不重生，所以你的校訂會留著**——代價是**你改壞了也沒人幫你修**，故它有格式要守（`decision-lint`／`fact-lint`／`readiness-lint` 會直接點名半形括號、狀態格寫成散文這類誤打）。宣告式綜合檔只剩 `參照/結構.ai.md` 一支（帶 `.ai.md` 但無單一源，`derived-sync check` 報 `declarative`、不必也不能 `stamp`）——**而「宣告式」這個格子本身已被判定是錯的**（`設計原則.md` **A6**），它的處置交功能 11。

推論：某份內容若屬第二類卻被寫成自由 prose、或住進源檔（裁決理由、正文釘死的事實），**正解是移進有投影工具的軸，不是定期刪它**。

三個落點（皆別整檔讀，走投影）：

- **`chapters/chNNNN.ai.md` 的 `## 本章事實`** — 該章正文釘下的事實，**可重生**：`狀態`（6 維 delta）／`錨〔名〕`（正文寫死的數字·專名·道具語彙）。作者改正文 → `derived-sync check` 報該章 stale → 重生即修正（重生時**以舊 delta 為輸入**，不空白重抽）。
  **內容欄只寫「這一幕改變了什麼」**：`知識前沿`／`持有`／`能力` 是集合運算（`＋已知〔X〕`／`〔X〕→已知`／`−〔X〕`），仍然成立的舊事**不重抄**（`fact-project --history <實體>/<維度>` 查得到）；`位置`／`所屬`／`關係` 是純量、維持覆蓋。
- **`story/物件/<名>.md` 的 `## 不得寫成什麼`** — 下游不得寫成 X 的排除線。**一條一列的規則表**（`約束名｜不得寫成｜生效自｜解除於`，實體＝檔名），**不可重生**（正文裡沒有這句話）。`生效自` 寫 `全書` 即回溯生效；延長射程＝改 `解除於` 一格，不新增列。**射程＝全書、不綁實體的書級方針住 `story/物件/全書.md`**（型別 `方針`）。
- **`story/參照/裁決流.md`** — 作者拍板的「為什麼」（判例／護欄／本輪拍板記錄），**append-only**（歷史本身就是內容）。查詢 `decision-project --target <源檔路徑> --active-only [--as-of arcNN]`（給了 `--as-of` 就由程式依射程欄自動判過期），**同一個查詢會一併吐出 `story/參照/待裁決.md` 的相關列**。
- **`story/參照/待裁決.md`** — 下游 skill 發現「根源在上游」的觀察，**四欄無狀態欄**（`日期｜來源｜標的｜發現`）。作者拍板後：結論落源檔／事實軸，理由 append 進裁決流，**這一列刪掉**。

前兩者由 `tools/fact_projection` 合流查詢，**且該由程式選 context、不是 LLM 自己填**：`fact-project --for-beat 幕NNN`（讀該幕的角色欄自動導出實體；**書級方針一律印，0 條也印**）。

**不要把這些內容寫進設定層 `.ai.md` 或源檔**——「硬事實」「下游硬約束」「反派備註」這類自創節、以及檔末的裁決 blockquote，都是違例。**在守的工具**：`derived-sync check`（新鮮度＋成長哨兵，含「事實行肥大／夾帶」）／`derived-sync validate`（`.ai.md` 的 front-matter 與節枚舉）／`fact-lint`（事實信封行的格式、落點、集合語法、命題名、純化三條）。改了上游事實後，用 `fact-refs --entity <名> --after 幕NNN`／`--anchor 〔名〕` 找**下游正文已經按舊事實寫下去的地方**——那是 `derived-sync` 看不到的一半。

## 三個組成部分

- `結構定義/` — 各產物（摘要／世界觀／角色／風格／大綱／幕綱／章節／參照檔）的權威格式（schema），含所有 skill 共用的 `共同約定.md`。設定層（世界觀／角色／風格）＋摘要＋章節採**源→衍生兩檔**（人寫 `<實體>.md`＝唯一真實來源，AI 重生 `<實體>.ai.md`；變動偵測 `tools/derived_sync`）。承重角色的源用**目錄形態**（`角色/<名>/{核心,來歷,能力,關係,水下}.md`，檔名即切面選擇器；`水下` 是揭底前的存取控制，`write` 拿不到）。
- `技巧知識庫/` — 整合後的寫作方法（創作流程／因果法則／結構公式／需求四象限／角色塑造／伏筆與意外／戲劇張力與懸念／主題題旨／結局類型／世界觀與背景／氛圍與筆調／選題與收斂／視角與敘事人稱／場景與情節推進／展示而非講述與五感描寫／對白／動作場面寫作／行文節奏與呼吸／網文與文學取捨）；扁平並列，檢索入口見 `技巧知識庫/_index.md`。技法有三種效力——**地板**（always-on）／**天花板**（依取向定位 opt-in）／**配額**（好東西但密度上限就是本體），對照表見 `_index.md`「範例與分層政策」。網文↔文學的技巧取捨（何時用哪套）見 `技巧知識庫/網文與文學取捨.md`；「看情況二選一」的 OR 抉擇對照表見 `技巧知識庫/_index.md`。
- `.claude/skills/` — 操作：skill 承載「操作」、知識庫承載「方法」。

## Skill 地圖（作者在哪個階段，用哪個）

| 情境 | skill |
|------|-------|
| raw 很亂、主線/題旨/結局未定（粗階段） | `develop` |
| 大方向就緒，整理成大綱（全書或局部 arc） | `organize` → 審稿 `outline-test` |
| 某個 arc/段落只有一個橋段、劇情單薄，要橫向長厚（偵測缺口→提案衝突/子情節/新角色） | `expand`（作者拍板後才寫入）|
| 粗階段後任何時候，世界觀累積待確認矛盾／某維度要深做補厚／力量體系要跑限制代價檢查／要折入外部 research | `worldbuild`（作者拍板後才修訂正文）|
| 粗階段後任何時候，要獨立設計單一角色（新反派／配角，或替既有角色補需求弧線）——非因劇情缺口、非冷啟動打包 | `character`（作者拍板後才寫源 `設定/角色/<名>.md` 並重生 `.ai.md`）|
| 大綱拆幕綱 | `beat-sheet` → 審稿 `beat-test` |
| 依幕綱寫正文 | `write` → 審稿 `write-test` → 修改 `revise` |
| 上游改動後找過期下游／回頭收斂前盤點已承諾事實 | `sync` |

大多數 skill 是**垂直**推進（摘要→大綱→幕綱→正文）；目前有兩個對稱的**橫向**能力——不往下一層走，而是把同一層某個產物就地補厚＋自洽：`expand` 做**劇情層**（把單薄的 arc 就地長厚）、`worldbuild` 做**設定層**（把 `設定/世界觀/<主題>.md` 就地補厚並消矛盾，並回頭消化下游追加到 `<主題>.ai.md`「待裁決回饋」的回饋列）。設定愈厚，`expand` 的世界觀自洽檢查愈準——兩者互相餵養。

## 沒有 skill 觸發、或需求太小不值得跑整套 skill 時

系統有三個「檔位」：**答問**（無產物）、**輕量輔助模式**（小型生成／定點編輯）、**完整 skill 流程**（大動作）。skill 是「大動作」的策展式操作，不是每個小需求都要動用它——小需求走輕量模式，避免大砲打小鳥。

- **答問**：回答任何寫作技巧問題（對白、角色動機、結構、伏筆、POV……）之前，先查 `技巧知識庫/`（入口 `_index.md`「何時查它」欄）有無對應方法，依方法作答並註明出處檔名；知識庫沒有的，明說是 LLM 自身判斷。
- **輕量輔助模式（小型生成／定點編輯）**：當作者要的是「只動單一產物的局部、不產生新結構承諾、不需拍承重抉擇」的小需求（想一則幕尾懸念／改好這一句對白／補一個感官細節／擬一小段文字）時，走輕量模式，**不跑整套 skill**：
  1. 用 `技巧知識庫/_index.md`「何時查它」欄定位對應技法檔，讀其檔頭標**核心**的小節；
  2. 依 `結構定義/共同約定.md` 資源定位，拉相關權威資料當 context（`00-摘要.ai.md` 取向定位／源 `00-摘要.md` 基調、`設定/世界觀/*.ai.md` 核心規則、`設定/角色/<名>.ai.md` 對應角色、`設定/風格/風格.ai.md` 腔調禁用詞——只讀需要的）；
  3. 產出**單點提案**，並註明取自哪個技法檔；
  4. **不寫入書內檔，或只提議一處編輯待作者拍板**，不套用整個 skill 的流程與儀式。
- **降檔閥（反僵化）**：即使某重 skill 的 description 命中，但作者實際要的只是上述單點需求，可用一行說明改在輕量模式處理，並主動問「要不要跑完整 `<skill>` 做系統版」——升不升級由作者決定。
- **升級閥**：小需求若牽出結構問題（要新增／移動幕、動到既定因果或伏筆、改承重抉擇），停在輕量模式、指回對應 skill（`beat-sheet`／`write`／`organize`…），不硬在小檔位裡解。
- **修改書內檔前**：先讀 `結構定義/` 對應 schema 與 `共同約定.md`，遵守格式與「AI＝共創＋審稿、非門檻」原則（輕量模式若只提議不寫檔，此條在作者採納、要落檔時才適用）。
