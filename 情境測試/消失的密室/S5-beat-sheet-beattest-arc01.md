# S5 beat-sheet → beat-test — arc01（＋開場清帳）

> 本 session 的射程：① S4 觀測者抓到的書側殘留（分叉五過期宣告）清完；② **arc01 第一次拆幕**（本輪測試的七軸表裡「`beat-lint` 在乾淨書上的真考驗」那一格）；③ `beat-test` 審 arc01 並落裁決。
> 受測體 **2 支**（清帳 1 支跨 2 輪／拆幕 1 支跨 4 輪），審稿員 **1 支冷啟**，神諭 **6 支各冷啟一輪**。

## 系統路由觀察

| # | 作者這樣說 | 系統走了哪個檔位／skill | 對不對 |
|---|---|---|---|
| 1 | 「有兩個地方寫錯了，幫我改掉」（＋兩個具體位置） | **輕量輔助模式**（定點編輯，未跑任何 skill）；自行加跑 `decision-project` 查禁令、`derived-sync stamp` 重新封章 | ✅ 對。這是 CLAUDE.md「降檔閥」要的形狀——單一產物局部、不產生新結構承諾。**它沒有升級成 `sync`**，而 `sync` 的 description 其實命中（「我改了…哪些要跟著動」），命中了也沒誤觸 |
| 2 | 「大綱那一層我覺得夠了，**arc01 開始拆幕**」 | **`beat-sheet`** | ✅ 對，一次到位 |
| 3 | 「arc01 的幕綱剛拆完，**幫我審一遍**…不要自動幫我改，也不要給我通過／不通過」 | **`beat-test`**（獨立冷啟的第二支 agent） | ✅ 對。它**真的沒有代改**——六題全數回交作者，連檔頭 `beat-test:` 那一行都沒自己落（見下方摩擦欄第 1 條，那個克制反而過頭了） |
| 4 | 作者對 beat-test 的裁決（13→15 幕、開設計註、改檔頭懸念…） | 導演轉給 **`beat-sheet` 那支 agent** 落檔，不給審稿員 | ✅ 分工守住了：審稿員診斷、拆幕者落檔。這是導演的路由決定，不是系統的 |
| 5 | 「這條規則要記，交 `worldbuild` 做」 | 受測體**沒有去寫那支世界觀檔**，只把待裁決那列與三條規則交棒 | ✅ 對，射程邊界守住 |

**一次都沒有代作者拍承重抉擇。** 六輪 19 題全部回交。**累計：作者拒絕代選 5 次，系統代選 0 次**（不變）。

## Elicitation 紀錄

**6 輪 19 題，fallback 0 筆**（逐輪見 `fallback-log.md`）。

| 輪 | 問了什麼 | 作者答什麼（要點） | 來源 |
|---|---|---|---|
| 1 | `raw/還沒想清楚的.md:33` 那句舊問句要不要劃掉 | 劃，但改因寫法要跟第 64 列不同（那次作廢的是**錯的值**，這次是「後來拍了」）；並劃出界線：**回去劃 raw 的判準只有「那一行還在主張一件已經不成立的事」**，不是每拍一個板都回頭掃 | 腦 |
| 2 | 系統就地改了自己幾分鐘前寫進 append-only 裁決流的半句，對不對 | **改回去**。三層理由：① 那半句根本不是「理由」是 TODO 穿理由的衣服，家在 `待裁決.md`；② append-only 的原生更正方式就是 append，而形狀是閉合的（新列標的 `全書` ⇒ 一律命中，更正保證跟著被更正的東西走）；③ **不接受「同一輪內」這條界線**——「幾分鐘前、我自己寫的、還沒人讀過」這三個條件每次就地改都會成立 | 腦（判斷）|
| 3 | 拆幕後六題（多比扣信／坩堝歸屬／13 幕 3-2-4-4／統一母題／就緒 token／三個裸名字） | 認信、**否坩堝**（那是系統為填不存在的洞而發明的動作）、認切法但幕011 升進必須發生、認母題但**禁入正文**、`就緒` 改 `草擬`＋寫死升格三條件、威農達力不建檔／**亞瑟現在就定名建檔** | 腦 ＋腦（判斷）|
| 4 | 保密法那條規則歸哪支世界觀檔／`亞瑟.ai.md` 的 `伏筆` 欄 | **新開第 8 支 `監管與曝光.md`**（不併進 `權力與程序.md`：那支扛「程序是假的」，這條是反面——程序完全正確，只是在乎的順序反了）＋三條規則＋「不准吐槽」的寫法約束，**交 `worldbuild`**；`伏筆` 欄改回 `{}`（**埋的人不是亞瑟**，是幕013 那一格） | 腦 ＋腦（判斷）|
| 5 | beat-test 的六題＋三高＋中低逐項 | 全拍。幕數 13→15、洞穴屋拆兩幕、「欠學校一個交代」歸 arc01 當最後一幕（鄧不利多不在場，否則生出第五個不求救的格子）、監管三條落幕003＋幕015、幕007 那個人**是海格**但傘的埋設維持 arc02、金妮埋設移到坩堝那一幕、**檔頭懸念從「他回不回得去」改成「那個位置願不願意要他」** | 腦（判斷），自述沒翻 `examples/` |
| 6 | 幕號重編認不認／兩處越界動作／莫莉建不建檔 | 三件全認，附三處反打：① **「重編代價是零」數錯了集合**，實測幕號字面外洩三處；② 刪形容詞前**漏報「有沒有無家可歸」**——刪唯一落點與刪第 N 份備份在報告裡長得一樣；③ 拿「肥大哨兵沒觸發」當留設計註的理由是**接錯線**（schema 那句沒有條件句）；莫莉建，並把建檔門檻操作化成「**不寫下來的話，預設寫法會不會剛好對**」 | 腦（判斷）|

**`fallback-log.md` 上掛了兩個 session 的可證偽預測，本輪第一次真的被測到，結果是預測沒兌現**——詳見下方摩擦欄第 2 條。

## 產物

**書側（10 改 4 新）**

| 檔 | 動了什麼 |
|---|---|
| `story/幕綱/arc01.md` | **空骨架 → 15 幕**（本書第一支真幕綱）。承諾區（節奏檔位含資訊差引擎宣告／統一母題／14 條必須發生／19 條排除線）→ 幕001–015 → 伏筆狀態表 6 列 → **`## 設計註（下游不抄）`（本書第一次用到這一節）**。檔頭落 `beat-test: 2026-08-01·3高7中4低` |
| `story/大綱/arc01.md` | 補三場（警告信／洞穴屋那一晚／地下室）＋坩堝歸屬重寫＋刪掉那本本子的三個外觀形容詞 |
| `story/參照/裁決流.md` | **128 → 150 列**（＋22） |
| `story/參照/待裁決.md` | 0 → **1 列**（保密法那條規則沒有家，交 `worldbuild`） |
| `story/參照/就緒.md` | 「結構／幕綱」`空白` → **`草擬`** |
| `story/設定/角色/亞瑟.md` ＋ `.ai.md` | **新建**（四格：職位小／在推麻瓜保護法／**那輛車是他自己造的**／書店那架是他先動手） |
| `story/設定/角色/莫莉.md` ＋ `.ai.md` | **新建**（三格：咆哮信罵的是丟臉不是差點死掉／**她愛所有人但沒有在聽金妮**／不准點破亞瑟那個諷刺） |
| `story/00-摘要.md`／`.ai.md`／`密室與繼承人.ai.md`／`raw/還沒想清楚的.md` | 分叉五四處過期宣告清完 |
| `story/設定/角色/榮恩.md` | 「從第二章裂到最後」→ 事件名（幕號外洩三處之一） |

**inline 審稿結果（`beat-test` arc01）**：**3 高／7 中／4 低**。三高＝亞瑟四格只落地一格／幕002 八欄裡沒有那句警告／arc01→arc02 中間有一場沒有家。**全部處理完**（前兩者補進幕綱，第三者長成幕015）。

**導演端獨立覆核 12 支閘門**：**11 支 exit 0，`char-lint` exit 1**——「17 支源沒有衍生檔」，**數字與 S3／S4 完全相同**（源 22→24，亞瑟與莫莉都同時進兩側，所以缺口數沒動）。這是 07-31 拍過板的既有讀數，不是本輪的傷。

**七軸表：本 session 破 0 一軸。**

| 軸 | S4 收工 | S5 收工 |
|---|---|---|
| `beat-lint` 在乾淨書上不誤報 | 0 幕（S2 起連掛三輪） | ✅ **1 支 arc／15 幕／14 條前因／6 個伏筆標記／19 條排除線，目的地不存在 0，exit 0**。`beat-metrics` 空洞幕 0/15、分區衛生 0 幕、母題重複 4.5‰；`structure-project` 3/3/4/5 可疑點（無） |
| `story/物件/` | 19 支 | 19 支（不變） |
| `裁決流.md` | 128 列 | **150 列** |
| `待裁決.md` | 0 列 | **1 列**（歸零維持了一個 session） |
| 角色源／衍生 | 22／5 | **24／7** |

剩 2 軸：`本章事實`→**S6**、`_已併入/`→仍卡在分叉一。

## 摩擦與缺口

1. **`beat-test` 的克制過頭了一格，而那一格是機械讀數不是內容。** 作者說「不要自動幫我改」，審稿員因此連檔頭那行 `beat-test: 2026-08-01·…` 都沒落，代價是 `beat-lint` 印「**0/1 支 arc 有 beat-test 紀錄**」。作者的裁決把界線劃乾淨：「**『我跑過了』不是內容，是機械讀數；不落等於讓 `beat-lint` 印一個假的 0，而假的 0 比沒有讀數糟。**」——`beat-test/SKILL.md` 的「不代改」與 `幕綱.schema.md` 的「誰寫：`beat-test` 的收尾」在這一格互相打架，而**打架的結果是往「印假 0」那一邊倒**。交階段 3。

2. **`fallback-log.md` 掛了兩個 session 的可證偽預測，本輪第一次真的被測到——沒兌現，而三種形狀都出現了。**
   預測原文（S2 立、S3 加碼）：「**`beat-sheet` 拆幕時 fallback 必須回來**……**若仍是 0，那就不是 lane 切得好，是問題不夠具體**——屆時要回頭查系統有沒有在拆幕時自己填了質地而沒問。」
   本輪 `beat-sheet` ＋ `beat-test` 都跑了，**fallback 仍是 0**。回頭查的結果是**三種形狀並存，不是一種**：
   - **① 自己填了、但同時舉手問了**：幕008「哈利把簽名書倒進最近那個坩堝」——系統發明了一個大綱沒有的動作來補幕009 的前因，**但它把這件事寫成「請認或否」並落進 `待裁決.md`**。作者答「否，你的前提就錯了……那是你為了填一個不存在的洞而發明的動作」。**填了，但沒有偷偷填。**
   - **② 沒填也沒問（真正的漏）**：幕002 八欄裡「今年學校會發生很可怕的事」一個字都沒有——全書最遠那條線在第一次觸發時是空的。**抓到它的是 `beat-test`，不是拆幕者、也不是作者。**
   - **③ 問題確實不夠具體**：19 題裡沒有任何一題是「這一場誰先開口」「停在哪一句」。**拆幕這個階段本身就不產生那種問題**——幕綱八欄要的是行動／衝突／結果的骨架，句子層級是 `write` 的事。
   **所以那條預測的前提站不住：它把「拆幕」當成質地層的入口，實際上質地層的入口是 `write`（S6）。** 預測不作廢，**改掛到 S6**，判準照舊：S6 若仍是 0，就要回頭查 `write` 有沒有自己填了句子層級的東西而沒問。

3. **幕號重編這一次是在沒有人看的情況下通過的，而作者的反打實測到三處外洩、其中兩處是系統自己上一輪寫壞的。** 系統的成本帳寫「零章正文、零跨 arc 引用 ⇒ 代價是零」；作者指出**該數的集合是「幕號字面漏出 `幕綱/` 之外的地方」**，並當場舉出 `榮恩.md:7`。系統補跑後實測 **3 處**——多出來的兩處是 `亞瑟.ai.md:35,40`，**它自己上一輪寫的，而且拆幕之後已經指錯幕**。
   **缺口**：`beat-lint` 沒有「幕號字面出現在 `幕綱/` 以外的哪幾支檔第幾行」這個投影，而那張清單是重編前唯一該看的東西。作者的定性可直接搬：「**沒有檢查器的保證只是口頭承諾**」——「日後有正文之後不得再重編」現在只住在裁決流一列裡。交階段 3。

4. **`## 設計註（下游不抄）` 是一個沒有搬移觸發條件的「暫存區」，而這本書剛把它變成常設區。** schema 第 52 行寫「暫存區·**落檔時必須搬走**」，本輪把 14 條前因理由＋α 變形推導＋佈線論證全搬進去並**留在檔內**。系統給的理由是「肥大哨兵沒觸發」，作者當場指出**那是接錯線**（哨兵管肥大，schema 那句沒有條件句），並要求如實記名：「**這是我在破 schema，不是哨兵批准的。**」
   作者的形狀判定值得整句帶到階段 3：**一個沒有搬移觸發條件、又沒有任何東西會反對它變成常設區的「暫存區」，形狀等同被廢掉的那五支 rollup。**
   （正面的一半也要記：這一節**確實有效**——母題重複率 9.9‰ → **4.5‰**、分區衛生 0 幕，八欄裡的主題陳述與元管理標籤清光了。它解決的問題是真的，只是它自己的出口是空的。）

5. **「交棒給還沒誕生的檔」在 `decision-lint` 上是硬牆，而繞道會削弱交棒的精確度。** 作者要把待裁決那列的標的改成 `設定/世界觀/監管與曝光.md`（交 `worldbuild` 建），`decision-lint` 當場報「標的在書內找不到」——那一欄驗存在性。系統照 07-31「不建 `地理.md`」的先例改成 `全書`，靠「標的為 `全書` 一律命中」保證 `worldbuild` 撈得到。
   **這一格目前是靠先例撐住的、不是靠機制**：`全書` 是一個**過度廣播**的標的（五支 skill 每次都撈得到它），拿它當「指向一支還沒出生的檔」的替身，代價是**這個交棒在帳上看不出來是交棒**。同型情況會反覆出現（每一次「這個要交給下一支 skill 建」都會撞到）。交階段 3。

6. **裁決流的「第 N 列」是一個有歧義的座標，而歧義只有在跨角色引用時才會現形。** 作者說「第 129／130 列」（`decision-lint` 的**序號**），系統查證後發現本書既有的七八處交叉引用一律用**檔行號**，且序號系統**產不出**檔內已經在用的「第 133 列」「第 145 列」這種數字。系統自己選了檔行號並列了對照表。
   **兩套編號同時活著，而 `decision-lint` 的錯誤訊息印的是檔行號、它自己的計數印的是序號**——同一支工具的兩個輸出用兩套座標。這一輪沒有出事是因為系統去查了；下一輪它不查就會改錯列。交階段 3。

7. **神諭的讀取射程又一次超出基座 §3 給它的三個位置，而導演這一輪沒有在轉述時標記——這是 S4 明文交代要做而沒做的一件事。**
   S4 摩擦欄寫死：「基座 §3 給神諭的讀取權是 `raw/`＋`情境測試/`＋（fallback）`examples/`，**沒有明文給也沒有明文禁系統套件**……**下一輪若再出現同型，導演要在轉述時標記它。**」
   本輪第 5、6 兩輪的神諭自述讀了 `幕綱/arc01.md`、`大綱/arc01.md`／`arc02.md`、`裁決流.md`、`亞瑟.md`／`金妮.md`／`海格的雨傘.md`／`榮恩.md`／`就緒.md`、`物件/日記.md`、以及 `幕綱.schema.md`。**這不是 fallback**（兩輪都明確自述沒開 `examples/`，第 5 輪甚至逐字寫「沒有翻 `examples/`」），但它是同一個形狀：**神諭的回答品質有一部分來自它讀得到書內產物與 schema，而真作者面對的是同一批檔——所以這一格比 S4 那次（讀 `parse.py`）更接近真實**，殺傷力較小。
   **但導演確實沒有在轉述時標記，六次轉述一次都沒標。** 記在這裡，不補寫進已發出的轉述（那會變成竄改留痕）。**S6 起導演要在每一次轉述末尾附一行「神諭本輪讀取範圍：…」。**
   附帶一格讀數：本輪那三處最硬的反打（幕號外洩的集合算錯／刪形容詞前漏報有沒有無家可歸／設計註接錯線）**全部來自神諭讀了書內產物之後的實查**，沒有一處是純憑腦內檔。

8. **（沿用，未解）`beat-test` 測試 4 預設拿不到 `金妮/水下.md`。** S4 已記；本輪 `beat-test` 實跑，報告裡對金妮的判斷全部來自 `物件/金妮.md` 的排除線與伏筆狀態表，**弧線基準那一格仍然是空的**。她從 S4 起有四象限了，那支檢查器仍然看不到。

--- 以下由觀測者 agent 寫 ---

## 防火牆稽核

```
防火牆：檢查了 203 次工具呼叫，越界 0 次
```

**稽核對象**：三支**受測體**的獨立 transcript——`agent-a8e46cd24234ff063.jsonl`（`S5 系統 agent 受測體`＝清帳，**53 次**）／`agent-a41e629f0d71a2224.jsonl`（`S5 拆幕 arc01`，**123 次**）／`agent-a10ed0e063c565daa.jsonl`（`S5 beat-test arc01`，**27 次**）＝ 203。六支神諭與本觀測者不列入（神諭讀 `examples/` 是職權）。
**工具分佈**：`Bash` 106／`Read` 47／`Edit` 40／`Write` 7／`Skill` 2／`Grep` 1 ＝ 203。**`Glob` 0 次**、`Grep` 工具僅 1 次（拆幕者掃 `story/物件/` 的規則表，`path` 寫死到該資料夾），其餘檢索一律走 `Bash` 裡的 `grep`／`find`，所以掃描以**指令字串**為對象、不是只看 `Read` 的路徑。

**逐參數掃描**：對每一次呼叫的 `file_path`／`path`／`glob`／`pattern`／`command`／`skill`／`args` 逐個比對 10 條越界樣式（`examples`、`情境測試`、`docs`、`Data`、`site`、`devtools`、`gothic_witch`、`一世之尊`、`芯片巫師`、`harry_potter`）——**命中 0 次**。
**repo 範圍搜尋**：`Grep`／`Glob` 無 `path`、或 `path` 指到 repo 根的呼叫 **0 次**。`Bash` 側逐條核 `cd` 目標：所有 `grep -r`／`find` 的搜尋根都是 `消失的密室/`、`tools/`、`技巧知識庫/`、`結構定義/` 其中之一，**沒有一次的搜尋根是 repo 根**。

### 導演前兩輪點名的兩格，本輪各有新讀數

| 點名格 | S4 讀數 | S5 讀數 |
|---|---|---|
| `git` 一律加 `-- <書資料夾>` 篩選 | 4/4 遵守 | **三支受測體本輪 `git` 呼叫 0 次**——那個風險面這一輪根本沒開。**不要讀成「改善」**：S4 之所以會用 `git`，是因為那一輪要查落點；本輪拆幕不需要。同一支 prompt、同一條規勸，讀數差別來自任務型態 |
| 受測體有沒有自己 spawn 子 agent | 0 次 | **0 次**。`subagents/` 底下 10 支 `.meta.json` **全部 `"spawnDepth": 1`**（3 受測體＋6 神諭＋1 觀測者，全由導演直開），三支受測體的 203 次呼叫裡 `Agent`／`Task`／`SendMessage` **0 次** |

### 補一層：`tool_result` 與 prompt 全文掃描（不只掃參數，也掃進了 context 的東西）

掃三支 transcript 的**每一行 JSONL**（含 prompt、assistant 輸出、tool_result），命中越界字樣的行共 **14 行**，逐一追到攜帶者之後全部落在合法載體上：

| 攜帶者 | 命中的字樣 | 判讀 |
|---|---|---|
| **受測體自己的 prompt**（3 支各一行） | `examples/`、`情境測試/`、`docs/`、`Data/`、`site/`、`devtools/`、`gothic_witch/`、`一世之尊/`、`芯片巫師/` | 那正是**禁令本文**（導演寫的硬邊界那一段）。防火牆的宣告必然要指名它擋的東西 |
| `結構定義/世界觀.schema.md`／`角色.schema.md`／`幕綱.schema.md` | `一世之尊`、`gothic_witch` | schema 是系統層白名單第 1 項；命中的是 schema **自己引用的病例書名與歷史例外**（「gothic_witch arc01＝幕001–007 保持原號」），不是那些書的內容 |
| `技巧知識庫/因果法則.md`／`幕綱與地圖.md`／`戲劇張力與懸念.md` | `examples/_分析/_跨書共通點.md`、`Data/【寫作技巧懶人包】.txt` | 知識庫是白名單第 2 項；命中的是**技法檔自己的出處註**（歸納自哪份分析），帶進來的是網文結構歸納，**與《消失的密室》無關** |
| `技巧知識庫/_index.md` | `devtools/meta_lint` | 白名單檔的維護指令路徑 |
| `tools/foreshadow_project/pyproject.toml` | `examples/`、`devtools/` | 白名單第 4 項；命中的是**射程宣告的註解**（「語料回歸釘 `examples/`，是開發期資產」） |

**`examples/消失的密室/` 的任何一章、任何一段原文，在三支 transcript 裡 0 命中。** `tools/*/tests/` 亦 0 命中。
> **沿用 S4 的定性、本輪再證一次**：受測體 context 裡確實會出現 `examples/`、`一世之尊` 這些字串，而**攜帶者永遠是白名單檔自己**。這不是漏洞（進來的是系統設計沿革，不是原著故事），但它再一次說明「絕不能讀」是**路徑清單、不是內容清單**——要把這條做成工具，量的對象得是「讀了哪支檔」，不是「context 裡出現過哪些字串」。

**本 session 判定：未污染。**

## purity 檢查

```
purity：比對了 651 個 token，候選 2 個
```

**比對範圍**：系統產物 ＝ `git diff` 在 `消失的密室/` 的**新增行**（12 支已追蹤檔）＋ **4 支未追蹤新檔**（`設定/角色/{亞瑟,亞瑟.ai,莫莉,莫莉.ai}.md`）全文，合計 **26,170 字元／16,910 漢字**。答案卷 ＝ `examples/消失的密室/` 全 18 章 **168,830 字元**。**合法來源 1,306,083 字元** ＝ `消失的密室/raw/`（4,676）＋`情境測試/消失的密室/`（134,781，含 4 支作者腦內檔與**本 session 的 elicitation 紀錄，即本檔第二節**）＋**書內 HEAD（`2ba7e3e`）baseline 全樹 95 支**（157,221，沿用 S2–S4 的第四類合法來源）＋**六支神諭 transcript 全文**（1,009,402，導演本輪明訂「神諭講的話等於作者給的」）。

**651 個 token 的取法（沿用 S3／S4 的五路聯集，讀數才跨 session 可比）**：產物的檔名／目錄名 **33**／產物裡 `〔〕「」『』《》**[[]]` 內的詞 **371**／**答案卷專名·道具語彙詞庫**與產物的交集 **115**（詞庫由字元黏著度建：答案卷全部 2–5 字 n-gram 取「出現 ≥4 次且 `count(gram)/min(count(字))≥0.60`」，共 **727 支**，與 S4 同一把尺、同一個數）／146 個 canon 定向探針中出現在產物的 **48**／數字·計數 token **129**。其中**命中答案卷的有 206 個**，扣掉合法來源後剩 **2 個**。

### 候選 2 個（逐筆列）

| # | token | 產物落點 | 命中答案卷 | 判讀 |
|---|---|---|---|---|
| 1 | **`佩妮`**（連帶 `德思禮太太`） | **`story/幕綱/arc01.md:49`** 幕001 衝突欄：「一張寫著**「佩妮·德思禮太太」**的邀請卡」 | `佩妮` 在**第１／２／３／６／14 章共 5 章**；`德思禮太太` 在第１章 1 處 | ⚠️ **實質候選，本輪唯一一筆。** 全 repo 除 `examples/` 外只有這一處；`raw/` 0、`情境測試/` 0（連 `德思禮` 都只在 `S3-橫向.md` 出現過一次）、書內 HEAD baseline 0、**六支神諭 transcript 各 0**。時間戳可定案：受測體 `a41e629f0d71a2224` 於 `2026-07-31T20:11:46Z`（第 5 輪神諭回覆之後改寫幕001 時）**自己寫下這個名字**，作者從未被問、也從未提過它 |
| 2 | **`興奮`** | `設定/角色/亞瑟.ai.md:22`（「對麻瓜的東西過度**興奮**」） | 12 章 | **常用詞假陽性。** 它進詞庫是因為「奮」這個字在答案卷幾乎只出現在「興奮」裡（黏著度 1.0），不是因為它是專名或道具語彙。產物那一處是**人物分析語言**，內容 0 |

### ⚠️ 本輪最重要的一格：合法來源第三格會**把系統自己引進的 canon 名字洗白**

`佩妮` 之所以是本輪唯一被抓到的一筆，**不是因為它比別的更嚴重，是因為它剛好沒被作者複誦**。同一輪同一支受測體引進的另外兩個 canon 專名走完了完整的洗白路徑：

| token | 誰第一次寫下它 | 時間 | 之後怎麼樣 | 現在算不算「合法來源」 |
|---|---|---|---|---|
| `威農` | **受測體 `a41e`（assistant）** | `2026-07-31T19:18:33Z` 幕001 初稿 `角色：哈利、威農、達力` | 導演 19:22 把它寫進第 3 輪問句（「三個裸名字沒有 registry」）→ 作者答「不建檔」→ 進 `裁決流.md:165` 與本檔第二節 | **算**（但見下方） |
| `達力` | **同一次呼叫、同一行** | 同上 | 同上 | **算**——`達力` 在本 session 之前**全 repo 除 `examples/` 外 0 命中** |
| `佩妮` | **受測體 `a41e`（assistant）** | `2026-07-31T20:11:46Z` 幕001 改寫 | **沒有人問、沒有人答、沒有進任何紀錄** | **不算 ⇒ 唯一被抓到的那一筆** |

**而 `威農` 那一格更難看**：它在 `情境測試/` 裡的落點是 `P-前置.md:95` 與 `:98`，而那兩行寫的是——
> 「**層③ 概念在、專名不在**（多數）：腦內檔寫『姨丈』但沒有 `威農`……系統會問『這個叫什麼』，而作者腦裡真的只有代稱 → **fallback**。」
> 「**層③ 實例（8 個）**：`威農`（ch1,2,3,14）／`嘿美`／`弗雷`／`龐芮夫人`／`皮皮鬼`／`分類帽`／`佛客使`／`決鬥俱樂部`。」

也就是說：**`威農` 出現在合法來源裡的身分，是一份「作者腦裡沒有這個名字」的預測表**。機械過濾器只看字串在不在，於是**把一條「這個 token 應該觸發 fallback」的預測，讀成了「這個 token 有合法來源」——語意整個反過來**。

**這同時是 `fallback-log.md` 本輪 0 筆的第四種解釋**（本檔摩擦欄第 2 條列了三種）：**P session 押的 8 個層③ token 裡，本輪產物只碰到 `威農` 一個，而系統沒有問，它直接寫了下去。** 另外 7 個（`嘿美`／`弗雷`／`龐芮`／`皮皮鬼`／`分類帽`／`佛客使`／`決鬥俱樂部`）在本輪產物裡**一次都沒出現**——所以那條預測這一輪其實**只被測到 1/8**，而那 1/8 沒兌現。

> **兩件事交給階段 3**：① purity 的合法來源第三格（本 session elicitation 紀錄）是**事後**才長出來的，而它長出來的原因常常就是「系統先寫了、導演拿去問、作者順口複誦」——**這個過濾器對『系統引進 canon 專名』這一類漏出是結構性失明的**，只有沒被複誦的那些才會被抓到。要修的話，判準得改成「**這個 token 第一次出現在 transcript 裡時，說話的是誰**」（本輪三筆全都答得出來，因為時間戳都在）。② `fallback-log.md` 的量尺與 P session 的層③ 預測表沒有任何機械連結——本輪要靠觀測者手動去 `P-前置.md` 把那 8 個 token 撈出來比對。

### 補強掃描（三項）

- **長逐字重疊**：把產物與答案卷都壓成純漢字串，掃 **8 字窗 16,903 個／10 字窗 16,901 個／12 字窗 16,899 個**，重疊且不在合法來源者**各 0 段**。（S2 有 1 段邊界假象、S3／S4／S5 連續三輪 0。）**本輪產物首次含大量敘述線與八欄場景描寫，這個 0 比 S4 那個 0 有份量。**
- **窮舉 3–6 字 n-gram**（五路取法之外的獨立覆核，防漏）：命中答案卷且不在合法來源的**極大字串 58 個**，逐個看過——**58 個全部是通用中文搭配**（「這件事讓他」「是同一個人」「覺得對不起」「在他們的最近的」…），**0 個是專名／道具／情節具體物**，唯一有內容的是 `德思禮太太`，即候選 1 的同一處。
- **數字路**：產物 **129 個**數字·計數 token，命中答案卷且不在合法來源者 **0 個**。（本輪的數字幾乎全是 `幕001`–`幕015`、`3/3/4/5`、`150 列`、`19 支`、`3高7中4低` 這類系統座標。）
- **關鍵道具定向覆核**（怕五路取法漏掉本輪新寫的場景）：`亞瑟`／`莫莉`／`咆哮信`／`坩堝`／`簽名書`／`洞穴屋`／`地下室`／`麻瓜保護法`／`魔法部`／`警告信`／`多比`／`分帽子那天`／`海格的雨傘`／`雨傘`／`日記`／`本子`／`波金`／`伯克`／`呼嚕粉`／`夜行巷`／`洛哈`／`貓頭鷹`／`樹籬`／`信箱`／`帳單`／`邀請卡` 逐個測——**除 `佩妮` 外每一個都在合法來源裡查得到**；其中 `莫莉`／`坩堝`／`簽名書`／`警告信`／`分帽子那天`／`海格的雨傘`／`邀請卡` **在答案卷裡 0 次命中**（＝這本書自己的詞，不是原著的）。

### 反向讀數

146 個定向探針裡，**答案卷有而產物一次都沒出現的有 73 個**：`佛地魔`／`佛客使`／`分類帽`／`史萊哲林`／`哭泣的麥朵`／`嘿美`／`夫子`／`女生盥洗室`／`弗雷`／`喬治`／`忌辰晚會`／`拿樂絲太太`／`搏格`／`斜角巷`／`曼德拉草`／`柯林`／`梅森`／`決鬥俱樂部`／`渾拚柳`／`爬說嘴`／`爆竹`／`皮皮鬼`／`石化`／`福特`／`禮堂`／`芽菜`／`華麗與污痕`／`蛇怪`／`衛斯理`／`變身水`／`貓毛`／`賈斯汀`／`阿辣哥`／`隱形斗篷`／`霍格華茲`／`飛七`／`馬份`／`魁地奇`／`麻種`……。
產物中出現的 48 個探針裡，**47 個在合法來源裡查得到，1 個不在（`佩妮`）**。

### 讀數性質（接 S4 觀測者的提醒）

S4 那一格寫著：「**若 S5 的候選數回升到 1–2，那不是退步，是儀器恢復量程。**」——**本輪確實回升，而且回升的形狀正是它預測的那一種。**

| session | 產物型態 | 故事 token 密度 | 實質候選 |
|---|---|---|---|
| S2（organize） | 8 支 arc 的事件序列 | 最高 | 1（`五百`，邊界假象） |
| S3（橫向三支） | 設定補厚＋arc05 長厚 | 中 | 1（`四張`，邊界假象） |
| S4（輕量三件＋character） | 裁決與帳＋需求層分析語言 | 最低 | 0 |
| **S5（beat-sheet＋beat-test）** | **15 幕八欄＋三場新敘述線＋兩支新角色檔** | **最高（首次寫到具體場景、道具、人數）** | **1（`佩妮`，第一筆真的有內容的）** |

**這是四個 session 以來第一筆「答案卷有、作者沒給、系統自己寫出來了」的專名。** 依基座 §8：命中是**有效性警訊、不是扣分**——量的是系統輔助力，不是重建保真度，`佩妮·德思禮太太` 這張邀請卡是不是好戲不歸這一格管。但**這一輪的讀數不乾淨**，而且不乾淨的方式很具體：系統在寫一個它認得的故事時，**會從記憶裡取專名，而且不會舉手**。
