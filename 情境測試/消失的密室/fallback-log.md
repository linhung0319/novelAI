# 消失的密室 · 端到端貫穿測試 · fallback log

> 測試 meta，**只放 `情境測試/消失的密室/`，永不進書資料夾**。
> 記每一筆「作者 agent 腦裡（`消失的密室/raw/` ＋ `情境測試/消失的密室/作者腦內-*.md`）真的沒有、去翻了 `examples/消失的密室/` 實檔」的 fallback。
>
> **這是 elicitation 品質的唯一量尺**（基座 §8-2、§9）。兩個方向都要看：
> - fallback **多** ＝ 腦內檔留的缺口太大，作者一直在背原著 → 重建偏誤。
> - fallback **恆空** ＝ **出事了**，不是好消息。那代表兩條 lane 被合成了一條（基座 §4）——腦內檔把整本書都寫進去了，於是每一答都算「腦」，量尺失效。**`fallback-log.md` 非空是階段 2 的出場條件之一。**
>
> **本輪的兩條 lane 是怎麼切的**（P session 拍板，見 `P` 列）：承重的（謎底、世界規則、角色為什麼是這樣、結局要成立什麼）進腦內檔；質地的（誰在第幾場說了什麼、配角癖好、具體數字、單場比賽過程）**兩邊都不寫**，留給 fallback。所以 fallback 預期會在 `beat-sheet`／`write` 這種要臨場顆粒的階段出現，而不是在 `develop`／`organize`。

## 進度

| Session | 階段 | 作者 agent 輪次 | 該輪 fallback 筆數 |
|---------|------|----------------|-------------------|
| P | 前置（產 raw＋腦內檔） | — | —（P 沒有神諭輪次：沒有系統 agent 發問，所以沒有作者 agent 被冷啟） |
| S1 | develop | 第 1 輪（5 處矛盾＋機制／主線／題旨） | **0** |
| S1 | develop | 第 2 輪（貓的名單判準／飛七／基調／取向／視角） | **0** |
| S1 | develop | 第 3 輪（句式顆粒度／稱謂／配角功能／少年實體度） | **5** |
| S2 | organize | 第 1 輪（arc 切分／卷級方向／停在哪／結構公式／四個分叉／六個角色／那輛車／貓毛） | **0** |
| S2 | organize | 第 2 輪（審稿 8 格裁決） | **0** |
| S2 | organize | 第 3 輪（不求救第三四次落點／裁決流落點） | **0** |
| S3 | worldbuild | 第 1 輪（水下處置／壞魔杖規則／傘的兌現點／授權／郵政落點／順帶那條） | **1** |
| S3 | worldbuild | 第 2 輪（驗收那一行／兩處自承判斷／伏筆表補列／`伏筆` 欄） | **0** |
| S3 | worldbuild | 第 3 輪（arc02:72 過期宣告／lint 讀數處置） | **0** |
| S3 | character | 第 4 輪（三份草稿＋題旨四格表／飛七選對／弧線型／麥朵兌現／約束落點／三個欄位） | **0** |
| S3 | character | 第 5 輪（`伏筆` 欄／「設定層·角色」token／下一輪交代） | **0** |
| S3 | expand | 第 6 輪（缺口 1 算不算／A B C D 四組提案／伏筆表兩列／方向確認） | **0** |
| S3 | expand | 第 7 輪（四個駁回補不補／就緒 token／40.7%／下一輪交代） | **0** |

| S4 | 輕量三件 | 第 1 輪（題旨兩支源檔／水龍頭檔名射程／arc05 括號逐格／標籤／arc06·08） | **0** |
| S4 | 輕量三件 | 第 2 輪（金妮那半句／`洛哈.ai.md:19`／28.4%／自行驗收） | **0** |
| S4 | 輕量三件 | 第 3 輪（同源界線怎麼補／駁回「零殘留」讀數） | **0** |
| S4 | 輕量三件 | 第 4 輪（護欄落點 A/B/C→D／收尾清單） | **0** |
| S4 | character | 第 5 輪（四象限住哪／分叉二怎麼並存／核心.md 洩不洩底／建不建物件檔／過期宣告／就緒 token／待裁決那列） | **0** |
| S4 | character | 第 6 輪（`揭示層級` 值／`核心.md` 末行／逐格驗收） | **0** |

| S5 | 清帳（輕量） | 第 1 輪（`raw:33` 那句舊問句劃不劃、改因怎麼寫） | **0** |
| S5 | 清帳（輕量） | 第 2 輪（就地改 append-only 那半句對不對、同型怎麼辦） | **0** |
| S5 | beat-sheet | 第 3 輪（多比扣信／坩堝歸屬／3-2-4-4 切法／統一母題／就緒 token／三個裸名字） | **0** |
| S5 | beat-sheet | 第 4 輪（保密法歸哪支世界觀檔／`亞瑟.ai.md` 的 `伏筆` 欄） | **0** |
| S5 | beat-test | 第 5 輪（六題＋三高＋七中四低逐項） | **0** |
| S5 | beat-sheet | 第 6 輪（幕號重編／兩處越界動作／莫莉建不建檔） | **0** |

| S6 | write | 第 1 輪（分章提案 7 題） | **0** |
| S6 | write | 第 2 輪（ch0001 收法過不了單射，A／B／C） | **0** |
| S6 | write | 第 3 輪（首批三章審稿四件＋三題裁＋篇幅） | **0** |
| S6 | write | 第 4 輪（嘿美進不進角色欄／自修四處／三個讀數／布丁） | **0** |
| S6 | write-test | 第 5 輪（1 高 4 中 7 低逐條＋檔頭那一行） | **0** |
| S6 | write | 第 6 輪（角色欄三格／第三個工具誤用／多比 ID／三次同型） | **0** |
| S6 | revise | 第 7 輪（第 8／10 條提案＋兩個要點頭的／第 1 條修法） | **0** |
| S6 | revise | 第 8 輪（繩子三句驗收／更正／交給 S7） | **0** |

| S7 | character（榮恩） | 第 1 輪（分叉三／埋設點／排除線落點／射程字） | **0** |
| S7 | worldbuild（監管與曝光） | 第 2 輪（第 8 支檔的界線／兩支檔的分界措辭／動物園那次為什麼沒有信） | **0** |

| S8 | beat-sheet（定點補幕005） | 第 1 輪（哥哥定不定名／幕006 要不要讓他在場） | **1** |
| S8 | beat-sheet（誤導線落地） | 第 2 輪（誰扔／多做的半句／那條誤導線書內查無） | **1** |
| S8 | character＋write 前置 | 第 3 輪（派西建檔形狀／ch0004 涵蓋到哪一幕） | **0** |
| S8 | write（ch0004） | 第 4 輪（章末鉤／空殼／幕005 金妮／風格宣告／三格記不記帳／裁決流那張便條／讀稿意見） | **0** |
| S8 | write（收工） | 第 5 輪（工具缺口記不記待裁決／檔頭那句／今天收不收） | **0** |

| S9 | write-test（三個高） | 第 1 輪（句長中位那一列／Deep POV 內心配額／喜劇引文段天花板） | **0** |
| S9 | write-test（六題） | 第 2 輪（章末型別配比／段首公式／街名／親屬關係／ch0003 厚薄＋動作戲第四檔／讀數落不落檔頭） | **0**（`examples/` **觸及 1 次·0 命中·非 fallback**：為街名做全域 grep，「女貞路｜水蠟樹街」在答案卷都不存在。**它自述「一次都沒翻」，而它翻了**——0 命中所以沒有任何 canon 進轉述或產物，但自述與工具呼叫不符這一格要記） |
| S9 | revise（退回兩件） | 第 3 輪（③ 的落點／`:175` 與 `:219` 矛盾＋既有違反） | **0** |
| S9 | revise（收工） | 第 4 輪（`ch0004:111` 互述／收不收工／沒人問到的那一格） | **0** |

**S1 合計：3 輪 22 題，fallback 5 筆（22.7%）。**
**S2 合計：3 輪 18 題，fallback 0 筆（0.0%）。**
**S3 合計：7 輪 30 題，fallback 1 筆（3.3%）。**
**S4 合計：6 輪 26 題，fallback 0 筆（0.0%）。**
**S5 合計：6 輪 19 題，fallback 0 筆（0.0%）。**
**S6 合計：8 輪 40+ 題，fallback 0 筆（0.0%）。**
**S7 合計：2 輪 14 題，fallback 0 筆（0.0%）。**（**這兩列是 S8 補登的**——S7 收工時漏了更新本檔，而 S7 記錄第 20 行明寫「兩輪 fallback 都是 0」、觀測者還機械驗過 `examples/` 觸及 0 次。**漏的那一格與 0 長得一模一樣，正是基座 §5 對「漏跑觀測者」的同一句警告，只是這次漏的是導演的表。**）
**S8 合計：5 輪 16 題，fallback 2 筆（12.5%）。**
**S9 合計：4 輪 14 題，fallback 0 筆（0.0%）。**
**累計：44 輪 199+ 題，fallback 8 筆（4.0%）。**

## 讀數判讀（S1）

**量尺活著，而且切在預期的那一條線上。** P session 拍的兩條 lane 說「承重的進腦內檔、質地的兩邊都不寫，所以 fallback 預期出現在 `beat-sheet`／`write` 這種要臨場顆粒的階段，而不是在 `develop`／`organize`」——實際落點比預期早了一階，但**切分本身完全成立**：前兩輪 16 題問的全是承重項（謎底機制、主線引擎、題旨因果句、基調主從、取向定位、視角），腦內檔一題不漏地接住了，0 fallback；第三輪一問到「句子多長」「大家怎麼互相稱呼」「那個少年碰不碰得到東西」，腦內檔立刻見底。

**提前的原因不是腦內檔留錯缺口，是 `develop` 本身就要落 `風格.md`**——而風格的閘門欄位（`可用句式` 要「碎在分句／句／段 ＋ 阿拉伯數字」）是**可數的顆粒度**，讀感描述過不了。也就是說：**這一階段有一支閘門，它的規格天然要求質地層的答案**。這不是缺口設計失準，是流程圖上「質地何時被需要」這一格畫錯了一階。

**作者自述的共同形狀（值得帶進階段 3）**：五筆 fallback 全是「**我知道結果、沒記過程**」——句子多長（結果：讀起來偏疏）／誰拿了魔杖（結果：武器被拿走）／能力被誰聽過（結果：決鬥社引爆）。作者的筆記格式擅長記規則與結局，不記執行細節，而**細節正好是進正文的那一層**。

## 讀數判讀（S2）

**整個 session 0 筆，而這一次它不是警訊。** 基座 §9 說「`fallback-log.md` 恆空不是好消息」——判準是**恆**空，而本輪的 0 有一個**當輪就可驗證的解釋**，且解釋是作者自己在第 1 輪末主動給的（沒有人問他）：

> 這輪八題我一次都沒去翻原稿，全部落在 `raw/` 跟腦內檔裡答得掉。這對你是好消息也是壞消息——**你問的還是承重層**。等你開始問「這一場誰先開口」「這句髒話砸下來之後現場停了幾秒」，我就得去翻了。

**這正是 P session 那條 lane 切分的原話**（承重進腦內／質地兩邊都不寫）。S2 的 18 題逐題檢查，**一題都不在質地層**：arc 界在哪、要不要立卷級方向、停在哪一秒、哪一個結構公式、分叉怎麼排、哪幾個角色要建、物件在哪兌現、規則要不要載體、四個審稿判斷認不認、計數器的落點——全部是「這本書的結構要長什麼樣」，沒有一題是「那一場實際發生了什麼」。

**所以 S2 的 0 與 S1 第 1、2 輪的 0 是同一種 0**（S1 前兩輪 16 題承重、同樣 0 fallback），不是 S1 第 3 輪那種見底。兩條 lane 仍然分開，量尺仍然活著。

**但這一格要盯著，因為它會在 S4 被證實或證偽。** 可證偽的預測（寫在這裡，讓下一個 session 回頭對）：

- **`beat-sheet`（S4）拆幕時 fallback 必須回來。** 拆幕會逼出「這一場誰先開口」「這一場停在哪一句」，那是腦內檔明文不寫的那一半。**S4 若仍是 0，那就不是 lane 切得好，是問題不夠具體**——屆時要回頭查系統有沒有在拆幕時自己填了質地而沒問。
- **S3（橫向 skill）可能仍是 0，且那也正常**——`worldbuild`／`character`／`expand` 問的是設定與需求，仍在承重層。

**另一個方向的讀數（本輪新出現，值得記）**：作者這一輪雖然 0 fallback，卻**主動去逐條對了一支系統沒讀的 `raw/` 檔**（`名字與人物.md`），撿回 5 格承重內容。這是「翻自己的檔」不是「翻原稿」，所以**不計 fallback**——但它說明腦內檔／raw 這一側**還沒被榨乾**，離見底還有距離。詳見 `S2-organize.md` 摩擦欄第 2 條。

## 讀數判讀（S3）

**S2 那條可證偽的預測，第一半兌現了。** 上一輪寫下「**S3（橫向 skill）可能仍是 0，且那也正常**——`worldbuild`／`character`／`expand` 問的是設定與需求，仍在承重層」。實際 7 輪 30 題只有 1 筆 fallback（3.3%），**而那一筆恰好是唯一一題不在承重層的**——見下方第 6 筆：問的是「這把傘在原著裡到底怎麼處理的」，那是**質地與執行史**，正是兩條 lane 都刻意不寫的那一半。

**這一筆 fallback 的形狀跟 S1 那五筆同源，但更值得記**：S1 那五筆是「我知道結果、沒記過程」；這一筆是**作者連自己有沒有想過都不確定**——他自承這把傘是 S2 第 2 輪才從 `raw/` 撿回來的，撿回來時只寫「目前沒排兌現點」，而**他自己的伏筆帳七節裡根本沒有這一列**。也就是說：fallback 觸發的不是「細節忘了」，是**一個從來沒被登記進收支表的承諾**。

**而這一筆 fallback 的結果是「翻完更確定不照它做」。** 作者翻完的結論是原著「說破＋不兌現」是最差的組合，於是他反而把 `不得被解釋` 留著、改用自己排的 arc04 那一行付掉。**這是 fallback 這條量尺第一次示範它不等於重建偏誤**——翻書可以是「確認別人怎麼做，然後不那樣做」，log 記的是**翻了**，不是**抄了**。基座 §9 那條擔憂（作者過度翻原著→重建偏誤）在這一筆上沒有兌現，但也不能因此放鬆：n=1。

**S4 那條預測仍然掛著、且現在更關鍵**：`beat-sheet` 拆幕會逼出「這一場誰先開口」「這一場停在哪一句」。S3 這一輪讓「承重層＝0 fallback」再多一個 session 的證據（S1 前兩輪 16 題、S2 全部 18 題、S3 的 29 題，合計 63 題承重、fallback 1），所以 **S4 若仍是 0，那就幾乎不可能是 lane 切得好了**——要回頭查系統有沒有在拆幕時自己填了質地而沒問。

## 讀數判讀（S4）

**那條可證偽的預測這一輪沒有被測到，而原因要寫清楚，不能讓它看起來像「又通過了一次」。** S2／S3 兩輪都預告「**S4 若仍是 0，那就幾乎不可能是 lane 切得好了**」——但**本 session 沒有跑 `beat-sheet`**（拆幕移到 S5，理由見 `S4-*.md` 檔頭）。所以 S4 的 26 題全部仍在**承重層**（落點、射程、機制、四象限、兩案並存），與 S2／S3 同性質。**這一輪的 0 不構成那條預測的任何證據，正反都不算。預測原封不動轉給 S5。**

**但這一輪長出一個新形狀，值得單獨記：作者六輪裡有四輪去讀了「系統套件的原始碼」，而不是腦內檔也不是原稿。** 具體是 `tools/decision_projection/…/parse.py:219`＋`cli.py:163`（找出護欄的第四條落點）／`objects.py:387`（證明那行資訊與名字無關）／`objects.py:411`（抓出子字串 near-miss）／`sources.py:344`。
**這不是 fallback**（`examples/` 一次都沒開），但它意味著兩件事：① **腦內檔／raw 這一側在「機制問題」上根本不是候選來源**——問題不在故事裡，在工具裡；② **作者這個角色在承重層的回答品質，有一部分來自他讀得到系統套件**。基座 §3 給神諭的讀取權是 `raw/`＋`情境測試/`＋（fallback）`examples/`，**沒有明文給也沒有明文禁系統套件**。這一輪它被用了四次，而且每一次都推翻了受測體的結論。
**交階段 3 的一個問題**：真作者讀不讀得懂 `parse.py`？若讀不懂，本輪那四格「作者比系統強」的讀數就有一部分是**神諭超能力**，不是系統可用性——而那會讓「摩擦與缺口」欄少記幾筆真正的摩擦。**下一輪若再出現同型，導演要在轉述時標記它。**

## 讀數判讀（S5）——**掛了兩個 session 的可證偽預測，第一次被真的測到，沒兌現**

預測原文（S2 立、S3 加碼、S4 因為沒跑 `beat-sheet` 而原封轉交）：

> **`beat-sheet`（S4）拆幕時 fallback 必須回來。** 拆幕會逼出「這一場誰先開口」「這一場停在哪一句」，那是腦內檔明文不寫的那一半。**若仍是 0，那就不是 lane 切得好，是問題不夠具體**——屆時要回頭查系統有沒有在拆幕時自己填了質地而沒問。

本輪 `beat-sheet` ＋ `beat-test` 都真的跑了，**fallback 仍是 0**。照約定回頭查了，結果是**三種形狀並存，不是預測假設的那一種**（逐筆證據見 `S5-*.md` 摩擦欄第 2 條）：

- **① 自己填了，但同時舉手問了。** 幕008 那個「把簽名書倒進最近那個坩堝」是系統發明來補前因的動作，**而它把這件事寫成「這是大綱沒有的動作，請認或否」並落進 `待裁決.md`**。作者答「否，你的前提就錯了……那是你為了填一個不存在的洞而發明的動作」。**填了，但沒有偷偷填**——這正是預測要抓的那個病，而它抓到的是病的**良性版本**。
- **② 沒填也沒問（真正的漏）。** 幕002 的八欄裡「今年學校會發生很可怕的事」一個字都沒有。**抓到它的不是作者也不是拆幕者，是 `beat-test`。**
- **③ 問題確實不夠具體，但原因不是問得不好。** 19 題裡沒有一題是「誰先開口」「停在哪一句」——**幕綱八欄要的就是行動／衝突／結果的骨架，句子層級不在這一層**。

**所以那條預測的前提站不住：它把「拆幕」當成質地層的入口，實際上質地層的入口是 `write`。**

**預測不作廢，改掛 S6，判準照舊**：`write` 跑完若仍是 0 fallback，就要回頭查它有沒有自己填了句子層級的東西而沒問——而那一層**沒有 `beat-test` 這種等級的第二道眼睛**（`write-test` 有 11 項，但它跟 `write` 讀的是同一批上游）。

**另一格要一起記的**：本輪神諭六輪全部標 `腦`／`腦（判斷）`，但第 5、6 兩輪自述讀了大量**書內產物與 schema**（`幕綱/arc01.md`、`裁決流.md`、`幕綱.schema.md`、五支角色／物件檔…）。**這不計 fallback**（兩輪都明確自述沒開 `examples/`），但它是 S4 那條「神諭超能力」的同型第二例——差別是這次讀的是**真作者也讀得到的東西**，所以殺傷力較小。**S4 交代導演「下一輪要在轉述時標記」，本輪六次轉述一次都沒標**，如實記在 `S5-*.md` 摩擦欄第 7 條。

## 讀數判讀（S6）——**掛了四個 session 的預測在它自己指定的到期日被測到，沒兌現，而這一次應該作廢**

預測原文（S2 立、S3 加碼、S4 因沒跑 `beat-sheet` 原封轉交、S5 因「拆幕不是質地層入口」改掛 S6）：

> **質地層的入口是 `write`（S6）。** S6 若仍是 0，就要回頭查 `write` 有沒有自己填了句子層級的東西而沒問——而那一層**沒有 `beat-test` 這種等級的第二道眼睛**。

本輪 `write`＋`write-test`＋`revise` 三支全跑、寫出約 3,700 字正文，**fallback 仍是 0**。照約定回頭查：

- **它確實自己填了句子層級的東西**——每一句都是它寫的，那是 `write` 的定義，這一半的預測是廢話。
- **它問了 40+ 題，而且問的全是「這一句該不該存在」層級的**（章末停在哪／哪半句是註腳／貓頭鷹要不要名字／客人姓什麼／那本本子能不能給外觀）。**沒有一題是「這一場誰先開口」**——但那不是它沒問到，是那個層級的東西**已經有規則管著了**。
- **真正的原因是 S5 那三種以外的第四種**：**這本書的質地判準被寫成了規則，不是實例。** `raw/基調-我想寫成什麼感覺.md` 有 15 條可執行的句子層規矩（笑點原樣引出來然後閉嘴／恐怖靠錯的東西在錯的位置＋生理細節／章末不總結只留鉤子／可信度與台詞長度成反比／破折號是中途換檔的記號）。八輪裡每一個質地問題都在 raw 裡查得到落點，**所以沒有一題需要翻原稿**。

**這條預測到此作廢，不再改掛。** 它的前提是「質地問題必然需要翻書」，而這本書提供了反例：**質地寫成規則就不必翻書**。P session 那條 lane 切分（「質地的兩邊都不寫，留給 fallback」）在**這本書上不成立**——因為作者把質地寫進了 `raw/`，而那是 lane 的「腦」那一側。

**兩件事跟著這條作廢一起交階段 3**：
1. **`fallback-log.md` 作為 elicitation 品質量尺，對「規則型作者」是失效的。** 基座 §9 說「恆空不是好消息，那代表兩條 lane 被合成了一條」——本輪的恆空**不是 lane 合併**（腦內檔與原稿仍然分開，原稿一次都沒開），是**質地被提前結構化**。量尺讀到的 0 與 lane 崩壞讀到的 0 長得一模一樣。
2. **S1 那五筆 fallback 的形狀反過來解釋了這件事**：那五筆全是「我知道結果、沒記過程」，而其中一筆（`可用句式` 三檔位的實際數字）**當場逼作者把讀感量化成規則寫進 `風格.md`**。也就是說——**S1 的 fallback 生產了讓 S6 不需要 fallback 的那批規則**。這條因果鏈是這本測試最漂亮的一個讀數，但它同時說明：**這條量尺會隨著書變成熟而自然歸零，而歸零不代表 elicitation 變差。**

## 讀數判讀（S8）——**S6 宣告作廢的那條預測，其實只殺對了一半**

S6 把掛了四個 session 的預測作廢，理由是「**質地寫成規則就不必翻書**」——這本書的作者把 15 條句子層規矩寫進了 `raw/基調-我想寫成什麼感覺.md`，所以 `write` 的 40+ 題一題都不必翻原稿。

**S8 連續兩輪出現 fallback，而且兩筆都不是質地。** 逐筆看（第 7、8 筆）：

- 第 7 筆＝**六個哥哥的名字**；第 8 筆＝**其中一個哥哥的名字、他的徽章、他那條誤導線由誰揭曉、第二個倒下的女生是誰**。
- 兩筆的共同形狀是 **「一個從來沒有被登記進任何一張表的人」**：`raw/名字與人物.md` 作者自承「我只列到榮恩跟金妮」，四支腦內檔對六個哥哥只有「朋友的哥哥」四個字加一句功能。

**所以正確的結論不是「規則型作者不需要 fallback」，是：規則管得住『怎麼寫』，管不住『有誰』。**
S1 那五筆是「我知道結果、沒記過程」；S8 這兩筆是「**我知道功能、沒記是誰**」——同一個作者、同一種筆記習慣的第二個切面。而人物誌那一支腦內檔存在、也被讀了，它只是**照功能排的，不是照人排的**。

**這一格對階段 3 的意義**：`fallback-log` 這條量尺在 S6 被判定「會隨著書變成熟而自然歸零」，S8 說明它會**在新的專名進場時重新開機**。歸零不是終點，是週期。arc02 起要進場的新名字（斜角巷那一組、洛哈的班、球隊）數量遠多於 arc01，**預期 S9–S12 的 fallback 率會高於 S8**——若反而掉回 0，那才要回頭查是不是有人在替作者發明名字。

**另一格要一起記的（本輪唯一一次觸及原稿的兩輪，其餘三輪全 0）**：第 3、4、5 輪神諭都自述讀了**書內產物**（`ch0004.md` 草稿、`幕綱/arc01.md` 承諾區、`裁決流.md` 指定列、`物件/{金妮,派西}.md`、`風格.ai.md`、`待裁決.md`），並且第 5 輪還讀了 **`tools/settings_select/src/.../select.py`／`cli.py` 的行號**。這是 S4 交出的「神諭超能力」同型第三例，**而本輪導演在轉述時標記了**（S4 交代、S5 漏做的那一條）：第 4、5 輪的轉述都附了「我引的行號你要自己 `grep -n` 核一次」。**受測體照做，並且第 4 輪核出作者自己把未來列號寫錯（作者寫「217、218、219」，實際落 219–222，那個編號會指到受測體自己身上），當場擋下。**

## 逐筆紀錄

格式：`SNN ｜ 問題 ｜ 腦內為什麼沒有 ｜ 翻到 examples 的哪一章 ｜ 答案要點`

1. **S1 ｜ `可用句式` 三檔位的實際數字 ｜** 腦內只有「中等偏疏」「突然變短」＝讀感，從來沒數過 **｜ 全 18 章敘述段實測（n=2334 句） ｜** 常態段碎在句、2–3 分句、20–30 字；恐怖檔碎在分句、1–2、8–14；喜劇引文碎在段、一段 20–35 分句不插嘴。**作者量完發現自己記錯了**——恐怖高潮整段平均 28 字/句（比常態長），真正變短的是撞擊那一瞬間的 3–4 句，於是檔位名從「恐怖高潮段」改成「恐怖高潮的觸發句群」。
2. **S1 ｜ 稱謂系統（師生／朋友／敵對／大人之間） ｜** 「寫的時候手會自己做的事，所以從來沒寫成規則」 **｜ ch2·4·6·9·11·12·18 的對白 ｜** 敵對只用光禿禿的姓、永不用名；多比對主角一律全名＋自稱第三人稱（那條硬約束在語法層的痕跡）；瑞斗從全名改叫名的那一刻＝他開始佔有。
3. **S1 ｜ 那個少年從日記裡出來時有多實體 ｜** 腦內只有高潮的「卸除清單」和「現形要耗盡一個人的性命」，**「武器被拿走」寫成被動語態就跳過去了**，沒問過是誰拿的、拿得起來嗎 **｜ ch17 ｜** 實體度是上升曲線、刻度＝女孩還剩多少命；能拿走魔杖但**不能施法**（存量全拿去維持形體）——這條同時是「他為什麼不直接動手」的機械答案。**五筆裡唯一真正承重的一筆**：它反過來產出了兩條下游硬約束（不得施法、不得寫成手下留情）。
4. **S1 ｜ 他以前對蛇說過話嗎（動物園那次） ｜** 作者替自己記的上一本摘要只有劇情，**沒有「能力的使用史」這個筆記格式** **｜ ch11 ｜** 有過一次、在麻瓜面前，但麻瓜沒有「爬說嘴＝黑巫師標誌」的概念；由此反推出新規則「爬說嘴不會自己漏出來，只會在有人把一條蛇放到他面前時漏出來」。
6. **S3 ｜ 海格那把傘（裡面那根斷魔杖）在原著裡是怎麼處理的、兌現在哪 ｜** 這把傘是 S2 第 2 輪作者自己從 `raw/名字與人物.md` 撿回來的，raw 裡只有一句「我一直覺得他把折斷的舊魔杖藏在裡面」——**連他自己都寫「覺得」**；撿回來後只登記了「目前沒排兌現點」，而**他的伏筆帳七節裡根本沒有這一列**，它從來沒進過收支表 **｜ 全書「傘」三處，一處是溫室的傘菌無關，實質只有第 7 章一處 ｜** 那一處**用敘述直接說破**（舊魔杖藏在傘裡、他不能使用魔法），然後**整本沒有任何兌現場**。作者的結論是**反過來用**：「說破＋不兌現是最差的組合——把最重的一件道具拿去換一句設定說明」，於是 `不得被解釋` 那條約束留著，改用自己排的 arc04 一行（「他拎著那把粉紅色的雨傘走出去。沒有一個人看它一眼。」）付掉。**這是 log 上第一筆「翻了但沒抄，而且翻的結果是往反方向走」。**
5. **S1 ｜ 劍上刻的全名寫法 ｜** 腦內只有「刻著名字的劍」和「那位主張純血的創辦人的對手」，全名沒寫下來過 **｜ ch18 ｜** 高錐客葛萊芬多。（**只有全名這一格是 fallback**；「載體是分類帽」與代價是從既有規則推的，記腦。）
7. **S8 ｜ 洞穴屋那個上午，扔蜘蛛整榮恩的哥哥叫什麼 ｜** `raw/名字與人物.md` 作者自承「我只列到榮恩跟金妮，**六個哥哥我一個都沒定過**」；四支腦內檔對他們零筆 **｜ 未指章（只取名字） ｜** 弗雷、喬治。**只有名字是 fallback**——選雙胞胎（而不是單一個哥哥）的理由、擋「相聲雙簧」那條負向承諾、以及「不指定誰扔」的鏡頭紀律，全部是作者當場推的，記腦。作者自述的判準值得記：「**兩個人做同一件事就變成天氣**，一個人做就是針對，而針對要交代、交代就得停格」——而停格違反上游那條已經釘死的溫度紀律。
8. **S8 ｜ 那條誤導線（哥哥深夜在地下層遊蕩、臉色慘白、阻止妹妹說話、揭曉是私人戀愛）的名字與細節 ｜** 腦內檔只有「朋友的哥哥」四個字加一句功能（異常不等於有罪）；**書內全查無**——受測體掃 `大綱/arc01`–`arc08`＋`raw/`＋`00-摘要` 零命中，是它先報「這條排除線指著一個沒有家的東西」 **｜ 未指章（取四格：名字／徽章／揭曉由誰講／第二個倒下的女生是誰） ｜** 派西·衛斯理；級長徽章（同時是誤導線的地基與「那種你會託他看門的年輕人」那個錯誤的載體）；④由妹妹在最後說出口；arc07 走廊上另一個倒下的女生就是他的人。**四格之外的排法全是作者自己下的**：①在②前面（誤導線從 arc05 才開機）／②那次封口不准算進三次坦白（會把排了兩輪的計數器變成四次）／arc09 那一格記成依賴不是待辦。
   **這一筆的形狀是本測試前所未有的**：前六筆 fallback 都是「書內已有的東西，作者忘了細節」；**這一筆是「作者口頭給了一條約束，而被約束的東西在書裡不存在」**——約束先落地、對象後補。抓到它的是受測體，而 `beat-lint` 抓不到（它只驗檔案路徑存不存在，驗不到「這條排除線指的人有沒有家」）。
