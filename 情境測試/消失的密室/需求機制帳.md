# 消失的密室 · 需求→機制帳

> 測試 meta，**只放 `情境測試/消失的密室/`，永不進書資料夾**。
> **誰寫**：觀測者 agent，每 session **append**（不改舊列——舊列是那個 session 當下的事實）。
> **為什麼要它**：系統的 12 支閘門量的是「格式對不對」，沒有任何一支量「作者的一個需求，系統給了它幾個家」。**0 個家 ＝ 機制缺口（Q-M）**、**N 個家 ＝ 機制重複（Q-R）**。這一格是逐軸重構看不到的——重構是**直著**看每一軸，這本帳是**橫著**數一個需求落在幾軸。
> **落點計數的工具**：`grep` ＋ `fact-refs --entity <名>`／`decision-project --target <路徑>`／各 `*-lint --emit`。**`--book` 一律傳絕對路徑**（見 `session台帳.md` 操作事實 2）。

## 判定規則（每一條都有機械症狀，不是讀感）

| 落點數 | 意思 | 機械症狀（滿足任一即可記） |
|---|---|---|
| **0** | **機制缺口**（Q-M） | 閘門報 `夾帶`／自創節／落點已廢除；或內容被塞進自由散文；或作者答了但根本沒落檔（`SNN-*.md` 的 elicitation 紀錄有、`git diff` 沒有）。`derived-sync check` 的成長哨兵「事實行肥大／夾帶」是現成偵測器 |
| **1** | 正常 | — |
| **N** | **機制重複**（Q-R） | 同一個值出現在 **≥2 個落點，且沒有一個是投影**。逐筆做全書落點計數。同形前例可當基準：`節奏檔位` 三份、`基調參照` 第四份拷貝、五支 rollup 獨有內容 47–78% |

> **「沒有一個是投影」是這一格的關鍵**。投影（`*-lint --emit`／`fact-project`／`readiness`／`structure-project`）**不落檔**，所以它不是拷貝——同一個值在三支檔裡出現、其中兩支是 `--emit` 的輸出，那是 **1 個落點**不是 3 個。把投影算進去會讓這本帳把系統做對的事報成病。
> **證據欄不准寫讀感。**「感覺重複」不是一列；`file:line` ＋ 閘門輸出 ＋ `git diff` hash 才是。理由與基座 §8「不自評打分」同源：**這本帳是給階段 3 拍板用的輸入，不是評分表。**

---

## P（前置）— 本 session 新增 0 列（落點 0：0／落點 1：0／落點 N：0）

（P 沒有系統 agent、沒有 skill 執行，因此沒有「需求 → 落點」的流動可數。這個 0 是**結構性的空，不是漏記**：這本帳數的是「作者說了一個需求 → 系統把它存到哪幾支檔」，而 P 全程沒有任何一方在扮演「系統」——冷啟的三支 subagent 是**佈場工具**（兩支讀者產節拍筆記、一支寫手產素材），受測體第一次上場是 S1 的 `develop`。作者也沒有對系統說過任何需求：P 的產物是**替未來的作者預先寫下的腦**，不是一次互動的結果。

**第一列預期在 S1（`develop`）出現**，而且大概率來自 `raw/還沒想清楚的.md` 裡刻意留的 4＋1 個兩案並陳承重分叉——作者在 S1 拍板選案之後，那個決定會落到某處（或不落到任何一處），那一刻才有落點可數。**同 session 要一併留意 `raw/` 裡那 5 處自相矛盾**：系統若發現矛盾卻沒有任何機制承接它（`story/參照/待裁決.md` 是設計上的家），那就是一列落點 0。）

---

## S1（develop）— 本 session 新增 8 列（落點 0：2／落點 1：4／落點 N：2）

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| 第 1 輪口頭新增的一句：瑞斗「把自己交給了『我必須是特別的』，**最後真的分成了好幾塊**」 | **0** | 該句只在 elicitation（導演→系統 `SendMessage`「作者回答 S1 第一輪 8 題」）裡；`grep -rn "好幾塊" 消失的密室/story/` 在第 2 輪爭議前無任何落點。檔內只有**乙案內部**的另一句：`raw/還沒想清楚的.md:23`＋`情境測試/作者腦內-走向與主題.md:73` 的「少掉一塊」。第 2 輪冷啟的作者 agent 回「**這句話不在我的檔案裡**」並退回訂金③。事後補救＝`story/參照/裁決流.md:47` 的**更正列**（記「兩案皆未定，不得記成已定案」）——**是事後補救，不是機制**：缺的是「本輪對話中拍板／新生成但尚未落檔的事實」的落點 |
| 配角的**結構功能位置**（麥朵＝「答案一直在場沒人去撿」的活示範／阿辣哥＝「證詞成立但給證詞的人不救你」／飛七＝題旨四格表缺的第五格） | **0** | 三處都在源檔的**自創節自由散文**：`story/設定/角色/麥朵.md:7`（`## 她不是喜劇配角，她是這本書的方法論`）／`阿辣哥.md:7`（`## 他是「證詞成立、但給證詞的人不救你」那一格`）／`飛七.md:12`（`## 兩條寫法約束`）。角色源檔是自由源、無節枚舉 lint；`char-lint` 的 slot 只數 `.ai.md`（本輪「四節共 37 個 slot」全部來自僅有的 2 支 `.ai.md`），而這三支源檔**連 `.ai.md` 都沒有**（`char-lint`：「13 支源沒有衍生檔」，exit 1）。**無任何 lint／投影逐欄取值** ⇒ 符合「內容被塞進自由散文」這條機械症狀。系統要的是四象限，作者給的是功能位置，目前只有前者有欄位 |
| 「整本不准有任何人替看門人說情，**包括敘述者**」 | **6** | `story/物件/全書.md:24`（書級方針規則表，設計上的家）＋`設定/世界觀/血統與歸屬.md:21`＋`血統與歸屬.ai.md:33`＋`設定/角色/哈利/關係.md:9`＋`設定/角色/飛七.md:14`＋`設定/風格/風格.md:20` 與 `:32`（同檔兩處）。**無一是投影**（`fact-project --for-beat` 會印 `全書.md` 那一條，但另外五處是實體拷貝、不因此消失）。`story/參照/裁決流.md:40` 記的是**理由**（射程不同），不計入拷貝 |
| 五條達標線（含單位與核對者） | **1** | 唯一權威在源 `story/設定/風格/風格.md:5–11`。`風格.ai.md` 的 front-matter 只有 5 欄（`基調參照`／`禁用詞表`／`稱謂系統`／`可用句式`／`語域`），**未收達標線** ⇒ 沒有第二份。核對者欄指向 `write-test`／`beat-test`，本 session 尚未跑。`style-lint` exit 0（「掃了 0 章正文／5 個字面 entry」） |
| 三檔位句式數字（常態 20–30／恐怖觸發句群 8–14／喜劇引文段一段 20–35 分句） | **1**（＋1 hash 同步衍生，不計為拷貝） | 源 `風格.md:52–56` 的三檔位表；衍生 `風格.ai.md:7` 的 `可用句式` 壓成**單行 slot**（`style-lint` 的取值者）。判為②**新表述**而非拷貝：源是 67 行含實測方法與反例（`:58` 解釋為何檔位名是「觸發句群」不是「高潮段」），衍生是 9 行純 front-matter；且 `generated-from: 25f3b6535eea` 由 `derived-sync check` 守（本輪「0 個需處理」），源一動即報 stale |
| 兩條時序排序約束：雞被殺早於第一次襲擊／「幽靈不能死第二次」早於賈斯汀那一場 | **1**（＋衍生） | **導演提的「③被放進②的容器」疑慮，驗證結果是否定的——兩條在源檔都有**：`設定/世界觀/那個東西.md:21`（幽靈那條，寫在中介物對照表列內）／`那個東西.md:48`＋`密室與繼承人.md:60`（雞那條）。`那個東西.ai.md:28,29` 是**衍生的新表述**，不是唯一落點，重生不會弄丟。**但半格是 0**：`.ai.md:29` 夾帶的下游待辦「目前 raw 沒有標時序，**落幕綱時要盯**」沒有進 `story/參照/待裁決.md`（該檔 `decision-lint` 報 2 列，不含此筆）——待辦本身無落點，而它住在會被重生的容器裡 |
| 獎牌「在很前面、完全無關的場合出現一次，之後整本不提」 | **3** | `story/物件/獎牌.md:21`（規則表 `只出現一次`，`object-lint` 掃 6 支物件檔／14 條約束、0 問題）＋`設定/世界觀/密室與繼承人.md:34`（源散文，同一句話）＋`密室與繼承人.ai.md:36`（衍生自洽節）。**無一是投影**。已有機械可查的家（物件檔），世界觀源檔那一份是多的 |
| 日記的三條硬約束（不是會說話的道具／密室內不得施法／不得手下留情） | **1** | 三條都只在 `story/物件/日記.md:23–25`；`grep -rn "手下留情\|不得施法\|會說話的道具" 消失的密室/story/` 各僅 1 處命中。`object-lint`「物件檔格式乾淨」。**這一列是對照組**——同一個 session 裡，走物件軸的約束落點乾淨、走自由散文的（看門人那條）散成 6 份 |

**本 session 的形狀**：兩個落點 0 都不是「系統忘了存」，是**沒有欄位可存**——一個是對話中新生成的事實（無容器），一個是角色的結構功能位置（schema 只有四象限）。兩個落點 N 都發生在**同一條規則同時有「機械的家」與「敘述的家」**時，系統兩邊都寫；而對照組（日記三條）證明只要約束一開始就走物件軸，落點就是乾淨的 1。

> `derived-sync check` 成長哨兵本輪報「（無）」，`derived-sync validate`「所有 `.ai.md` 格式合規」（比對 29 個 `##` 節、51 個 front-matter 鍵、0 個 schema 外的鍵、裁決 blockquote 0 支命中）——**上面兩列落點 0 沒有被任何現成哨兵抓到，這正是本帳存在的理由**。

---

## S2（organize）— 本 session 新增 13 列（落點 0：2／落點 1：5／落點 N：6）

> 產物範圍 ＝ `git diff f36eba6` 的 11 支已追蹤檔 ＋ 23 支未追蹤新檔，限 `消失的密室/`。
> 本 session 的形狀與 S1 相反：S1 的兩個落點 0 都是「**沒有欄位可存**」；S2 的落點 0 與落點 N **全部發生在同一件事上——「兌現點／計數／已裁決」這一類跨檔的排定值，系統有很多地方可以寫，但沒有一個地方能被查**。

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| 七個新角色各一句「不得寫成什麼」（**柯林／賈斯汀／賓斯／芽菜** 四人） | **1** | 各自唯一在 `story/物件/<名>.md` 的 `## 不得寫成什麼` 規則表：`柯林.md:14,15`／`賈斯汀.md:14`／`賓斯.md:14,15`／`芽菜.md:14`。對應的 `story/設定/角色/<名>.md` 四支源檔 `grep -n "不得\|不准\|不要\|別寫"` **0 命中**。`object-lint`：「14 支物件檔／28 條約束／2 個揭示層級」、「物件檔格式乾淨」exit 0。**這一列是本 session 的對照組**——同一個需求、同一輪、同一支 skill，走物件軸就是乾淨的 1 |
| 七個新角色的同一句（**尼克／夫子／石內卜** 三人） | **2** | 同一條排除線同時寫在物件檔的規則表**與**角色源檔的自由散文：`物件/尼克.md:14`「不准拆成兩個鬼」↔`設定/角色/尼克.md:9`「這兩件事不准拆給兩個鬼」；`物件/夫子.md:20,21`↔`設定/角色/夫子.md:13,15`（「他不准被寫成壞人」「不准有任何角色指控『這不合法』」逐句對應）；`物件/石內卜.md:21`「不得有人指出來」↔`設定/角色/石內卜.md:17`「全書不准有任何一句話指出這件事」。**無一是投影**：角色源檔是自由源、無 lint，`object-lint` 只掃 `story/物件/`，兩邊沒有任何東西比對。blob `bfa27336f596`（`物件/石內卜.md`） |
| **「他不求救 ＝ 恰四次」這條基數約束本身**（作者第 1 輪要求從「至少」收成確數） | **2** | `大綱/arc01.md:32`（卷級方向「確數四次，不是『至少』」）＋`設定/角色/哈利/核心.md:14`（「整本**恰四次**」）。`哈利.ai.md:26` 是衍生（`derived-sync stamp 55060d330399`，源一動即報 stale），依 S1 判法**不計為拷貝**；`裁決流.md:48` 記的是理由，同樣不計。**兩處都是實體拷貝、無投影**。⚠️**沒有任何閘門量得了「恰好 N 次」**：`outline-lint` 第 10 項只驗 `## 本 arc 伏筆狀態` 是不是三欄表（`outline.py:423`），不數列、不跨檔加總；`foreshadow-project` 存在但它的輸入是**幕綱**（本輪實跑：「掃 1 個 arc」「（無符合條件的伏筆）」「小計：0 條」），看不到大綱層 |
| 「不求救」**四個落點的排定**（一：arc03／二：arc04／三：arc06／四：arc07） | **3** | ①`大綱/arc01.md:43–51` 的排定表（四列，含落點與價碼）②各 arc 的 `## 本 arc 伏筆狀態` 列：`arc03.md:91`／`arc04.md:108`／`arc06.md:118`／`arc07.md:169` ③各 arc 的敘述線散文：`arc03.md:52`／`arc04.md:70`／`arc06.md:87`／`arc07.md:96`。**同一個排定寫了三遍、無一是投影**。blob `316da0a13063`（`arc01.md`）。作者立這張表的理由本身就是缺投影：「散在七支檔裡就數不出來了」 |
| 「金妮想坦白 ＝ 確數三次」的**三個落點排定** | **2** | `大綱/arc01.md:54`（一行列出三個落點）＋各 arc 的伏筆狀態表：`arc02.md:95`／`arc06.md:116`／`arc07.md:168`。**無一是投影**，同上 |
| **那輛車的兌現點 ＝ arc07 阿辣哥那一場** | **4** | `大綱/arc07.md:161`（伏筆狀態表「收」）＋`大綱/arc01.md:115`（「兌現點已排在 `大綱/arc07.md`」）＋`大綱/arc03.md:95`＋`大綱/arc04.md:110`（兩支都寫「兌現在 `大綱/arc07.md`」）。`裁決流.md:75` 記理由、且 `decision-project --book … --target 物件/那輛車.md --active-only` **確實投影得出來**（實跑：「掃描了 58 列裁決／命中 7 列」，含「那輛車的兌現點＝arc07 禁忌森林深處，阿辣哥那一場」）——**所以判例那一份不算拷貝**。⚠️**但 `story/物件/那輛車.md:13` 本輪未被修改（不在 `git status` 的異動清單裡），至今仍寫著「而承諾的兌現點目前還沒排」**——同一個事實的第五個落點裝著**相反的舊值**，而 `object-lint`（exit 0、「物件檔格式乾淨」）與 `decision-lint` 都不比對散文與判例。旁證：系統自己在 `物件/海格的雨傘.md:17` 寫「同 `物件/那輛車.md` 在 2026-07-31 之前的狀態」——它知道那支檔的狀態已過期，仍然沒有回去改 |
| **海格的雨傘的兌現點** | **0** | `物件/海格的雨傘.md:17`「這條承諾目前沒有排兌現點」＋`大綱/arc02.md:93`「**兌現點尚未排**」——兩處都只是**宣告缺席**，全書 `grep -rn "雨傘" 消失的密室/story/` 沒有任何一支 arc 收它。機械症狀有二：①`物件/那輛車.md:19` 有 `必須回來一次` 這條**受 `object-lint` 檢查的約束列**，而 `物件/海格的雨傘.md:21–25` 的三條約束**全部是禁止式**（不得被解釋／不得被沒收／埋設時不得加重），**沒有任何一條表達「必須兌現」**——同一輪、同一支 skill、同一個作者需求，一個進了規則表、一個沒有 ②`foreshadow-project` 本輪實跑 exit 0、「小計：0 條，其中 0 條為可疑點」——**「埋了沒收」在大綱層沒有偵測器**，因為它的射程是幕綱 |
| 分叉五拍板甲案（她自己把日記丟掉、他撿到） | **1** | 唯一落值在 `大綱/arc06.md:7`「**分叉五已於 2026-07-31 拍板甲案**（她自己丟掉、他撿到），本 arc 不留岔口」。`裁決流.md:72` 記理由（可投影，不計）。⚠️**但三支宣告「未定」的檔一支都沒被更新**：`story/00-摘要.md:31` 仍把它列在「**未定**（作者保留，兩案都要活著）」清單裡（該檔本輪**有**被改，`git diff` 只加了 `## 貫穿大懸念` 一節，未動未定清單）／`story/設定/角色/金妮/水下.md:27` 仍寫「**兩案並存，作者拍板。**」——**而這支正是 `裁決流.md:72` 指名的標的**／`story/設定/世界觀/密室與繼承人.ai.md:35` 仍寫「一條尚未回答、但綁在**未拍板**分叉上的」。`decision-project --target 設定/角色/金妮/水下.md` 命中 9 列（含這一列），**投影查得到，但沒有任何東西回頭比對標的檔裡的那句話** |
| 咆哮信落在開學第二天早上（郵政規則「任何私人的消息都是公開事件」的第一次執行） | **1** | 唯一在 `大綱/arc02.md:92` 的伏筆狀態表。`設定/世界觀/魔法物品與規則.md:38` 寫的是**規則**（咆哮信是什麼）、S1 baseline 已有，不是這一輪的兌現排定；`裁決流.md:60` 記理由 |
| 呼嚕粉→夜行巷→波金伯克斯整條（魯休斯動機唯一能被讀者看見的地方） | **1** | 唯一在 `大綱/arc01.md:118` 的伏筆狀態表（＋同檔 `:85` 的敘述線是同一場戲的展開，不是第二個落點）。`裁決流.md:59` 記理由 |
| 拿樂絲太太＝飛七的眼睛（**作者據此駁回系統的審稿判斷**：那一格演的是學校有多冷，不是他很可憐） | **4** | `大綱/arc03.md:36`（敘述線）＋`大綱/arc03.md:92`（伏筆狀態表）＋`設定/角色/飛七.md:7`＋`設定/世界觀/密室與繼承人.md:50`（後兩支是 S1 baseline 已有）。**無一是投影**——四處都是同一句話的重寫，而 `char-lint` 對 `飛七.md` 這支源檔只印「無衍生檔」（本輪 20 支之一，exit 1），根本沒讀進去 |
| 「三十年前」作廢、全書一律以五十年為準（P 埋的 5 處之外的**第 6 處**矛盾） | **1** | 唯一落點是 `raw/名字與人物.md:17` 就地劃掉並註明改因（`git diff` 該檔 `1 +/-`）。`裁決流.md:64` 記理由。**這是本 session 唯一一次寫入 `raw/`，作者明令授權**——形狀正確：矛盾消在源頭，不是在下游各處補釘 |
| **「同一個事實有兩個落點，而沒有任何東西比對它們」——作者第 1 輪親口給出的裁決規則：「一支檔有機制、有下游兌現點，另一支只有一句『還沒決定』。以有機制的那支為準。」** | **0** | 這條規則本身沒有任何容器。機械症狀：本 session **同一個病在四個地方獨立復發**（分叉五的三支未更新檔／`物件/那輛車.md:13` 的反向舊值／`大綱/arc02.md:93` 與 `物件/海格的雨傘.md:17` 兩處平行宣告「未排」／`密室與繼承人.ai.md:35` 的過期宣告），而全部 12 支閘門本輪讀數是 **11 支 exit 0＋`char-lint` exit 1（20 支源無衍生檔）**——**沒有任何一支報這四筆**。`derived-sync check` 本輪「0 個需處理」（它守的是 hash 新鮮度，這四筆的源檔根本沒動）。這與 S1 第 1 列是同一個病的第二階段：S1 是「拍板了只活在對話裡」，S2 是「拍板了只活在**另一支檔**裡，而宣告未定的那一支沒被更新」 |

**本 session 的形狀**：六個落點 N 全部是**跨檔排定值**（計數器的落點表、兌現點、逐 arc 的伏筆狀態表），而它們重複的原因是同一個——**大綱層沒有「排定」的投影**。`foreshadow-project` 是這個形狀對的工具，但它的輸入是幕綱（本輪實跑：掃 1 個 arc、0 條），所以在 `beat-sheet` 跑完之前，大綱層的每一筆排定只能靠人抄進 N 個地方再靠人維持一致。兩個落點 0（雨傘的兌現點、「同一事實兩落點無比對器」）也指向同一格：**系統能記「這件事排在哪」，不能查「這件事排了沒／排的還是不是同一個值」。**

> **一格提醒下一輪的觀測者**：本帳第 6（那輛車）、7（海格的雨傘）、13（無比對器）列描述的是**同一個機制缺口的三個面**（值重複／值缺席／值互相矛盾），不要因為三列讀起來像三件事就當成三個獨立缺口。它們合起來是**一個**問題：大綱層的排定值沒有唯一權威，也沒有比對器。
> **12 支閘門本輪讀數**（導演端已獨立覆核，本帳只引用機械輸出）：`outline-lint` exit 0（8 支 scoped／枚舉外節 0／檔內路徑引用 67 個·0 個不存在）／`object-lint` exit 0（14 支物件檔／28 條約束）／`decision-lint` exit 0／`derived-sync check` 0 個需處理／`derived-sync validate` 全合規／`char-lint` **exit 1（20 支源無衍生檔，S1 的 13 支長到 20）**——**上面 13 列裡沒有任何一列是被這些閘門抓出來的，這仍然是本帳存在的理由。**

---

## S3（橫向：worldbuild＋character＋expand）— 本 session 新增 14 列（落點 0：1／落點 1：5／落點 N：8）

> 產物範圍 ＝ `git diff 56edda9` 在 `消失的密室/` 的 12 支已追蹤檔（104 新增行）＋ 8 支未追蹤新檔。
> **本 session 的形狀與 S1／S2 都不同，而這正是基座 §6 增修 4 排入橫向 skill 的理由**：S1 的落點 0 是「沒有欄位可存」、S2 的落點 0／N 全部是「跨檔排定值沒有比對器」；**S3 第一次出現「作者當場數落點、然後叫系統不要建第二個家」**——本帳 14 列裡有 **5 列是主動避免的重複**（第 3、6、9、10、11 列），其中 3 列是作者**逐條查過落點之後**推翻系統提案的結果（`裁決流.md:104`）。**這是這本帳第一次量到「機制在被使用」，不只是「機制存在或缺席」。**

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「分帽子那天」的三條輸出禁令（不准由哈利說出口／不准由敘述點破／不准掛章尾鉤） | **2** | ①`物件/分帽子那天.md:25,26,27` 的規則表（新建，blob `b7b56530189c`；`object-lint` exit 0「17 支物件檔／34 條約束／3 個揭示層級」，並印一行資訊「揭示點待落幕（arc02 尚未拆幕）」）②`大綱/arc08.md:68` 括號裡的前兩條（「本場不得由哈利把這件事說出口，也不得由敘述點破」）——**本 session 未動，仍是實體拷貝**。兩處射程不同（物件檔＝全書／arc08＝本場），但擋的是同一件事。**無投影**。第三條「不得掛章尾鉤」是新的、目前只有一份（`00-摘要.ai.md:9` 寫的是正面規則「鉤子掛表層那一格」，不是禁令，不計） |
| **2.**「分帽子那天」這件事實本身（作者明令**四處指名一字不洗**） | **6** | `設定/角色/哈利/水下.md:7`（權威）＋`哈利.ai.md:13` 落差欄＋`00-摘要.ai.md:9` `引信` 鍵＋`00-摘要.md:19,39`＋`大綱/arc02.md:24`＋`大綱/arc08.md:68,113`（4 源＋2 衍生，**無一是投影**）。**但這一列是本 session 最重要的反例，不是病**：`裁決流.md:79` 記著作者的裁定（「水下只對讀者、不對筆」），而**新開的 `物件/分帽子那天.md:11` 明寫「內容的權威在 `設定/角色/哈利/水下.md`，本檔不複述，只登記它的輸出紀律」**——新機制**沒有**把落點推到 7。`decision-project --target 物件/分帽子那天.md --active-only` 實跑「掃描了 94 列裁決／命中 18 列」，那三筆裁決（型別不寫伏筆／收點不綁分叉一／不開第九型）**投影撈得到，不計為拷貝** |
| **3.** 壞掉的魔杖會反噬，**越大的咒語漏得越兇**、修不好 | **1** | 唯一權威在源 `設定/世界觀/魔法物品與規則.md:20,21`（本輪新增；`world-lint` exit 0「7 支 `<主題>.ai.md`／0 支仍帶已廢除的欄／源檔第一段 0/7 支是空的」）。`魔法物品與規則.ai.md:13,22,30,31` 是衍生的新表述（`derived-sync check` 0 個需處理、`validate` 全合規，源一動即報 stale，依 S1 判法不計）。**這一列是主動避免的重複**：作者第 1 輪駁回候選 C 的理由逐字是「**第二個家**」——`物件/海格的雨傘.md:15` 那句已經住著制度側的那一半（「程序拿走了他的資格」），本檔只回答物理面。逐檔核對過：`海格的雨傘.md` 全檔**沒有**「漏得越兇／導不準／修不好」任何一句 |
| **4.**「任何私人的消息都是公開事件」 | **2** | ①`設定/世界觀/消息與公開性.md:7`（新建第 7 支主題，blob `bb3e70529b11`）②`大綱/arc02.md:32` 敘述線那句＋`:92` 伏筆表的列名 `私人消息即公開事件`。**兩個名字、一件事**——`消息與公開性.md:9` 自己承認這一點。**無投影**：`world-lint` 印「0 個伏筆名（0 個 registry 查無）」、`outline-lint` 對這個名字報 registry 查無而作者裁定**那不是誤報**（`裁決流.md:90`：下標權在幕綱，不在大綱）。所以在 `beat-sheet` 跑完之前，這兩份沒有任何東西比對 |
| **5.** 海格那把傘：**沒有人來收它**（＋兌現點排進 arc04 一行） | **4** | `物件/海格的雨傘.md:17,19`＋`大綱/arc02.md:93` 伏筆表＋`大綱/arc04.md:108` 伏筆表＋`大綱/arc04.md` 新增的敘述線一行（`git diff` +「他拎著那把粉紅色的雨傘走出去。沒有一個人看它一眼。」）。**無一是投影。** ⚠️**這一列是 S2 第 7 列（落點 **0**：兌現點沒排）的續**——缺口確實補上了，代價是**同一個兌現點現在寫了四遍**，形狀直接掉進 S2 第 4／5／6 列那一類。`foreshadow-project` 仍看不到（輸入是幕綱）；`outline-lint` exit 0 不比對兩支 arc 的伏筆表 |
| **6.**「飛七不准自己說破他是爆竹」 | **1** | 唯一在 `物件/飛七.md:8,20`（新建，blob `3c8845b22bd7`，同檔散文＋規則表算一個落點）。`設定/角色/飛七.ai.md:25` 是衍生且**指過去**；`設定/角色/飛七.md:20` 本輪**從拷貝改寫成指標**（`git diff`：刪「2. **他不准自己說破他是爆竹。**」，改成「本檔不重述」）。**這一列與第 7 列是同一組，是本 session「作者主動避免製造重複」的樣本** |
| **7.**「整本不准有任何人替看門人說情，包括敘述者」（S1 第 3 列的續） | **7**（S1 記 6；本輪**源側 −1、衍生側 +2**） | 現況逐檔：`物件/全書.md:24`（規則表，設計上的家）＋`設定/世界觀/血統與歸屬.md:21`＋`血統與歸屬.ai.md:33`＋`設定/角色/哈利/關係.md:9`＋`設定/風格/風格.md:20` 與 `:32`＋**本輪新增的 `設定/角色/飛七.ai.md:23`** 與 **`設定/角色/洛哈.ai.md:29`**（後者寫「同源於『不替看門人說情』」）。**減的那一份**：`設定/角色/飛七.md:14` 的整句拷貝本輪刪掉、改成 `:20` 的指標。**沒有一個是投影。** ⚠️**這一列說明「作者手動去重」擋不住總量**：他刪了 1 份源檔拷貝，同一輪的 `character` 又在兩支新衍生檔裡各寫了 1 份——而衍生檔會被重生，所以那兩份不會累積成永久債，但**它們也不會自己消失**，`derived-sync check`（0 個需處理）與 `char-lint` 都不數「同一句話在幾支檔裡」 |
| **8.** 麥朵的證詞要**無用兩次、關鍵一次** | **4** | `物件/麥朵.md:10,20`（新建，blob `30a2cca39864`）＋`大綱/arc05.md:93` 伏筆表＋`大綱/arc07.md:162` 伏筆表＋`設定/角色/麥朵.md:13`（＋`麥朵.ai.md:22,26` 衍生，不計）。**無一是投影。** 值得記的是**作者用落點推翻了系統的提案**：`character` 第 4 輪問「她的『需要』兌不兌現」給了四個選項，作者四個全不選，理由是 `大綱/arc07.md:162` **已經寫著**「麥朵｜收（關鍵一次）」（`裁決流.md:101`）——**作者是靠自己記得那一行，不是靠任何查詢工具**；`decision-project --target 設定/角色/麥朵.md` 撈得到裁決理由，撈不到大綱伏筆表的排定值 |
| **9.**「洛哈的結局不得拆開三筆帳」 | **3** | `物件/榮恩的魔杖.md:20,27`（規則表，S2 baseline）＋`大綱/arc07.md:138,142`（敘述線＋括號）＋`大綱/arc07.md:164` 伏筆表。**本輪 −1**：`設定/角色/洛哈.md:21` 的整句拷貝改寫成指標（`git diff`：「**這一格結清幾筆帳、為什麼不得拆開，見 `story/物件/榮恩的魔杖.md`**（約束名：不得拆開三筆帳）」）。**這一列是主動避免的重複**：作者裁定**不建 `物件/洛哈.md`**、改指既有的 `物件/榮恩的魔杖.md`（`裁決流.md:104`「五條裡有三條已經有機械的家」） |
| **10.**「洛哈負弧線的反諷，全書不准有任何一句話點出來」（護欄） | **1** | 唯一落點是 `參照/裁決流.md:100`，而該列自己寫明「**這條沒有落成物件檔的排除線，因為作者本輪裁定洛哈不建物件檔——它是護欄，家在本檔**」。`decision-project --book … --target 設定/角色/洛哈.md --active-only` **實跑撈得到**（「掃描了 94 列裁決」），所以下游查得到 ⇒ 判 **1 不是 0**。⚠️**但它沒有機械核對者**：該列自承「最接近的機械核對者是 `設定/風格/風格.md` 達標線 1，核對者 `write-test`」——與 S1 那條「整本不准替他說情」同型（`血統與歸屬.ai.md:33` 也自承「這條的違反不會被任何機械閘門抓到」）。`decision-lint` exit 0（94 列／3 列待裁決／標的 34 個·0 個在書內找不到）只驗格式，不驗兌現 |
| **11.** arc05 那四個補丁（A2 括號／B1 一行／C2 一場飯／C1 一句報時／D1 吵第三份） | **1** | 五格全部落在 `大綱/arc05.md`（8 處，blob `eb86f37c240a`；`outline-lint` exit 0「8 支 scoped／枚舉外的 `##` 節 0 個／檔內路徑引用 78 個·0 個不存在」），理由 append 進 `裁決流.md:110–113`（**4 列不准併**，`decision-project --target 大綱/arc05.md` 實跑命中 21 列 ⇒ 可投影，不計為拷貝）。**C1 那一格是主動避免的重複**：「只報時、一個字的安慰都沒有」這條排除線已有機械的家 `物件/芽菜.md:14`（S2 建，約束名 `只報時不安慰`），`arc05.md:27` **指過去而不重述** |
| **12.**「全書不准有任何人把『他花完兩次機會、然後機會就過期了』這兩件事連起來說出口」（禁令，射程全書） | **4** | `大綱/arc01.md:38` 卷級方向（權威）＋`設定/角色/哈利/核心.md:16`＋`設定/角色/哈利.ai.md:30`（衍生）＋**本輪新增的 `大綱/arc05.md:47`**。**無一是投影。** arc05 那一份**指名了 arc01**（「`大綱/arc01.md` 卷級方向寫死」），但它同時**複述了那句話的內容**才能解釋為什麼不准吵「該不該告訴大人」⇒ 仍計為一份。值得記：`expand` 第 6 輪把 D1 的風險標成「撞到不求救計數器」，**作者拆掉了那個標的並換上真的**——擋它的是這條禁令、不是計數器（`裁決流.md:113`）。**而這條禁令沒有任何閘門在守**，作者是靠讀出來的 |
| **13.**「等到 X 之後才填」的欄，要有東西回頭把它填掉（作者自陳**這是第三次**看到同一個形狀） | **0** | 處置是 `裁決流.md:88` 一列通則 ＋ `大綱/arc02.md:92` 伏筆表那一列尾巴的一句回填提示（「`beat-sheet` 在本 arc 標下伏筆名之後，**回填 `設定/世界觀/消息與公開性.ai.md`** 的 `伏筆` 欄」）。**機械症狀：沒有任何閘門讀得懂那句提示。** ①`outline-lint` exit 0，它驗伏筆意圖表的欄數與檔內路徑引用（78 個·0 個不存在），**不驗表格內的自然語言待辦** ②`derived-sync check` 報「0 個需處理」，因為 `消息與公開性.ai.md` 的 `伏筆: {}` 是**合法的空值**，不是 stale ③`derived-sync validate` 全合規（14 支 `.ai.md`／73 個鍵／0 個 schema 外）——空 `{}` 完全合格。**三個 session 三個實例**（S2 那輛車的兌現點／S2 分叉五那三支沒更新的檔／S3 `消息與公開性.ai.md` 的 `伏筆` 欄），而處置一路都是**規勸**。`CLAUDE.md` 對這個形狀的判準逐字是「規勸不會被執行，工具會」 |
| **14.** 2×2 改判之後，三處已過期的字面要跟著改（`血統與歸屬.md` 四格表那段／`00-摘要.md` 的「第五格」說法／`所屬arc` 欄要不要留） | **1** | 三筆全部登記進 `參照/待裁決.md:19,20,21`（0 列 → **3 列**，`decision-lint` exit 0「裁決流 94 列／待裁決 3 列／標的 34 個·0 個在書內找不到」）。**這一列是 S2 第 13 列那個病第一次被正確承接**：S2 的形狀是「拍板了只活在另一支檔裡，而宣告未定的那一支沒被更新，且沒有任何閘門報它」；S3 的 `character` **自己發現了三處過期字面、自己判定本輪不動、自己寫進設計上的家**。⚠️**但承接的是 skill 的自覺，不是機制**——`derived-sync check` 對這三筆一樣是「0 個需處理」（那三支源檔本輪根本沒動，hash 不會 stale） |

**本 session 的形狀**：

1. **落點 N 的 8 列裡，有 5 列（第 1、2、5、7、12 列）的重複源頭是同一個**——**一條「寫法禁令」同時需要住在「規則表」（機械的家、`fact-project --for-beat` 送得到下游）與「戲的現場」（大綱那一格的括號，寫的人當場看得見）**。這與 S2 的「跨檔排定值」不同軸：S2 重複的是**值**（兌現點排在哪），S3 重複的是**禁令**（不准怎麼寫）。**兩者共用同一個缺口**——沒有任何工具比對「規則表的那一條」與「散在正文層的那幾份」是不是還在講同一件事。
2. **第一次量到機制被主動使用。** 五列主動避免的重複（3／6／9／10／11）全部發生在**作者逐條查落點**之後，而查的方法是 `grep` ＋ 記憶，**不是任何一支投影**。系統這一側也做對了三次：`物件/分帽子那天.md:11` 明寫不複述內容、`設定/角色/飛七.md:20` 與 `洛哈.md:21` 把拷貝改成指標。**「指過去而不重述」這個動作目前沒有任何 schema 在要求，也沒有任何閘門在獎勵它**——它這一輪出現了 5 次，下一輪可能 0 次。
3. **唯一的落點 0 是一個規勸。** 而它已經是同一形狀的**第三次**，作者自己把三次併成一個缺口。

> **12 支閘門本輪讀數**（觀測者端獨立覆核，本帳只引用機械輸出）：`derived-sync check` exit 0（0 個需處理；成長哨兵掃描範圍：設定層源 30／設定層衍生 14／大綱 9／參照 2／行長 64 支／搬移目的地 3 個·0 個不存在）／`derived-sync validate` exit 0（14 支 `.ai.md`／44 個 `##` 節／73 個鍵·0 個 schema 外／裁決 blockquote 0 支命中）／`world-lint` exit 0（7 支主題）／`style-lint` exit 0／`summary-lint` exit 0／`readiness-lint` exit 0（狀態表 20 格／就緒清單 6 條·5 條已勾）／`object-lint` exit 0（**17 支物件檔／34 條約束／3 個揭示層級**）／`fact-lint` exit 0／`decision-lint` exit 0（**94 列／3 列**）／`outline-lint` exit 0（8 支 scoped／枚舉外節 0／路徑引用 78 個·0 個不存在）／`beat-lint` exit 0（跳過 1 支骨架）／`ch-lint` exit 0（**住 `tools/beat_metrics`，不住 `tools/chapter_metrics`**——本輪觀測者先跑錯套件拿到 exit 2，同 `S3-橫向.md` 產物節那條操作事實）／`char-lint` **exit 1（17 支源沒有衍生檔，S2 的 20 支降到 17）**。
> **上面 14 列裡沒有任何一列是被這 12 支閘門抓出來的**——第 13 列的落點 0 尤其值得看：三支閘門（`outline-lint`／`derived-sync check`／`validate`）**全部對它印綠燈，而且三支都印對了**，因為那個空 `{}` 在格式上完全合法。這仍然是本帳存在的理由。
> **一格提醒下一輪的觀測者（`beat-sheet` 會是下一站）**：本帳 S2 第 6、7 列與 S3 第 5 列講的是**同一把傘**——S2 記它落點 0（兌現點沒排），S3 記它落點 4（排了，寫了四遍）。`beat-sheet` 一跑，`foreshadow-project` 的輸入就有了，**那四份會第一次有機會被同一支工具讀到**。請在 S4 專門量一次：那四份是不是還是同一個值。

---

## S4（拆幕前置清帳 ＋ character·金妮）— 本 session 新增 14 列（落點 0：2／落點 1：7／落點 N：5）

> 產物範圍 ＝ `git diff` 在 `消失的密室/` 的 21 支已追蹤檔（138 新增行／88 刪除行）＋ 2 支未追蹤新檔（`物件/那個從來不出水的水龍頭.md` blob `2175d3ec`、`物件/金妮.md` blob `a8ad341e`）。`裁決流.md` blob `bbcd1f41`（94 → **128 列**）。
> **本 session 的形狀是前三輪都沒出現過的第四種**：S1 的落點 0 是「沒有欄位可存」、S2 的落點 0／N 全部是「跨檔排定值沒有比對器」、S3 第一次量到「機制在被使用」；**S4 第一次出現「作者為了讓機制生效而刻意保留一份重複，並給它一個拆除日期」**（第 7 列的 `核心.md:13`）——重複不是失手，是**對「投影今天還不通」的一次有意識的計價**。
> **另一個新形狀是落點 0 的性質變了**：S1／S2／S3 的落點 0 都是「書內沒有容器」；S4 兩筆落點 0 有一筆（第 12 列）是「**缺陷在系統層，而系統層在這本書裡結構性地沒有家**」——受測體寫不進 `書本模板/`，`decision-lint._target_exists` 也不認它當標的。
> **計數約定（本節逐列採用，與 S2 略有出入，請勿讀成成長）**：**一支檔算一個落點**，同檔多行（散文＋規則表、卷級方向＋伏筆表）算 **1**。S2 第 5 列曾把「各 arc 的伏筆狀態表」整組算 **1**，本節改為逐檔數——第 11 列因此看起來從 2 變 5，**那是尺換了，不是東西變多了**。

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.** 摘要題旨那一列**不再報數字**，看門人從「第五格」提上來與其餘四人並列成五人名單 | **1** | 唯一落點 `story/00-摘要.md:13`（`git diff`：「罩住四個人，不然就是標語」→「罩住下面每一個人，少一個就是標語」，末句「四格填滿才叫題旨」整句刪除、不補替代句）。`00-摘要.ai.md` 同輪重生（衍生，依 S1 判法不計）。`summary-lint` exit 0（1 支源／1 支衍生·0 支無衍生；6 個語意欄·0 個是空的；基調兩方比對相符；源檔 0 處駁回語彙）。**已駁回「在摘要補一句指向 2×2 的指標」**（`裁決流.md:123`：2×2 已有三份，第四份指標＝下次改表要同步四個地方）——**這是本輪第一個「主動避免的重複」** |
| **2.** 2×2 的「沒有能力 × 選對」格填**飛七**（用「飛七」當鍵，不用「看門人」）；表底下第 40 行整句刪、不換說法 | **1** | 唯一權威在源 `story/設定/世界觀/血統與歸屬.md`（那張表本身）。`血統與歸屬.ai.md:22` 是衍生的新表述（`derived-sync check` exit 0「0 個需處理」，源一動即報 stale，不計）。`world-lint` exit 0（7 支 `<主題>.ai.md`／0 支仍帶已廢除的欄／源檔第一段 0/7 支是空的）。理由在 `裁決流.md:124`（`decision-project --target 設定/世界觀/血統與歸屬.md` 撈得到，可投影、不計） |
| **3.** **「2×2 的表幾何只准住表本身」護欄**（誰在同一欄／同一列／隔壁／對角） | **1** | **落點＝`story/參照/裁決流.md:136`，標的 `全書`。判 1 不判 0 的三條機械理由**：① 落點存在且格式合規——`decision-lint` exit 0（**裁決流 128 列／待裁決 0 列／標的 36 個·0 個在書內找不到**）；② **下游真的查得到**——`decision-project --target 設定/角色/洛哈/ --active-only` 實跑「掃描了 128 列裁決／**命中 28 列**」、飛七 30 列、金妮 39 列，三支的輸出裡「幾何」各命中 3 處。機制在 `tools/decision_projection/src/decision_projection/parse.py:217` 的 `target_matches` 首行 `if row_target.strip() == TARGET_ALL: return True`，**標的『全書』一律命中、不需要幕號**；③ 跑這支查詢的有五支 skill（`character`／`worldbuild`／`develop`／`beat-sheet`／`sync`），正好覆蓋所有會重生設定層 `.ai.md` 的人。**同形前例**：S3 第 10 列（洛哈負弧線反諷護欄，唯一落點也是裁決流）已判 1。⚠️ **它與 S3 第 10 列共享同一個缺陷——沒有機械核對者**：本輪驗收只能靠人選 pattern 的概念 grep（`同一欄\|同一列\|相鄰\|隔壁\|不是同一格\|對角\|哪一格挨著` 掃 `story/`、排除裁決流與待裁決，實跑 **5 筆命中：4 筆假陽性**（`arc01.md:79` 對角巷／`arc03.md:66` 走廊隔壁／`密室與繼承人.md:15`、`.ai.md:30` 兩處房間相鄰）**＋1 筆合法**（`血統與歸屬.ai.md:22`，源即表所在源檔 ⇒ 合乎同源尺））。**違規 0 筆，而這個 0 是人選 pattern 選出來的**——`S4-*.md` 摩擦 1 記的正是同一支 grep 前一次 pattern 選錯而印出假 0 |
| **4.**「飛七」與「看門人」的用詞界線（指格·指人用飛七；演戲·講規則用看門人） | **1** | 唯一落點 `裁決流.md:130`，標的 `全書`（`decision-project` 三支角色查詢皆命中）。**它是一條可判單句的尺**（「這個詞如果換成檔名指得到一支檔，它就是選擇器」），而不是檔名列舉——`裁決流.md:136` 明寫「界線不准寫成檔名列舉（第 129 列已吃過那個虧）」。本輪按這把尺實際判過四處：`血統與歸屬.ai.md` 三處看門人留（腔調）／`洛哈.ai.md` 一處改（選擇器）／`洛哈.ai.md` 的「不替看門人說情」不准碰（那是 `物件/全書.md` 的**約束名字面**，動了就查不到）／`00-摘要.md` 五人名單留 |
| **5.** arc05 的括號**沒有目標值**，要動只能逐格說出「刪掉之後正文的寫法會不會變」 | **1** | 唯一落點 `裁決流.md:132`，標的 `全書`。實測讀數 44.5%（1,036/2,329 字）→ **28.4%**，**淨敘述 1,293 字一字未動**。`outline-lint` exit 0（8 支 scoped／連續敘述佔全檔 54.1%／枚舉外節 0 個／檔內路徑引用 **72 個·0 個不存在**／arc 引用 74 筆·73 筆指得到既有 arc 檔）。**這一列是「反目標值」的護欄**，形狀與第 1 列同源：**不報數字** |
| **6.** **水龍頭那條「不得被提起」禁令**（S3 時它只活在 `大綱/arc05.md` 的一對括號裡） | **4** | ①`story/物件/那個從來不出水的水龍頭.md:26,27`（本輪新建，blob `2175d3ec`；兩條約束 `水龍頭不得給鏡頭`／`水龍頭不得被指涉`；`object-lint`／`fact-lint` exit 0「**19 支物件檔／38 條約束／4 個揭示層級**」）②`大綱/arc03.md:88`「本 arc 不給任何人看見它」（baseline，本輪未動）③`大綱/arc05.md:90`「埋設的方式是完全不提它」（baseline，本輪未動）④`大綱/arc05.md:31`「那間廁所裡唯一被寫到的東西是那個鬼、那攤水、和那鍋在冒泡的東西」（本輪保留的**正面限定清單**＝同一條禁令的執行形式）。**無一是投影。** 本輪的動作是把 baseline `arc05.md:33` 的**顯式禁令句**（「這是全書最喜歡的一個玩笑，而它必須完全不被提起。本段敘述不得給水龍頭任何一個鏡頭」）搬進①——**所以 S3 的「只活在一對括號裡」現在是 4 個家，而新的那個家是唯一進了約束 registry 的**。⚠️ **但新家今天送不到任何人手上**：`物件/<名>.md` 的約束只走 `fact-project --for-beat 幕NNN`，而 `beat-lint` 實跑 **0 支 arc／0 幕**。所以拆幕的人今天讀得到的仍然只有②③④，**而那三份沒有任何東西比對** |
| **7.** **「金妮異常不給特寫」**（作者自陳「全書最貴的一條寫法規矩，開一次例外這本書就沒了」） | **8** | 逐檔（一支檔算一個落點）：`story/物件/金妮.md:44`（本輪新建規則表，約束名 `金妮異常不給特寫`）／`設定/角色/金妮/核心.md:13`／`00-摘要.md:59`／`大綱/arc01.md:63`（卷級方向寫法約束）＋`:120`（伏筆表）／`arc03.md:94`／`arc04.md:112`／`arc05.md:95`／`arc07.md:32`。**8 支檔、9 行，無一是投影**（`fact-project --for-beat` 印不出來：0 幕）。**這一列不是判它是病**——作者本輪逐份給了不同的身分：`核心.md:13` 是**帶拆除日期的鷹架**（`裁決流.md:143`：解除條件＝八支 arc 幕綱齊了、`--for-beat` 真的印得出這一條之後，那半句規則刪掉）；`00-摘要.md:59` 是**第三種東西**（給讀摘要的人看的定調，`裁決流.md:148` 明寫**不在解除範圍內**）；八支 arc 伏筆表是**帳**（帳本來就該逐 arc 記）。**記在帳上的理由是機械的：沒有任何工具能同時看到這 8 份**，改一份不會有東西提醒另外七份——而 S2 第 6 列（那輛車，第五個落點裝著相反的舊值）就是這個形狀最後真的走歪的樣本 |
| **8.** 金妮的**需求層**（需求四象限＋兩難＋馬斯洛＋預期弧線兩案） | **1** | 唯一落點 `story/設定/角色/金妮/水下.md` 的 `## 需求層`／`### 需求四象限`／`### 兩難`／`## 預期弧線`／`## 馬斯洛層次`（同檔＝1）。**刻意不建 `金妮.ai.md`**（`裁決流.md:137`；第 36／133 列維持）——`char-lint` **exit 1「17 支源沒有衍生檔」，金妮補完仍在其中且設計如此**，數字與 S3 完全相同。**本輪第二個「主動避免的重複」**：「期盼」那一格明寫「展開見 `核心.md` 的『## 正當需求』，**本節不複述**」；`## 兩難` 那一節也只寫「三次的落點已排齊，見 `大綱/arc01.md` 卷級方向」而不重抄三個 arc 號 |
| **9.**「需要」那一格＝**差別不在她身上，在有沒有人問對問題** | **4** | `設定/角色/金妮/水下.md:19,20`＋`設定/角色/麥朵.md:13`＋`大綱/arc07.md:162`（麥朵伏筆表「收（關鍵一次）」）＋`設定/世界觀/爬說嘴.md:28`（「這本書的每一道門……只擋誠實的人，或者只擋沒問對問題的人」，同一條方法論換一件衣服）。`麥朵.ai.md:26` 是衍生（不計）。**無一是投影。** ⚠️ **這一列是作者本輪明令「不准分兩案」的那一格**（`裁決流.md:140`：「把它拆成兩案，等於承認這本書有兩種主題，而它只有一種」）——**主題句是全書最該只有一個家的東西，而它現在有四個**，且四個之間沒有任何比對器。與 S3 第 1／2／7／12 列同軸（禁令／主題句散在多層） |
| **10.** **已拍板的分叉，要有東西回頭把「宣告未定」的檔改掉**（分叉五 2026-07-31 上午已拍甲案） | **0** | **本輪改掉兩處**（`金妮/水下.md:83–87` 的小分叉節改寫成已拍板、`物件/日記.md:17` 改成指 `大綱/arc06.md`，`裁決流.md:145`；受測體自陳這是「今天上午認過的那個洞的**第三次現形**」）。**但改完仍有兩處寫著相反的舊值**：①`story/00-摘要.md:31` 至今仍把分叉五列在「**未定**（作者保留，兩案都要活著）」清單第 5 條——**而 `00-摘要.md` 本輪被編輯過**（`git diff` 只動 `:13` 那一行），檔案開著、那一行沒被碰；②`設定/世界觀/密室與繼承人.ai.md:35` 仍寫「一條尚未回答、但綁在**未拍板**分叉上的：日記怎麼到主角手上」。**機械症狀**：`derived-sync check` exit 0「0 個需處理」（①是源檔自己過期、hash 守的是源檔集合；②的源 `密室與繼承人.md` 本輪沒動，所以衍生不 stale）／`summary-lint` exit 0／`world-lint` exit 0／12 支閘門**無一報它**。**S2 第 8 列記過（三支宣告未定的檔沒更新）、S2 第 13 列把它併成缺口、S3 第 14 列記它被 `待裁決.md` 正確承接一次——本輪 `待裁決.md` 清成 **0 列**（首次歸零）而這兩處沒進去。這是同一形狀的第五個實例，五次的偵測者都是人眼** |
| **11.** 伏筆名 `金妮想坦白` → `三次坦白`，＋這條計數器的三個落點排定 | **5** | `大綱/arc01.md:39`（卷級方向）＋`:54`（落點排定行）／`arc02.md:95`／`arc06.md:116`／`arc07.md:168`（三支伏筆狀態表）／`設定/角色/金妮/水下.md:26,33`（兩處引用）＝ **5 支檔、7 行，無一是投影**。改名理由是機械的：`fact_projection/objects.py:411` 的 `check_near_miss` 判準是 `if ref in n or n in ref`，而新建的物件檔名 `金妮` 是 `金妮想坦白` 的**子字串**。⚠️ **本輪最乾淨的一格「還沒到期的紅燈」**：改名**前**`object-lint` exit 0，因為 near-miss 的輸入（幕綱的 `埋/收[[伏筆:X]]` 引用名，`sources.py:344`）是**空的**——`beat-lint` 實跑 0 幕；改名**後**仍 exit 0。**兩次的 0 都是真的，而它們之間差著一顆會在 `beat-sheet` 標下第一個 `金妮想坦白` 當天引爆的雷。沒有任何機制能表達「這一格現在綠、下一階段會紅」**（抓到它的是作者讀 `objects.py` 原始碼）。**計數約定提醒**：S2 第 5 列把同一件事記成 2（把三支 arc 伏筆表整組算 1），本列逐檔數 ⇒ 5。**尺換了，不是東西變多了** |
| **12.** **`書本模板/` 對第二把尺謊報進度，要修掉**（不只修這一本） | **0** | **這一本修好了**：`幕綱/arc01.md` 清掉 `## 幕001 · （幕名）` 樣板、`幕綱/_順序.md` 的 spine 填成真的八支（`裁決流.md:150`）。修完 `object-lint` 的資訊行從「（arc02 尚未拆幕）」變成「（**arc01、arc02、arc03、arc04、arc05、arc06、arc07、arc08** 尚未拆幕）」——本觀測者實跑已確認；`beat-lint` 現印「spine 讀自 `_順序.md`（舊落點已不在）」。**但散布器一個字都沒改，而且沒有任何容器可以記它**：`書本模板/story/幕綱/arc01.md:20` 至今仍是 `## 幕001 · （幕名）`、`書本模板/story/幕綱/_順序.md:8` 仍是 `arc01 → arc02 → …`（本觀測者實跑 `grep` 確認）。**每一本從 `書本模板/` 開的新書都會帶著同一個假進度。** 機械上這是**結構性的落點 0**：`書本模板/` 是系統層白名單第 5 項，受測體**唯讀**、寫不進去；而 `.claude/skills/`／`書本模板/` 都不是 `decision_projection` 的 `_target_exists` 認得的標的（只認 `全書` 與 `story/` 底下的路徑），所以它連 `待裁決.md` 都進不去——寫進去 `decision-lint` 當場報壞標的。**同形本輪共 3 筆**（另兩筆：`beat-test` 測試 4 的 `settings_select.UNDERWATER_FACET` 預設拿不到 `水下.md`／表幾何護欄的終局是一支 lint），三筆全部只能寄放在 `裁決流` 的**理由欄**裡（`:138`／`:139`／`:135`）或本測試側檔案 |
| **13.** 金妮這條線的 `揭示層級` ＝ 水下、揭示於收伏筆 | **1** | 唯一落點 `story/物件/金妮.md` 的 front-matter（`揭示層級: 水下｜揭示於 收[[伏筆:金妮]]`）。`object-lint`／`fact-lint` exit 0，讀數從 S3 的「17 支／34 條／**3** 個揭示層級」變成「19 支／38 條／**4** 個揭示層級」＋一行資訊「揭示點待落幕」。⚠️ **這一列的價值不在落點數，在它示範了「閘門印出了讀數，而那個讀數與被驗的命題不相干」**：`fact_projection/objects.py:387` 那個分支是 `elif t in planted or pending:`——**`pending` 非空就印，與伏筆名對不對無關**（作者的反證：把 `金妮` 改成 `金泥`，同一行照樣印）。而受測體當時把這一行當成「實跑驗證了你要的狀態」回報。**與 `S4-*.md` 摩擦 1 互為另一面**：那一格是 pattern 選錯讓覆蓋範圍是空的（0 是真的、但掃錯東西），這一格是讀數非零、但讀數與命題無關。**兩者合起來是同一句話——覆蓋率行報的是它掃了幾筆，不報它掃的是不是對的東西** |
| **14.** **海格那把傘：沒有人來收它**（＋兌現點）——導演點名本輪重量一次 | **4，值未變，本輪一份都沒動** | 逐檔覆核：①`story/物件/海格的雨傘.md`（`## 為什麼存在` 的兌現點段「兌現點＝`大綱/arc04.md` 他被帶走那一場……**這本書對這把傘只回答一件事：沒有人來收它**」＋`## 不得寫成什麼` 三條約束）②`大綱/arc02.md:93`（埋：「`大綱/arc04.md` 有一行回答『沒有人來收它』，**傘裡是什麼本書不答**」）③`大綱/arc04.md:108`（推：「兌現的是『沒有人來收它』；**傘裡是什麼本書不答**」）④`大綱/arc04.md:76` 敘述線（「他拎著那把粉紅色的雨傘走出去。沒有一個人看它一眼。」）。**四份逐字仍是同一個值**（②③的後半句甚至逐字相同）。`git diff` 不含 `物件/海格的雨傘.md` 與 `arc04.md`；`arc02.md` 本輪只改了伏筆改名那一行。**無一是投影。** **導演要的那一格答案**：**本輪沒跑 `beat-sheet`，所以 `foreshadow-project` 的輸入仍然不存在**——實跑 `foreshadow-project --book <書>` 印「## 全書伏筆帳（**掃 1 個 arc**；spine 讀自 `_順序.md`）／（無符合條件的伏筆）／**小計：0 條**」，與 S2、S3 完全相同的讀數；`beat-lint` 印「**0 支 arc／0 幕**」。**S2 第 6、7 列與 S3 第 5 列講的同一把傘，到 S4 仍然沒有任何工具讀得到那四份，而它已經連續三個 session 是同一個狀態。** 一個新讀數：`foreshadow-project` 的「揭示層級」節本輪掃到 **4 處**（S3 是 3 處，本輪 `物件/金妮.md` 加一），**而傘不在那 4 處裡**（它是 `揭示層級: 公開`）——所以連那一節也碰不到它 |

**本 session 的形狀**：

1. **落點 N 的 5 列（6、7、9、11、14）全部是「同一個值散在多層、而唯一能收攏它們的工具還沒有輸入」。** 那個工具是 `fact-project --for-beat`（第 6、7 列）與 `foreshadow-project`（第 11、14 列），**兩支的輸入都是幕綱，而幕綱是 0 幕**。這與 S2 的診斷（「大綱層沒有排定的投影」）是同一句話的第三次覆述，差別只在：**S3、S4 兩輪把新的排除線／禁令都乖乖送進了 `story/物件/<名>.md` 的規則表（S2 的 14 支 → S3 的 17 支 → S4 的 19 支，約束 28 → 34 → 38 條），而那個新家至今一次都沒有被任何投影讀出來過。** 機制已經蓋好、住進去了，**而通往它的那條路要等 `beat-sheet`**。
2. **第一次出現「有意識地計價的重複」。** 第 7 列的 `核心.md:13` 不是失手：作者明知它是第 N 份，仍然裁定保留，理由逐字是「本檔只走 `--for-beat` 載入，而這本書 `beat-lint` 現在是 0 支 arc／0 幕——**指過去等於指向一條今天還不通的路**」，並給了拆除條件。**這在這本帳上是新的一格**：前三輪的重複要嘛是沒發現、要嘛是作者當場砍掉；本輪第一次是「知道它是重複、算過代價、留著、寫下解除條件」。**下一輪的觀測者請追蹤這條鷹架有沒有真的被拆**——沒有拆除記錄的鷹架就是第 N 份複本。
3. **兩筆落點 0 的性質不同，別合併看**。第 10 列是**老病第五次**（書內有容器，只是沒有東西回頭改它，五次的偵測者都是人眼）；第 12 列是**新形狀**——缺陷在系統層（`書本模板/`），而系統層在這本書裡**結構性地沒有家**：受測體唯讀、`decision-lint._target_exists` 不認它當標的。本輪同形共 3 筆，全部只能寄放在裁決流的理由欄或測試側檔案裡。

> **12 支閘門本輪讀數**（觀測者端獨立覆算，本帳只引用機械輸出）：`derived-sync check` exit 0（0 個需處理）／`derived-sync validate` exit 0（14 支 `.ai.md`／44 個 `##` 節／73 個鍵·**0 個是 schema 外的**／裁決 blockquote 0 支命中）／`outline-lint` exit 0（8 支 scoped／連續敘述 54.1%／枚舉外節 0／檔內路徑引用 **72 個·0 個不存在**／arc 引用 74 筆·73 筆指得到）／`object-lint` exit 0（**19 支物件檔／38 條約束／4 個揭示層級**）／`fact-lint` exit 0（同上讀數）／`decision-lint` exit 0（**裁決流 128 列／待裁決 0 列**／標的 36 個·0 個查無）／`world-lint` exit 0（7 支主題／registry ＝幕綱 0 個標記名 ∪ 物件檔 19 支）／`char-lint` **exit 1（17 支源沒有衍生檔，與 S3 相同）**／`summary-lint` exit 0／`readiness-lint` exit 0（狀態表 20 格／就緒清單 6 條·5 條已勾）／`beat-lint` exit 0（**0 支 arc／0 幕**／spine 讀自 `_順序.md`／跳過 1 支骨架）／`style-lint` exit 0（禁用詞命中 0 次／掃了 **0 章正文**）／`ch-lint` exit 0（0 章正文源）。
> **上面 14 列裡，被這 12 支閘門抓出來的是 0 列**——第 10 列（分叉五那兩處相反的舊值）尤其值得看：`derived-sync check`／`summary-lint`／`world-lint` **三支都印綠燈，而且三支都印對了**，因為那兩行在格式上完全合法、它們的源檔本輪根本沒動。這仍然是本帳存在的理由。
> **一格提醒下一輪的觀測者（S5 大概率是 `beat-sheet`）**：本帳有 **4 列（6、7、11、14）在等同一件事**——幕綱一出現，`fact-project --for-beat` 與 `foreshadow-project` 的輸入就同時到位。請在 S5 一次量三件：① 那四份傘（S2 記 0、S3 記 4、S4 記 4·未變）第一次被 `foreshadow-project` 讀到時，**四份是不是還是同一個值**；② `物件/金妮.md` 與 `物件/那個從來不出水的水龍頭.md` 的約束**第一次被 `--for-beat` 印出來**時，第 7 列的鷹架（`核心.md:13`）有沒有照 `裁決流.md:143` 的解除條件拆掉；③ `check_near_miss` 第一次拿到非空輸入時，`三次坦白` 這個新名字與 19 支物件檔名**是不是真的互不為子串**（本輪的 exit 0 是輸入為空時的 0，不是驗過的 0）。

---

## S5（beat-sheet arc01 ＋ beat-test arc01 ＋ 開場清帳）— 本 session 新增 15 列（落點 0：2／落點 1：6／落點 N：7）

> 產物範圍 ＝ `git diff` 在 `消失的密室/` 的 12 支已追蹤檔（新增行）＋ 4 支未追蹤新檔（`設定/角色/{亞瑟,亞瑟.ai,莫莉,莫莉.ai}.md`）。關鍵 blob：`幕綱/arc01.md` `dfe0dd77`（空骨架 → **15 幕**）、`大綱/arc01.md` `2c34620d`、`裁決流.md` `7e4097cf`（128 → **150 列**）、`亞瑟.md` `7ece0721`、`莫莉.md` `9164cdb3`。
> **計數約定沿用 S4：一支檔算一個落點**，同檔多行（承諾區＋八欄＋伏筆表＋設計註）算 **1**。
> **本 session 的形狀是第五種**：S1「沒有欄位可存」／S2「跨檔排定值沒有比對器」／S3「第一次量到機制在被使用」／S4「第一次為讓機制生效而刻意留一份帶拆除日期的重複」；**S5 第一次出現「投影的輸入到位了，而它照到的東西比預期少得多」**——`beat-lint` 從 0 幕變 15 幕、`fact-project --for-beat` 與 `foreshadow-project` 兩支等了三個 session 的工具同時開機（第 11、12 列），**但八支 arc 只拆了一支**，所以三個 session 累積下來的落點 N 只被照亮了其中一格。**「機制通了」與「機制照得到你要的那一格」是兩件事，本輪第一次量得出差別。**

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「回頭劃 `raw/` 的判準」＝只有當那一行**還在主張一件已經不成立的事**才回去劃（＋這次的改因寫法必須與第 64 列不同：那次作廢的是錯的值，這次是「後來拍了」） | **1** | 判準本體唯一落點 `story/參照/裁決流.md:158`，標的 `全書`（`decision-project --target … --active-only` 一律命中 ⇒ 可投影）。執行結果落 `raw/還沒想清楚的.md:33`（`git diff` 該檔 `1 +/-`：原問句加刪除線並註明「當天寫下這一問時確實還沒決定，不是寫錯」）。**判準的反面同時被寫死**（「其餘四個分叉都還真的開著，一個字都不要動」）——這是本帳第一次看到一條規則**連同它的射程上限**一起落檔，而射程上限正是 S3 第 13 列那種「規勸被下一輪擴大解釋」的解藥 |
| **2.**「更正一律 **append**、不就地改；`狀態` 欄是唯一可就地改的一格。理由欄只寫『當下為什麼這樣定』、不寫『接下來打算怎樣』」 | **1** | 唯一落點 `裁決流.md:159`，標的 `全書`。**觸發事件本身有機械痕跡**：受測體在 `:157` 末句寫了「本輪不動，待作者裁」，作者當輪拍板後受測體**就地改掉**那半句；作者裁定改回原文並 append 更正——`:158` 理由欄開頭逐字是「**更正第 157 列**」。⚠️ **`decision-lint` 對整件事全程 exit 0**（本輪讀數：裁決流 150 列／待裁決 1 列／標的 40 個·0 個查無）：它驗七欄、日期、狀態格、射程寫法、標的存在性，**不驗「這一列有沒有被就地改過」**。append-only 這條斷言 15 輪重構之後仍然**無人守**，而本輪它第一次被實際違反、又被人眼抓回來 |
| **3.** 多比扣信那條線（信不拿回／不解釋機制／兌現點是洞穴屋一句話） | **2** | 逐檔：①`大綱/arc01.md:75`（敘述線＋寫法約束括號）＋`:141`（伏筆表）②`幕綱/arc01.md:14`（必須發生）＋`:11`（兩個受害面）＋`:26`（排除線）＋`:61`（幕002 結果欄）＋`:213`（伏筆狀態表）＋`:239`（設計註）。`裁決流.md:161` 記理由（可投影，不計）。**無一是投影。** ⚠️ **第三個落點在物件軸缺席**：`grep -rn "那疊信\|不拿回" story/物件/` **0 命中**——「不准把信拿回來、不准解釋機制」這條排除線**沒有進約束 registry**。本觀測者實跑 `fact-project --for-beat 幕002`：那一條確實印出來了，**但送它的是幕綱承諾區、不是物件檔**（射程因此綁死在 arc01，出了這個 arc 就沒有人送） |
| **4.** 書店那個坩堝**從頭到尾是金妮的**（駁回系統發明的「哈利把簽名書倒進最近那個坩堝」） | **2** | `大綱/arc01.md:111,113`（權威＋畫面邊緣那個動作）／`幕綱/arc01.md:18,147,149,216`（必須發生＋幕010 八欄＋伏筆表）。**無一是投影。** ⚠️ 但這一列的價值不在落點數：**這是本輪唯一一次「系統發明了一個大綱沒有的動作，而它把這件事寫成『請認或否』並落進 `待裁決.md`」**（`S5-*.md` 摩擦欄第 2 條①）。作者答「否，你的前提就錯了……那是你為了填一個不存在的洞而發明的動作」。**填了，但沒有偷偷填**——`待裁決.md` 這個設計上的家，**第一次在「系統自己不確定」這個情境下被正確使用** |
| **5.** 13 幕 → **15 幕**、階段切法 3/2/4/4 → **3/3/4/5**（＋幕號重編對照表） | **1** | 唯一落點 `裁決流.md:168`，理由欄開頭逐字「**更正第 135 列**」，含完整的舊→新對照表與「第 135 列的『幕011』＝現在的幕012、第 133 列的『幕007』＝現在的幕008」這種跨列座標修正。`幕綱/arc01.md` 是**執行結果**、不是第二份宣告（`beat-lint` 實跑：**1 支 arc／15 幕／14 條前因／目的地不存在 0**、exit 0）。⚠️ **`beat-lint` 完全看不見「重編發生過」**：它驗幕號唯一性、跳不跳號段、懸空前因，**不比對任何歷史版本**，所以一次全檔重編在它眼裡與一次首度拆幕長得一模一樣 |
| **6.** 統一母題那一行**禁止以任何形式出現在正文** | **1** | 唯一落點 `幕綱/arc01.md:9`（母題本體）＋`:40`（排除線，同檔）。`裁決流.md:164` 記理由（可投影，不計）。全書 `grep -rn "多欠一筆\|收費的都是想幫他的人" story/` 除該檔外 **0 命中**。**這一列是本 session 的對照組**：同一輪、同一支 skill，走「承諾區排除線」的禁令落點乾淨；第 9 列走「規則＋戲的現場」的就散成五份。⚠️ **但它與 S3 第 10 列、S4 第 3 列共享同一個缺陷——沒有機械核對者**：`beat-metrics` 量得出母題重複率（9.9‰ → **4.5‰**），量不出「這一行有沒有進正文」，而正文要到 S6 才存在 |
| **7.**「結構／幕綱」由 `空白` 升 `草擬`，＋升 `就緒` 的**三條件**（八 arc 全有幕綱／全跑過 `beat-test`／`待裁決.md` 無指向幕綱的列） | **1** | 狀態落 `story/參照/就緒.md:15`（`readiness-lint` exit 0：狀態表 20 格·**0 格不是五 token 之一**／就緒清單 6 條·5 條已勾）；三條件落 `裁決流.md:164`，標的 `參照/就緒.md`（可投影）。**這是 schema 分工正確執行的樣本**：就緒表只准五個 token，「為什麼」的家在裁決流——與 `裁決流.md:114`（S3 那次 `expand`）同形，**同一個形狀第二次自動走對，沒有人提醒** |
| **8.**「角色建檔門檻」＝**「不寫下來的話，預設寫法會不會剛好對」**（威農／達力不建、亞瑟建、莫莉建） | **2** | ①`裁決流.md:165`（門檻第一版「有沒有東西會被寫錯」）＋`:178`（**操作化版本**，理由欄自陳 AI「問對了但問得虛」）——同檔算 1、可投影 ②`設定/角色/莫莉.md:7`：「**因為不寫下來的話，預設寫法剛好是反的。**（威農／達力不建檔的理由正好相反……）」——**判準本身被複述進了一支角色源檔**。⚠️ **兩份射程不同而沒有東西比對**：`:178` 是全書方法論、`莫莉.md:7` 是單案辯護；`char-lint` 對 `莫莉.md` 只驗「有沒有衍生檔」（本輪 exit 1「17 支源沒有衍生檔」——源 22→**24**、衍生 5→**7**，缺口數與 S3／S4 完全相同），**不讀內容**。附一格正面讀數：**`near-miss` 是建檔前該由 AI 自己跑的，本輪它跑了**（實測 `莫莉`／`亞瑟` 與 21 個 registry 名互不為子串），這是 S4 第 11 列那顆雷的正確處置第一次自發出現 |
| **9.**「那封咆哮信罵的是被麻瓜看見、不是差點死掉」＝**輕重錯置寫在懲罰上、一個字不吐槽** | **5** | 逐檔：`大綱/arc01.md:125`（麥教授那一場的括號）／`大綱/arc02.md:30`（咆哮信敘述線，baseline·本輪未動）／`幕綱/arc01.md:30`（排除線）／`設定/角色/莫莉.md:11`（規格第一格）／`設定/角色/莫莉.ai.md:12`（衍生的「想要」欄；`derived-sync check` exit 0「0 個需處理」，依 S1 判法可扣掉 ⇒ 保守讀 **4**）。**無一是投影。** ⚠️ **這條規則的權威本體今天不存在**：作者把它交給 `worldbuild` 去建 `設定/世界觀/監管與曝光.md`（第 10 列），**所以現在是「五份執行、零份規則」**。與 S3 第 1／2／7／12 列同軸（禁令散在多層），但更極端——那幾列至少有一個規則表當家 |
| **10.** 保密法三條規則要有一支自己的世界觀檔（**不准併進 `權力與程序.md`**），＋「不准吐槽」的寫法約束 | **0** | 落點是 `story/參照/待裁決.md:19` 一列（0 列 → **1 列**；`decision-lint` exit 0：待裁決 1 列／標的 40 個·0 個查無）＋`裁決流.md:166` 的理由欄。**判 0 不判 1 的三條機械理由**：①**那支檔不存在**——`story/設定/世界觀/` 仍是 7 支，`world-lint` exit 0「**7 支 `<主題>.ai.md`**」，與 S3／S4 完全相同；②作者指示待裁決那一列的標的寫 `設定/世界觀/監管與曝光.md`，**受測體實測 `decision-lint` 當場報「標的在書內找不到」**，故照 07-31「不建 `地理.md`」的先例改寫 `全書`——**於是這個交棒在帳上看不出來是交棒**（`全書` 是五支 skill 每次都撈得到的過度廣播標的）；③三條規則的**內容**現在只活在 `裁決流.md:166` 的理由欄與 `待裁決.md:19` 的發現欄，**兩處都是自由散文，沒有任何 lint 逐欄取值**。**同形第三次**（S4 第 12 列記過兩筆「缺陷在系統層、沒有家」；本輪這一筆是新變種——**內容在書內，但它的家還沒出生**） |
| **11.** **`fact-project --for-beat` 的輸入第一次到位**（S4 第 6／7 列等的就是這一格） | **9** | **本觀測者實跑 `fact-project --book <書> --for-beat 幕010`：`### 金妮` 節第一次印出「約束〔金妮異常不給特寫〕……←來源 全書· `物件/金妮.md`」——三個 session 以來第一次有東西從 `story/物件/<名>.md` 被投影出去。** ⚠️ **而這條規則的落點同一輪從 8 漲到 9**：`00-摘要.md:58`／`大綱/arc01.md:63,142`／`arc03.md`／`arc04.md`／`arc05.md`／`arc07.md:32`／`設定/角色/金妮/核心.md:13`／`物件/金妮.md:9,44`／**本輪新增的 `幕綱/arc01.md:34`**（arc01 排除線，指向 `物件/金妮.md`）。**S4 第 7 列那個帶拆除日期的鷹架（`核心.md:13`）本輪沒有拆，而且不拆是對的**——`裁決流.md:143` 的解除條件是「**八支 arc 幕綱齊了**、`--for-beat` 真的印得出這一條之後」，現在是 1/8：**條件的第二半今天成立了，第一半還要等七個 arc。請 S6–S12 每輪追蹤這一格——沒有拆除記錄的鷹架就是第 N 份複本。** 對照：`物件/那個從來不出水的水龍頭.md` 的兩條約束**今天仍然送不到任何人手上**（它的埋設點在 arc03／arc05，兩支都未拆幕），S4 第 6 列的判讀原封不動成立 |
| **12.** **`foreshadow-project` 的輸入第一次到位**（S4 第 14 列的傘） | **4，值未變，本輪一份都沒動** | `foreshadow-project` 實跑：`## 全書伏筆帳（掃 1 個 arc）` **6 條**（多比的警告／魯休斯的動機／日記／金妮／榮恩的魔杖／那輛車，全部 `[未收·已宣告]`），**小計 6 條、0 條可疑點**——S2／S3／S4 連續三輪的「小計：0 條」終於破了。⚠️ **而傘不在那 6 條裡**：它埋在 arc02，而 `beat-lint` 印「**1 支 arc**」。四份逐字覆核仍是同一個值：`物件/海格的雨傘.md:17,19`／`大綱/arc02.md:93`／`大綱/arc04.md:108`／`大綱/arc04.md:76` 敘述線（`git diff` 不含這三支檔）。`foreshadow-project` 的「揭示層級」節本輪掃到 **4 處**（與 S4 同），**傘仍不在裡面**（它是 `揭示層級: 公開`）。**回答 S4 觀測者第①問：儀器開機了，這把傘仍在射程外，四份仍是同一個值，而它已經連續四個 session 沒有任何工具讀得到。** 本輪新增的 `幕綱/arc01.md:31`（arc01 不得指出那把傘）是**另一個射程的另一個值**，不併入這 4 |
| **13.** **幕號字面不得出現在 `幕綱/` 以外的檔**（事件名是穩定座標，幕號不是） | **1** | 唯一落點 `裁決流.md:177`，標的 `設定/角色/榮恩.md`。執行結果：三處全部改成事件名（`榮恩.md:7`「撞樹那一下裂的」／`亞瑟.ai.md:35,40`「被問你覺得呢的那個晚上」）。**本觀測者獨立覆算**：`grep -rn "幕[0-9][0-9][0-9]" --include=*.md .` 排除 `story/幕綱/` 與 `裁決流.md` 之後 **僅 1 筆命中，且是 `待裁決.md:22` 的欄位說明樣板**（「例：`beat-test 測試4（幕017–020）`」）⇒ **外洩實質 0 筆**。⚠️ **而這個 0 是人選 pattern 選出來的，同 S4 第 3 列**——`beat-lint` 沒有這個投影，作者的定性逐字：「**沒有檢查器的保證只是口頭承諾**」。**更硬的一格**：三處外洩裡**兩處是系統自己上一輪寫的**（`亞瑟.ai.md:35,40`），而且拆幕之後**已經指錯幕**（寫「哈利在幕005 摸到的溫度」，但重編後亞瑟根本不在幕005）——**一次全檔重編在沒有任何工具看著的情況下通過了** |
| **14.** `## 設計註（下游不抄）` 那一節（14 條前因理由＋α 變形推導＋佈線論證搬進去並**留在檔內**） | **2** | ①`幕綱/arc01.md:224–247`（本書第一次用到這一節；`beat-lint` exit 0，「設計註沒有搬移目的地」這一項**不報**，因為它有目的地——目的地就是它自己）②`裁決流.md:176`，理由欄開頭「**更正第 147 列**：該列拿『肥大哨兵沒有觸發』當理由，**是接錯了線**」。**判 2 不判 1 是刻意的**：`幕綱.schema.md:52` 寫著「暫存區·**落檔時必須搬走**」而這一節留下了，所以檔內那一份是**一次記名的破例**、裁決流那一份是**破例的授權書**，兩份都必須在。⚠️ **落點 0 的那一半**：作者的形狀判定（「一個沒有搬移觸發條件、又沒有任何東西會反對它變成常設區的暫存區，形狀等同被廢掉的那五支 rollup」）**射程是系統層**（`結構定義/幕綱.schema.md`），受測體唯讀、寫不進去，`decision-lint._target_exists` 也不認它當標的 ⇒ **只能寄放在裁決流的理由欄裡**。**這是 S4 第 12 列那個結構性落點 0 的第四個實例** |
| **15.**「**刪掉任何一段內容之前，先報它是唯一落點還是第 N 份備份**」＋「下游改上游源檔的正常路線是 `待裁決`→拍板→改源檔」 | **0** | 落點是 `裁決流.md:179` 一列通則，標的 `全書`。**判 0 不判 1 的機械理由：沒有任何工具在做這件事，而現成的工具查得出來。** 受測體刪 `大綱/arc01.md` 那三個外觀形容詞時只報「我把射程讀寬了」，作者自己 `grep` 找到 2 處、受測體補跑找到 **3 處**（`物件/日記.md:9`／`大綱/arc06.md:28`／`大綱/arc08.md:26`）⇒ 那次刪的是第四份備份、完全安全，**但安全是查出來的、不是猜出來的**。`fact-refs --entity <名>` 正是為這件事存在的工具（`sync`／`revise` 的 SKILL.md 都指名它），而 `beat-sheet` 的刪除動作**沒有任何地方掛它**。這條通則現在的執行者是「AI 記得要跑」。**與第 2 列同型**（規矩落了檔、沒有任何東西在守），**而這是本帳第三次記到「處置是規勸」**（S3 第 13 列、S4 第 10 列、本列） |

**本 session 的形狀**：

1. **三個 session 的等待結束了，而結果是半格。** 第 11、12 列量的是同一件事：`fact-project --for-beat` 與 `foreshadow-project` 的輸入同時從空變成非空。**第一支真的送到了東西**（`物件/金妮.md` 的約束在幕010 印出來），**第二支照到的 6 條伏筆裡沒有任何一條是這本帳記過的落點 N**。S2 第 6、7 列與 S3 第 5 列、S4 第 14 列講的那把傘，**連續四個 session 沒有任何工具讀得到**——原因從「工具沒有輸入」變成了「工具的輸入還沒排到它」，**而這兩件事在覆蓋率行上長得一模一樣**（`beat-lint`：1 支 arc；`foreshadow-project`：掃 1 個 arc）。**這是 E2 的一個新變種**：覆蓋率行誠實地報了它掃了幾筆，而讀的人要自己算出「那幾筆裡有沒有我要的那一筆」。
2. **兩筆落點 0 ＋第 14 列的半格，三種不同的「沒有家」。** 第 10 列是**家還沒出生**（`監管與曝光.md` 交給了 `worldbuild`，而標的只能寫 `全書`，交棒在帳上看不出來是交棒）；第 15 列是**規矩落了檔、沒有工具在守**；第 14 列後半是**家在系統層**（`結構定義/幕綱.schema.md` 的暫存區缺搬移觸發條件），受測體唯讀、`decision-lint._target_exists` 也不認它當標的，結構性寫不進去。**S4 第 12 列點出的那個形狀，本輪一輪就出現三個新實例——請不要因為它們讀起來像三件事就當成三個獨立缺口，也不要因為只有兩列標了 `0` 就漏算第 14 列那半格。**
3. **本輪第一次量到「系統自己不確定，然後正確地舉手」**（第 4 列）。`beat-sheet` 發明了一個大綱沒有的動作來補前因，**但它把這件事寫成「請認或否」並落進 `待裁決.md`**，作者答否。這與 S3 第 2 點不同：那一輪守門的是**作者**（逐條查落點推翻提案），這一輪是**系統**自己標記了不確定性。**這個動作目前沒有任何 schema 在要求、也沒有任何閘門在獎勵它**——同 S3 記過的「指過去而不重述」，它這一輪出現了 1 次，下一輪可能 0 次。
4. **append-only 第一次被實際違反**（第 2 列）。抓到它的是作者的眼睛，`decision-lint` 全程 exit 0。**本帳前四個 session 都只能記它「沒有守衛」，本輪第一次記到它「被違反了」**——而違反者是系統、更正者是人、更正的形式是 append，三件事都留在檔上可查。

> **12 支閘門本輪讀數**（觀測者端獨立覆算，本帳只引用機械輸出）：`beat-lint` exit 0（**1 支 arc／15 幕／14 條前因／6 個伏筆標記（6 名）／6 列狀態表／1 個承諾區／19 條排除線／19 支物件檔**；檔內書內路徑 11 筆·**目的地不存在 0**；spine 讀自 `_順序.md`；**1/1 支 arc 有 `beat-test` 紀錄**·格式不合 0 支；跳過 0 支骨架；提示 2/6 個伏筆名無物件檔·合法不計）／`outline-lint` exit 0（8 支 scoped／連續敘述 56.0%／枚舉外節 0／檔內路徑引用 **72 個·0 個不存在**／arc 引用 77 筆·76 筆指得到）／`ch-lint` exit 0（0 章正文源／幕綱 registry **15 幕**／15 幕還沒寫成正文）／`decision-lint` exit 0（**裁決流 150 列／待裁決 1 列**／標的 40 個·0 個查無）／`object-lint` exit 0（**19 支物件檔／38 條約束／4 個揭示層級**／核對 6 個引用名）／`fact-lint` exit 0（同上讀數）／`char-lint` **exit 1（17 支源沒有衍生檔）**（源 22→**24**、衍生 5→**7**，缺口數與 S3／S4 相同；四節 116 個 slot·**0 個是佔位**；幕綱角色欄 15 個·括號外 49 個 token·6 個 registry 查無·**0 個出現 ≥3 次**）／`style-lint` exit 0（掃了 **0 章正文**／禁用詞命中 0 次／5 個字面 entry）／`summary-lint` exit 0（6 個語意欄·0 個是空的／基調兩方比對相符／掃了 0 個幕號·arc 引用·**0 個懸空**·registry ＝幕綱 15 幕 ∪ 1 支 arc 檔）／`world-lint` exit 0（**7 支主題**／registry ＝幕綱 6 個標記名 ∪ 物件檔 19 支）／`readiness-lint` exit 0（狀態表 20 格／就緒清單 6 條·5 條已勾）／`derived-sync check` exit 0（**16 個衍生檔·0 個需處理**；成長哨兵掃描範圍 幕綱 1／設定層源 32／設定層衍生 16／大綱 9／參照 2／行長 68 支／搬移目的地 3 個·0 個不存在；**（無）**）／`derived-sync validate` exit 0（16 支 `.ai.md`／52 個 `##` 節／85 個鍵·**0 個是 schema 外的**／裁決 blockquote 0 支命中）。
> **上面 15 列裡，被這 12 支閘門抓出來的是 0 列。** 三筆落點 0 尤其值得看：第 10 列的 `監管與曝光.md` 不存在，而 `world-lint` 印「7 支主題」並 exit 0——**它印對了**，因為它數的是資料夾裡有幾支，不是「作者已經拍板要建幾支」。這仍然是本帳存在的理由。
> **一格提醒下一輪的觀測者（S6 大概率是 `write`）**：本帳有 **3 列（6、9、11）在等同一件事**——**正文一出現，`ch-lint`／`style-lint`／`write-test` 三支「掃了 0 章正文」的閘門就同時有輸入了**。請在 S6 一次量三件：① `style-lint` 的「5 個字面 entry、禁用詞命中 0 次」第一次掃到真正文時是不是還是 0；② 第 9 列那條「罵的是被麻瓜看見」現在是**五份執行、零份規則**（權威檔還沒建），寫 arc02 咆哮信那一場時**系統會從哪一份取值**；③ 第 6 列的統一母題禁令沒有任何機械核對者，而它的違反只會出現在正文裡——**`write` 是它第一次有機會被違反的地方**。
> **一格交給導演，不屬本帳但同源**：本輪 purity 抓到的 `佩妮`（見 `S5-*.md` purity 節）在本帳上的形狀是**落點 0 的一個新變種**——系統從自己的記憶裡取了一個 canon 專名寫進 `幕綱/arc01.md:49`，**而「這個名字是誰給的」在書內沒有任何容器**。`char-lint` 第 8 項數的是「registry 查無的 token」（本輪 6 個·0 個出現 ≥3 次·依門檻不報），它答得出「這個名字沒有檔」，答不出「**這個名字沒有來源**」。

---

## S6（write arc01 ＋ write-test ch0001–0003 ＋ revise ch0001–0003）— 本 session 新增 15 列（落點 0：3／落點 1：8／落點 N：4）

```
需求機制帳：本 session 新增 15 列（落點 0：3／落點 1：8／落點 N：4）
```

> 產物範圍 ＝ `git diff` 在 `消失的密室/` 的 **6 支已追蹤檔·32 行新增**＋**7 支未追蹤新檔**全文。關鍵 blob：`chapters/ch0001.md` `59f420e6`、`ch0002.md` `2c11f34e`、`ch0003.md` `f295a98b`、`ch0001.ai.md` `118a03f0`、`ch0002.ai.md` `6892688b`、`ch0003.ai.md` `b04bd1fa`、`物件/嘿美.md` `41fcf990`、`幕綱/arc01.md` `9ed71284`、`裁決流.md` `c40970c4`（150 → **157 列**）、`待裁決.md` `67735ddc`（1 → **3 列**）、`物件/全書.md` `d9fb53dc`、`物件/石內卜.md` `c37886822`、`大綱/arc01.md` `ab65d83c`。HEAD ＝ `d07c82f`。
> **計數約定沿用 S4／S5：一支檔算一個落點**；投影（`*-lint --emit`／`fact-project`／`readiness`／`structure-project`）不計。
> ⚠️ **本節有一個前五輪沒有過的前提**：`git diff` 的新增行**不全是受測體寫的**——`幕綱/arc01.md:82` 整行、`chapters/ch0003.md:63` 後半句、`ch0003.ai.md:2` 的 `generated-from` 由**第 8 輪神諭**寫入（見 `S6-*.md` 防火牆節）。本帳逐列已標。
> **本 session 的形狀是第六種**。S1「沒有欄位可存」／S2「跨檔排定值沒有比對器」／S3「第一次量到機制在被使用」／S4「第一次為讓機制生效而刻意留一份帶拆除日期的重複」／S5「投影的輸入到位了，而它照到的東西比預期少得多」；**S6 第一次出現「需求的家存在、規格也對，而那個家開不了門」**——第 6 列的排除線該住 `story/物件/多比.md`，那支檔**被 `fact-lint` 擋著建不出來**（檔名與伏筆〔多比的警告〕互為子字串），於是一條作者講了兩輪的全書級寫法約束**只能落到 0**。這與 S5 第 10 列（家還沒出生）不同：那一格是還沒做，這一格是**做不了**。

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「哈利的貓頭鷹不能變成他的傾訴對象」——牠只接受指令與沉默 | **1** | 唯一落點 `story/物件/嘿美.md:20`（`不得成為傾訴對象｜…｜全書｜—`）。本觀測者實跑 `fact-project --book <書> --for-beat 幕001`：`### 嘿美` 節印出「約束〔不得成為傾訴對象〕……←來源 全書· `物件/嘿美.md`」——**這是 `write` 而非 `character`／`organize` 建的第 20 支物件檔，也是本帳第一次看到一支物件檔在它被建立的同一個 session 內就被投影出去**。`object-lint` exit 0（**20 支物件檔／41 條約束／4 個揭示層級**）。⚠️ **落點對、但它進不了 `char-lint` 的 registry**：見第 13 列 |
| **2.**「未成年施法的偵測**按地點不按施法者**」 | **1（搬家：0 → 1，本帳第一次記錄「同一個需求換了家」）** | **搬家前**：`story/參照/待裁決.md` 一列，來源欄自陳「作者拍板」。**判它當時是 0 的兩個機械理由**：①那支檔的合約逐字是「這裡住**還沒裁決的**」（`待裁決.md` 檔頭），一列標明已裁完的東西在契約上不該在那裡；②**`write` 不載入 `待裁決.md`**——規則會被違反的時刻（寫 arc02 的懲罰）沒有任何讀者。**搬家後**：`story/物件/全書.md:27`（`偵測按地點｜把未成年施法的偵測寫成認得出施法者是誰｜全書｜—`），理由 append 進 `裁決流.md:185`（標的 `全書`·可投影·不計）。**搬家後實跑驗證**：`fact-project --for-beat 幕001` 的 `### 全書方針（射程＝全書·物件/全書.md）` 節印出這一條，**每一幕無條件印、0 條也印**。`decision-lint` exit 0（裁決流 157 列／待裁決 3 列／標的 40 個·**0 個查無**）。⚠️ **搬家本身沒有任何工具偵測得到**：`decision-lint` 驗七欄與標的存在性，`待裁決.md` 少一列與多一列在它眼裡一樣乾淨——**這一列是靠作者讀出「這一列在打自己」才搬的**，而它已經在錯的家躺了半個 session |
| **3.**「任何 `*-test` 的檔頭讀數行不在『不代改』的射程內」（判準：**那一行有沒有判斷成分**） | **2** | ①`story/參照/裁決流.md:183`，標的 `全書`（可投影，但它是規則本體所在的源檔 ⇒ 計 1）②**`結構定義/章節.schema.md:103` 早就寫死了同一件事**：`write-test: 2026-07-28·0高3中3低　# 選填·測試執行紀錄（缺席合法）；**由 write-test 收尾寫**`。**兩份宣告、無一是投影、射程不同（一份書級一份跨書）、沒有任何東西比對它們**——而這正是它連錯兩輪的機制：schema 的那句話**在系統層**，受測體唯讀且不會被它擋，於是 S5 `beat-test` 與 S6 `write-test` 各漏一次。執行結果落 `chapters/ch000{1,2,3}.ai.md:10`（三支同值、**各是自己那一章的讀數，不算重複**），由 `revise` 補落。**閘門讀數在同一輪內翻面**：`ch-lint` 現在印「**3/3 支章衍生有 `write-test` 紀錄**」，`write-test` 交稿當下印的是「0/3」。⚠️ **那個 0 到現在仍然無法與「沒測」區分**——`ch-lint`／`beat-lint` 的覆蓋率行是同一個形狀，`readiness` 第 3 節會照著它判本層沒審過 |
| **4.**「四條查詢紀律」（`--target` 用**相對 `story/` 的路徑**／「標的 1 個」＝前綴寫錯的指紋／講機制必須帶檔名＋行號／0 命中先驗選擇器） | **1** | 唯一落點 `裁決流.md:184`，標的 `全書`。**這一列只能住 `全書`，而理由是機械的**：`tools/decision_projection/src/decision_projection/parse.py:217-223` 的 `target_matches()` 是分段雙向前綴相符，前綴寫錯時**唯一還印得出來的就是標的＝全書的列**——它的功能是從故障內部把故障講出來。作者實跑三組（錯前綴→命中 33·標的 1 個／對前綴→45·2 個／亂打→33·1 個）。⚠️ **同一條規則只守了一半，而沒守的那一半是靜默的**：`decision-lint` 驗**寫入側**的標的存在性（本輪 40 個·0 個查無、exit 0），`decision-project` 在**查詢側**沒有任何對應驗證。**指紋一直印在螢幕上**（coverage 行的「標的 N 個」），只是沒有人讀它。**本帳第四次記到「處置是規勸」**（S3 第 13 列、S4 第 10 列、S5 第 15 列、本列） |
| **5.**「石內卜的敵意不得鋪開」——精準的一句就是上限 | **1** | 唯一落點 `story/物件/石內卜.md:22`（`敵意不得鋪開｜他的敵意鋪成一段、被加上解釋、或讓他享受它被聽見……｜全書｜—`），`git diff` 該檔 `1 +`。`object-lint`／`fact-lint` exit 0。**這一列是本輪的正面對照組**：同一輪、同一批作者裁決，走「既有物件檔加一列約束」的落點乾淨、射程明確、投影送得到；第 6 列走同一條路卻撞牆，差別只在**那支檔開不開得出來** |
| **6.**「**枕頭套全書只留在多比身上**」（小精靈的衣物是結局那隻髒襪子的唯一機關；哈利自己用一個枕頭套裝東西＝第三章就把符號用掉） | **0** | **全書 `grep -rn "枕頭套" story/` 只有三處，沒有一處是規則**：`大綱/arc01.md:75`（敘述線）／`幕綱/arc01.md:60`（衝突欄）／`chapters/ch0002.md:7,59`（正文）。`裁決流.md` 0 命中、`待裁決.md` 0 命中、`story/物件/` 0 命中。**它的家在規格上就是 `story/物件/多比.md`，而那支檔建不出來**——`裁決流.md:182` 逐字記著上一次嘗試：「被 `fact-lint` 當場擋下並使 `fact-project --for-beat 幕002` 整個 exit 非零……閘門判定〔多比〕與〔多比的警告〕是同一件事的兩個名字」。本輪 `story/物件/` 20 支，**無 `多比.md`**。⚠️ **正文目前合規是因為 `revise` 這一輪剛好取了甲案**（「塞進口袋」），**不是因為有任何東西在守**——arc02 之後誰再寫一個枕頭套，沒有任何閘門、投影或載入路徑會提醒他。作者在 R7、R8 各講了一次（`S6-*.md` 第 7／8 輪），**兩輪都沒落到書內** |
| **7.**「**ch0004 起，那口行李箱是活的**」——不准補一句「繩子綁得很牢」去安撫讀者，也不准在洞穴屋落地時解釋它怎麼還在 | **0** | **`grep -rn "行李箱" 消失的密室/story/` 只命中 `幕綱/arc01.md:83` 的結果欄**（「行李箱從半空晃著」＝事件，不是約束）；`裁決流.md`／`待裁決.md`／`story/物件/` 全 0。作者逐字說的是「**記進第 4 題交給 S7**」，於是這條約束**現在唯一的載體是 `情境測試/消失的密室/S6-*.md` 與神諭 transcript ——兩者都在書資料夾外，系統看不到**。這是本帳的**新變種**：不是沒有家（第 6 列）、不是家還沒出生（S5 第 10 列），是**這條需求的射程是「下一個 session」，而書內沒有任何容器裝「跨 session 的寫作約束」**。`story/物件/行李箱.md` 是現成的正解（射程可寫 `arc04–` 或 `全書`），沒有人開它。**若 S7 沒有人記得翻 `S6-*.md`，這一條就不存在** |
| **8.**「**梅森那把尺要收成兩個條件**」（① 下游沒有任何一格會因為這個名字寫錯 ② **名字本身不承載東西**——不暗示血統、不押韻、不會被回頭引用） | **0** | `grep -rn "梅森" 消失的密室/story/` **0 命中**（`梅森` 只在正文 `ch0001.md:49`／`ch0002.md:93,99,101,115,129,141,147`）。這條判準是 S5 第 8 列那把尺（`裁決流.md:165`／`:178`「不寫下來的話預設寫法會不會剛好對」）的**第二個條件**，作者 R3 逐字補完並說「**這個補完的判準下一輪不必再攤給我看**」——**也就是他明確要求系統記住它，而系統一個字都沒寫下來**。⚠️ **這是本帳最乾淨的一筆「作者答了但根本沒落檔」**（判定規則落點 0 的第三條症狀），而且它有加重情節：**判準的第一版有家**（`裁決流.md:165`、`:178`），**補完的第二版沒有**，於是那支檔上現在躺著一把**已知不完整**的尺，而沒有任何東西標示它不完整 |
| **9.**「魔法部寄出的**全書第一封信不簽名**」（後面幾封才開始有簽名——「真正嚇人的不是怪物，是簽名」） | **1** | 唯一落點 `裁決流.md:180`，標的 `全書`。執行結果 `chapters/ch0002.md:161`（那封「本部……」無落款）。⚠️ **這一列的守衛是 0**：它是一條**跨 arc 的遞進安排**（第 1 封不簽 → arc03 以後簽），而書內沒有任何機制數「已經寄出第幾封信」。`foreshadow-project` 不收它（不是伏筆標記）、`fact-project` 不收它（不是 delta 也不是約束）。**同 S5 第 6 列的統一母題**：規則落了檔，違反只會出現在正文裡，而正文一次只看一章 |
| **10.**「ch0002 那兩處『張開嘴』**不得以重複用詞為由改掉任何一句**」（同一個動作、同一個下場，兩次的施術者都是多比） | **1** | 唯一落點 `裁決流.md:182`，標的 `幕綱/arc01.md`。**這一列的價值在理由欄，它逐字記下了一次失敗的落點嘗試**：AI 先想把它記成 `story/物件/多比.md` 的排除線 → `fact-lint` 當場擋下、`fact-project --for-beat 幕002` exit 非零 → 檔已刪除、投影已復原 → 退而指向承載幕002／幕003 的幕綱檔。**理由欄還寫下了退而求其次的原因**：「標的欄只吃 `story/` 底下的路徑，**構不到**〔正文〕」——約束的真正標的是一支**已成文的正文檔**（`decision-project --target chapters/ch0002.md` 撈得到），而 `_target_exists` 不認 `chapters/`。⚠️ **與第 6 列是同一顆雷的兩次引爆**，兩次都指向同一個未解的 ID 衝突 |
| **11.**「**角色欄 ＝ 站在該幕『時空』欄那個場所裡的實體 ∩（設定層檔名 ∪ 物件檔名）**」——是人、是鳥、是會自己開走的車都一樣 | **1** | 唯一落點 `裁決流.md:181`（標的 `幕綱/arc01.md`，理由欄開頭逐字「**更正第 179 列**」）。執行結果落 `幕綱/arc01.md` **九幕角色欄**（`git diff` 可見 `:46`／`:57`／`:79`／`:156`／`:167`／`:178`／`:189` 等，新增 `嘿美`×5·`那輛車`×6·`日記`·`海格的雨傘`·`榮恩的魔杖`），**執行結果不算第二份宣告**。效果實測：`fact-project --for-beat 幕001` 現在印得出 `### 嘿美` 節，補之前一條都不印。⚠️ **代價是兩支閘門對同一個欄位的型別假設打架，而沒有任何東西在守這兩個假設要一致**：`fact_projection/beats.py:45-73` 的 `_entity_vocabulary()` 認為角色欄裝**實體**（設定層檔名 ∪ 物件檔名），`derived_sync/char_lint.py` 第 8 項拿它比對**角色** registry——本輪 `char-lint` **exit 1**，第二個問題逐字是「**2 個角色欄 token 在角色 registry 查無且出現 ≥3 次：嘿美×7、那輛車×6**」（括號外 67 個 token·24 個查無）。作者事前認了這一格，**但他預期的是提示、實際拿到的是計入問題數的紅燈** |
| **12.**「嘿美是**母的**」 | **2** | ①`story/物件/嘿美.md:8`「哈利的貓頭鷹，**母的**。」②`chapters/ch0001.ai.md` 錨〔他的貓頭鷹〕原文是「嘿美，**母的**；整個暑假……」（`write` 於 `00:36:06Z` 寫入）。**無一是投影。** 作者 R5 裁「**認，刪『母的』**」，`revise` 於 `01:06:45Z` 把 ② 改成「嘿美；整個暑假……」——**而 ① 原封不動留到現在**。⚠️ **這是本帳第一次逮到「刪除只傳到 N 份裡的一份」**：兩處都是**源**（物件檔不重生、錨由正文重抽），中間**沒有任何 hash、投影或 lint 連著它們**，`derived-sync check` exit 0「19 個衍生檔·0 個需處理」、`object-lint`／`fact-lint` 全綠。**判定不是「作者的裁決沒執行」**（那條裁決的標的確實只寫了 `ch0001.ai.md`），**是「一個值住在兩個家，而動其中一個沒有任何回音」**——落點 N 的典型症狀，第一次在本書上以「不一致」的形式現形而非以「冗餘」的形式 |
| **13.**「貓頭鷹**定名嘿美**」（作者 R3 當場拍的：「腦內檔與 raw 全掃過，寵物名沒有落點，這是我當場拍的」） | **3** | ①`story/物件/嘿美.md`（**檔名即 ID**）②`story/幕綱/arc01.md` 九幕角色欄共 **7 個 token** ③`chapters/ch0001.ai.md:18` 錨〔他的貓頭鷹〕。**無一是投影**（正文 `ch0001.md:65`／`ch0003.md:73` 是渲染，不計）。⚠️ **三份分別被三支不同的閘門看，而三支的判斷互相矛盾**：`object-lint` 說它合法（20 支物件檔·0 問題）／`fact-lint` 說它合法（17 筆事實行·6 個引用名核對過）／**`char-lint` 說它 registry 查無並把整支閘門推到 exit 1**。作者的處置逐字是「**不建 `設定/角色/嘿美.md`**，那個提示的文案講的是集合名與別名，對物件檔實體不適用」——**也就是這 3 份是對的，紅燈是錯的**，而沒有任何機制記得住這個豁免。**S5 第 8 列問的「兩份射程不同而沒有東西比對」，本輪升級成「三份射程相同而三支閘門結論不同」** |
| **14.**「姨媽的名字 ＝ **佩妮·德思禮太太**」（S5 purity 候選，本輪下沉） | **2** | ①`story/幕綱/arc01.md:49`（幕001 衝突欄，S5 寫的）＋`:68`（幕003 角色欄，本輪作者拍板「佩妮**進**」）——同檔算 1 ②`chapters/ch0001.ai.md:16` 錨〔姨媽的名字〕：「佩妮·德思禮太太（燙金邀請卡上的七個字；**姓氏「德思禮」同時釘死**）」。**無一是投影**（正文 `ch0001.md:27`／`ch0002.md:137` 是渲染）。**本帳記它不是因為重複，是因為第 ② 份把它變成了不可翻案的東西**：錨進 `fact-project` 之後，`fact-refs --anchor 〔姨媽的名字〕` 會替它守住下游，**而這個名字在書內沒有任何來源**——`raw/` 0、腦內四支 0、HEAD baseline 0（見 `S6-*.md` purity 節）。`char-lint` 第 8 項答得出「這個名字沒有檔」，**答不出「這個名字沒有來源」**，S5 那一格原封不動成立、而且賭注變大了 |
| **15.**「幕綱衝突欄的 `窗格` 要改成 `窗框`」（大綱＋正文＋錨三比一） | **1（⚠️ 由神諭寫入，不是受測體）** | 唯一落點 `story/幕綱/arc01.md:82`。**`git show "HEAD:消失的密室/story/幕綱/arc01.md"` 第 82 行逐字是 `窗格`，而 `write` 本輪一次都沒碰那一行**（神諭 R8 的 `Edit old_string` 逐字等於 HEAD 版）⇒ **這一行進 `git diff` 100% 因為神諭**（`2026-08-01T01:33:44Z`）。⚠️ **本帳記它的理由是機制不是歸屬**：這是**同一個 session 內第二次**「大綱↔幕綱↔正文的動詞字面分岔」（第一次是 `鋸斷` vs 「用繩子鉤住往後拉」，`大綱/arc01.md:85` 本輪已改），**兩次都是人眼抓到的**。`outline-lint` exit 0（8 支 scoped／檔內路徑引用 72 個·0 個不存在）／`beat-lint` exit 0（1 支 arc／15 幕／11 筆檔內路徑·目的地不存在 0）——**兩支都印對了，因為它們守格式不守用詞**。**這一格是 S5 第 13 列「幕號字面外洩」的下沉一層版本**，S5 當時只是假設，本輪給了它第一與第二筆實證命中 |

**本 session 的形狀**：

1. **三筆落點 0，三種都是新的，而且沒有一種是「機制不存在」。** 第 6 列的家**存在且規格正確，但建不出來**（`fact-lint` 的 ID 唯一性把 `物件/多比.md` 鎖死在伏筆〔多比的警告〕後面）；第 7 列的家**存在且開得出來，沒有人開**（射程是「下一個 session」，而書內沒有容器裝跨 session 的寫作約束）；第 8 列**連家都不必找**——那是既有那把尺的第二個條件，直接補在 `裁決流.md:178` 那一列旁邊就好，而系統沒補。**三者共同的機械症狀是同一個**：作者在 elicitation 紀錄裡講了，`git diff` 裡沒有。**請不要把它們合併成「落點 0 三筆」交上去**——第一筆要改工具（或改檔名），第二筆要開一支檔，第三筆只要 append 一行。
2. **本帳第一次記錄「同一個需求換了家」，而搬家這件事本身沒有任何工具看得見**（第 2 列）。`待裁決.md` 從 1 列變 3 列（進 3 出 1），`decision-lint` 對兩個狀態都印「乾淨」。**這一格值得單獨交**：這本帳量的是「一個需求落在幾個家」，而本輪證明了**同一個落點數 1 可以是好的也可以是壞的**——差別在那個家有沒有讀者。搬家前是 1（`待裁決.md`），搬家後也是 1（`物件/全書.md`），**數字沒動，而 `write` 在 arc02 能不能讀到它從「不能」變成「每一幕無條件印」**。**落點數這把尺量不到「射程」，S7 起這本帳的每一列應該多回答一句「誰會在它被違反的那一刻讀到它」。**
3. **落點 N 的四筆裡有兩筆不是冗餘，是不一致**（第 12、13 列）。前四個 session 記的 N 全部是「同一個值抄了 N 份」；本輪第一次出現**N 份互相打架**：`母的` 刪了一份留一份（第 12 列）、`嘿美` 三份被三支閘門判成三種狀態（第 13 列，其中一支把整個 `char-lint` 推到 exit 1）。**這是 Q-R 的一個新形態**：機制重複的代價過去是「維護成本」，本輪第一次變成「**閘門讀數不可信**」。
4. **12 支閘門本輪抓出來的是 1 列，而它抓錯了。** 15 列裡唯一被閘門點名的是第 13 列（`char-lint` 報 `嘿美`×7 registry 查無），**而作者已裁定那是誤報**。三筆落點 0、兩筆不一致、一次搬家、一次神諭寫入，**12 支閘門全部沉默且全部印對**。這仍然是本帳存在的理由，但本輪它第一次同時證明了反面：**閘門唯一開口的那一次，講的是一件已經被裁定為非問題的事**。

> **12 支閘門本輪讀數**（觀測者端獨立覆算，本帳只引用機械輸出）：`beat-lint` exit 0（**1 支 arc／15 幕／14 條前因／6 個伏筆標記（6 名）／6 列狀態表／1 個承諾區／19 條排除線／20 支物件檔**；11 筆檔內書內路徑·**目的地不存在 0**；spine 讀自 `_順序.md`；**1/1 支 arc 有 `beat-test` 紀錄**；跳過 0 支骨架）／`ch-lint` exit 0（**3 章正文源／4 個幕錨點／1 個過渡錨點／3 支章衍生／0 列章序**；比對 3 章的對應幕·0 章不一致／5 個錨點幕號·0 個對不到 registry／3 個 `風格` 欄指向的檔·0 個不存在；非標準錨點寫法 0 筆；**3/3 支章衍生有 `write-test` 紀錄**；11/15 幕還沒寫成正文）／`outline-lint` exit 0（8 支 scoped／連續敘述 56.0%·最低 `大綱/arc01.md` 43.9%／枚舉外節 0／arc 引用 77 筆·76 筆指得到／檔內路徑引用 72 個·0 個不存在）／`decision-lint` exit 0（**裁決流 157 列／待裁決 3 列**／標的 40 個·**0 個查無**）／`object-lint` exit 0（**20 支物件檔／41 條約束／4 個揭示層級**／3 支章 delta·17 筆事實行／核對 6 個引用名）／`fact-lint` exit 0（同上讀數）／`char-lint` **exit 1（2 個問題）**：①**17 支源沒有衍生檔**（源 24／衍生 7，**與 S3／S4／S5 完全相同**，既有恆紅）②**幕綱角色欄 2 個 token registry 查無且出現 ≥3 次：嘿美×7、那輛車×6**（本輪新增；括號外 67 個 token·24 個查無）；四節 116 個 slot·0 個是佔位／`style-lint` exit 0（1 支 `風格.ai.md`／5 個語意欄·0 個是空的／3 個可用句式檔位／**掃了 3 章正文·5 個字面 entry·禁用詞命中 0 次**）／`summary-lint` exit 0（6 個語意欄·0 個是空的／基調兩方比對相符／掃了 0 個幕號·arc 引用·0 個懸空·registry ＝幕綱 15 幕 ∪ 1 支 arc 檔）／`world-lint` exit 0（**7 支主題**·與 S3–S5 相同／registry ＝幕綱 6 個標記名 ∪ 物件檔 20 支）／`readiness-lint` exit 0（狀態表 20 格·0 格不是五 token 之一／就緒清單 6 條·5 條已勾）／`derived-sync check` exit 0（**19 個衍生檔·0 個需處理**；成長哨兵掃描範圍 幕綱 1／設定層源 32／設定層衍生 16／**章衍生 3**／大綱 9／參照 2／摘要 1／**正文源 3·刻意豁免體積**／raw 6·刻意豁免／行長 68 支／**事實行 4 支檔·17 行**／搬移目的地 3 個·0 個不存在；**（無）**）／`derived-sync validate` exit 0（**19 支 `.ai.md`**／55 個 `##` 節／112 個鍵·**0 個是 schema 外的**／裁決 blockquote 0 支命中）。
> **`style-lint` 的第一次真讀數**（S5 觀測者交代 S6 要量的三件之一）：「掃了 **3 章正文**／5 個字面 entry，**禁用詞命中 0 次**」——**5 個字面 entry 在 3,576 漢字上仍然是 0**。第二件（第 9 列「罵的是被麻瓜看見」五份執行零份規則）本輪**沒有到期**：那條線在 arc02 的咆哮信，本輪只寫到 arc01 幕004。第三件（S5 第 6 列統一母題禁令）**到期了且沒有違反**：`grep -rn "多欠一筆\|收費的都是想幫他的人" chapters/` 0 命中，**但抓到這個 0 的是本觀測者的 `grep`，不是任何閘門**。
> **一格交給 S7 的觀測者**：本節第 6、7、8 三列是**三個不同的修法**，而它們在覆蓋率行上長得一模一樣（都是「作者說了、`git diff` 沒有」）。**請不要在 S7 把它們讀成「落點 0 三筆」**——那正是本帳一直在警告的那種聚合。另外第 2 列開的那個新問題（落點數量不到射程）建議 S7 起在每一列的證據欄末尾補一句「**誰會在它被違反的那一刻讀到它**」，本輪的 15 列裡有 4 列（6、7、8、9）對這一問的答案是「沒有人」。

---

## S7（character 榮恩 ＋ worldbuild 監管與曝光）— 本 session 新增 17 列（落點 0：1／落點 1：10／落點 N：6）

```
需求機制帳：本 session 新增 17 列（落點 0：1／落點 1：10／落點 N：6）
```

> 產物範圍 ＝ `git diff` 在 `消失的密室/` 的 **8 支已追蹤檔·38 行新增**＋**4 支未追蹤新檔**全文（`story/物件/榮恩.md`、`story/設定/世界觀/監管與曝光.md`＋`.ai.md`、`story/設定/角色/榮恩.ai.md`）。HEAD ＝ `65fde9b`。`裁決流.md` 157 → **177 列**（append 20 列：`:186`–`:205`）／`待裁決.md` 3 → 4 → **3 列**（刪 2 增 2）／`物件/全書.md` +3 列／物件檔 20 → **21 支**／約束 41 → **46 條**／衍生檔 19 → **21 支**／世界觀主題 7 → **8 支**。
> **計數約定沿用 S4／S5／S6，本節逐條複述以免歧義**：①**一支檔算一個落點**；②**投影不計**（`*-lint --emit`／`fact-project`／`decision-project`／`readiness`／`structure-project`）；③**`裁決流.md` 記「為什麼」，在該值另有落點時計為「可投影·不計」，只有當它是唯一的家時計 1**；④**源＋其 hash 同步衍生（`derived-sync check` 報 fresh）計 1（＋衍生），不計為拷貝**——這條沿用 S3 第 4／5 列；⑤**執行／渲染不算宣告**（正文寫出來、大綱演出來，不算一份規則）。
> **本輪的歸屬前提是乾淨的**：兩支神諭寫入型呼叫 **0 次**、導演 7 次寫入 7/7 都在 `情境測試/`（機械覆核見 `S7-*.md` 防火牆節）⇒ **38 行新增全部是兩支受測體寫的**。S6 那個「先扣掉神諭寫的行」的步驟本輪不需要執行。
> **本 session 的形狀是第七種。** S1「沒有欄位可存」／S2「跨檔排定值沒有比對器」／S3「第一次量到機制在被使用」／S4「第一次為讓機制生效而刻意留一份帶拆除日期的重複」／S5「投影的輸入到位了，而它照到的東西比預期少得多」／S6「需求的家存在、規格也對，而那個家開不了門」；**S7 第一次出現「診斷完全正確、處置卻是 append 一份而不是搬家」**——第 16 列的 `裁決流.md:204` 逐字寫著三條約束「散在 `幕綱/arc01.md:29`／`:30`＋`莫莉.md:14`＋`亞瑟.md:15`，咆哮信在 arc02、那輛車回來在 arc07，兩處都有洞」，**每一個字都對，而處置是新增第 4 份、一份舊的都沒拆**。與 S6 第 2 列（`偵測按地點` 真的搬了家、`待裁決` 那一列刪掉）是同一支系統在相隔一個 session 的兩種處置。
> **另外三格是本帳的新位置**：⑴**重複第一次發生在 `裁決流.md` 內部**（第 5、10 列——append-only 事件流裡同一條全書規則有 2–3 列）；⑵**第一次量到「跨檔衍生而沒有 hash 邊」**（第 13 列）；⑶**第一次量到「通則的射程是全書、但它的標的欄寫的是一支具體檔，所以投影撈不到」**（第 17 列，本輪唯一的落點 0）。

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「『想要』那一格是**他要當第一個講出答案的人**，不是先搶著動手」＋作者當場給的新事實「他咬定死對頭就是兇手，咬了一整本」 | **1（＋hash 同步衍生，不計為拷貝）** | 唯一權威在**源** `story/設定/角色/榮恩.md:9`（`git diff` 新增一行）；`story/設定/角色/榮恩.ai.md:12`「想要」欄從那一句展開，`derived-sync check` exit 0「21 個衍生檔·**0 個需處理**」、逐行印 `榮恩.ai.md ← 榮恩.md` **fresh** ⇒ 有 hash 邊、依約定④不計。`裁決流.md:186` 是理由（標的＝該源檔·可投影·不計）。**誰會在它被違反的那一刻讀到它**：`character`／`write` 載入角色源檔 ⇒ 會。⭐ **本輪最乾淨的一筆正面讀數**：受測體逐字引 `設計原則.md` D3 第③格判定「共創問出來的具體事實是新資訊」，把它寫進**源檔**而不只 `.ai.md`。**S6 第 14 列（`佩妮` 那個名字在書內沒有任何來源）的相反面第一次出現** |
| **2.**「預期弧線＝**平弧線**，變的是讀者對他的算法不是他；下游不准替他排醒悟戲」 | **1** | 唯一落值 `story/設定/角色/榮恩.ai.md:21`＋`:31`（同檔算一個落點）。**源檔 0 命中**：`grep -n "平弧線\|醒悟" story/設定/角色/榮恩.md` ⇒ 0。耐久備份是 `裁決流.md:187`（標的 `設定/角色/榮恩.md`）。⚠️ **這一列與第 1 列是同一輪的相反處置**：那一格回寫了源檔，這一格沒有。**機械後果可預測**：任何人改 `榮恩.md` → `榮恩.ai.md` 報 stale → 重生時「平弧線」能不能回來，取決於重生者有沒有跑 `decision-project --target 設定/角色/榮恩.md`。同型對照 `莫莉.ai.md:28`／`飛七.ai.md:21`／`亞瑟.ai.md:20` 三支也都只住衍生側 ⇒ **這是本軸的既有形狀，不是本輪新生的**。**誰會在它被違反的那一刻讀到它**：`beat-sheet`／`write` 載入 `<名>.ai.md` ⇒ 會 |
| **3.**「『需要』那一格**永不兌現**，不在 arc08／arc09 補一場戲」——作者逐字：「**寫在散文裡的紀律不會被執行，寫成排除線的會。**」 | **2** | ①`story/物件/榮恩.md:24` 排除線 `不得替他結帳`（**源·不重生**；`fact-project --for-beat` 在該幕的 `### 榮恩` 節無條件印出）②`story/設定/角色/榮恩.ai.md:17` 散文（衍生）。`大綱/arc07.md:40` 是**執行**且逐字寫「（見 `物件/榮恩.md` 的 `不得替他結帳`）」⇒ 依約定⑤不計。**無一是投影。** `object-lint`／`fact-lint` 皆 exit 0（**21 支物件檔／46 條約束／4 個揭示層級**）。⭐ **N=2 但第二份是指標不是拷貝**——`.ai.md:17` 那一段逐字寫「可執行的形式在 `story/物件/榮恩.md` 的排除線 `不得替他結帳`」。**保守讀 1。** 這是本帳第一次看到「同一個需求刻意分成『分析一份·可執行一份』並且互相指名」，與 S6 第 12 列（`母的` 兩份互不知情、刪一份留一份）是同一個結構的反例 |
| **4.**「選填的『兩難』欄**留空**」（AI 草擬了「站出去 vs 待在原地」，作者駁回） | **1** | 唯一落點 `裁決流.md:189`（標的 `設定/角色/榮恩.md`）——依約定③，這是它唯一的家 ⇒ 計 1。**機械覆核**：`derived-sync validate` exit 0「21 支 `.ai.md`／**62 個 `##` 節**／122 個鍵·**0 個是 schema 外的**」，`榮恩.ai.md` 四節（需求四象限／預期弧線／馬斯洛層次／對衝關係）**無 `## 兩難` 節**。⚠️ **「刻意不寫」這種需求在本系統沒有容器，只能靠裁決流**——`char-lint` 的佔位偵測（本輪 139 個 slot·**0 個是佔位**）看得見「填了空殼」，看不見「該不該有這一節」 |
| **5.**「衍生檔 `伏筆` 欄填 `{}`，`榮恩的魔杖` 與 `三次坦白` 兩個都不宣告」（分叉六） | **1（＋裁決流內部第 2 列）** | 值落 `story/設定/角色/榮恩.ai.md:7` `伏筆: {}`（`derived-sync validate` 122 個鍵·0 個 schema 外）。**規則本體是 `裁決流.md:107`**（2026-07-31·射程全書·生效中），而本輪 append 的 `:190` 是**同一條全書規則的第二列**。⚠️ **本帳第一次記到「重複發生在 append-only 事件流內部」**：`decision-lint` exit 0（**177 列／43 個標的·0 個查無**）——它驗七欄與標的存在性，**同一條規則寫兩列在它眼裡跟寫一列一樣乾淨**。而 `decision-project --target 設定/角色/榮恩.md` 會把兩列一起吐給下游，下游要自己判它們是不是同一條。**這一列的加重情節在 elicitation 紀錄裡**：受測體開分叉六去問作者，而 `:107` 的理由欄**逐字就是拿榮恩的魔杖當例子寫的**（見 `S7-*.md` 摩擦欄第 2 條）——**規則早就存在、寫的時候還想著這支檔，而查詢沒撈到它** |
| **6.**「他怕蜘蛛」三格：源檔『中段』→『**最後三分之一**』／戲在**禁忌森林**（大綱漏寫）／埋設點放**洞穴屋拔地精那個上午** | **3（＋1 個下游缺口）** | ①源 `story/設定/角色/榮恩.md:11`（`中段`→`最後三分之一`，`git diff` 一行）②`story/大綱/arc01.md:87`（埋設點，新增段，明寫「屬於溫度、不屬於伏筆」）③`story/大綱/arc07.md:40`（兌現，新增段）。**無一是投影**；`outline-lint` exit 0（8 支 scoped／檔內路徑引用 **76 個·0 個不存在**／連續敘述 56.5%）。⚠️ **缺口在幕綱**：`grep -n "蜘蛛" story/幕綱/arc01.md` ⇒ **0 命中**，而 arc01 已拆 15 幕、下一批正文正要動筆。已落 `待裁決.md:20`（標的 `幕綱/arc01.md`，`decision-lint` 驗得到標的存在）。**而沒有任何閘門會因為「大綱有、幕綱沒有」而報**——`beat-lint` exit 0（1 支 arc／15 幕／**6 個伏筆標記（6 名）**／6 列狀態表），因為作者明令這一格「**不進伏筆狀態表**」，所以它**照定義就不會被 registry 比對看到**。**誰會在它被違反的那一刻讀到它**：寫幕005 的 `write` 讀幕綱、不讀大綱 ⇒ **沒有人，除非有人翻 `待裁決.md`** |
| **7.**「『他怕蜘蛛』與『那列蜘蛛必須沒有人注意到』的衝突，處置是**釘成排除線**，不是改設定」（作者自己抓到的，不在 AI 清單裡） | **1** | 唯一落點 `story/物件/榮恩.md:23` 排除線 `不得注意到那列蜘蛛`（**新建的第 21 支物件檔**，型別 `角色`）。`大綱/arc01.md:87` 括號裡指回它（依約定⑤不計）。`object-lint` exit 0。**誰會在它被違反的那一刻讀到它**：寫 arc02 那一場的 `write` 跑 `fact-project --for-beat` ⇒ **會**，因為那一幕的角色欄有榮恩。⭐ **與 S6 第 6 列（`枕頭套` 該住 `物件/多比.md` 而那支檔被 `fact-lint` 擋著建不出來）恰好對照**：同一個落點形狀，這一次檔名沒有與任何伏筆名互為子字串，**於是它一次就開出來了** |
| **8.**「那根魔杖沒被換掉的理由**是窮**，但書裡已經寫夠了、不再補一個字；推導只准住分析檔」＋「**不把『窮』立成一條與血統平行的收費軸**」 | **2** | ①`story/設定/角色/榮恩.ai.md:39`（馬斯洛「安全／生理」那一格，逐字「這條推導只准住在這支分析檔裡」）②`裁決流.md:194`（界線本體）＋`:195`（不立軸）。**無一是投影。** **本觀測者實跑約束驗證**：`grep -rn "窮" 消失的密室/story/ 消失的密室/chapters/` ⇒ **大綱 0、正文 0**（命中只有 `裁決流:161,194,195`＋`榮恩.ai.md:39`＋`莫莉.ai.md:12`）⇒ **約束當下成立**。⚠️ **守衛 0**：沒有任何閘門會在有人往大綱或正文寫一句「他沒換魔杖是因為窮」時吭聲。同型的 `物件/全書.md:26`（`答案先給說明不給`）**是有守衛的那一種寫法，而這一條沒有選它**——作者的界線畫在「檔的種類」上（分析檔可以、大綱正文不可以），而**排除線表沒有「只准住某類檔」這個欄位**。這是本帳第一次量到「約束的形狀與容器的形狀不匹配」 |
| **9.**「`暫定: false`」（分叉七）＋通則「`暫定` 問的是**這支檔的內容作者認了沒**，不是這支檔提到的事都做完了沒」 | **1** | 值落 `story/設定/角色/榮恩.ai.md:6`（8 支 `<名>.ai.md` 全部 `暫定: false`，`char-lint` 讀得到）。**通則本身只住 `裁決流.md:196` 的理由欄**——⚠️ **結論欄寫的是 `暫定: false`（一個值），通則寫在理由欄裡**，所以 `decision-project` 撈得到但下游要讀完整列才看得見。`角色.schema.md` 沒有這句話。**這是第 17 列那個形狀的輕量版**：射程是全書的語意定義，住在一支具體檔的標的下 |
| **10.**「**引裁決流一律用檔案行號**，投影輸出的行號不是檔案行號」＋「**帶對行號不等於讀對那一列**——引用還要驗那句話的因果方向」 | **3** | ①`裁決流.md:197`（本輪·character 那一輪）②`裁決流.md:205`（本輪·worldbuild 那一輪，**逐字自陳是 `:184` 第③條的下一步**）③既有 `裁決流.md:184` 第③條（「講工具為什麼這樣的句子必須帶檔名＋行號」）。**同一族查詢紀律現在在裁決流裡有三列，全部射程全書、全部生效中、無一是投影。** ⚠️ **這是本輪機制重複的主症狀**：`decision-project --target <任何檔>` 會把三列一起吐出來（本觀測者實跑 `--target 設定/世界觀/新主題.md` ⇒ 命中 39 列），**而下游要自己判它們是同一條規則的三個層次還是三條規則**。三列的字數合計超過 3,000 字元。**本帳第五次記到「處置是規勸」**（S3 第 13 列／S4 第 10 列／S5 第 15 列／S6 第 4 列），而這一次規勸的對象是**規勸本身的品質** |
| **11.** 第 8 支世界觀檔建成：一句話寫成「**它不是把輕重排錯了，是它根本沒有量傷害的那一格儀器**」；**三個漏洞全寫**（甲案），第 3 條明標「本書的怪物是從這個漏洞遞出來的」；並採「**有落款 vs 沒有落款**」當與 `權力與程序.md` 的分界 | **1（＋hash 同步衍生）** | 唯一權威在**源** `story/設定/世界觀/監管與曝光.md`（**新建 3,982 B**；一句話 `:3`／三條 `:7-9`／三個漏洞 `:21-23`／寫法約束 `:27-30`／分界 `:34`）；`監管與曝光.ai.md`（新建 6,409 B）從它展開，`derived-sync check` 逐行印 **fresh**、`world-lint` exit 0「**8 支 `<主題>.ai.md`**」（S3–S6 一直是 7）。`裁決流.md:198`／`:201` 是理由（可投影·不計）。**誰會在它被違反的那一刻讀到它**：`settings-select`／`write` 依主題選檔 ⇒ 會。⭐ **正面讀數兩條**：⑴ 受測體把 Sanderson 那一關讀成「**這條規則向誰收費**」並自陳「這是制度規則不是力量體系」——`.ai.md:9` 逐字引 `權力與程序.ai.md`／`血統與歸屬.ai.md` 為既有慣例，**照本書慣例不是自創**；⑵ 它**沒有把分界那句話再抄一份**——`權力與程序.md:19`「真正嚇人的不是怪物，是簽名」＋`裁決流.md:180`「全書第一封信不簽名」是它的兩個既有地基，`:34` 只寫「（展開見 `監管與曝光.ai.md` 的〈自洽〉，**本檔不複述**）」 |
| **12.**「動物園那次為什麼沒有信」＝**第四案：他那時候還不在那張表上**（帳從他收到第一封信那天才開始跑） | **1** | 唯一落值 `story/設定/世界觀/監管與曝光.md:13`。`裁決流.md:199` 記理由與三個被駁回的候選。**刻意的落點 1 而不是 2**：作者附一條落檔紀律「`爬說嘴.md:40` 那個『無意中』是同一件事的第二層理由，**不得寫進本檔**——一件事給兩個理由就是自己拆自己的台」，機械可驗 ⇒ `grep -n "無意中" story/設定/世界觀/監管與曝光.md` **0 命中**。⚠️ **落點是 1，檢索覆蓋是 0**：答案已經在 `story/設定/角色/哈利/來歷.md:5`（「十一歲才知道自己是什麼」）與 `:15`（「動物園是十一歲以前」）躺了**六個 session**（S1 `b59aa91` 寫的），而受測體開了 A／B／C 三個候選、作者三個全駁。它讀了 7 支世界觀檔、裁決流、8 支大綱、幕綱，**沒讀主角的來歷切面**。**本帳量不到「該讀而沒讀」**，而這與 S5「一條承重規則沒有任何檔在記」同族：**沒有守衛，而且失敗長得像成功**（三個候選、每個都有代價分析、每個都自洽） |
| **13.**「血統 × 監管相乘出來的盲點分佈**寫進〈自洽〉、正文永不觸及**，並同步在 `物件/全書.md` 落一列排除線」（A 案，作者附條件） | **2** | ①`story/物件/全書.md:30` 排除線 `不算血統那筆帳`（**源·不重生**，`fact-project --for-beat` 每一幕無條件印）②`story/設定/世界觀/監管與曝光.ai.md:30`〈自洽〉的展開（**衍生**）。**無一是投影。** ⚠️ **本帳第一次量到「跨檔衍生而沒有 hash 邊」**：②的內容在它自己的源檔裡**種子是 0**——`grep -c "血統" story/設定/世界觀/監管與曝光.md` ⇒ **0**；而 `derived-sync check` 逐行印的邊是 `監管與曝光.ai.md ← 監管與曝光.md` **單一條**。②自陳的來源是 `血統與歸屬.ai.md:11`（「規則的受益者感覺不到規則存在」）——**另一支衍生檔，兩支之間沒有任何 hash 邊**。⇒ **規則本身安全（①是源），但重生 `監管與曝光.ai.md` 時這一段分析能不能回來，取決於重生者會不會去讀 `血統與歸屬.*`，而沒有任何東西保證它會。** 作者拍板時已經預判了一半（他要求「同時落一列排除線」，理由逐字引 `裁決流.md:185`「寫在世界觀檔的〈自洽〉裡，在它會被違反的那一刻沒有人會讀到」）——**他防住了『讀不到』，沒有防住『重生後不見』** |
| **14.**「**不把『那輛車開進森林沒有人收帳』認領成漏洞的第四次執行**」（駁回；作者：「一被認領，它就被管住了」） | **1** | 唯一落值 `story/設定/世界觀/監管與曝光.ai.md:34`——一條**刻意的不作為**，逐字「本檔不認領、見那支物件檔」＋「**下一輪不要把它接回來。**」耐久備份 `裁決流.md:203`（標的 `物件/那輛車.md`；`decision-project --target 物件/那輛車.md` 撈得到）。⚠️ **與第 13 列同一個病，而這一個更嚴重**：`grep -n "那輛車" story/設定/世界觀/監管與曝光.md` ⇒ 只有 `:22`（漏洞二的例子），**「不認領」這件事在源檔 0 命中** ⇒ **這條專門用來防止下一輪重新提案的備忘，本身會在下一次重生時消失**。第 13 列掉的是分析，**這一列掉的是防重工的那句話**。**這是本帳第一次記到「防止重複工作的機制，自己住在會被重複工作沖掉的地方」** |
| **15.**「亞瑟那個『因為』子句拆成兩句；『**被自己起草的法條反咬**』這件事**不存在**；諷刺維持原狀（他不是偽君子，他是合規的）」（AI 誤讀更正） | **1（＋hash 同步衍生）** | 唯一權威在**源** `story/設定/角色/亞瑟.md:16-18`（`git diff` +3 −1，新增「這一格有兩件事，別併成一句話講……**他自己那部法從頭到尾沒有咬過他**」）；`亞瑟.ai.md:25-26` 同步補一格（「那是一顆沒有引爆的地雷，不是一張要兌現的支票」）並重新 stamp（`generated-from` `e2a48d86fe13` → **`46eacdb19d93`**，`derived-sync check` 報 fresh）⇒ 依約定④不計。`裁決流.md:202` 記兩個被駁回的候選。⭐ **本輪唯一由「AI 誤讀」觸發的落檔，而且落的是源檔。** 作者的定性可直接抄走：「**它三段推論每一段都帶行號、每一格都可覆算，而錯的是最上面那個讀法——行號不保證讀對。**」**誤讀本身成了那句話該修的證據**：`亞瑟.md:7` 那個「因為」一句話幹兩件事，**而它今天就被這樣讀了一次** |
| **16.**「三條寫法約束**升為書級排除線**，一條一列、不塞成一列；**不建 `物件/亞瑟.md`**」 | **N（逐條數：7／5／2）** | 三條逐條做全書落點計數，**無一是投影、無一有 hash 邊**：<br>▸ **`不吐槽輕重順序` ＝ 7**（S5 第 9 列記 **5**，本輪 **+2**）：`物件/全書.md:28`（**新**）＋`設定/世界觀/監管與曝光.md:27`（**新**·源檔寫法約束）＋`幕綱/arc01.md:30`（既有排除線）＋`大綱/arc01.md:127`＋`大綱/arc02.md:30`＋`設定/角色/莫莉.md:11`＋`莫莉.ai.md:12`（後兩者有 hash 邊 ⇒ **保守讀 6**）。**S5 診斷「五份執行、零份規則」，本輪補上了規則——但一份都沒有拆掉，於是變成「四份執行、三份規則」。**<br>▸ **`不併講亞瑟那兩件事` ＝ 5**：`物件/全書.md:29`（**新**）＋`幕綱/arc01.md:29`＋`大綱/arc01.md:95`＋`設定/角色/莫莉.md:14`＋`設定/角色/亞瑟.md:17`。<br>▸ **`不算血統那筆帳` ＝ 2**：見第 13 列。<br>`object-lint`／`fact-lint` exit 0（21 支物件檔／**46 條約束**，41 → 46）。⚠️ **`裁決流.md:204` 的診斷逐字完全正確**——它自己寫著「第一、二列現在散在 `幕綱/arc01.md:29`／`:30`（射程綁 arc01）＋`莫莉.md:14`（只綁莫莉）＋`亞瑟.md:15`（只在亞瑟在場的幕被載入），而**咆哮信在 arc02、那輛車回來在 arc07，兩處都有洞**」——**每一格都對，而處置是 append 第 4／第 5 份，一份舊的都沒拆**。**與 S6 第 2 列是同一支系統的兩種處置**：那一次 `偵測按地點` 真的搬了家（`待裁決.md` 那一列刪掉），這一次沒有。**兩次的差別不在難度**（刪 `幕綱/arc01.md:29` 那一行與刪 `待裁決.md` 那一列一樣容易），**而在有沒有人說「舊的那份現在該刪」**——作者拍的是「升為書級」，沒有人拍「拆掉舊的」，而**沒有任何工具會問這一句**。⭐ 正面的那一半：「一條一列而不塞成一列」被執行了（`git diff` 三行三列），理由逐字是「`不得寫成` 欄溢位是本系統的舊病」；「**不建 `物件/亞瑟.md`**」也守住了（`物件/` 21 支，無 `亞瑟.md`），理由「建它會把『禁敘述者』誤裝成『禁一個角色』」 |
| **17.** 通則：「**每建一支新的世界觀檔，都要先替它與最像的那一支畫界線**」（作者明白認下這是 AI 本輪做對的、他沒想到的一件事） | **0** | **機械症狀（本觀測者實跑）**：這條通則的**結論欄**住在 `裁決流.md:201`，而該列的**標的欄寫的是 `設定/世界觀/監管與曝光.md`**（一支具體的檔），不是 `全書`。⇒ 模擬下一次建第 9 支世界觀檔：`decision-project --book <書> --target "設定/世界觀/新主題.md"` ⇒ **exit 0／掃描 177 列·命中 39 列／標的 1 個**，而 `grep -c "先替它與最像的那一支畫界線"` ⇒ **0 命中**。對照組 `--target "設定/世界觀/監管與曝光.md"` ⇒ 命中 43 列／標的 2 個／該句 **1 命中**。**也就是說：這條射程全書的通則，在下一次它該生效的那一刻，投影撈不到它。** 唯一還撈得到的殘影是 `裁決流.md:205` **理由欄裡的一句夾帶**（「AI 主動替新檔與最像的舊檔畫界線……那個習慣以後每建一支檔都要做」，該列標的＝`全書` ⇒ 會被印出來）——**一條通則的可及性，靠的是另一條通則的理由欄順口提了一句。** `decision-lint` exit 0（177 列／43 個標的·**0 個查無**）：它驗標的**存不存在**，**不驗標的與射程矛不矛盾**。**這是落點 0 的第五種症狀，前四種都在 S6**（家建不出來／家沒人開／連家都不必找／射程是下一個 session）；**這一種是「家對了、值也落了，而查詢的鑰匙開的是別一把鎖」** |

**本 session 的形狀**：

1. **唯一的落點 0 不是「沒寫」，是「寫了而查不到」**（第 17 列）。前六個 session 的落點 0 全部是「作者答了、`git diff` 沒有」；本輪這一筆 `git diff` **有**——`裁決流.md:201` 那一列存在、七欄合規、`decision-lint` exit 0。**壞的是標的欄與射程欄之間的一致性，而沒有任何閘門看這兩欄的關係。** 可機械化的最小處置：`decision-lint` 加一項——**射程欄寫 `全書`、而標的欄寫一支具體檔的列，印一行提示**（本輪 20 列新增裡有 **9 列**是這個形狀，其中至少第 9、17 兩列的通則性質是明確的）。
2. **本輪的機制重複第一次長在「規則本身」而不是「規則的執行」上。** S3–S5 記的 N 是「一個設定值抄了 N 份」，S6 記的是「N 份互相打架」；**本輪第 5、10 列是同一條規則在 `裁決流.md` 裡有 2–3 列**，而那支檔是 append-only、**依設計不刪任何一列**。⇒ **`decision-project` 的輸出會逐輪變長，而變長的部分是同一件事的複述。** 本輪 `--target 全書` 類查詢已經會吐出三列查詢紀律（`:184`／`:197`／`:205`，合計 >3,000 字元）。**這一格與 `CLAUDE.md` 廢除五支 rollup 的理由同形**：一支「關於全部東西」的檔會吸走所有找不到家的內容——**而 `裁決流.md` 的 `標的＝全書` 那一格正在變成那支檔。**
3. **第 13、14 列合起來是一個新的、可機械偵測的病**：**`.ai.md` 裡住著源檔沒有種子的內容**。兩筆的症狀相同（`grep <關鍵詞> <源檔>` ⇒ 0 命中，而 `.ai.md` 有整段），代價不同（一筆掉分析、一筆掉防重工備忘）。**`derived-sync validate` 驗節枚舉與鍵枚舉，`derived-sync check` 驗 hash 新鮮度——沒有任何一支驗「這一段的種子在源檔裡找得到嗎」。** 這正是 `設計原則.md` D3 第②／③格的邊界，而**它現在完全靠寫的人自律**。第 1 列（受測體逐字引 D3 第③格把新事實回寫源檔）證明**自律是做得到的**；第 13、14 列證明**同一輪的另一支 agent 沒做**。
4. **12 支閘門本輪抓出來的仍然是 1 列，而且是同一列。** 17 列裡唯一被閘門點名的是 `char-lint` exit 1 的第二個問題（`嘿美`×7／`那輛車`×6 registry 查無），**而那是 S6 已裁定的誤報、與本輪 17 列沒有一列相關**。三筆機制重複、一筆落點 0、兩筆源檔無種子、一次「診斷對而處置錯」，**12 支閘門全部沉默且全部印對**。第一個問題（源無衍生檔）從 17 支降到 **16 支**——本輪唯一由閘門讀數反映出來的進度，而它反映的是「多了一支 `.ai.md`」，不是本帳裡的任何一列。

> **12 支閘門本輪讀數**（觀測者端獨立覆算，取真 exit code＝`out=$(cmd 2>&1); echo $?`，**未用管線**；本帳只引用機械輸出）：`outline-lint` exit 0（8 支大綱檔·0 支全書版／8 支 scoped／跳過 1 支骨架；連續敘述 56.5%·最低 `大綱/arc01.md` 45.6%／枚舉外節 0／arc 引用 **79 筆**·78 筆指得到／檔內路徑引用 **76 個**·0 個不存在／`## 選用結構公式` 8 支宣告·registry 查無 0）／`beat-lint` exit 0（1 支 arc／15 幕／14 條前因／6 個伏筆標記（6 名）／6 列狀態表／1 個承諾區／**19 條排除線**／**21 支物件檔**；11 筆檔內路徑·目的地不存在 0；spine 讀自 `_順序.md`；1/1 支 arc 有 `beat-test` 紀錄；提示：2/6 個伏筆名沒有物件檔〔多比的警告〕〔魯休斯的動機〕·**不計入問題**）／`ch-lint` exit 0（3 章正文源／4 個幕錨點／1 個過渡錨點／3 支章衍生／0 列章序；0 章不一致／5 個錨點幕號·0 個對不到；**3/3 支章衍生有 `write-test` 紀錄**；11/15 幕還沒寫成正文）／`decision-lint` exit 0（**裁決流 177 列**／待裁決 3 列／**標的 43 個·0 個查無**／`已升為通則` 0 列）／`object-lint` exit 0（3 支章 delta／17 筆事實行／**21 支物件檔／46 條約束**／4 個揭示層級；核對 3 支章的對應幕區間、6 個引用名）／`fact-lint` exit 0（同上讀數）／`char-lint` **exit 1（2 個問題）**：①**16 支源沒有衍生檔**（24 支源／**8 支 `<名>.ai.md`**；S3–S6 一直是 17／7，**本輪 −1**）②幕綱角色欄 2 個 token registry 查無且出現 ≥3 次：**嘿美×7、那輛車×6**（S6 起既有，作者已裁定誤報，本輪兩支 agent 都正確判「與我動的檔無關」而沒去碰）；四節共 **139 個 slot·0 個是佔位**（S6 是 116）／`style-lint` exit 0（1 支 `風格.ai.md`／5 個語意欄·0 個是空的／3 個可用句式檔位／5 個字面 entry·稱謂系統 9 組 12 個詞／基調參照 vs `00-摘要.ai.md` 相符／**掃了 3 章正文·禁用詞命中 0 次**）／`summary-lint` exit 0（1 支源／1 支衍生·0 支源沒有衍生檔／6 個語意欄·0 個是空的／人稱合法／基調兩方相符／掃了 0 個幕號·arc 引用·0 個懸空／源檔 0 處駁回語彙）／`world-lint` exit 0（**8 支 `<主題>.ai.md`**·S3–S6 是 7／0 個伏筆名查無·registry ＝幕綱 6 個標記名 ∪ 物件檔 21 支／源檔第一段 **0/8 支是空的**）／`readiness-lint` exit 0（2 個 `##` 節／狀態表 20 格·0 格不是五 token 之一／就緒清單 6 條·5 條已勾）／`derived-sync check` exit 0（**21 個衍生檔·0 個需處理**；成長哨兵掃描範圍 幕綱 1／設定層源 **33**／設定層衍生 **18**／章衍生 3／大綱 9／參照 2／摘要 1／幕綱索引 1／正文源 3·刻意豁免／raw 6·刻意豁免；行長 **71 支**；事實行 4 支檔·17 行；搬移目的地 3 個·0 個不存在；**（無）**）／`derived-sync validate` exit 0（**21 支 `.ai.md`**／**62 個 `##` 節**／**122 個鍵·0 個是 schema 外的**／裁決 blockquote 0 支命中）。
> **與 S6 收工讀數的 diff（全部可由本輪 17 列解釋，無不明漂移）**：物件檔 20→21（第 7 列）／約束 41→46（第 7、13、16 列）／衍生檔 19→21（第 1、11 列）／世界觀主題 7→8（第 11 列）／裁決流 157→177（20 列 append）／標的 40→43／源無衍生 17→16（第 1 列）／`char-lint` slot 116→139（第 1 列新增一支四節檔）。**`char-lint` 的 exit code 與問題數（1／2 個）開場收工完全相同。**
> **兩格交給 S8 的觀測者**：⑴ **第 16 列那個「診斷對、處置是 append」的形狀，S8 起請在每一列的證據欄回答「舊的那幾份拆了沒」**——本輪 17 列裡只有第 16 列出現這個問題，但它一次就製造了 3 份新的重複（`不吐槽輕重順序` 5→7、`不併講亞瑟那兩件事` 4→5）。⑵ **第 13、14 列的症狀是可以機械掃全書的**：對每一支 `<X>.ai.md` 的每一個 `##` 節，抽名詞短語去 `grep` 它的源檔，命中 0 的印出來。**本觀測者本輪只手做了 2 支，全書有 21 支。** 這比本帳其他任何一格都更接近「可以做成第 13 支閘門」。

---

## S8（beat-sheet 補幕＋誤導線落地＋write ch0004）— 本 session 新增 19 列（落點 0：5／落點 1：10／落點 N：4）

```
需求機制帳：本 session 新增 19 列（落點 0：5／落點 1：10／落點 N：4）
```

> 產物範圍 ＝ `git diff 48c06b3` 在 `消失的密室/` 的 **4 支已追蹤檔·38 行新增**＋**5 支未追蹤新檔**全文（`chapters/ch0004.md`＋`.ai.md`、`story/物件/派西.md`、`story/設定/角色/派西/{核心,水下}.md`）。HEAD ＝ `48c06b3`。`裁決流.md` 177 → **194 列**（append 17 列：`:206`–`:222`）／`待裁決.md` 3 → 4 → **3 列**（結 1 加 2，另刪檔頭 2 行）／承諾區排除線 19 → **29 條**／物件檔 21 → **22 支**／衍生檔 21 → **22 支**／章衍生 3 → **4 支**／正文源 3 → **4 支**／事實行 4 支檔·17 行 → **5 支檔·25 行**／裁決標的 43 → **45 個**／角色源 24 → **25 支**（目錄形態 4 → **5 支**）／`char-lint`「源無衍生檔」16 → **17 支**。
> **計數約定沿用 S4／S5／S6／S7，本節逐條複述以免歧義**：①**一支檔算一個落點**；②**投影不計**（`*-lint --emit`／`fact-project`／`decision-project`／`readiness`／`structure-project`）；③**`裁決流.md` 記「為什麼」，在該值另有落點時計為「可投影·不計」，只有當它是唯一的家時計 1**；④**源＋其 hash 同步衍生（`derived-sync check` 報 fresh）計 1（＋衍生），不計為拷貝**；⑤**執行／渲染不算宣告**（正文寫出來、大綱演出來、幕綱的行動欄演出來，不算一份規則）。
> **本輪的歸屬前提是乾淨的**：五支神諭寫入型呼叫 **0 次**、導演 11 次寫入 11/11 都在 `情境測試/`（機械覆核見 `S8-*.md` 防火牆節）⇒ **38 行新增＋5 支新檔全部是兩支受測體寫的。**
> **本 session 的形狀是第八種。** S1「沒有欄位可存」／S2「跨檔排定值沒有比對器」／S3「第一次量到機制在被使用」／S4「第一次為讓機制生效而刻意留一份帶拆除日期的重複」／S5「投影的輸入到位了，而它照到的東西比預期少得多」／S6「需求的家存在、規格也對，而那個家開不了門」／S7「診斷完全正確、處置卻是 append 一份而不是搬家」；**S8 第一次出現「落點 0 是作者刻意裁出來的」**——本輪 5 個落點 0 裡有 **2 個是裁定的結果**（第 3、19 列），而前七個 session 的落點 0 **全部是缺口**。這一格必須在本帳裡分開，否則下一輪盤點的人會把「系統正確地什麼都沒存」讀成「系統忘了存」。
> **另外三格是本帳的新位置**：⑴**S7 交辦第①項（「舊的那幾份拆了沒」）本輪逐列回答了，而答案是「本輪沒有製造需要拆的舊份」**——19 列裡 0 列是「診斷對、處置是 append 第 N 份」，`不吐槽輕重順序`（S7 記 7）與 `不併講亞瑟那兩件事`（S7 記 5）本輪一格未動、也未新增；⑵**S7 第 17 列那個「射程全書、標的寫一支具體檔、投影撈不到」的形狀，本輪原樣復發**（第 15 列），而且**同一輪裡有一個正面對照組**（第 2 列，標的寫 `全書`、三個 target 全撈得到）——**兩列相隔兩行、同一支 agent、同一天**；⑶**第一次量到「一條約束的射程綁在 arc，而那個 arc 是它唯一的家」**（第 11 列）。
> **S7 交辦第②項（掃全書 `<X>.ai.md` 的節，抽名詞短語去 `grep` 源檔）本輪未執行**——導演本輪沒有排這件事，本觀測者不改任何產物。**但本輪新增一支不生 `.ai.md` 的角色（派西），那一支結構上免疫這個病**，可當該項未來的對照組。

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「榮恩怕蜘蛛，**埋設點在拔地精那個上午**；屬溫度不屬伏筆——不停格、不加重、不進伏筆狀態表」 | **2（＋伏筆軸 0／事實軸 0，皆為裁定）** | ①`story/大綱/arc01.md:87`（S7 落，本輪未動）②`story/幕綱/arc01.md`（本輪新增，同檔三處算一個落點：`:15` 承諾區正向、`:28` 承諾區負向、`:103` 幕005 行動欄）。`chapters/ch0004.md:67`（「甩了三下才把那隻蜘蛛甩進土裡」）依約定⑤不計；`裁決流.md:206` 記理由（標的＝`幕綱/arc01.md`，可投影·不計）。**無一是投影。** ⚠️ **這一列是 S7 第 6 列標的那個缺口的結清**：S7 記「`grep -n "蜘蛛" story/幕綱/arc01.md` ⇒ **0 命中**……誰會在它被違反的那一刻讀到它：**沒有人，除非有人翻 `待裁決.md`**」，本輪 `grep` 同一條 ⇒ **4 命中**，`待裁決.md` 那一列已刪（`decision-lint` exit 0，**待裁決 3 列**）。**補上的路徑正是它被記下時預期的那一條**（`待裁決.md` → 下游 skill 消化 → 刪列），這是本帳第一次看到一個落點 0 走完全程被關掉。⭐ **而兩個 0 是裁定的**：`beat-lint` exit 0 印「**6 個伏筆標記（6 名）／6 列狀態表**」（S7 也是 6/6，本輪未增），`ch0004.ai.md` 的 **8 筆本章事實**逐筆核過**無一與蜘蛛有關**——見第 3 列 |
| **2.** 通則：「**一格內容若屬『溫度』，它在伏筆軸與事實軸上的正確存量都是零。事實軸只裝『下游改了會出錯的東西』，不裝『這一格寫得好』**」 | **1** | 唯一落點 `story/參照/裁決流.md:221`，**標的欄寫 `全書`**。**本觀測者實跑三個不同 target 驗可及性**：`decision-project --target 幕綱/arc02.md`（下一支要拆的幕綱）／`--target 設定/風格/風格.md`／`--target 參照/待裁決.md` ⇒ **3/3 都撈得到這一列**（各命中 42／45／42 列裁決）。`decision-lint` exit 0（**194 列／標的 45 個·0 個查無**）。⭐ **這是第 15 列的正面對照組，而兩列是同一支 agent 在同一輪 append 的（`:221` 與 `:219`，相隔兩行）**：一條寫 `全書`、一條夾在具體檔的理由欄裡，**而沒有任何東西告訴寫的人這兩種寫法的下游差別**。**誰會在它被違反的那一刻讀到它**：任何跑 `fact-project`／`decision-project` 的下游 ⇒ 會 |
| **3.**「**幕006 那第八個碗**」與「**派西那個寫了名字的鹽罐**」——「這兩格＋怕蜘蛛，三格全部不要留機讀痕跡」 | **0 —— 而這是裁定的結果，不是缺口** | **機械症狀（本觀測者實跑）**：`ch0004.ai.md` 的 `## 本章事實` 共 **8 筆**，逐筆列全：位置×3（哈利在洞穴屋／嘿美繞煙囪／那輛車在棚子正中間）、錨×4（〔洞穴屋的形狀〕〔榮恩寫過的信〕〔查抄的節奏〕〔隱形裝置〕）、關係×1（亞瑟問他開關該裝哪）。**`grep -n "碗\|鹽罐\|蜘蛛" chapters/ch0004.ai.md` ⇒ 0 命中**，而 `grep` 同三個詞在 `chapters/ch0004.md` ⇒ **3 命中**（`:67` 蜘蛛、`:129`／`:131` 鹽罐）。`fact-lint`／`object-lint` exit 0（**5 支章 delta／25 筆事實行**）。<br>⚠️ **這一列與真缺口的差別，逐條講清楚**：<br>▸ **真缺口的機械症狀是「作者答了、`git diff` 沒有」**（本帳判定規則第 1 條），例如 S1 第 1 列（「最後真的分成了好幾塊」只活在對話裡）、S2 第 7 列（雨傘兌現點兩處都只宣告缺席）——**那些 0 的下游後果是「這件事沒有人記得」**。<br>▸ **這一列的 `git diff` 有東西**：`裁決流.md:221` 逐格寫了三格為什麼是 0，**而那一列本身就是這個 0 的家**（依約定③計為第 2 列的 1，不重複計）。**0 指的是「事實軸／伏筆軸這兩個特定容器裡的存量」，不是「這個需求沒有落點」。**<br>▸ **作者給的機械理由可覆算**：`fact-project --for-beat` 會把事實軸的每一筆印進**每一支含該實體的幕**；`grep -c "榮恩" story/幕綱/arc01.md` ⇒ 榮恩出現在多幕 ⇒ 若記一筆〔怕蜘蛛〕，它會被印進那幾幕的前面——**而 `幕綱/arc01.md:28` 那條排除線明文禁止任何人回頭提它**。⇒ **記帳與排除線會互相打架，而先動手的是投影。**<br>▸ **所以正確的讀法是**：本系統目前**沒有辦法把「這一格不要被記帳」這件事機械化**——擋住它的是作者當場口述、落成一條裁決流通則（第 2 列）。**若下一輪有人（或有工具）建議「補上這三筆事實讓 `fact-project` 完整」，唯一會反對的是那一列。** 交階段 3：這是「**負向記帳**」這個概念第一次出現，而事實軸的 schema 沒有它 |
| **4.**「那條誤導線**不是一條、是四格**」：①arc02 打斷妹妹的人是他／②arc05 深夜地下層由變身水那一小時撞見／③arc07 走廊上另一個女生是他的人／④arc09 揭曉由金妮講 | **N（逐格數：2／2／2／3，＋大綱三支 0 新增）** | 逐格做全書落點計數：<br>▸ **①arc02** ＝ **2**：`物件/派西.md:9,17` ＋ `設定/角色/派西/核心.md:19`。**`大綱/arc02.md` 一個字沒加**（`grep -c "派西" story/大綱/arc02.md` ⇒ **0**），照 `裁決流.md:178`「大綱寫人用關係不用名字」。<br>▸ **②arc05** ＝ **2**：同兩支檔（`物件:9`／`核心:20`）。`大綱/arc05.md` 本輪只改具名（「她哥」→「榮恩」，`git diff` 2 處：`:77`＋`:95` 伏筆表），**派西一個字沒進去**（`grep -c` ⇒ 0）。<br>▸ **③arc07** ＝ **2**：同兩支檔（`物件:9`／`核心:21`）。`大綱/arc07.md` `grep -c "派西"` ⇒ **0**。<br>▸ **④arc09** ＝ **3**：外觀在 `物件/派西.md:9` ＋ `核心.md:22`，**內容**在 `設定/角色/派西/水下.md:7` ＋ **`裁決流.md:214` 的裁決欄與理由欄**（見第 8 列）。<br>⭐ **正面讀數，而且是機械的**：三支大綱一個字沒加，**而 `decision-project --target 大綱/arc0{2,5,7}.md --active-only` 三支全部撈得到派西**（本觀測者實跑：命中 45／54／45 列，`grep -c "派西"` ⇒ 3／4／3）。⇒ **那三格是「1（投影）」不是「0」，形狀正確。** ⚠️ **但可及性不等於會被查**：`beat-sheet` 拆 arc02 時**沒有任何東西要求它跑 `decision-project --target 大綱/arc02.md`**——`beat-lint` exit 0（`17 筆檔內書內路徑·目的地不存在 0`）不驗這件事。**與 S3 第 8 列（作者靠自己記得 `大綱/arc07.md:162` 那一行）同族**：撈得到、沒有人去撈 |
| **5.**「派西是誰、他扛什麼」——正當需求（他要一個自己掙來的位置）＋一句話功能（**異常不等於有罪**）＋四件事的外觀 | **3** | ①`story/物件/派西.md:13`「他扛的功能是一句話：**異常不等於有罪。**」＋`:9` 四件事清單 ②`story/設定/角色/派西/核心.md:15` **逐字同一句** ＋`:19-22` 四件事清單（同一份，換行排版） ③`story/幕綱/arc01.md:35`「**煩不等於有罪**正是那條線的地基」。**無一是投影、無 hash 邊**（派西**不生 `.ai.md`**，所以連約定④的豁免都不適用）。`char-lint` exit 1 但兩個問題與本列無關（見末段）；`object-lint` exit 0（**22 支物件檔**）。⚠️ **本帳第一次看到「同一輪、同一支 agent、同一個作者需求，在兩支剛建好的新檔裡各寫一份」**：`核心.md:26` 逐字寫「寫法上的排除線在 `story/物件/派西.md`（五條），**本檔不複述**」——**它知道要指過去，而且對排除線真的做到了（第 6 列）；但對「他扛什麼」這一句，兩支檔都寫了全文。** 分界不是能力問題，是**規則有指標語法、分析沒有**：排除線是規則表的一列，可以說「見那支檔的約束名」；「異常不等於有罪」是一句散文，指過去等於什麼都沒說。**與 S7 第 3 列（`.ai.md:17` 指向 `物件/榮恩.md` 的排除線）恰好互為正反面** |
| **6.**「派西的五條排除線」（不得併給雙胞胎／不得連兩枚徽章／不得用那個藉口／不得替他解釋／不算進三次坦白） | **1** | 唯一落點 `story/物件/派西.md:39-43` 的規則表（**新建的第 22 支物件檔**，型別 `角色`）。`設定/角色/派西/核心.md:26` 明寫「本檔不複述」，`grep -c "不得併給雙胞胎\|不得連兩枚徽章\|不得用那個藉口\|不得替他解釋\|不算進三次坦白" 設定/角色/派西/` ⇒ **0**；`水下.md:17` 也只提到「那五條排除線一條都不動」而不重述。`object-lint`／`fact-lint` 皆 exit 0（22 支物件檔）。**送達驗證（本觀測者實跑）**：`fact-project --for-beat 幕006` ⇒ 導出實體含**派西**，五條約束**逐條印出**（輸出第 36–40 行）。⭐ **這是本輪最乾淨的一筆**：新開一支物件檔、五條一條一列、旁邊的角色檔明文不複述、投影當場驗證送達。**與 S7 第 16 列（診斷對、處置是 append 第 4 份）是同一支系統的相反處置** |
| **7.**「派西**要建，但不是那個形狀**——照金妮的目錄形態 `{核心,水下}`、**不生 `.ai.md`**」（駁回 AI 提的「照七支最小單檔先例辦」） | **1** | 唯一落點 `裁決流.md:215`（標的＝`設定/角色/派西/核心.md`），檔案本身是它的執行（`ls story/設定/角色/派西/` ⇒ `核心.md`／`水下.md`，`char-lint` 印「**25 支源（5 支目錄形態）**」，S7 是 24／4）。⚠️ **帳單是機械可預期的、而且本輪如期出現**：`char-lint` exit 1 第一個問題「源沒有衍生檔」16 → **17 支**，派西進名單。**這與 S7 第 1 列的方向相反**：那一列是「多一支 `.ai.md`、讀數 17→16」，本列是「刻意不生、讀數 16→17」——**同一個讀數的兩個方向都出現過了，而它對兩者印的字完全一樣**（「跑 `character` 替它生成 `<名>.ai.md`」）。⇒ **那份名單量的是「有沒有衍生檔」，不是「該不該有」**，而本書現在有兩支是裁定的（金妮、派西）。**這正是 `S8-*.md` 摩擦欄第 1 條交的缺口 A 的同一格，本帳從落點側再記一次** |
| **8.**「揭曉的**內容**從 `物件/派西.md` 的散文節刪掉、搬進 `水下.md`」（作者點名：「在門上掛了『請勿窺視』的牌子，然後把門開著」） | **1（＋一份洩漏在 append-only 軸上，不可回收）** | 唯一落點 `story/設定/角色/派西/水下.md:7`（「**私人戀愛。** arc09 揭曉，由金妮講出來」）。`物件/派西.md` `grep -c "戀愛"` ⇒ **0**（已搬乾淨）；`grep -rn "戀愛" 消失的密室/` 全書 ⇒ 4 命中：`水下.md:7`／`物件/全書.md:38`（`不寫感情線` 排除線，不同事）／**`裁決流.md:207`**／**`裁決流.md:214`**。⚠️ **後兩筆是同一份答案寫在 append-only 的軸上，而那支檔依設計不刪任何一列**。受測體自己回報了這一格，作者判**低嚴重度**並**駁回「append 一列寫『第 214 行不得引用』」**（`裁決流.md:222` 逐字：「那一列會跟第 214 行在同一個查詢裡一起被吐出來……**牌子不是鎖，而且這次牌子還額外指了一次門在哪**」），改 append **往前生效**的通則。**機械後果可算**：`decision-project --target 物件/派西.md` 會同時吐出 `:214`（含答案）與 `:222`（禁止再寫答案）——**存量無法回收，只能不再增加**。⇒ **這一列是「落點 1 而總量 3」的第一個實例**，而多出來的兩份**沒有任何工具能刪**。交階段 3：`decision-lint` exit 0，它驗七欄與標的存在性，**不驗「標的檔標著 `揭示層級: 水下` 而本列理由欄寫出了答案」**——而這正是 `:222` 那條通則要求的事，**它自己沒有守衛** |
| **9.**「**不得連兩枚徽章**」——任何人（含敘述者、含哈利）把他的級長徽章與五十年前那個十六歲的那一枚連起來提一次 | **1** | 唯一落點 `story/物件/派西.md:40`（排除線，射程全書）；同檔 `:21` 是它的散文理由（同檔＝一個落點）。`裁決流.md:214` 記理由（標的＝該物件檔·可投影·不計）。**全書其餘的「級長徽章」逐筆核過都不是這一條**：`大綱/arc08.md:30`（**瑞斗那一枚**，S2 baseline）／`大綱/arc06.md:50`（同）／`幕綱/arc01.md:34`（**另一條約束**，見第 10 列）。**誰會在它被違反的那一刻讀到它**：`fact-project --for-beat` 在任何含派西的幕無條件印 ⇒ **會**（實跑驗證見第 6 列）。⚠️ **但它擋不到另一半**：這條約束的違反可以發生在**瑞斗那一側**（arc06／arc08 有人把那一枚跟派西那一枚連起來），而那兩幕的角色欄不會有派西 ⇒ **`--for-beat` 導不出他 ⇒ 送不到**。這與 S7 第 16 列的「兩處都有洞」是同一個機制（實體驅動的投影只覆蓋在場者），**但這一次只有一份、不是四份**，所以本帳計 1 而不是記重複 |
| **10.**「**那枚級長徽章本 arc 不出現**——第一次讓讀者看見它要放在他做級長的事那個場合（辦法同海格那把傘）」 | **1** | 唯一落點 `story/幕綱/arc01.md:34`（承諾區負向一列，**射程綁 arc01**）。`裁決流.md:217` 記理由（標的＝`幕綱/arc01.md`·可投影·不計）。`beat-lint` exit 0（**29 條排除線**，S7 是 19）。**執行側可驗**：`grep -c "徽章" chapters/ch0004.md` ⇒ **0**。⭐ **這一列的形狀是對的而且有先例可循**（`物件/海格的雨傘.md`：arc01 可以在手上，第一次被看見是 arc02），**而它與第 9 列是同一個道具的兩條不同約束、住在兩支不同的檔、射程一條全書一條綁 arc**——**這是本帳第一次看到同一個物件的約束按射程正確分居兩軸**（全書的進物件檔、綁 arc 的進承諾區），沒有互相複製 |
| **11.**「**弗雷與喬治不得互相接話成段**（雙簧）／**不得拿『分不出誰是誰』當笑點**／幕006 零台詞不跟進棚子」；**且明令不建他們的角色檔或物件檔** | **1（射程綁 arc01，而那是它唯一的家）** | 唯一落點 `story/幕綱/arc01.md:29,30,31` 三條承諾區排除線（同檔＝一個落點）。`裁決流.md:207`／`:209`／`:208` 記理由（標的皆 `幕綱/arc01.md`·可投影·不計）。**沒有 `story/物件/弗雷.md`／`喬治.md`，也沒有 `設定/角色/`**（`ls` 實查；`object-lint` 22 支物件檔逐支核過無此二人）——作者明令不建。<br>⚠️ **兩支工具在這一格上同時靜默，而且都印對了**：<br>① **`fact-project --for-beat 幕006` 導出的實體是「哈利、榮恩、亞瑟、莫莉、金妮、派西、那輛車」——弗雷與喬治不在裡面**（本觀測者實跑），儘管 `幕綱/arc01.md:109` 的角色欄逐字寫著他們。原因是投影拿角色欄 token 去比對**檔名 registry**，沒有檔就導不出實體 ⇒ **這三條約束永遠不會走那條無條件送達的路。**<br>② **`char-lint` 的「角色欄 token registry 查無」門檻是「出現 ≥3 次」**，而 `弗雷`×2、`喬治`×2（幕005＋幕006）⇒ **落在門檻下，不印**。本輪它印的仍是 `嘿美`×7、`那輛車`×6。<br>⇒ **一條約束的射程綁在 arc01 的承諾區，而雙胞胎在 arc02 以後還會出場**（`大綱/arc02.md` 起）。**arc02 拆幕時會產一份新的承諾區，而沒有任何東西會把這三條帶過去。** 這與第 10 列的差別是關鍵：那一條**本來就只該綁 arc01**（第一次被看見的時機），這三條是**寫法紀律、射程本該是全書**，卻住在一個 arc 的容器裡。**交階段 3：`beat-lint` 不比對相鄰兩支 arc 的承諾區** |
| **12.**「幕006 那張桌子：**滿桌的人同時在講話，沒有一句是對哈利講的**；莫莉不停加東西卻沒問他一句話；**不准內心戲、不准比較**」 | **1** | 落點 `story/幕綱/arc01.md`（同檔兩處：`:114` 行動欄那兩個相鄰分句、`:32` 負向排除線）。`chapters/ch0004.md:109-149` 是**執行**（依約定⑤不計）。`裁決流.md:208`／`:210` 記理由（可投影·不計）。⭐ **本輪最好的一筆「不製造第二份」**：`幕綱/arc01.md:32` 那條排除線逐字寫「……敘述者也不得點明——**同 `設定/角色/莫莉.ai.md` 那條紀律（不准由任何人說出來，讀者自己算）**」——**它指過去、不重述**，而 `莫莉.ai.md:34`（「她從沒短缺過歸屬，所以她不知道一個坐在滿桌人中間的孩子也可能缺它」）是 S3 baseline、本輪一個字沒動、`derived-sync check` 報 fresh。⇒ **同一個紀律在兩支檔裡出現，其中一支是指標 ⇒ 1 個落點不是 2 個。** 對照第 5 列（同一輪、同兩支新檔、同樣可以指卻沒指），**差別在於這一次要指的東西已經有名字了**（`莫莉.ai.md` 的那條紀律有一句可引的話），第 5 列那句「異常不等於有罪」在被寫下的那一刻兩支檔是同時誕生的、誰都不是權威 |
| **13.**「**章末鉤是兩型不是一個強度刻度**」——`風格.md` 達標線 3 寫的是「1 個沒解開的物件**或** 1 句挑釁」，承諾區壓成「章末鉤強度拉滿」 | **2** | ①`story/設定/風格/風格.md:9`（達標線 3，**原始權威**，S1 落，本輪未動）②`story/幕綱/arc01.md:7`（承諾區，**本輪改措辭**：「章末不總結，一律停在未解物件或一句挑釁；強度拉滿的是挑釁型的那幾章，收在幕006 的那一章走物件型」）。`裁決流.md:219` 記理由——**作者明令兩處都寫**（逐字：「只寫本檔＝那條規矩到不了筆」），依約定③不另計。**無一是投影。** ⚠️ **機械症狀是「壓縮發生了一整個 session 而兩支閘門都印綠燈」**：`beat-lint` exit 0（驗幕號／前因／八欄／spine／分區）不讀承諾區的**語意**，`style-lint` exit 0（「5 個字面 entry……掃了 4 章正文·禁用詞命中 0」）只掃禁用詞、**不比對達標線與承諾區**。⇒ **`風格.md` 的達標線與 `幕綱/arcNN.md` 的承諾區之間沒有任何比對器**，而 `write` 只讀後者（`裁決流.md:219` 逐字：「`write` 不讀大綱、也不會來讀本檔，落筆依據就是承諾區與八欄」）。**與 S6 那條「同一個東西在四層之間被叫成兩個名字」同族**，但這一次**上游那一份是對的、下游那一份把它壓扁了**——落點數沒有變（一直是 2），**壞的是兩份之間的保真度，而本帳的落點欄看不見這件事**。交階段 3：這是「N 份互相打架」（S6）與「N 份一模一樣」（S3–S5）之外的第三種 N |
| **14.**「**不得把 `可用句式` 常態段的數字（20–30 字／2–3 分句）往下調到實測值去遷就現況**；要收斂只能往上收。另一半（寫回 20–30 還是重切恐怖檔）本輪不拍」 | **1** | 唯一落點 `story/參照/裁決流.md:220`（標的＝`設定/風格/風格.md`）——依約定③，這是它唯一的家 ⇒ 計 1。**`設定/風格/風格.md` 本輪一個字沒動**（不在 `git status` 異動清單裡），`風格.ai.md` 亦未重生（`derived-sync check` 報 fresh、22 個衍生檔 0 需處理）。未拍的另一半落 `待裁決.md`（本輪新增列，標的＝`設定/風格/風格.md`，`decision-lint` 驗得到標的存在；`decision-project --target 設定/風格/風格.md` 實跑 ⇒ 命中 45 列裁決＋**待裁決 2 列**）。⚠️ **這條規則保護的是一支它自己不住在裡面的檔**：下一個要動 `可用句式` 那三個數字的人，讀的是 `風格.md`（源）或 `風格.ai.md`（衍生，`style-lint` 印「3 個可用句式檔位·0 個無顆粒度 token·0 個無數字」），**兩支都沒有這句話**。撈得到（`decision-project`），但要有人去撈。**與 S7 第 8 列（「這條推導只准住分析檔」而排除線表沒有「只准住某類檔」欄位）同族**：約束的形狀與容器的形狀不匹配 |
| **15.** 通則：「**限射程一律用幕、不用章號**——章號是 `write` 下游提的、會隨分章方案改，把上游的幕綱釘在下游的章號上是反向依賴」 | **0** | **機械症狀（本觀測者實跑）**：這條通則**夾在 `裁決流.md:219` 的理由欄末尾**（「**附帶釘一格**：限射程一律用幕、不用章號……」），而該列的**標的欄寫的是 `幕綱/arc01.md`**、射程欄寫 `全書`。⇒ 模擬下一次拆幕：`decision-project --book <書> --target "幕綱/arc02.md" --active-only` ⇒ exit 0／掃描 194 列·**命中 42 列**，而 `grep -c "章號"` ⇒ **0 命中**。對照組 `--target "設定/風格/風格.md"`／`--target "參照/待裁決.md"` ⇒ 同樣 **0 命中**。**三支不同的 target，這條射程全書的通則一次都沒有出現。**<br>⚠️ **這是 S7 第 17 列那個形狀的第二次命中，而且更貴**：S7 那一條是「建新世界觀檔要先畫界線」（下一次生效還早），**這一條下一次生效的時機是 arc02 拆幕，而那正是台帳排的下一批工作**。<br>⭐ **同一輪有正面對照組（第 2 列）**：`:221` 那條通則標的欄寫 `全書`，三個 target 全撈得到；`:219` 這條寫具體檔，三個 target 全撈不到。**兩列由同一支受測體在同一次 append 裡寫下，相隔兩行。** ⇒ **不是能力問題，是「標的欄該寫什麼」沒有任何規則、也沒有任何閘門**。`decision-lint` exit 0（194 列／標的 45 個·**0 個查無**）：它驗標的**存不存在**，**不驗標的與射程矛不矛盾**。<br>**S7 第 1 條形狀建議的最小處置本輪原樣重提，而且有了第二個實例**：`decision-lint` 加一項——**射程欄寫 `全書`、而標的欄寫一支具體檔的列，印一行提示**（本輪 17 列新增裡有 **14 列**是這個形狀，其中至少本列與第 8 列的通則性質是明確的） |
| **16.**「`待裁決.md` 檔頭那句『**尚未產出**』是假話——**刪，只刪那一句，什麼都不要補回去**；不准換成『本檔現有 3 列』（那是投影）；**檔頭 blockquote 從今天起只准減不准加**」 | **0** | 前半（刪那兩行）已執行：`git diff` 顯示 `story/參照/待裁決.md` **−2 行**，`decision-lint` exit 0（待裁決 3 列）。**但後半那條通則沒有任何落點**：`grep -rn "只准減\|不准加\|檔頭" 消失的密室/story/參照/` ⇒ **0 命中**；`grep -rn "只准減\|不准加" 消失的密室/` 全書 ⇒ **0 命中**；`decision-project` 三個 target 實跑 ⇒ **0 命中**。⇒ **一條射程是「所有源檔的檔頭 blockquote」的落檔紀律，本輪只活在對話與 `S8-*.md` 第二節裡。** ⚠️ **這是 S1 第 1 列那個病的第八個 session 復發**（「拍板了只活在對話裡」），但這一次的內容**不是故事事實、是落檔紀律**——而落檔紀律沒有任何一支 schema 收它：`結構定義/待裁決.schema.md` 定義的是欄位，不定義檔頭 blockquote 該長怎樣。`derived-sync validate` exit 0（22 支 `.ai.md`／63 個 `##` 節／130 個鍵·0 個 schema 外）——**它的射程是 `.ai.md`，而 `待裁決.md` 是源檔**。**與第 3 列的對照要講清楚**：那一個 0 是作者裁的（他要求那三格什麼都不記），**這一個 0 是他要的東西沒有容器**（他明確要一條往後生效的紀律） |
| **17.**「**不替嘿美建 `設定/角色/` 檔**去消 `char-lint` 那份 registry 查無名單」（駁回一個會被重提的提案） | **1** | 唯一落點 `裁決流.md:218`，**標的欄寫 `全書`** ⇒ `decision-project` 任何 target 都撈得到（本觀測者實跑三個 target，3/3 命中）。`char-lint` exit 1 第二個問題本輪原樣重印：**`嘿美`×7、`那輛車`×6**，與 S6／S7 完全相同。⭐ **這一列是本帳第一次看到「駁回被主動落檔以防止重新提案」，而理由欄自己說出了機制**：「`char-lint` 每跑一次就會再報一次，而**沒落檔的駁回下次一定會被重新提案**」。**與 S7 第 14 列（「不認領那輛車」那句防重工的話住在會被重生的 `.ai.md` 裡）恰好互為正反面**：同一個需求（防止下一輪重提），一個住不重生的源、一個住會被沖掉的衍生 |
| **18.**「`write-test` **綁成下一次開工的第一件事**，範圍 ch0001–ch0004，跑完裁完之前**不准動 ch0005**」 | **0** | **機械症狀**：`grep -rn "ch0005\|write-test" 消失的密室/story/參照/` ⇒ 裁決流無此列、待裁決無此列；`story/參照/就緒.md` 本輪**未被修改**（不在 `git status` 異動清單裡，`readiness-lint` exit 0／狀態表 20 格·就緒清單 6 條·5 條已勾，與 S7 完全相同）。唯一的機讀痕跡是 **`ch-lint` 的一行資訊**：「**3/4 支章衍生有 `write-test` 紀錄**（格式不合 0 支·**缺紀錄不計入問題數**）」——**它看得見 ch0004 缺紀錄，但它明說這不算問題，也不知道作者已經下了「跑完之前不准動 ch0005」這道令。** ⇒ **一個帶順序約束的工作排程，在書內沒有容器**：`就緒.md` 記的是「哪一層就緒了」不是「下一步不准做什麼」，`待裁決.md` 記的是「根源在上游的觀察」。**這一列與 S6「射程是下一個 session」那一種落點 0 同族，但更硬**：那一種是「這一輪還用不到」，這一種是**已經生效、而且會擋住下一輪的第一個動作**。**它現在只活在 `情境測試/session台帳.md`——測試 meta 裡，系統看不到。** 交階段 3 |
| **19.**「`settings-select` 那個工具缺口（把『還沒做』與『已裁定永遠不做』印成同一行、處方寫死跑 `character`）**不記進 `待裁決.md`**」——三條理由，並拆成 A／B 兩格交階段 3 | **0 —— 而這也是裁定的結果** | **機械症狀**：`grep -rn "settings-select\|settings_select" 消失的密室/` ⇒ **0 命中**；`待裁決.md` 本輪淨 3 列，逐列核過無此筆（`decision-lint` exit 0）。**作者的三條理由都可覆算**：①`待裁決.schema.md` 的 `標的` 欄要求是**書內源檔路徑**，`tools/settings_select/src/settings_select/select.py:285-297` 不是書內檔 ⇒ 硬寫就是說謊（`decision-lint` 驗「標的 45 個·**0 個在書內找不到**」，寫進去會當場變成 1 個查無）；②決定權在系統維護者不在作者；③**書內那一半早就登記了**——`裁決流.md:36` 與 `:215` 兩列都寫著「金妮／派西不生 `.ai.md` 是裁定」，`decision-project --target 設定/角色/派西/核心.md` 撈得到。⇒ **這個 0 是正確的**：需求本身（「別讓工具的處方把我剛關上的門打開」）在書內的那一半有家，工具側那一半**依定義不該有書內的家**。⚠️ **但它暴露了一件本帳量不到的事**：**這套系統沒有任何一個地方可以記「工具本身的缺口」**——`待裁決.md` 的射程是書內源檔，`裁決流.md` 的標的欄同理。本輪它落在 `S8-*.md` 摩擦欄（測試 meta，系統看不到）。**這與第 18 列是同一個形狀的兩個面：一個是排程、一個是工具缺口，兩者都沒有書內容器，而兩者都是真需求。** |

**本 session 的形狀**：

1. **落點 0 第一次分成兩種，而區分它們的是「`git diff` 有沒有東西」。** 5 個 0 裡，第 3、19 列的 `git diff` **有**（裁決流那一列就是這個 0 的家，而那一列逐格寫了為什麼是 0）；第 15、16、18 列的 `git diff` **沒有**（通則夾在別人的理由欄裡／落檔紀律無容器／工作排程無容器）。**前七個 session 的落點 0 全部屬於後一種**，所以本帳的判定規則第 1 條（「作者答了但根本沒落檔」）一直夠用；**S8 起不夠了**。可機械化的最小處置：本帳的落點欄改成兩個值——**「這個需求有沒有家」與「這個需求在某個特定容器裡的正確存量」**，而後者可以是刻意的 0。
2. **`裁決流.md` 的標的欄是本輪唯一一個決定「通則撈不撈得到」的欄位，而它沒有規則、沒有閘門、沒有提示。** 第 2 列與第 15 列是同一支 agent 在同一次 append 裡寫的兩條全書通則，一條寫 `全書`（三個 target 全撈得到）、一條寫 `幕綱/arc01.md`（三個 target 全撈不到）。**S7 第 17 列已經交過這一格，本輪它復發、而且下一次生效的時機就在下一輪。** 17 列新增裡有 14 列是「射程全書 ＋ 標的具體檔」的形狀——**大多數是對的**（那些列講的確實是某一支檔的事），所以閘門不能報錯，只能印提示；**但提示要印在哪一格，本帳現在有兩個實例可以標定了。**
3. **本輪的機制重複全部長在「新檔剛誕生的那一刻」。** 第 5 列（`物件/派西.md` 與 `核心.md` 各寫一份「異常不等於有罪」）是唯一一筆真重複，而它發生在兩支檔**同時**被建立的那一輪——**沒有一支是另一支的權威，所以指標語法無從用起**。對照第 6、12 列：排除線指得過去（規則表有約束名）、莫莉那條指得過去（`.ai.md` 有一句可引的話），**而「兩支新檔同時說同一句話」在本系統沒有解法**。這與 S3 第 2 列（`物件/分帽子那天.md:11` 明寫「權威在 `水下.md`，本檔不複述」）的差別是：那一次權威**早就存在**。
4. **12 支閘門本輪抓出來的仍然是 1 列，而且仍然是同一列。** 19 列裡唯一被閘門點名的是 `char-lint` exit 1 的兩個問題，而**兩個都不是本輪造成的**：第一個（源無衍生 16→17）是第 7 列**裁定的帳單**、第二個（`嘿美`×7／`那輛車`×6）是第 17 列**明文駁回去消的誤報**。**五筆落點 0、一筆機制重複、一筆保真度損失（第 13 列）、一筆不可回收的洩漏（第 8 列）、一筆射程綁錯容器（第 11 列），12 支閘門全部沉默且全部印對。**

> **12 支閘門本輪讀數**（觀測者端獨立覆算，取真 exit code＝`out=$(cmd 2>&1); code=$?`，**未用管線**；本帳只引用機械輸出）：`outline-lint` exit 0（8 支 scoped／枚舉外節 0／**檔內路徑引用 76 個·0 個不存在**／`## 選用結構公式` 8 支宣告·registry 查無 0）／`beat-lint` exit 0（1 支 arc／15 幕／14 條前因／6 個伏筆標記（6 名）／6 列狀態表／1 個承諾區／**29 條排除線**（S7 19）／**22 支物件檔**／17 筆檔內路徑·目的地不存在 0／1/1 支 arc 有 `beat-test` 紀錄；提示：2/6 個伏筆名沒有物件檔·不計入問題）／`ch-lint` exit 0（**4 支正文源／5 個幕錨點／1 個過渡錨點／4 支章衍生**；非標準錨點寫法 0 筆；**3/4 支章衍生有 `write-test` 紀錄**·缺紀錄不計入問題數；資訊：9/15 幕還沒寫成正文）／`decision-lint` exit 0（**裁決流 194 列**／待裁決 3 列／**標的 45 個·0 個查無**／`已升為通則` 0 列）／`object-lint` exit 0／`fact-lint` exit 0（**5 支章 delta／25 筆事實行／22 支物件檔**；3 支物件檔印「揭示點待落幕」資訊，含**本輪新建的 `物件/派西.md`**）／`char-lint` **exit 1（2 個問題）**：①**17 支源沒有衍生檔**（**25 支源·5 支目錄形態**／8 支 `<名>.ai.md`；S7 是 24／4／8，**本輪 +1／+1／0**）②幕綱角色欄 2 個 token registry 查無且出現 ≥3 次：**嘿美×7、那輛車×6**（S6 起既有，作者本輪明文駁回去消它，見第 17 列）；四節共 **139 個 slot·0 個是佔位**（與 S7 相同——派西不生 `.ai.md`，不進這個分母）；幕綱角色欄 15 個·括號外 **72 個 token（28 個 registry 查無·其中 2 個 ≥3 次）**／`style-lint` exit 0（1 支 `風格.ai.md`／5 個語意欄·0 個是空的／3 個可用句式檔位·0 個無數字／**掃了 4 章正文·禁用詞命中 0 次**）／`summary-lint` exit 0／`world-lint` exit 0（8 支 `<主題>.ai.md`／0 個伏筆名查無·registry ＝幕綱 7 個標記名 ∪ **物件檔 22 支**／源檔第一段 0/8 支是空的）／`readiness-lint` exit 0（2 個 `##` 節／狀態表 20 格·0 格不是五 token 之一／就緒清單 6 條·5 條已勾——**與 S7 完全相同，本輪未動**）／`derived-sync check` exit 0（**22 個衍生檔·0 個需處理**；成長哨兵掃描範圍 幕綱 1／設定層源 **34**／設定層衍生 18／**章衍生 4**／大綱 9／參照 2／摘要 1／幕綱索引 1／**正文源 4**·刻意豁免／raw 6·刻意豁免；行長 **73 支**；**事實行 5 支檔·25 行**；搬移目的地 3 個·0 個不存在；**（無）**）／`derived-sync validate` exit 0（**22 支 `.ai.md`／63 個 `##` 節／130 個鍵·0 個是 schema 外的**；裁決 blockquote 0 支命中）。
> **與 S7 收工讀數的 diff（全部可由本輪 19 列解釋，無不明漂移）**：物件檔 21→22（第 6 列）／衍生檔 21→22·章衍生 3→4·正文源 3→4·事實行 4 支 17 行→5 支 25 行（`ch0004` 那一對）／裁決流 177→194（17 列 append）／標的 43→45／承諾區排除線 19→29（第 1、10、11、12 列）／角色源 24→25·目錄形態 4→5·源無衍生 16→17（第 7 列）／設定層源 33→34（派西兩支源檔算一支實體）／行長 71→73。**`char-lint` 的 exit code 與問題數（1／2 個）開場收工完全相同，`readiness-lint`／`summary-lint`／`world-lint`／`style-lint` 的每一個數字與 S7 相同（除 style-lint 掃描章數 3→4）。**
> **三格交給 S9 的觀測者**：⑴ **第 15 列那條通則的下一次生效時機是 arc02 拆幕**——S9 若跑 `beat-sheet arc02`，請直接驗「承諾區或幕綱裡有沒有出現章號字面」，那是這條落點 0 的第一個可觀測後果。⑵ **第 11 列同理**：arc02 的承諾區會不會自己長出「弗雷喬治不得雙簧」這三條，是「射程綁 arc01 的寫法紀律會不會遺失」的直接讀數。⑶ **S7 交辦的「掃全書 `<X>.ai.md` 的節、抽名詞短語去 `grep` 源檔」本輪仍未做**，而衍生檔已經從 21 長到 22；**派西那一支（不生 `.ai.md`）可當該掃描的陰性對照。**

---

## S9（write-test ch0001–ch0004 四章合審 → revise）— 本 session 新增 17 列（落點 0：3／落點 1：9／落點 N：5）

```
需求機制帳：本 session 新增 17 列（落點 0：3／落點 1：9／落點 N：5）
```

> 產物範圍 ＝ `git diff 5636530` 在 `消失的密室/` 的 **18 支已追蹤檔·56 行新增／28 行刪除·0 支新檔**，合計 **20,015 字元／11,291 漢字**。baseline ＝ `5636530`（HEAD，本 session 尚未 commit）。`裁決流.md` 195 → **211 列**（append **16 列**，`:223`–`:238`，全部 08-03）／`待裁決.md` 4 → **5 列**（刪 1 加 2）／承諾區排除線 29 → **30 條**／物件檔約束 51 → **53 條**／幕綱檔內路徑 17 → **19 筆**／章衍生 `write-test` 紀錄 **3/4 → 4/4**／物件檔 22 支·衍生檔 22 支·角色源 25 支·`char-lint` 源無衍生 17 支——**後四項與 S8 完全相同，本輪一支檔都沒新增。**
> **⚠️ 三處讀數更正（機械可覆算）**：①`S9-*.md` 產物節寫「`裁決流` 194 → 207 列（＋13）」，實際 `decision-lint` 印 **210 列**、`grep -c "^| 2026-"` 得 **211**、`grep -c "^| 2026-08-03"` 得 **16** ⇒ **+16 不是 +13**（差的 3 列是第一批用 `Edit` 落的，導演只數了兩支 append 腳本的 9＋4）。②`S8-*.md:313` 記 `ch-lint`「5 個幕錨點／1 個過渡錨點」，而 `git show bf4f848`／`5da2cb3`／`5636530` 三個 commit 逐支數 `<!-- 幕` 都是 **8 個**（幕001–006 六個 ＋ `幕003→幕004`、`幕004→幕005` 兩個過渡），本輪 `ch-lint` 印 **6／2**，而**本 session 的 diff 一個錨點都沒動** ⇒ **S8 那個數字在寫下時就是錯的**。③ 兩處都不影響任何判定，但記進來——**本帳的「與上輪 diff 全部可解釋」紀律要求上輪的數字也是真的。**
> **計數約定沿用 S4–S8，逐條複述以免歧義**：①**一支檔算一個落點**；②**投影不計**（`*-lint --emit`／`fact-project`／`decision-project`／`readiness`／`structure-project`）；③**`裁決流.md` 記「為什麼」，在該值另有落點時計為「可投影·不計」，只有當它是唯一的家時計 1**；④**源＋其 hash 同步衍生計 1（＋衍生），不計為拷貝**；⑤**執行／渲染不算宣告**（正文寫出來、幕綱行動欄演出來，不算一份規則）。
> **本輪的歸屬前提比前八輪弱一格，而弱在哪裡是機械可測的**：四支神諭寫入型 2 次（**全在 repo 外的共用 scratchpad**）、導演 5 次寫入 5/5 在 `情境測試/` ⇒ **56 行新增全部由受測體按鍵**；**但 6 行正文的 108 個 10 字視窗 108/108（100%）逐字出自神諭自己的 assistant 訊息**（R2 `16:39:33Z` 四句、R4 `17:17:45Z` 兩句），規則層 18,219 個視窗只有 23% 是。⇒ **本輪計的是「受測體把作者的裁決存到了哪幾支檔」，不是「受測體想出了什麼」**——這一輪本來就是裁決落地輪，形狀正確，但下一輪若沿用「`git diff` ＝產物」的直覺會誤讀。
> **本 session 的形狀是第九種。** S1「沒有欄位可存」／S2「跨檔排定值沒有比對器」／S3「第一次量到機制在被使用」／S4「為讓機制生效而刻意留一份帶拆除日期的重複」／S5「投影的輸入到位了，照到的東西比預期少」／S6「需求的家存在、規格也對，而那個家開不了門」／S7「診斷正確、處置是 append 而不是搬家」／S8「落點 0 第一次是作者刻意裁出來的」；**S9 第一次出現「落點在、送不到」**——本輪 9 個「落點 1」裡有 **5 個的唯一的家是 `裁決流.md`**（第 1 列正向半、第 4、7、12、14 列），而**逐支核過 12 支 SKILL.md，只有 5 支跑 `decision-project`（`beat-sheet`／`character`／`develop`／`sync`／`worldbuild`），正文管線三支（`write`／`revise`／`write-test`）一支都沒有**。⇒ **這一輪 append 的 16 列裁決，有 5 條的生效時機是下一支 `write`，而 `write` 結構上讀不到它們。**

| 作者說的需求 | 系統把它存到哪（落點數 0／1／N） | 證據 |
|---|---|---|
| **1.**「**哈利的內心一律寫成『算式』，不寫成『感受』**——他算階梯、算誰會看見他、算門在哪個方向；不覺得害怕、不感到孤獨、不終於明白。範本 `chapters/ch0002.md:97`」 | **1（兩個半各一個家，無重複；但兩半的送達路徑不同）** | **負向半**（不得寫成帶過濾詞／情緒標籤的內心句）＝ `story/物件/全書.md:31` 的排除線一列，型別 `方針`、射程 `全書` ⇒ **`fact-project --for-beat` 對任何一幕無條件印書級方針**（`fact-lint` exit 0：22 支物件檔／**53 條約束**，S8 是 51）。**正向半**（「寫成算式」＋範本行號）＝ **`grep -rn "算式" 消失的密室/` ⇒ 全書只有 `裁決流.md:224` 一處**，依約定③它是唯一的家 ⇒ 計 1。<br>⚠️ **兩半的下游可及性差一個數量級，而這是機械的**：負向半走 `fact-project`（`write/SKILL.md:75` 明文要求跑）；正向半只走 `decision-project`，而 **`grep -rln "decision-project" .claude/skills/` ⇒ 5 支 SKILL.md 命中，`write`／`revise`／`write-test` 都不在裡面**。⇒ **下一支 `write` 會被告知「不要寫成感受」，不會被告知「要寫成算式」，也拿不到範本行號。** ⭐ **與 S8 第 14 列（「這條規則保護的是一支它自己不住在裡面的檔」）同族但更精確**：那一次是「撈得到、要有人去撈」，這一次是**撈的人依它自己的 SKILL.md 不會去撈**。⚠️ 另記：`:224` 的**標的欄寫 `幕綱/arc01.md`**，而該檔為這一條**一個字沒加**（`grep -c "算式" story/幕綱/arc01.md` ⇒ 0）——同 S8 第 15 列的形狀 |
| **2.**「**喜劇引文段的 35 是量洛哈量出來的**：洛哈不設上限、其他角色每 arc 一段（同角色全書上限兩段）、**arc01 兩格已用滿（威農＋亞瑟），幕011–015 不得再有第三段**」 | **N ＝ 3** | ①`story/物件/全書.md:32`（書級約束 `喜劇引文段每 arc 一段`，射程全書）②`story/幕綱/arc01.md:55`（承諾區「不得發生」的計數器一列，射程 arc01）③`story/設定/角色/洛哈.md:33`（例外的權威：「他不設上限」）。`裁決流.md:225` 記理由（標的＝`設定/角色/洛哈.md`·可投影·不計）。**無一是投影**；`洛哈.ai.md` 本輪只換 `generated-from`／`generated-at`（`git diff` 逐行實證，**0 行內容拷貝**），依約定④不計。<br>⚠️ **「洛哈不設上限」這一個值出現在三支檔，而這是本帳的第三種重複形狀**：`全書.md:32` 逐字「（洛哈不設上限，見 `設定/角色/洛哈.md`）」、`幕綱:55` 逐字「洛哈不佔格（…不設上限，見 `設定/角色/洛哈.md`）」——**兩處都同時「複述結論」與「指出權威」**。對照 S8 第 5 列（`物件/派西.md` 與 `核心.md` 各寫一份「異常不等於有罪」，**複述、不指**）與 S8 第 12 列（`幕綱:32` 指 `莫莉.ai.md`，**指、不複述**）。⇒ **本輪這一種是中間態：漂移仍會發生（三處要各改一次），但漂移發生時讀的人查得到誰是權威。** 沒有任何閘門在守這一格——`object-lint`／`beat-lint`／`char-lint` 三支本輪全綠 |
| **3.**「**洛哈的每一段引文必須整段『不可兌現』**——威農那段的每一句第二天都真的發生了，洛哈那段一句都不會有後續；長度打平時兌現率替我做分級」 | **1** | 唯一落點 `story/設定/角色/洛哈.md:29`（`## 寫法` 節新增三段，`git diff` +6 行／656 字元）。`裁決流.md:225` 同一列記理由（可投影·不計）。**`grep -rn "不可兌現" 消失的密室/` ⇒ 2 命中：`洛哈.md:29` ＋ `裁決流.md:225`** ⇒ 依約定③計 1。⭐ **本輪最乾淨的一筆**：源檔改 → `derived-sync stamp` 三次（受測體 #67／#74／#110）→ `check` 最終 exit 0（**22 個衍生檔·0 個需處理**）→ `.ai.md` 只換 hash、不抄內容。**與第 2 列是同一輪同一支檔的兩條規則，一條 1 一條 3，差別只在「這條規則要不要在別的容器裡被計數」** |
| **4.**「**ch0005 起 `才` 上限兩個，且不得有兩個相鄰段落同時收在 `才`**」（本章五個 `才` 記成刻意的，不立段尾規矩） | **1 —— 而它的生效時機是下一支 `write`，而 `write` 讀不到它** | **機械症狀（本觀測者實查）**：`grep -rn "上限兩個|相鄰段落" 消失的密室/` ⇒ **全書只有 `裁決流.md:235` 一處**。該列**標的欄寫 `幕綱/arc01.md`**，而 `story/幕綱/arc01.md` 為這一條**一個字沒加**（`git diff` 該檔 6 行新增逐行核過：`:7` 溫度極性、`:8` 釋放累積軸、`:55` 喜劇引文段、`:32` 女貞路換字、`:148`／`:159` 街名）。<br>⚠️ **這是本輪「落點在、送不到」最硬的一格**：ch0005 就是下一個 session 的第一件事（`session台帳.md` 已排），而 `write/SKILL.md` 逐行核過**只跑 `settings-select`（:66）與 `fact-project --for-beat`（:75）**，**沒有任何一步跑 `decision-project`、也沒有一步讀 `裁決流.md`**（它對 `裁決流` 的提及全部是**寫入方向**：`:122` 說「裁決理由屬 `裁決流.md`」，`revise/SKILL.md:46` 步驟 7 說「落裁決流」）。⇒ **這條規矩要生效，唯一的路徑是有人在下一輪手動提醒。** ⭐ **與 S8 第 18 列（「跑完之前不准動 ch0005」只活在測試 meta）互補**：那一條連書內的家都沒有，這一條有家而讀的人不會去 |
| **5.**「**章末型別不設配比，改守溫度極性**——本 arc 只有收在幕006（他很晚才回來）的那一章章末不帶威脅，其餘各章末句都留一個要付的代價」 | **1** | 唯一落點 `story/幕綱/arc01.md:7`（承諾區第一列第二個分句重寫，`git diff` 該行 −1／+1）。`裁決流.md:226` 記理由（標的＝`幕綱/arc01.md`·可投影·不計）。`beat-lint` exit 0（1 支 arc／15 幕／**30 條排除線**／1 個承諾區）。**正文一個字沒動**（四章章末句在 `git diff` 0 命中）。⭐ **這一列是 S8 第 13 列的結清**：那一列記「承諾區把 `風格.md` 達標線 3 的兩型壓成一個強度刻度，而沒有任何比對器」，**本輪作者實查後判定那個配比「從頭到尾沒有上游依據，而且它一長出來就已經與 ch0001／ch0002 牴觸」**——`風格.md` 達標線 3 逐字是「1 個沒解開的物件**或** 1 句挑釁」、`00-摘要.ai.md:39` 同樣不含配比。⇒ **落點數沒變（一直是 1，`風格.md` 那一份是不同層級的權威），修的是保真度。** ⚠️ **而閘門依然沉默**：`beat-lint` 驗幕號／前因／八欄／spine／分區，**不讀承諾區的語意**；`style-lint` exit 0（掃 4 章·禁用詞命中 0）不比對達標線與承諾區。**S8 第 13 列交的「沒有比對器」本輪原樣成立，只是這一次錯的那一份被人工修掉了** |
| **6.**「**篇幅分配走『釋放／累積』軸，不走『動作／日常』軸**；釋放幕壓薄；**幕012–014 表面是動作、本質是累積，不得拿幕004 當篇幅先例**」 | **1** | 唯一落點 `story/幕綱/arc01.md:8`（承諾區新增一列，839 字元中的主要部分）。`裁決流.md:230` 記理由（標的＝`幕綱/arc01.md`·可投影·不計）。`grep -rn "釋放／累積|釋放幕壓薄" 消失的密室/` ⇒ 2 命中（幕綱 ＋ 裁決流）。⭐ **本輪唯一一個「作者主動換軸而不是接受 AI 的軸」的落點**：受測體提的是「動作幕壓薄」，作者換成「釋放幕壓薄」，理由逐字「否則幕012–014 會被 ch0003 帶著一起壓薄」。**射程綁 arc01，而它是一條寫法紀律**——**與 S8 第 11 列（弗雷喬治雙簧三條紀律住在 arc01 承諾區）同一個形狀**：arc02 拆幕時會產一份新的承諾區，**沒有任何東西會把這一條帶過去**，而 `beat-lint` 仍不比對相鄰兩支 arc 的承諾區 |
| **7.**「**紀律：每次 `write` 落章封章之後，順手看一眼 `就緒.md` 那五列**」（拒絕替它加反向檢查器：要先定義「幾章算草擬」，那是判斷不是機械） | **1 —— 而紀律綁的那個動作在系統層，本輪不動** | 唯一落點 `裁決流.md:238`（標的＝`參照/就緒.md`）。**`grep -rn "封章後|順手看一眼" 消失的密室/` ⇒ 只有這一處**；`story/參照/就緒.md` 本輪**未被修改**（不在 `git status` 異動清單）。<br>**機械症狀（本觀測者實跑）**：`git log --oneline -- 消失的密室/story/參照/就緒.md` ⇒ 最後一次是 **`bfabe2a`（2026-08-01，S5 拆幕）**，此後 S6／S7／S8／S9 **四個 session 沒動**；`readiness-lint` exit 0（狀態表 20 格·0 格不是五 token 之一／就緒清單 6 條·5 條已勾——**與 S7、S8 逐字相同**）；`readiness` 投影逐行印 **`結構／正文　作者＝空白　｜ 未接 ch-lint`**，而 `chapters/` 裡躺著四章、`ch-lint` exit 0（**4 章正文源／6 個幕錨點／2 個過渡錨點／4 支章衍生**）。<br>⚠️ **「未接」那三個字是 E2 最糟那一格的字面實現**：`readiness` 自己印出「這一格我沒有接上機械前哨」，而作者判斷欄寫著 `空白`——**兩邊都沒有說謊，合起來就是一句謊**。作者本輪明確**拒絕**加反向檢查器並改立紀律，**而紀律的真正落點是 `write/SKILL.md` 的收尾步驟（系統層），本輪不動** ⇒ **這一列買到的是「查得到」，買不到「會自動發生」**（`S9-*.md` 摩擦欄第 5 條已記，本帳從落點側再記一次，並補上「四個 session」這個數字） |
| **8.**「**拿 `prose-metrics` 的 `句長中位` 去對 `可用句式` 宣告的推論一律無效**——兩邊語料不同。工具那一半交套件擁有者，不是作者」 | **2** | ①`story/參照/裁決流.md:223`（書內那一半：量法紀律 ＋ 刪 `待裁決` 舊列 ＋ 替 `:220` 的「另一半」結案，標的＝`設定/風格/風格.md`）②`story/參照/待裁決.md:20`（工具那一半，**標的欄寫 `全書`**，四欄無狀態欄）。`decision-lint` exit 0（裁決流 210 列／**待裁決 4 列**／標的 45 個·**0 個查無**）。**`設定/風格/風格.md` 本輪一個字沒動**（不在 `git status` 異動清單），`風格.ai.md` 亦未重生（`derived-sync check` exit 0·0 個需處理）。<br>⭐ **這一列是 S8 第 19 列的反例，而兩列的判準其實一致**：S8 作者裁「`settings-select` 的工具缺口**不記進** `待裁決.md`」，理由是「`待裁決.schema.md` 的 `標的` 欄要求書內源檔路徑」；**S9 同一個作者裁「記」**，而**差別是這一次書內有一半**——`風格.md:50` 的宣告語料與 `rhythm.py:305-309` 的量法語料不同，前者是書內檔，所以標的可以誠實地寫 `全書`。⇒ **兩列合起來給出一條可實作的判準：工具缺口進不進 `待裁決.md`，看它有沒有一端落在書內源檔上。** 這條判準目前不在 `待裁決.schema.md` 裡（`decision-lint` 只驗標的存不存在），交階段 3 |
| **9.**「**街名統一成『女貞路』，以正文為準**；`raw/名字與人物.md` 那個舊名就地劃掉留形」 | **N ＝ 2** | ①`消失的密室/raw/名字與人物.md:21`（`~~水蠟樹街~~**女貞路**四號`，**改的是作者自己的源檔**）②`story/幕綱/arc01.md:32`（承諾區排除線引用的街名換字）。`chapters/ch0003.md:99` 是既有正文（依約定⑤不計）。`裁決流.md:228` 記理由（標的＝`全書`）。<br>⚠️ **這一列不只是重複，它是本測試第一個「系統的發明被回寫進作者的源檔」**：`git log -S"女貞路" -- 消失的密室/` ⇒ 最早 **`416ab05`（2026-08-01，S6 `write` 第一批正文）**，而 `raw/名字與人物.md` 從 P session 起寫的是 `水蠟樹街`。⇒ **上游被下游改寫，而改寫它的那個值是 S6 的系統產物。** 作者拍板的理由（「名字的工作是被讀到」「那是全書唯一一次那條街被命名」）是對的，**但這條路徑本身沒有任何守衛**：`derived-sync` 只看 `.md → .ai.md` 的方向，`raw/` 依功能 13 拍板**零 lint、成長哨兵刻意豁免** ⇒ **一個 skill 修改 `raw/` 這件事，全系統沒有任何一支工具會印一行。** 交階段 3（purity 側的同一格見 `S9-*.md` purity 節） |
| **10.**「**街名定為『斜角巷』，廢『對角巷』**——這次抓在正文之前」 | **N ＝ 3（2 支檔 3 處）** | ①`story/大綱/arc01.md:97` ②`story/幕綱/arc01.md:148` ③`:159`（同檔兩處依約定①算一個落點 ⇒ 落點數 2 支檔，3 處要各改一次）。`raw/名字與人物.md:21` baseline 就寫 `斜角巷`（**未動**）。`裁決流.md:236` 記理由（標的＝`全書`）。**收工核對**：`grep -rn "對角巷" --include="*.md" 消失的密室/` ⇒ **0**（受測體 #128 實跑）。<br>⭐ **這一列與第 9 列是同一輪、同一支 agent、同一種操作，方向相反，而這個對照組是機械的**：`斜角巷` 在 `raw/`（作者，P session）有、在答案卷有（2 章 7 次）；`對角巷` 是系統 `3435b69`（S2 `organize`）寫進 `大綱/arc01.md` 的發明、答案卷 0 次。⇒ **一條把系統的發明升成源（第 9 列），一條把系統的發明降回作者的詞（本列），而兩次的裁判都是作者、沒有一支閘門參與。** ⚠️ **兩條街名合起來給出一個可機械化的缺口**：`raw/名字與人物.md` 是一份專名清單，而**沒有任何工具拿它去比對下游產物**——`char-lint` 比對的是幕綱角色欄 vs 角色檔名 registry，`world-lint` 比對的是伏筆名，**地名／店名／道具名沒有 registry** |
| **11.**「**幕號可以當機讀 key 與帳本主鍵，不可以當散文座標**」（`:175` 射程收窄，append 往前生效；三處散文座標改成「幕NNN（事件名）」雙寫） | **N ＝ 3** | ①`story/物件/嘿美.md:14`（換成事件名：「幕011」→「推不動那道牆的那一場」）②`story/物件/派西.md:11` ③`story/設定/角色/派西/核心.md:24`（後兩處是雙寫「幕006（他很晚才回來）」）。`裁決流.md:234` ＋ `:237` 記理由與更正（標的皆 `全書`·可投影·不計）。`beat-lint` exit 0（**19 筆檔內書內路徑·目的地不存在 0**，S8 是 17 筆）。<br>⚠️ **②③ 是 S8 第 5 列那筆重複的第一張帳單，而它是本帳第一次量到重複的維護成本**：`物件/派西.md:11` 逐字「**`幕綱/arc01.md` 幕006（他很晚才回來）的角色欄有他，但那是常態不是埋設**：他在自己家過暑假，本來就在那張桌子上，不算第五件事」／`核心.md:24` 逐字「他在 `幕綱/arc01.md` 幕006（他很晚才回來）的餐桌上是**常態、不是埋設**：他在自己家過暑假，本來就在」——**同一件事、近乎逐字、兩支檔，而本輪一條裁決要下兩次 `Edit`**（受測體 #96／#98）。S8 記那筆重複時只能推論成本，**本輪 `git diff` 直接把成本印出來了**。⇒ **重複的代價不是儲存，是每一次規則變更的乘數，而這是第一次有數字（×2）** |
| **12.**「**段首『X 的時候，』只留 `ch0002:185`↔`ch0004:5` 那一對（那是括號、是刻意的韻），其餘五處全改掉**」 | **1 —— 而「那一對是刻意的」這件事只住在 `裁決流`** | 五處已改（`git diff` 六處正文有五處是這件事：`ch0001:39`／`ch0002:87`／`ch0004:101,109,137`，第六處 `ch0004:111` 是互述）。**規則側 `grep -rn "的時候" 消失的密室/story/設定/風格/` ⇒ 0**；`grep -rn "刻意" 消失的密室/chapters/` ⇒ 0。唯一的家是 `裁決流.md:227`（標的＝`全書`）⇒ 依約定③計 1。<br>⚠️ **下一個讀到 `ch0002:185` 與 `ch0004:5` 兩章開頭同一個四字公式的人（下一輪 `write-test` 或 `revise`），會把它報成第 10 項「句子讀感」的一筆**，而它已經是裁過的；**唯一能查到它是刻意的路徑是自己跑 `decision-project`，而 `write-test/SKILL.md` 不跑**。⭐ **這一列與第 4 列是同一個機制的兩個方向**：那一條是「未來要遵守的規矩送不到」，這一條是「已經裁過的事會被重複報一次」。**而作者本輪已經吃過一次同型的虧**（第四批他自己回頭實掃、發現 `ch0001` 從頭到尾沒被普查過、補砍第五處）——**普查射程與裁決射程不一致，兩次都沒有工具在守** |
| **13.**「**本輪讀數 `2026-08-03·3高6中8低` 落回 ch0001–ch0004 四支 `.ai.md` 的 `write-test:` 欄，四支同蓋同一個彙總值**，數字用裁決前的原始讀數」 | **1** | 四支 `chapters/ch000{1,2,3,4}.ai.md:10` 各一格 front-matter 鍵——**每一格是該章自己的收據，不是同一個值的拷貝**（形狀同 `beat-test:` 檔頭）。`ch-lint` exit 0：**4/4 支章衍生有 `write-test` 紀錄**（S8 是 3/4·格式不合 0 支）；`derived-sync validate` exit 0（22 支 `.ai.md`／63 個 `##` 節／**131 個鍵·0 個 schema 外**，S8 是 130——`ch0004.ai.md` 的 `write-test:` 是新增鍵）。`裁決流.md:232` 記理由（可投影·不計；`:183` 已是書級通則）。⭐ **作者的機械理由可覆算**：不落的代價是「ch0001–0003 掛著 `2026-08-01·1高4中7低`，會**主動主張**這三章最後一次審是 08-01」——`readiness` 第 3 節確實照著這一行判本層審過了。**「一個假的舊值比一個誠實的 0 更難發現」在 `ch-lint` 的輸出裡有直接對應**：它印「缺紀錄**不計入問題數**」⇒ **一個舊值永遠不會被它報出來** |
| **14.**「**那一家人的親屬關係不加碼**（父母兩格 ch0003 就由榮恩自己的嘴掛好了）；真正會漏的是第八個碗，**改法是讓數的動作有過程**」 | **1** | 執行在 `chapters/ch0004.md:137`（「衛斯理太太盛湯，一個一個數過去，數到八」，依約定⑤不計）。**規則側 `grep -rn "親屬|數的動作|第八個碗" 消失的密室/` ⇒ 只有 `裁決流.md:229`** ⇒ 唯一的家，計 1。⚠️ **這一列的價值在它是「審稿員整段沒看見」的那一格**：作者逐字「父母兩格在 ch0003 就由榮恩自己的嘴掛好了，審稿員整段沒看見」——**而 `ch-lint` exit 0、`fact-lint` exit 0（4 支章 delta／25 筆事實行）都不可能看見，因為那是語意**。⭐ **但有一格是機械的、而且沒人在做**：`ch0003.ai.md` 的 `## 本章事實` 逐筆核過**沒有任何一筆記「榮恩的父母是誰」**（那是溫度不是事實，同 S8 第 3 列的裁定）⇒ **下一個審稿員仍然會再問一次同樣的問題，而唯一擋得住它的是 `裁決流:229`，它讀不到。** 第 4、12、14 三列在這一點上完全同構 |
| **15.**「**幕號射程收窄的三類豁免明列**」，其中**豁免 ③ 在本書內是一條零實例規則，它的射程是系統層** | **0** | **機械症狀（本觀測者實跑）**：`grep -rn "豁免" 消失的密室/` ⇒ 4 命中，**其中 3 個與本條無關**（`裁決流:42` 基調、`亞瑟.md:17`、`風格.md:7`），本條只在 `裁決流.md:234` ＋ `:237`。而 `:237` 逐字承認**豁免 ③ 的射程是系統層**——對應的落點在 `結構定義/`，而**本 session 一個位元組都沒有進 `結構定義/`**（`git status` 全樹核過：異動只有 `消失的密室/`）。<br>⇒ **一條作者親手拍板、射程是系統層的規則，本輪沒有任何落點**——它既不在書內（不歸書管），也不在系統層（沒人去改）。`meta-lint` 不掃書內裁決流、`decision-lint` 的標的欄依 schema 只收書內源檔路徑 ⇒ **這條規則在兩邊的 schema 之間掉下去了。** ⚠️ **與 S8 第 19 列（`settings-select` 工具缺口無書內容器）同族但更硬**：那一次作者**主動裁定不記**，這一次作者**記了、而記的地方是一支只管書內的檔**。⭐ **本輪第 8 列給出了處置方向**：那一條同樣是工具側，但因為「有一端落在書內源檔上」而進了 `待裁決.md` ⇒ **本條沒有書內的那一端，所以連那條路都走不了** |
| **16.**「**ch0005 分章提案要連章尾鉤候選一起過我**」＋「**幕010 那件『別的事』由我定**」（收工時綁下次的兩件） | **0** | **機械症狀**：`grep -rn "ch0005" 消失的密室/` ⇒ 只有 `裁決流.md:235`（那是第 4 列的 `才` 上限，不是本條）；`grep -rn "章尾鉤" 消失的密室/` ⇒ 5 命中，**全部是 `00-摘要` 的定位宣告與 `待裁決.md:19` 的新列（「arc01 的章尾鉤在結構上接不到表層懸念」），沒有一筆是「下一輪要先過作者」**；`grep -rn "幕010" 消失的密室/` ⇒ 8 命中，**全部是既有的幕標題與排除線**（`幕綱:20`／`:45`／`:156`、`裁決流:168`／`:173`／`:184`／`:233`），**沒有一筆說那件「別的事」還沒定**。`story/參照/就緒.md` 本輪未被修改。<br>⇒ **兩件帶順序約束的工作排程，在書內完全沒有容器，只活在 `情境測試/session台帳.md`「下一步」段——測試 meta，系統看不到。** ⚠️ **這是 S8 第 18 列的第二次復發，而且這一次是兩件**：S8 那一條（「跑完 write-test 之前不准動 ch0005」）在本輪**確實生效了**——但生效的機制是**導演在 prompt 裡逐字寫出來**，不是任何書內檔。⇒ **這條路徑可以走第三次、第四次，而它的可靠度等於導演的記憶。** 交階段 3：`就緒.md` 記「哪一層就緒了」、`待裁決.md` 記「根源在上游的觀察」、`裁決流.md` 記「為什麼這樣定」，**三者都不記「下一步不准做什麼」** |
| **17.**「**`beat-lint --emit 幕號外洩` 不要做**——`tools/` 是跨書套件，作者是一本書的作者、不是套件擁有者」 | **0 —— 而這一次連「裁定的 0」都不是** | **機械症狀**：`grep -rn "幕號外洩|--emit" --include="*.md" 消失的密室/` ⇒ **0 命中**（`--emit` 只出現在 `README.md`，那是系統層）。`待裁決.md` 5 列逐列核過無此筆（`decision-lint` exit 0）。<br>⚠️ **與 S8 第 19 列的差別要講清楚**：S8 那一次作者給了三條理由並**明文裁定不記**，於是那個 0 是「正確的 0」；**本輪這一件是受測體主動退回來問的（`S9-*.md` 第一節記為降檔判斷），作者的回答是所有權界線（`裁決流:179`），而那個回答本身沒有落成任何一列**——`grep -c "^| 2026-08-03" 裁決流.md` ⇒ 16 列逐列核過，**沒有一列的內容是「本輪駁回了一個工具提案」**。⇒ **下一輪的受測體會用完全相同的理由再提一次同一個提案**，而 S8 第 17 列（「不替嘿美建角色檔」）已經證明**落檔的駁回會擋住重提、沒落檔的一定會被重提**。⭐ **兩列擺在一起就是一條可實作的規則**：**駁回要落檔，而落檔的位置由「駁回的標的在不在書內」決定**——嘿美在書內（進 `裁決流`）、`settings-select` 不在（不記）、`prose-metrics` 有一端在（進 `待裁決`）、**`beat-lint --emit` 不在而且沒有一端在 ⇒ 本系統沒有它的家** |

**本 session 的形狀**：

1. **「落點 1」第一次不等於「這個需求會被執行」。** 9 個落點 1 裡有 5 個（第 1 列正向半、第 4、7、12、14 列）的唯一的家是 `裁決流.md`，而**逐支核過 12 支 SKILL.md，正文管線三支（`write`／`revise`／`write-test`）一支都不跑 `decision-project`、也沒有一步讀 `裁決流.md`**（三支對它的提及全部是寫入方向）。⇒ **`裁決流` 對正文管線是一支唯寫檔案。** 本輪 append 進去 16 列，其中至少 5 條的生效時機是下一支 `write`。**可機械化的最小處置有兩個方向，代價不同**：⑴ 在 `write`／`revise`／`write-test` 的步驟 1 加一次 `decision-project --target <幕綱檔> --active-only`（一行，但會把 40＋ 列裁決塞進 context）；⑵ 讓 `decision-lint` 對「標的是 `幕綱/*.md` 或 `全書`、而射程涵蓋未寫章節」的列印一行提示。**本帳只交讀數，不選案。**
2. **重複的維護成本第一次有數字，而且是 ×2。** 第 11 列：S8 第 5 列記下的那筆重複（`物件/派西.md` 與 `核心.md` 各寫一份同一句話），**本輪一條裁決要下兩次 `Edit`，`git diff` 兩支檔各一行、內容近乎逐字相同**。前八輪的機制重複全部只能推論成本；**這一輪成本進了 `git diff`。** ⇒ 建議下一輪起，`落點 N` 的證據欄一律補一格「**本輪有沒有為同一條裁決改動它兩次以上**」——那是重複從「存了兩份」變成「花了兩份」的分界線。
3. **三個落點 0 全部是「系統層／排程／跨書套件」，一個故事事實都沒有。** 第 15、16、17 列的共同形狀是**射程超出書資料夾**：系統層規則（豁免 ③）、跨 session 工作排程、跨書套件的功能提案。**而書內三支參照檔的射程都被 schema 限定在書內源檔**（`裁決流.schema.md`／`待裁決.schema.md` 的 `標的` 欄、`就緒.schema.md` 的五 token）。⇒ **這不是三個缺口，是同一個缺口的三個面**：作者在跑這本書的過程中會產生「不屬於這本書」的決定，而本系統沒有承接它們的地方。**S8 第 18、19 列已經是同一個缺口的另外兩個面，兩個 session 內累計五個實例。** 交階段 3——這已經足夠當一條需求：**測試 meta 之外，需要一個「跨書／系統層決定」的落點，否則它們一律靠導演的記憶。**
4. **13 支閘門本輪抓出來的仍然是同一列，而且逐字與 S8 相同。** 17 列裡唯一被閘門點名的是 `char-lint` exit 1 的兩個問題（17 支源無衍生檔／`嘿美`×7·`那輛車`×6），**兩個都不是本輪造成的**（S8 第 7、17 列已記為裁定的帳單與明文駁回的誤報）。**三筆落點 0、五筆機制重複、一筆保真度修復（第 5 列）、五筆「有家但送不到」，13 支閘門全部沉默且全部印對。**

> **13 支閘門本輪讀數**（觀測者端獨立覆算，取真 exit code ＝ `out=$(cmd 2>&1); code=$?`，**未用管線**）：`derived-sync validate` exit 0（**22 支 `.ai.md`／63 個 `##` 節／131 個鍵·0 個 schema 外**；裁決 blockquote 0 支命中）／`derived-sync check` exit 0（**22 個衍生檔·0 個需處理**；成長哨兵：行長 73 支／**事實行 5 支檔·25 行**／搬移目的地 3 個·0 個不存在／**（無）**）／`fact-lint` exit 0（**4 支章 delta／25 筆事實行／22 支物件檔／53 條約束／5 個揭示層級**；3 支物件檔印「揭示點待落幕」資訊）／`object-lint` exit 0（同上）／`beat-lint` exit 0（1 支 arc／15 幕／14 條前因／6 個伏筆標記（6 名）／6 列狀態表／1 個承諾區／**30 條排除線**／22 支物件檔／**19 筆檔內書內路徑·目的地不存在 0**／1/1 支 arc 有 `beat-test` 紀錄；提示：2/6 個伏筆名沒有物件檔·不計入問題）／`ch-lint` exit 0（**4 章正文源／6 個幕錨點／2 個過渡錨點／4 支章衍生**／0 列章序（幕綱 registry 15 幕）／非標準錨點寫法 0 筆／**4/4 支章衍生有 `write-test` 紀錄**；資訊：9/15 幕還沒寫成正文）／`outline-lint` exit 0（8 支 scoped／枚舉外節 0／**檔內路徑引用 76 個·0 個不存在**／arc 引用 79 筆·78 筆指得到／`## 選用結構公式` 8 支宣告·registry 查無 0）／`decision-lint` exit 0（**裁決流 210 列／待裁決 4 列／標的 45 個·0 個查無**／`已升為通則` 0 列）／`world-lint` exit 0（8 支 `<主題>.ai.md`／0 個伏筆名查無·registry ＝幕綱 7 個標記名 ∪ 物件檔 22 支／源檔第一段 0/8 支是空的）／`char-lint` **exit 1（2 個問題）**：①**17 支源沒有衍生檔**（25 支源·5 支目錄形態／8 支 `<名>.ai.md`）②幕綱角色欄 2 個 token registry 查無且出現 ≥3 次：**嘿美×7、那輛車×6**；四節共 **139 個 slot·0 個是佔位**；幕綱角色欄 15 個·括號外 **72 個 token（28 個 registry 查無·其中 2 個 ≥3 次）**／`style-lint` exit 0（1 支 `風格.ai.md`／5 個語意欄·0 個是空的／3 個可用句式檔位·0 個無數字／**掃了 4 章正文·禁用詞命中 0 次**）／`summary-lint` exit 0（1 支源／1 支衍生／6 個語意欄·0 個是空的／基調兩方比對相符）／`readiness-lint` exit 0（2 個 `##` 節／狀態表 20 格·0 格不是五 token 之一／**就緒清單 6 條·5 條已勾——與 S7、S8 逐字相同**）。
> **與 S8 收工讀數的 diff（全部可由本輪 17 列解釋，無不明漂移）**：裁決流 194→**210**（append 16 列）／待裁決 3→**4**（第 8 列加 2、刪 1）／承諾區排除線 29→**30**（第 2 列）／物件檔約束 51→**53**（第 1、2 列各一條進 `物件/全書.md`）／幕綱檔內路徑 17→**19**（第 2、6 列新增的兩處檔案引用）／`.ai.md` 鍵 130→**131**（第 13 列）／章衍生 `write-test` 紀錄 3/4→**4/4**（第 13 列）。**物件檔 22 支·衍生檔 22 支·角色源 25 支·目錄形態 5 支·源無衍生 17 支·事實行 5 支檔 25 行·行長 73 支·`char-lint` exit code 與問題數（1／2 個）——八項與 S8 逐字相同，本輪一支新檔都沒建。**
> **⚠️ 兩處與 S8 對不上而本輪 diff 無法解釋，逐條追過是 S8 的數字錯了**：①`ch-lint` 幕錨點 S8 記 5＋1，實際 `git show bf4f848`／`5da2cb3`／`5636530` 逐支數都是 **6＋2**，本輪 diff 未動任何錨點 ⇒ **S8 那個數字在寫下時就是錯的**；②`S9-*.md` 產物節的「裁決流 194→207（＋13）」是導演漏數了第一批用 `Edit` 落的 3 列，機械讀數 **+16**。**兩處都不影響任何判定，但本帳的 diff 紀律要求上輪的數字也是真的。**
> **四格交給 S10 的觀測者**：⑴ **第 4、12 列的下一次生效時機就是 S10 的 `write` ch0005**——請直接驗「ch0005 的 `才` 有沒有超過兩個／有沒有相鄰兩段同時收在 `才`」與「新寫的段首有沒有第三個『X 的時候，』」，那是「落點在、送不到」這個形狀的第一個可觀測後果。⑵ **S8 第 15 列（`裁決流` 標的欄寫具體檔導致通則撈不到）的檢驗場仍未到**（arc02 尚未拆幕），本輪未執行，原樣移交。⑶ **S8 第 11 列（弗雷喬治雙簧三條寫法紀律綁 arc01）同理**，並請一併驗本輪第 6 列（釋放／累積軸）——**兩條都是寫法紀律住在 arc01 的承諾區裡**。⑷ **S7 交辦的「掃全書 `<X>.ai.md` 的節、抽名詞短語去 `grep` 源檔」連續第三輪未做**；本輪衍生檔數不變（22 支），對照組（派西·不生 `.ai.md`）仍在。
