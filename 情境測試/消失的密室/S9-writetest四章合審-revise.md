# S9 write-test（ch0001–ch0004 一次審完）→ revise — arc01 前四章

> **本 session 的形狀**：台帳「下一步」是作者上一輪當場綁死的閘門，範圍與順序都不由導演選。**一支冷啟受測體跨 4 次 `SendMessage`**（審→落三批裁決）、**4 支冷啟神諭**。
>
> **本輪最大的讀數不是報告本身，是報告被裁的方式**：3 高 6 中 8 低共 17 條，作者**四次直接推翻提問前提**（Deep POV 不是內心配額／35 分句是量洛哈量出來的／章末配比沒有上游依據／親屬關係 ch0003 就掛好了），而**三個「高」裡沒有一個的結論是「正文有病」**——兩個是判準被讀在錯的粒度上，一個是**待裁的那一題的前提本身有病**。
>
> **正文最後只動了 6 處**（ch0001×1、ch0002×1、ch0004×4），全部是逐字替換句、由作者親手給。

## 系統路由觀察

**導演一次都沒有講出 skill 名字**，四次路由全對：

| 作者這樣說 | 系統走了哪一層 | 對不對 |
|---|---|---|
| 「一次把 ch0001 到 ch0004 全部審一遍，不是只審最新那一章」＋「我要的是報告，不是你動手改」 | `write-test`：11 項檢查＋13 支閘門＋`prose-metrics`，**一個字都沒改、沒建 ch0005** | ✅ |
| 「現在動手落檔，射程就是上面三題裁下來的東西」 | `revise` 檔位（定點修訂）：只動被點名的行，**未被點名的段落一個字沒動** | ✅ |
| 「這一批他授權動正文了」 | 逐字替換 4 行＋幕綱／raw／物件／裁決流連動＋重生封章 | ✅ |
| 「`beat-lint --emit 幕號外洩` 不要做」 | 未動 `tools/`，缺口寫進報告交出去 | ✅（`裁決流:179` 所有權界線） |

**一格值得單記的降檔判斷**：受測體在【高 1】發現 `prose-metrics` 的量法與 `風格.md` 宣告的語料不同，**它沒有去改 `rhythm.py`**，而是把「要不要動工具」列成候選交作者裁。作者的答案是「`tools/` 是跨書套件，作者是一本書的作者不是套件擁有者」——**受測體停在正確的那一格上，而那一格是它自己找到的。**

**另一格是反向的**：第 1 輪它把 ③（arc01 喜劇引文段封頂）寫進了導演沒有指定的落點（`洛哈.md`），**自己發現超出指示、自己改回來、並主動把「那 ③ 現在沒有到得了筆的落點」退回來問**。同 S6 第 5 條／S8 第 4 輪先例，**不算代選**。

## Elicitation 紀錄

**4 輪冷啟神諭、14 題，fallback 0 筆（0.0%）。系統代選 0 次。**

| 輪 | 問了什麼 | 作者答什麼 | 來源 |
|---|---|---|---|
| 1 | 三個「高」：①`待裁決:19` 句長中位那一列怎麼處置（A/B/C）②Deep POV 內心配額（A/B/C）③喜劇引文段天花板被威農佔滿（A/B/C） | **九個候選全駁，三題全給第四案。** ①**那一列不是題，是一個已經死掉的前提**——混合中位的四章變異被對白段佔比（23–41%）完全解釋，「arc02 萬聖節那一章對白一定會變少，那個數字會自己往上跑，而正文一個字都沒改」。刪列＋append 量法紀律＋**`:220` 的另一半當場結案**（常態段從來沒站在 16，它站在 21）。②**提問前提錯**：`敘事距離與DeepPOV.md:13-22` 六條技巧沒有一條是頻率，四章過濾詞實測 **0**；`心理描寫.md:53` 的配額是**上限** 2–3 處，四章按「處」數是 2／2／2／1，**沒有一章欠**。改裁一條**寫法判例**：哈利的內心一律寫成「算式」不寫成「感受」。③**前提錯在把台詞長度讀成單段分句峰值**——35 這個數字**是量洛哈量出來的**，威農站 34 不是佔了他的位置、是站上他的日常水位；**分級由次數兌現不由峰值兌現**。改裁「洛哈的每一段引文必須整段不可兌現」＋頻次紀律＋**arc01 額度封頂** | **腦** |
| 2 | 六題：中4 章末型別配比／中5 段首公式／裁決四 街名／裁決五 親屬關係／裁決六 ch0003 厚薄＋動作戲第四檔／裁決七 讀數落不落檔頭 | **七處更正，其中兩處換掉答案。** 中4：配比**沒有上游依據**，是 08-02 改措辭時在下游長出來的，**而它一長出來就已經與 ch0001／ch0002 牴觸**——`裁決流:219`「本 arc 唯一一章走物件型」**在落筆當天就是假的**；改軸（型別→溫度極性），正文一個字不動。中5：那一對是**括號不是重複**（天亮把他關進去／天亮把他放出來），**但韻只有在它是唯一一對時才讀得出來**，所以殺掉其餘四處是買下它的代價，**含審稿員漏報的 `ch0002:87`**（相鄰兩段同開頭，比跨章那一對嚴重）。中12：採 B（以正文為準統一「女貞路」），**「名字的工作是被讀到」**。中6：採 A 不動，**「父母兩格在 ch0003 就由榮恩自己的嘴掛好了」，審稿員整段沒看見**；真正會漏的是第八個碗，改法是讓數的動作有過程。中7：採 C 但**換軸**——寫「釋放幕壓薄」不寫「動作幕壓薄」，否則幕012–014 會被 ch0003 帶著一起壓薄。裁決七：落，**四支全蓋**（「一個假的舊值比一個誠實的 0 更難發現」） | **腦**（`examples/` 觸及 1 次·0 命中·**非 fallback**） |
| 3 | 受測體退回的兩件：①③ 沒有到得了筆的落點 ②`:175` 與 `:219` 矛盾＋4 處既有違反 | ①落承諾區「不得發生」（同節已有兩個計數器封頂先例），**但「幕011–015」是我原本寫錯的**——剩下沒寫的是幕007–015，那個跳法把魯休斯最可能長出第三段的兩幕留在門外；改成**按角色寫**。**外加一件執行者沒問的：② 也住錯檔了**——實跑 `settings-select --beats 幕011-幕015`，`洛哈.md` 不在選出的 11 支裡，「全書·不綁實體」該住 `物件/全書.md`。②**兩條不矛盾，是 `:175` 射程寫錯了**：它被寫成位置規則，理由欄卻在講指涉方式，於是掃到了 schema 強制的機讀 key。收窄成「**幕號可以當機讀 key 與帳本主鍵，不可以當散文座標**」，三類豁免明列 | **腦** |
| 4 | ①`ch0004:111` 互述動不動 ②收不收工 ③有沒有沒人問到的一格 | ①**診斷對了一半**：互述是真的（我今天的替換句造成的，我認帳），**「才…才…」不是**——ch0004 五個 `才` 是同一族，「這一章講的就是每件事都要花掉一段時間」，用 n=1 立段尾規矩是同一個過度擬合。**順帶自己補砍第五處段首公式 `ch0001:39`**（「那筆錢沒付完」，AI 與他自己都只掃了 ch0002／ch0004，**ch0001 從頭到尾沒被普查過**）。②收工，三件綁下次（**街名今天就定死**：對角巷→斜角巷，**這次抓在正文之前**／ch0005 分章提案要連章尾鉤候選一起過他／**幕010 那件「別的事」由他定**）。③`就緒.md` 三個 session 沒動而中間四章落地了 | **腦** |

**fallback 0 筆的性質**：本輪 14 題**一題都不在質地層**——全部是判準、量法、落點、射程、規則衝突。與 S2／S7 那兩個 0 同型（承重層問題腦內檔接得住），**不是 S1 第 3 輪那種見底**。唯一一次觸及 `examples/` 是 R2 為街名做的全域 `grep`，**兩個名字在答案卷裡一個都沒有**——查了、0 命中、沒有進任何轉述或產物。

## 產物

**18 支檔改（0 支新增）**，`git diff --stat` 56 insertions／28 deletions：

| 檔 | 動作 |
|---|---|
| `chapters/ch0001.md`／`ch0002.md`／`ch0004.md` | **正文 6 處逐字替換**（ch0001:39／ch0002:87／ch0004:101,109,111,137），全部由作者親手給句 |
| `chapters/ch0001–0004.ai.md` | `write-test: 2026-08-03·3高6中8低` 四支全蓋（ch0004 為新增鍵）；ch0001／ch0002／ch0004 重封章 |
| `story/幕綱/arc01.md` | 承諾區 `:7` 章末改**溫度極性軸**／新 `:8` **釋放·累積軸**／`:33` 街名／新 `:55` 喜劇引文段封頂（按角色）／`:148`,`:159` 對角巷→斜角巷 |
| `story/大綱/arc01.md:97` | 對角巷→斜角巷 |
| `raw/名字與人物.md:21` | `~~水蠟樹街~~**女貞路**四號`（依 `:158` 就地劃掉留形，形式照同檔 `:17`） |
| `story/物件/全書.md` | 約束 **7→9 條**：〔哈利的內心不得寫成感受〕〔喜劇引文段每 arc 一段〕 |
| `story/設定/角色/洛哈.md`（＋`.ai.md`） | 「## 寫法」加「每一段引文必須整段不可兌現」＋指向全書約束；**源檔三次變動、封章三次** |
| `story/物件/嘿美.md:14`／`派西.md:11`／`設定/角色/派西/核心.md:24` | 幕號座標：一處換事件名、兩處雙寫「幕006（他很晚才回來）」 |
| `story/參照/裁決流.md` | **194 → 210 列（＋16，全部 08-03）**（導演寫記錄時漏數第一批用 `Edit` 落的 3 列，報 +13；`decision-lint` 讀數 210、`grep -c "^| 2026-08-03"` = 16，**更正由觀測者機械查出**） |
| `story/參照/待裁決.md` | 刪 1（句長中位）／加 2（`prose-metrics` docstring·交套件擁有者／arc01 章尾鉤接不到表層懸念）／`:18` 解鎖條件重寫，**淨 4 列** |

**閘門（真 exit code，四批動手前後共跑 5 輪，讀數逐字相同）**：**12 支 exit 0，`char-lint` exit 1**（① 17 支源無衍生檔 ② 嘿美×7／那輛車×6 registry 查無）——**與導演開場基線完全相同，本 session 一格沒動**。

**inline 審稿**：`prose-metrics` 四項絕對門檻全清（破折號 0.18／顛簸 0.00／句-段 1.15／回聲 0.0%）；`style-lint` 掃 4 章禁用詞命中 0；解說段 0 段；**敘述段中位（宣告量法）修改後 20（n=208），仍在宣告 20–30 內**；段首「〈事件〉的時候，」全書只剩刻意留的那一對；**裸幕號（豁免清單外）0**。

## 摩擦與缺口

1. **一支閘門的量法與它被指示去對照的宣告，語料不同——而 docstring 就是那份指示。** `prose-metrics` 的 `句長中位` 掃全部段落（`rhythm.py:305-309`），`風格.md:50` 的 20–30 字宣告剔掉帶「」的行；而 `rhythm.py:46-47` 的 docstring **逐字指示拿前者去對後者**。在對白段佔 23–41% 的書上系統性低報 4–6 字。**代價不是讀數難看，是它生出了一列假的待裁**：`待裁決:19` 掛了兩天，題目是「該把常態段寫回 20–30，還是重切恐怖檔」，而**常態段從來沒有站在 16**。作者的處置是刪列＋立量法紀律＋把工具那一半交套件擁有者。**這一格與 S7 交出的「呼叫者吃掉 exit code」同族**：閘門印對了，讀的人拿到一個錯的東西——只是這一次錯的不是數字，是**該拿它去比什麼**。

2. **`裁決流:219` 那一列在寫下的當天就是假的，而它是 `write` 唯一的落筆依據。** 「本 arc 唯一一章走物件型」08-02 寫下時，ch0001／ch0002（08-01）已經是物件型。**S8 交出的第 3 格（承諾區措辭把兩型壓成單型）在同一個位置復發了第二次，而且更難看**：上一次是**壓縮**（兩型→一型），這一次是**憑空長出一個上游沒有的量**（`風格.md` 達標線 3 與 `00-摘要.ai.md:39` 兩支權威都寫「物件**或**挑釁」、都不含配比）。**沒有任何閘門看得見「承諾區宣告了一個上游沒有的約束」**——`beat-lint` 守格式，而這是內容。

3. **一條規則可以在自己那一列裡違反自己 26 次。** `裁決流:175`「幕號字面不得出現在 `幕綱/` 以外的檔」照字面全掃是 **68 行 13 支檔**，其中 **64 行是對的而且是 schema 強制的**（`<!-- 幕NNN -->`／`對應幕`／事實信封行行首，三支 lint 在守）。**受測體第一次只掃 `story/` 並自行 carve-out `裁決流.md`——而那個豁免不在規則裡，是它加的**；作者照字面全掃才看見。作者的定性可直接抄：「**一條規則在自己那一列裡違反自己 26 次，那不是違反，是射程寫錯了。**」**與 S5「幕號重編沒有檢查器在守」同一條線的下一格**：那一格是規則沒有守衛，這一格是規則**寫成了守不了的形狀**——照原字面實作出來的清單會有 94% 是雜訊，而作者明說那是「E2 最糟那一格的鏡像」。

4. **本輪三個「高」沒有一個的結論是「正文有病」，而三個都是真發現。** ①判準被拿去對錯的語料 ②判準被讀在錯的粒度（Deep POV↔內心密度、台詞長度↔單段峰值）③一列在下游長出來的假宣告。**這一格的形狀要交**：`write-test` 的 11 項全部瞄準正文，而本輪最貴的三個發現**全部在正文的上游**，且三次都是受測體用正文當**探針**探到的（它是為了驗正文才去讀 `風格.md:50`／`敘事距離與DeepPOV.md`／承諾區 `:7`）。**沒有任何一支 skill 的職責寫著「審上游判準」**——它是 `write-test` 的副產品，而副產品沒有守衛。

5. **`就緒.md` 是這本書唯一一支沒有任何新鮮度偵測器的源檔，而它坐在最貴的位置上。**（作者主動點出，本輪沒有任何問題問到它。）它三個 session 沒動，中間落了四章正文，而 `結構 × 正文` 仍寫著 `空白`。**十二支閘門全綠而它爛在那裡的原因是機械的**：`readiness` 第 1 節的不一致偵測**只有單向**（`就緒.schema.md:119` 限定「作者判斷＝就緒／已鎖定，而機械前哨報了 N 筆」），抓不到反向（機器已經有東西、這一格還寫著空白）；`readiness-lint` 只驗五個 token 的語法，而一個語法合法的 `空白` 可以永遠躺著；它不重生（沒有 hash stale）、沒有上游（`derived-sync` 看不見）。**作者的定性**：「它是全系統唯一一支專門用來回答『我到哪了』的檔，而它現在對這個問題說謊。」他**拒絕替它加反向檢查器**（要先定義「幾章算草擬」，那是判斷不是機械），改立一條紀律——而那條紀律**只買到「查得到」，買不到「會自動發生」**（真正的落點是 `write/SKILL.md` 的收尾步驟，系統層，本輪不動）。

6. **「引的人是錯的」本輪四次，四次的攜帶者各不相同。** ①受測體引 `裁決流:26–27`（真正的源頭是 `raw/基調:9`，拍板列 `:40` 一個字沒提長度）——**作者明說「如果我照你的話信了，我會為了不動一條不存在的裁決而放棄一個對的選項」**；②受測體拿平均 2.42 對中位宣告 2.0，**而它自己那一整條的論點就是「拿兩種語料互比」**；③導演引 `心理描寫.md:52-53`（實際 `:53-55`，整體低一行）；④**作者自己在 `裁決流:234` 引錯兩處，而那一列正是他親手診斷「射程寫錯了」的那一列**——其中「收窄後今天要印 1 行」（實際 0 行）是**給下一輪實作者看的驗收條件**，跑出 0 行的人會以為自己寫壞了。**S8 第 4 條那句導演 prompt（「他引的行號你要自己核一次」）本輪雙向執行，四次全部被核出來**；而第 ④ 筆是**受測體照抄作者的錯**（`待裁決.md:22`→`:23`），第二輪才被作者自己核出來——**同一個錯在兩個人身上各活了一輪。**

7. **（正面）作者三次把「刪掉某個東西的代價還沒付完」自己算清楚。** 中5 裁「留那一對就得殺掉其餘四處」，第 4 輪他自己回頭實掃、發現 **ch0001 從頭到尾沒被普查過**、補砍第五處，並說「那筆錢沒付完」。**兩次普查漏的都是同一支檔，而漏的原因是機械的**：受測體的清單來自它自己第 1 輪報的四處（ch0002／ch0004），而**作者的裁決是對著那張清單下的**——裁決繼承了清單的射程，沒有人重新定義過射程。**這一格與 S7 交出的「沒有任何閘門量得了一個提案的檢索覆蓋夠不夠」是同一格，只是這次的失敗者是裁決不是提案。**

--- 以下由觀測者 agent 寫 ---

## 防火牆稽核

```
防火牆：檢查了 128 次工具呼叫，越界 0 次
神諭寫入：檢查了 114 次工具呼叫，寫入型 2 次（**兩次都落在 repo 之外的共用 scratchpad，書資料夾與 `情境測試/` 0 位元組**）
導演：檢查了 32 次工具呼叫，`examples/消失的密室/` 觸及 0 次
```

**稽核對象**：**一支**受測體的 transcript——`agent-a93d8424868737bc8.jsonl`（`write-test` ch0001–0004 → 落三批裁決 → 收工，跨 **4 次** `SendMessage`，**128 次**呼叫）。四支神諭（114 次）與導演（32 次）各自另列，不進第一行的分母。`subagents/` 底下 6 支 `.meta.json` **全部 `"spawnDepth": 1`**（1 受測體＋4 神諭＋1 觀測者，全由導演直開）；受測體的 128 次呼叫裡 `Agent`／`Task`／`SendMessage` **0 次**。

**工具分佈**：`Bash` 86／`Edit` 25／`Read` 15／`Write` 2 ＝ 128。**`Grep` 0、`Glob` 0、`Skill` 0。**
> **S8 那條前提本輪維持**：128 次呼叫裡只有 15 次 `Read`，其餘所有讀取都是 `cat`／`sed -n`／`nl -ba`／`grep -n`。**只掃 `Read`／`Grep`／`Glob` 參數的稽核會掃到 15 次讀取，而實際讀取超過 90 次。**

**逐參數掃描**：對每一次呼叫的 `file_path`／`path`／`glob`／`pattern`／`command`／`skill`／`args`／`query` 逐個比對 10 條越界樣式（`examples`、`情境測試`、`docs`、`Data`、`site`、`devtools`、`gothic_witch`、`一世之尊`、`芯片巫師`、`harry_potter`），**路徑形狀比對＝樣式後面要接 `/` 或 `\`**——**路徑形狀命中 4 次，逐條核過 4/4 都是 `C:/Users/user/App`**`Data`**`/Local/Temp/claude/…` 的 scratchpad 路徑**（#106／#107／#122／#123／#126），**越界 0 次**。另有 2 次裸子字串 `docs`（#58／#62，來自受測體自己寫的 `docstring` 三個字），非路徑形狀。
> **S8 記的「假陽性消失」本輪如預期回來了**：S8 兩支受測體一次都沒用 scratchpad，所以 `AppData` 那條 0 命中；**S9 的受測體用了 scratchpad 兩次（`append_rows.py`／`append_b4.py`，把 13 列裁決 append 進 `裁決流.md`），`Data/` 那一條立刻回到 4 次**。這證實 S8 那句判讀是對的：**那個 0 是「這次沒人寫暫存檔」，不是防護變好。**

**repo 範圍搜尋（逐條核搜尋根）**：`Grep`／`Glob` **0 次**。`Bash` 側 **14 次**帶 `grep -r`／`rg`／`find`／`ls -R` 的呼叫逐條核過：**搜尋根全部明寫，沒有一次是 repo 根的無範圍遞迴**——`消失的密室/`（含 `chapters/`、`story/`、`raw/` 子樹）、`.claude/skills/write-test/SKILL.md`、`tools/*/pyproject.toml`、`tools/prose_metrics/src/`。**cwd 是 repo 根的呼叫有 33 次**（128 次裡佔 25.8%），但每一次的遞迴標的都是明寫的相對路徑，`.` 只出現在 cwd 已經是 `消失的密室/` 或其子目錄的四次（#71／#116／#127／#128）。
> **導演本輪把「檢索根一律明寫」寫進了 prompt 硬邊界。這是它第一次被明文要求，而受測體 14/14 照做。**

**`git` 呼叫 —— 這一格三輪之後第一次拿到真讀數**：受測體本輪 **3 次**，**3/3 帶 pathspec**：

| # | 時間 | 指令（節錄） | pathspec |
|---|---|---|---|
| #75 | `16:35:02Z` | `git -C . status --porcelain -- 消失的密室/chapters/`（同一次呼叫另有兩條，各自帶 pathspec） | ✅ `-- 消失的密室/chapters/`／`-- 消失的密室/`／`-- 消失的密室/story/設定/風格/ …` |
| #113 | `17:03:19Z` | `git -C . status --porcelain -- 消失的密室/` | ✅ |
| #127 | `17:23:18Z` | `git -C .. status --porcelain -- 消失的密室/` | ✅ |

> **S6「無 pathspec ＋剛好乾淨」／S7「有 pathspec ＋剛好乾淨」／S8「無 pathspec ＋剛好乾淨」／S9「有 pathspec ＋而且是被要求的」。** S8 觀測者交出的「可機械化的最小處置」逐字是「**把『`git` 一律帶 pathspec』寫進受測體 prompt 的硬邊界那一段，它現在不在裡面**」——**導演本輪照做，受測體 3/3 遵守，而且三次都額外帶了 `-C` 明寫 repo 根。** 這是本測試第一個「觀測者交辦 → 導演實作 → 下一輪拿到 100% 遵從讀數」的完整迴圈。
> **但這一輪仍然沒有被真的考到**：三次的 `tool_result` 逐條核過，輸出全部只有 `消失的密室/` 底下的路徑；而導演對 `情境測試/` 的 5 次寫入全部在 `17:26:57Z`–`17:30:34Z`，最後一次 `git` 是 `17:23:18Z`——**中間隔 3 分 39 秒（S8 是 41 分鐘，時間窗正在縮短）**。**pathspec 現在是這一格唯一在守的東西，而它是 prompt 規勸不是工具。**

### S8 交辦的第一格：harness 的 `tool-results/` 溢出目錄

**導演本輪把它加進了受測體 prompt 的硬邊界（逐字：「你不得讀取 harness 自己的溢出目錄 `C:\Users\user\.claude\projects\**\tool-results\`（包含 `ls` 它、`grep` 它、`cat` 它）」）。逐條核受測體 128 次呼叫的每一個參數：`tool-results`／`.claude/projects`／`subagents`／`jsonl` 全部 0 命中。**

本 session 的 `tool-results/` 共 **4 支 `.txt`**，逐支比對 transcript 確認擁有者與內容：

| 檔 | 大小 | 擁有者 | 內容 | 黑名單命中 |
|---|---|---|---|---|
| `bu0t9f3sr.txt` | 53,377 B | **受測體** | `fact-project --for-beat` 幕001–006 的輸出 | 0 |
| `bdiuylyws.txt` | 43,804 B | 神諭 R2 | `裁決流.md` 第 150 列起的原文 | 0 |
| `blivu7fyz.txt` | 89,373 B | 神諭 R3 | `裁決流.md` 第 165–235 列 | 0 |
| `bku5d99i1.txt` | 32,152 B | **觀測者（我）** | `S8-*.md` 的後兩節 | 26 |

**跨 agent 讀取 0 次。** 而且與 S8 不同的是，**本輪 4 支檔裡沒有任何一支帶 `情境測試/` 的腦內檔內容**（S8 的 `bhfilqirw.txt` 是四支腦內檔全文、37,408 B）——原因是本輪四支神諭一次都沒有整批讀腦內檔。唯一帶黑名單字樣的是我自己那一支，而它的建立時間（`17:31Z`）晚於受測體最後一次呼叫（`17:23:30Z`）**8 分鐘**，受測體在物理上讀不到。

### ⚠️ 本輪挖出的新防火牆面，而它比 `tool-results/` 更難處理：**共用 scratchpad**

`C:\Users\user\AppData\Local\Temp\claude\C--Users-user-Programming-novelAI\<sessionId>\scratchpad\` **是整個 session 共用的一個資料夾**，而它與 `tool-results/` 有一個關鍵差別：**受測體的 system prompt 明文要求它用這個資料夾**（「IMPORTANT: Always use this scratchpad directory for temporary files」）。

本輪的實況：

| 事實 | 讀數 |
|---|---|
| 神諭 R1 在 `16:14:24Z` 與 `16:15:12Z` 各 `Write` 一支 `.py`（`m.py`／`c.py`）進去 | **這就是「神諭寫入型 2 次」的全部內容** |
| 受測體同一時段仍在跑（`15:50Z`–`17:23Z`），它的 scratchpad 就是同一個資料夾 | **兩支神諭寫的檔在受測體的工作目錄裡躺了 68 分鐘** |
| 受測體 #126 逐字是 `python ".../scratchpad/narr.py" 2>/dev/null \|\| python - <<'PY'` | **它試圖執行一支它自己沒有寫過的檔名。`narr.py` 不存在，於是掉進 heredoc。** 若那個檔名剛好被別人建過，它會執行別人的程式 |
| `m.py`／`c.py` 逐行讀過 | **只讀 `消失的密室/chapters/`，0 個 `examples`／`情境測試` 字樣** |
| 受測體有沒有 `ls` 過 scratchpad | **沒有。** 它只用絕對路徑寫／讀自己那兩支 |

**判定：本輪 0 次跨 agent 讀取，越界仍是 0；神諭寫入 2 次但 0 位元組進 repo。** 但形狀要交清楚：
> **`tool-results/` 是「受測體不該去的地方」，可以用一行禁令關掉；scratchpad 是「受測體被要求去的地方」，禁令關不掉。** 而神諭讀得到 `情境測試/` 與 `examples/`，只要哪一輪它把一段答案卷寫進 `.py` 的註解或一支 `.txt` 中間檔，那段字就躺在受測體的工作目錄裡。**可機械化的最小處置只有一個方向：給每個 subagent 一個各自的子目錄**（例如 `scratchpad/<agentId>/`），這在 harness 層、不在 prompt 層。**基座 §3 的「絕不能讀」欄現在漏掉的是第四種地方，而前三種（repo 黑名單、`tool-results/`、跨書）都已經有一行禁令。**
> **另一格順帶記**：基座 §3 給神諭那一欄寫的是「**能寫：無**」。本輪機械讀數是 **2 次 `Write`**。它們沒有造成任何後果，**但那一欄現在與實況不符**——要嘛把它改成「無（repo 內）」，要嘛把神諭的工具清單真的拿掉 `Write`／`Edit`（S6 觀測者已提過一次，S7／S8 各復述一次，**本輪是第一次真的有 `Write` 被呼叫**）。

### 補一層：`tool_result` 與 prompt 全文掃描（逐行）

掃受測體 transcript 的**每一行 JSONL**（共 **368 行**，含 prompt、assistant 輸出、thinking、tool_result），路徑形狀命中 **13 行**，逐一追到攜帶者：

| 攜帶者 | 行 | 命中字樣 | 判讀 |
|---|---|---|---|
| **受測體自己的 prompt**（第 1 行） | 1 | 全部 9 條 | 那正是**禁令本文**（導演寫的硬邊界） |
| `結構定義/幕綱.schema.md` 沿革 | 25 | `docs/` ×2 | 白名單第 1 項 |
| **`結構定義/章節.schema.md` 沿革** | 42 | `docs/` ×8、`Data/` ×2、**`一世之尊` ×12** | 白名單第 1 項 |
| **`tools/*/pyproject.toml` 檔頭註解** | 51 | **`examples/` ×10、`devtools/` ×10** | 白名單第 4 項；逐字「生產包一律**不含 `tests/`**——黃金檔釘病例書、語料回歸釘 `examples/`」 |
| **`tools/prose_metrics/src/prose_metrics/rhythm.py` 檔頭** | 102、106 | **`一世之尊` ×10** | 白名單第 4 項；命中的是**語料對照表**：「語料 一世之尊：非對白段中位 34 字＝句長中位 27 × 句/段 1.22／生成 一世之尊：…21 字＝16 × 1.69」 |
| `技巧知識庫/敘事距離與DeepPOV.md:28` | 119 | `Data/`（`Data/【創作技巧】.txt` L1779） | 白名單第 2 項 |
| scratchpad 的 `Write`／`Bash` 三組 | 309–311、353–355、361 | `AppData/` | 見上 |

**`examples/消失的密室/` 的任何一章、任何一段原文 0 命中；`第１章`…`第18章` 檔名 0 命中；`tools/*/tests/` 內容 0 命中。**
> **S7 交出的那一格本輪換了攜帶者、而且升級了。** S7 是 `技巧知識庫/世界觀與背景.md:87` 的「不複製，指向 `examples/…`」；S8 是 `write/SKILL.md` 的依據行；**S9 是 `tools/*/pyproject.toml` 的封閉性註解（`examples/` ×10）＋ `rhythm.py` 的語料對照表（`一世之尊` ×10，帶數字）**。**這是三輪三支不同的生產包檔案，同一個黑名單。** 而 `rhythm.py` 那一格特別值得記：它送進受測體 context 的不是一個資料夾名，是**另一本書的四個實測數字**，而受測體本輪最貴的那條發現（【高 1】）正是在讀那支檔時發生的。**它沒有用那四個數字，但沒有任何東西擋著它用。**
> **S7 建議的 `meta-lint` 第 14 項連續第三輪沒有被做。**

### 導演那一格（基座 §3 收窄後的第三次稽核）

| 問 | 機械答案 |
|---|---|
| 導演有沒有讀 `examples/消失的密室/`？ | **0 次。** 32 次呼叫全掃：`Read` 7 次（流程 doc／`session台帳.md`×2／`就緒.md`／`S8-*.md`／`fallback-log.md`／`待裁決.md`）、`Glob` 2 次、`Bash` 12 次（13 支閘門×2 輪、`ls` transcript 目錄、`sed -n` 書內兩支檔、`情境測試/` 的四次寫入）、`Agent` 6 次、`SendMessage` 3 次、`ToolSearch` 1 次。**沒有一次的參數指向 `examples/`** |
| 那為什麼有 9 次呼叫的 JSON 裡含 `examples/`？ | 逐條核過：**6 次是 `Agent` 的 `prompt` 欄**（受測體禁令本文＋4 支神諭的授權行＋觀測者 prompt），**3 次是寫進 `情境測試/`**（`fallback-log.md` 記神諭觸及、`S9-*.md` 第二節）。**9/9 都是「寫下這個字」，不是「打開那個資料夾」** |
| `**/session台帳.md` 這個 repo 根 glob 危險嗎？ | 本輪同 S8：只比對檔名、回傳 1 個路徑。**連續第二輪是導演唯一一次對 repo 根做的無範圍檢索**，記一筆 |
| 導演的 5 次寫入落在哪？ | **5/5 都在 `情境測試/消失的密室/`**（`S9-*.md` 1 次 `Write`、`fallback-log.md` 2 次、`session台帳.md` 2 次，後四次都是 `python - <<EOF` 就地改）。**書資料夾 0 次** |

**⇒ `git diff 5636530` 在 `消失的密室/` 的每一個新增位元組都是受測體寫的。** 這是下一節 purity 的歸屬前提，本輪機械成立——**但它只成立到「誰按下的鍵」為止，不成立到「誰想出來的字」。見 purity 節第一段。**

### 神諭（4 支，114 次呼叫）

| 輪 | agentId | 呼叫數 | 工具分佈 | 寫入型 | `examples/` 觸及 |
|---|---|---|---|---|---|
| R1 三個高 | `a9cbb851b8f34b107` | 40 | `Bash` 25／`Read` 13／**`Write` 2** | **2**（scratchpad） | 0 |
| R2 六題 | `ad0d01037f8915362` | 21 | `Bash` 13／`Read` 8 | 0 | **1（間接·結構上不可能命中）** |
| R3 退回兩件 | `a213d2ddf4d440251` | 19 | `Bash` 16／`Read` 3 | 0 | **1** |
| R4 收工 | `a882b44fcf27e691e` | 34 | `Bash` 19／`Grep` 8／`Read` 7 | 0 | **1** |
| **合計** | | **114** | | **2** | **3** |

**Bash 側寫入逐條核過 0 次**：73 次 `Bash` 的指令字串逐條掃重導向（`>`／`>>`，排除 `>=`／`2>/dev/null`／`2>&1`）·`sed -i`·`tee`·`mv`／`cp`／`rm`·`git add/commit/checkout/reset`·`stamp` ⇒ **0 次**。三筆 `open(` 初掃命中逐條核過**全部是讀模式**（R1 #21 讀三章的指定行；R4 #10／#15 讀 `物件/*.md` 與 `裁決流.md`）。

**⚠️ `examples/` 那三次逐條核，而結論與導演記在 `fallback-log.md` 的那一筆不同**：

| 呼叫 | 逐字 | 實際能拿到什麼 |
|---|---|---|
| **R2 #10** | `cd novelAI && grep -rn "女貞路\|水蠟樹街" --include=*.md .` | 這是 repo 根的遞迴，**確實走進了 `examples/消失的密室/`**——但那個資料夾裡是 **18 支 `.txt`、0 支 `.md`**，`--include=*.md` 在讀取前就把它們全部排除。**結構上不可能返回任何一個字的原文。** |
| **R3 #19** | `ls examples/消失的密室/ \| head; grep -rln "幕號字面\|事件名是穩定座標" examples/` | 真的打開了那個資料夾，但 `-l` 只產出檔名、而且**0 命中**。**返回 0 行** |
| **R4 #2** | `ls -la "…/examples/消失的密室/" 2>/dev/null` | 只列目錄。**返回 0 行內容** |

⇒ **三次觸及、0 行原文進入任何一支神諭的 context，fallback 0 筆機械成立。** 但 `fallback-log.md` 本輪那一列把觸及記在 **R2（第 2 輪·為街名做的全域 grep）**，而機械讀數是：**R2 那一次結構上撈不到答案卷，真正打開 `examples/消失的密室/` 的是 R3 與 R4，兩者都沒有被記**。**這一格不影響 fallback 的 0，但它影響「哪一輪碰過答案卷」這條線**——那條線是 purity 的第四類濾網要用的，記錯輪次就會扣錯東西。**建議下一輪的 `fallback-log.md` 改記機械讀數（哪一支 agentId、哪一次呼叫、返回幾行），不記神諭自述。**

**⚠️ 神諭讀取範圍：S8 第一次數出來是 58%，本輪是 88%（100/114）** ——在基座 §3 給神諭的授權三格（`raw/` ＋ `情境測試/` ＋ fallback `examples/`）**之外**的呼叫佔 88%：`消失的密室/story/`＋`chapters/`（含四章正文全文、承諾區、裁決流指定列、`物件/*.md`、`風格.md`）、`結構定義/`、`技巧知識庫/`（R1 讀了 `敘事距離與DeepPOV.md`、`心理描寫.md`）、`tools/prose_metrics/src/`（R1 讀了 `rhythm.py` 三次）、以及 `git log`／`git show HEAD:`（R4）。
> **方向仍然是好的**——本輪四次「作者當場駁回 AI 的前提」（見第二節）沒有一次做得到，如果神諭只讀得到 `raw/` 與腦內檔：**駁回【高 1】要讀 `rhythm.py:305-309`，駁回【高 2】要讀 `敘事距離與DeepPOV.md:13-22`，駁回【高 3】要讀 `風格.md:60`，駁回【中 4】要讀 `裁決流:219` 與四章正文。**
> **但 58% → 88% 是一個趨勢，不是一次波動**：`SNN-*.md` 第二節的「來源：腦」現在涵蓋的東西，有 88% 不在基座定義的「腦」裡面。**交階段 3：基座 §3 那張表要嘛改對，要嘛承認「神諭讀書內產物與系統套件」是刻意的能力並寫進去——連續第二輪提。**

### ⚠️ 一格與越界無關、但只有讀 transcript 才看得到：**受測體從頭到尾沒有載入 `write-test` 的方法本體**

- 受測體 128 次呼叫裡 **`Skill` 工具 0 次**。
- 它與任何一支 `SKILL.md` 的唯一接觸是 **#15**：`grep -rn "uv run --project" .claude/skills/write-test/SKILL.md | head -30`。
- 拿 `write-test/SKILL.md` 的 **77 條長行**逐條去比對受測體 transcript 全文 ⇒ **命中 5 條，5/5 都是 `uv run --project …` 那幾行**（`settings-select`／`prose-metrics` ×2／`ch-lint`／`style-lint`）。
- **11 項檢查表、執行步驟、輸出格式、「三條分流路」、「禁用詞讀 `style-lint` 別自己數」——一個字都沒有進入它的 context。**
- 導演的 prompt 逐字核過：**沒有出現 `write-test` 這五個字**，也沒有列那 11 項。

**這與越界稽核無關，但它改寫了第一節「路由全對」的意思**：本輪測到的是「作者用自然語言描述需求 → 產出長得像 `write-test` 報告的東西」，**而中間那支 skill 的方法本體從未被載入**。它照樣跑了 13 支閘門＋`prose-metrics`、照樣分了高/中/低、照樣附了「我需要作者裁決的事」——**但那是模型本身的行為，不是 skill 的行為**。**沒有任何工具量得到「skill 被觸發了幾次 vs 只有它的指令被 grep 走了幾行」**，而這正是基座 §9 最後一條「鷹架有沒有用（循環性）」要的那種讀數，**本輪是它第一次以機械形式出現**。交階段 3。

**本 session 判定：受測體未污染（越界 0）；神諭未寫進 repo（2 次寫入全在 repo 外的共用暫存區）；導演未觸及答案卷（0 次）。四格全部是機械讀數，不是自述。**

## purity 檢查

```
purity：比對了 541 個 token，候選 1 個（不套節層級過濾）／2 個（套上，依 S8 交辦）
purity（三把尺分開印）：主濾網 1 ／ 再扣神諭 transcript 0（**假的**）／ 時間戳判準 1
purity（實質）：兩個候選都是通用中文詞，**帶 canon 內容的候選 = 0**
purity（洗白帳）：183 個命中裡，11 個是被系統自己／觀測者自己／神諭自己的字扣掉的（6.0%）
```

### ⚠️ 先拆歸屬前提——**本輪「`git diff` 的新增行＝受測體的產物」不成立，而它是可以機械拆開的**

導演交辦的那一格是對的，但拆法不必靠讀感。做法：把 56 行新增逐行切成 **10 字視窗（18,327 個）**，逐個去問「這個視窗在不在**神諭自己的 assistant 文字**裡（25,374 字，只取神諭說的話，不含它讀到的 `tool_result`）」與「在不在 **baseline 書內全樹**裡（228,711 字）」：

| 產物分層 | 新增行 | 10 字視窗 | 在神諭自己的話裡 | 兩邊都不在（＝受測體自己寫的字） |
|---|---|---|---|---|
| **正文（`chapters/ch000{1,2,4}.md`）** | **6** | **108** | **108（100%）** | **0（0%）** |
| 規則層（裁決流 15,513／幕綱 739／待裁決 622／洛哈 597／其餘） | 50 | 18,219 | 4,202（23%） | 13,447（74%） |
| **合計** | 56 | 18,327 | 4,310（24%） | 13,447（73%） |

**⇒ 六句正文 100% 是作者親手給的字，逐句可追到時間戳**：4 句最早出現在 **R2 的 assistant 訊息（`16:39:33.557Z`）**、2 句最早出現在 **R4（`17:17:45.689Z`）**，兩者都早於受測體的 `Edit`。而且六處**全部是既有句子的語序改寫**，不是新寫的句子（`git diff -U0` 逐處可見：`把最後一封放回去的時候，他的手指…` → `他把最後一封放回去，手指…`）。
**⇒ 本輪 purity 的正文層樣本 ＝ 167 字元／143 漢字，而且那 143 個漢字沒有一個是受測體發明的。** 所以本節的三把尺**分開量了三次**（全部／正文／規則），下面每一欄都標明是哪一層。
> **這件事本身是本測試第一次**：前八輪的產物歸屬只有兩種可能（受測體寫的／導演寫的），本輪出現第三種——**受測體按鍵、作者出字**。`git diff` 分不出來，`derived-sync` 分不出來，**分得出來的只有 transcript 的時間戳**。

**比對範圍**：系統產物 ＝ `git diff 5636530` 在 `消失的密室/` 的**新增行 56 行·18 支已追蹤檔·0 支新檔**，合計 **20,015 字元／11,291 漢字**（其中 **167 字元是正文**——本測試正文量最小的一輪，S8 是 7,613 B）。答案卷 ＝ `examples/消失的密室/` 全 18 章 **168,830 字元**。合法來源（**靜態三類**）**534,262 字元**（不套過濾）／**459,667 字元**（套上 S8 交辦的節層級過濾）＝ `消失的密室/raw/`@baseline（4,677）＋`情境測試/消失的密室/`（300,874 → **226,279**，17 支）＋**書內 baseline（`5636530`）全樹**（115 支檔／228,711）。神諭 transcript（1,242,916 字元）**單獨列為第四類**。

**541 個 token 的取法（沿用 S3–S8 的五路聯集）**：產物的檔名／目錄名／產物裡 `〔〕「」『』《》**[[]]` 內的詞／**答案卷專名·道具語彙詞庫**與產物的交集（詞庫由字元黏著度建：答案卷全部 2–5 字 n-gram 取「出現 ≥4 次且 `count(gram)/min(count(字))≥0.60`」，共 **727** 支——**與 S4／S5／S7／S8 完全相同，同一把尺在同一份答案卷上的第五次重跑**）／數字·計數 token／**100 個 canon 定向探針**（S8 的 89 個＋本輪對象 `對角巷`／`女貞路`／`水蠟樹街`／`級長徽章`／`泥巴種`／`咬人`／`茶杯`／`老太太`／`貓頭鷹信`／`蒙當葛`／`弗列契`）中出現在產物的 **31**。去重併集 **541**，其中**命中答案卷的有 183 個**。

### S8 交辦的第二格：節層級過濾（**套上／不套上兩個數字都印**）

**實作**：`情境測試/消失的密室/` 的 `SNN-*.md` 一律砍掉 `--- 以下由觀測者 agent 寫 ---` 之後的兩節（`## 防火牆稽核`／`## purity 檢查`），其餘檔（`P-前置.md`、四支腦內檔、`fallback-log.md`、`session台帳.md`、`需求機制帳.md`、各 `SNN-*.md` 的前四節）原樣保留。**砍掉 74,595 字元 ＝ 該資料夾的 24.8%。**

| 過濾階段 | 全部產物 | 正文層 | 規則層 |
|---|---|---|---|
| 產物 token | 541 | 28 | 538 |
| 命中答案卷 | 183 | 17 | 180 |
| 扣掉靜態三類（**`情境測試/` 不過濾**·主濾網舊版） | **1** | **0** | **1** |
| 扣掉靜態三類（**`情境測試/` 套節層級過濾**·S8 交辦版） | **2** | **0** | **2** |
| 再扣掉四支神諭 transcript（第四類·單列） | **0** ← **假的，見下** | 0 | 0 |
| **時間戳判準**（S6 那把尺：這個 token 第一次被寫下時，說話的是誰） | **1** | **0** | **1** |

**兩個候選逐筆列**：

| token | 產物出處 | 答案卷出處 | 為什麼不在合法來源 | 判讀 |
|---|---|---|---|---|
| **`公共`** | `裁決流.md:225`「它不是先搶先贏的**公共**天花板，是洛哈的常態被寫進了規格」 | 「葛萊芬多**公共**休息室」，7 章共 20 次 | `raw` 0／書內 baseline 0／`情境測試/` 0（**兩版都 0**） | **通用中文詞的同形。** 產物講的是「共用的上限」，答案卷講的是 common room。**首現：受測體 `16:31:22Z` 的 assistant 訊息**（`grep` 四支 transcript：神諭 R1 從頭到尾沒說過這兩個字，導演的轉述也沒有）⇒ **時間戳判準下它是受測體自己的字，留在候選裡** |
| **`幾張`** | `裁決流.md:230`＋`幕綱/arc01.md:8`「每俯衝一次多**幾張**臉抬起來」 | 「**幾張**從《與山怪同行》裡掉出來的紙頁」等 4 章各 1 次 | `raw` 0／書內 baseline 0／`情境測試/`**不過濾** 1（**在 `S3-*.md` 的 purity 報告表格裡**）／`情境測試/`**過濾後** 0 | **首現：神諭 R2 的 assistant 訊息 `16:39:33.557Z`**——**這是作者親口說的字**，時間戳判準下它出局 |

### ⭐ 節層級過濾拿到了它要證明的東西，而且是同一輪內的閉環

**`幾張` 唯一一次出現在 `情境測試/消失的密室/` 的位置，逐字是 `S3-*.md` 的 purity 報告裡我的前任寫的一句話**：「raw／腦內檔／S3 elicitation 都沒有說過學院有**幾張**桌子」。

⇒ **S8 觀測者的假設（「觀測者在報告裡列過的字，下一個 session 起就永遠白了」）本輪拿到第一個乾淨的正例，而且是可覆算的**：套上過濾，`幾張` 從「本來就合法」變成候選；不套，它連出現都不會出現。S8 是用 `弗雷`／`喬治`／`派西` 三個**名字**推論的，**本輪這一個是「觀測者在論證『系統沒抄』的那句話裡，親手把要防的字寫進了合法來源」**——攜帶者不只是量測報告，是**量測報告裡的否定句**。
> **代價也要記**：套上過濾之後候選從 1 變 2，而多出來的那一個（`幾張`）**在時間戳判準下馬上又出局**（作者先說的）。⇒ **節層級過濾本輪的淨效果 ＝ 讓一個假的白洗浮出水面、但沒有改變最終候選數。** 它的價值不在這一輪的數字，在**它把「合法來源」這個分母從 534,262 縮到 459,667 且不再單調膨脹**——不套過濾的話，每一輪的反向讀數清單（本輪 65 個 canon 專名）都會直接進分母。**建議定為常設，兩個數字繼續並印。**

### 第四類（神諭 transcript）為什麼是假的 0

`公共` 在四支神諭 transcript 裡只出現在 **R3 `16:41:03.410Z`** 與 **R4 `17:07:53.409Z`**，兩者都是 **`tool_result`**——那是它們讀 `裁決流.md` 讀到的。**受測體寫下那一列是 `16:31:22Z`（assistant），第一批三列 append 進檔案早於 R3 的讀取。⇒ 晚 9 分 41 秒。**
**扣掉一個下游的東西，等於用產物洗產物。** S8 已證一次（6/7 個候選），S9 再證一次（1/1）。**這一格連續兩輪成立，可以定案了：第四類濾網必須帶時間戳，否則它結構上會把所有候選洗成 0**——只要神諭在收工前讀過產物，而本測試的協定就是讓它讀。

### 長逐字重疊掃描（S6–S8 那一組的原樣重跑）

| 掃描窗 | 全部產物 | 命中答案卷 | 扣掉靜態三類後 | 正文層命中 |
|---|---|---|---|---|
| 12 字 | 10,829 | **0** | 0 | **0** |
| 10 字 | 10,910 | **0** | 0 | **0** |
| 8 字 | 10,992 | **0** | 0 | **0** |
| 7 字 | 11,033 | **1** | **0** | **0** |
| 6 字 | 11,074 | **4** | **0** | **0** |

**正文層 6 字以上零重疊（113 個 6 字窗、0 命中）——本測試九個 session 以來第一次連 6 字都是 0。** 全部產物的 5 筆逐筆追過，**5/5 都來自同兩支檔的既有內容**：`raw/名字與人物.md` 那一行改寫（`華麗與污痕書店`／`麗與污痕書店`／`華麗與污痕書`／`麥朵的盥洗室`——**該行 baseline 就有，本輪只是把 `水蠟樹街` 劃掉**）與 `裁決流.md` 引 `raw` 的 `水蠟樹街四號`。**沒有一筆是新寫的字。**

### ⚠️ 本輪最重要的 purity 讀數，而這把尺看不見它：**`女貞路`**

| 欄 | 讀數 |
|---|---|
| 這是什麼 | 本輪【中 12】的裁決：街名統一成「女貞路」，並在 `raw/名字與人物.md:21` 把作者原本寫的 `水蠟樹街` 就地劃掉 |
| `女貞路` 在答案卷 | **0 次**（18 支檔全掃） |
| `水蠟樹街` 在答案卷 | **5 支檔** |
| `女貞路` 最早進書的時間 | **`416ab05`（2026-08-01，S6 `write` 第一批正文）**——`git log -S` 實查 |
| 那時 `raw/名字與人物.md` 寫的是什麼 | **`水蠟樹街四號`**（P session 起，作者自己寫的） |
| 本輪發生了什麼 | 作者拍板「以正文為準」，於是 **S6 系統發明的那個名字被回寫進作者的源檔**，作者自己原本的詞被劃掉 |

**判讀：這是本測試第一次出現「系統的發明升格成作者的源」，而 purity 的靜態濾網結構上看不到它——因為那個發明不在答案卷裡。**
> `女貞路` 是同一個 canon 實體（Privet Drive）的**另一個中文譯名**。答案卷用的是一種譯法，模型寫出來的是另一種。⇒ **`grep examples/` 這把尺量的是「有沒有抄這份語料」，量不到「有沒有從訓練資料裡認出這本書」**，而基座 §8 的 purity side-lane 原文要防的正是後者（「系統 agent 即便不讀 `examples/` 仍可能**認出**這本書而憑記憶重建」）。**本輪這一格是那句話的第一個機械實例，而它是靠反向讀數（探針表裡 `女貞路` 落在『不在答案卷』那一欄）偶然照出來的，不是主濾網照出來的。**
> **同一輪還有一個方向相反的對照組，這讓它更可讀**：另一條街名 `對角巷`→`斜角巷`。**`斜角巷` 是作者 `raw/名字與人物.md:21` 從 P session 就有的詞，也是答案卷的詞（2 章共 7 次）；`對角巷` 是系統在 S2 `organize` 寫進 `大綱/arc01.md:97` 的發明，答案卷 0 次。** ⇒ **同一輪、同一支 agent、同一種操作（統一街名），一條把系統的發明升成源、一條把系統的發明降回作者的詞**，而**決定方向的兩次都是作者，不是任何一支閘門**。
> **可機械化的最小處置（交階段 3）**：現行 purity 只有「產物 → 答案卷」一個方向。要照到這一格，需要第二個方向——**盯「作者源檔（`raw/`＋腦內檔）明文寫過一個值，而產物用了另一個值」的每一格**。這在本書是可 `grep` 的（`raw/名字與人物.md` 就是一份專名清單），而**目前沒有任何工具在產這份清單**。S8 交出的那一條（「幕綱行動欄有量化敘述而沒給數字」）是同族的另一半：**那一條盯的是上游沒給值，這一條盯的是上游給了值而下游改了。**

### 洗白帳：183 個命中逐個歸類

| 來源類別 | 幾個 | 判讀 |
|---|---|---|
| **A 作者側來源**（`raw/`＋`情境測試/`**已剔除觀測者節**） | **172** | 乾淨 |
| **B 只在書內 baseline**（系統自己前幾 session 寫進去的字） | **9** | `兄弟`／`我媽`／`我爸`／`椅子`／`踉蹌`／`躺在`／`輩子`／`陌生`／`頭髮`——逐個查首現，**9/9 最早都出現在受測體讀 `chapters/ch000*.md` 的 `tool_result`**（S6／S8 寫的正文） |
| **X 只靠觀測者自己的稽核／purity 節才合法** | **1** | `幾張`——見上，**節層級過濾的唯一實例** |
| **D 只在神諭 transcript** | **1** | `公共`——**不是洗白，是候選被錯誤洗白** |
| **E 不在任何合法來源** | **0** | — |

**洗白數 ＝ 11（9＋1＋1），佔 183 個命中的 6.0%**（S7 是 13.4%、S8 是 12.7%，**本輪腰斬**）。原因是機械的：**本輪正文只有 167 字元**，而 B 類（系統自己寫進書裡的字）主要由正文貢獻；規則層用的是元語言（「射程」「承諾區」「排除線」），與答案卷的重疊本來就低。
**帶內容的洗白：0 個。** S8 那筆 `徽章`（B 類、整條誤導線的地基）本輪沒有新的同型。

### 反向讀數

100 個 canon 定向探針裡，**答案卷有而產物一次都沒出現的有 65 個**：`佛地魔`／`佛客使`／`佩妮`／`分類帽`／`史萊哲林`／`哭泣的麥朵`／`多比`／`夫子`／`女生盥洗室`／`學校董事會`／`家庭小精靈`／`忌辰晚會`／`拿樂絲太太`／`掃帚`／`搏格`／`斑斑`／`曼德拉草`／`決鬥俱樂部`／`湯姆瑞斗`／`皮皮鬼`／`石內卜`／`石化`／`破釜酒吧`／`福特安格里亞`／`禮堂`／`芽菜`／`董事`／`蛇怪`／`貓毛`／`跩哥`／`達力`／`鄧不利多`／`阿茲卡班`／`阿辣哥`／`隱形斗篷`／`飛七`／`馬份`／`麥教授`／`麻種`／`龐芮`／`保密法`／`咆哮信`／`妙麗`／`日記`／`未成年`／`爬說嘴`／`純血`／`繼承人`／`魔法部`／`潘妮`／`比爾`／`查理`／`柯林`／`海格`／`賈斯汀`／`變身水`／`麻瓜`／`級長`／`徽章`／`咬人`／`九家`／`抄查`／`蒙當葛`／`弗列契`／`級長徽章`。
**產物裡的 31 個探針，31/31 在靜態合法來源裡查得到**（`夜行巷`／`威農`／`斜角巷`／`洛哈`／`渾拚柳`／`華麗與污痕`／`霍格華茲`／`血統`／`開除`／`魔杖`／`喬治`／`嘿美`／`弗雷`／`派西`／`衛斯理`／`貓`／`貓頭鷹`／`亞瑟`／`哈利`／`密室`／`榮恩`／`洞穴屋`／`莫莉`／`金妮`／`魯休斯`／`地精`／`拔地精`／`花園`／`對角巷`／`女貞路`／`水蠟樹街`）。
> **三格值得單記**：⑴ **S8 的 `九家` 本輪 0 次**——那一格是「上游沒給值而正文必須給」的實例，本輪正文只改了 6 句語序，**這種格子的數量是 0，命中也是 0**。⇒ **S6 第①條假設（「命中率是『上游有沒有給值』的函數，不是模型記不記得」）本輪拿到第三筆一致證據，而且這一筆是零分母那一端。** ⑵ **`級長`／`徽章`／`級長徽章` 本輪 0 次**——S8 記過「這本書整條誤導線的地基是系統六個 session 前放進去的詞」，**本輪那條線動了四格（派西的四件事）而那三個詞一次都沒出現**。⑶ **`女貞路` 與 `對角巷` 兩個探針都在產物裡，而兩者在答案卷都是 0** ——見上一節。

**專屬這本書的詞（逐個實測答案卷命中數）**：`喜劇引文段` **0**（產物 8 次）／`溫度極性` **0**（2）／`釋放幕壓薄` **0**（2）／`哈利的內心不得寫成感受` **0**（1）／`整段不可兌現` **0**（1）／`承諾區` **0**（13）／`句長中位` **0**（2）／`段首公式` **0**（3）／`三次坦白` **0**（1）／`幕號字面` **0**（1）／`隱形裝置` **0**（2）。**本輪新建的 2 條約束名（`哈利的內心不得寫成感受`／`喜劇引文段每 arc 一段`）在答案卷 0/2 命中。**

### 讀數性質（接 S6／S7／S8 交的三條）

| session | 產物型態 | 漢字 | 候選（主濾網／時間戳） | 8 字以上逐字重疊 |
|---|---|---|---|---|
| S2（organize） | 8 支 arc 事件序列 | — | 5／1（邊界假象） | — |
| S3（橫向三支） | 設定補厚＋arc05 長厚 | — | 7／1（邊界假象） | — |
| S4（輕量三件＋character） | 裁決與帳 | — | 2／0 | — |
| S5（beat-sheet＋beat-test） | 15 幕八欄＋兩支角色檔 | 16,910 | 2／1（`佩妮`） | — |
| S6（write＋write-test＋revise） | 全書第一批正文 | 8,122 | 13／13（2 個 🔴） | 0 段 |
| S7（character＋worldbuild） | 設定層＋裁決層，0 字正文 | 11,699 | 0／0 | 0 段 |
| S8（beat-sheet＋write ch0004） | 設定＋裁決＋第二批正文 | 12,110 | 7／7（帶內容 1：〔九家〕） | 0 段 |
| **S9（write-test 四章合審＋revise）** | **規則層 15,513 窗＋6 句語序改寫** | **11,291（正文僅 143）** | **1／1（帶內容 0）** | **0 段（連 6 字都 0）** |

1. **S6 第①條假設本輪第三次成立，而且是最乾淨的一次**：產物漢字量與 S8 相當（11,291 vs 12,110），而「必須填一個上游沒給的值」的格子數量是 **0**（本輪不寫新正文），候選就掉到 1 且帶內容 0。**兩個變數（產量、格子數）本輪第一次分離**——S6→S8 的產量與格子數同向變動，S9 產量持平而格子數歸零。
2. **S6 第②條（三段修法做完後回頭重算）本輪仍未執行**，但 S8 交出的那條理由本輪已可結案：**三把尺在 S8 給 7／0／7，在 S9 給 1／0／1，兩輪都是「第四類 = 假 0」**。⇒ **建議把第四類從主濾網永久除名，只保留「靜態三類（套節層級過濾）＋時間戳」兩把尺**，`SNN-*.md` 的覆蓋率行照這兩個數字印。
3. **本輪新交一條可證偽的**：purity 現行的單一方向（產物 → 答案卷）**照不到「模型認出這本書、用了答案卷之外的另一種譯名」**（`女貞路`）。**檢驗場是下一輪任何一次 `write`**——具體做法：把 `raw/名字與人物.md` 那一行的專名逐個抽出來，`grep` 產物，看有沒有第二個「作者寫 A、產物寫 B、而 B 不在答案卷」的格子。**若有第二例，這一格就不是偶然。**

**依基座 §8：候選 1 是有效性讀數、不是扣分；洗白 11 是有效性警訊、不是扣分。** 這一輪只回答一件事：**產物沒有抄任何一段原文（連 6 字都零），而正文那 143 個漢字全部出自作者的嘴——所以本輪 purity 的讀數乾淨，但它同時也是本測試迄今樣本最小的一次。**
