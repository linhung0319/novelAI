# 一世之尊 · 端到端貫穿測試 · session 台帳

> 測試 meta，**只放 `情境測試/一世之尊/`，永不進書資料夾**（進去＝污染系統 agent）。
> 新 session 讀這份 → 照 `../端到端貫穿測試流程.md` §5 續跑協定開跑。

## 進度總覽
| Session | 階段 | 範圍 | 狀態 | commit |
|---------|------|------|------|--------|
| P  | 前置：產 raw＋情境測試腦＋init 書架 | — | ✅ | dfec88f |
| S1 | develop（raw→摘要＋設定冷啟動） | 全書 | ⬜ 未開始 | — |
| S2 | organize→outline-test（→大綱） | 全書大綱 | ⬜ 未開始 | — |
| S3…| 逐 arc：beat-sheet→beat-test | story/幕綱/arcNN | ⬜ 未開始 | — |
| …  | 逐 arc：write→write-test→revise | chapters/chNNNN | ⬜ 未開始 | — |

## 下一步（新 session 讀這段）
- **要做**：S1 = `develop`，範圍＝全書（實則卷一冷啟動）。讓系統從 `一世之尊/raw/`
  收斂主線／題旨／基調／風格，冷啟設定層（世界觀／角色／風格）與 `00-摘要`。
- **前置狀態**：raw 已就位（刻意凌亂、留缺口）；書架已 init（模板空骨架）；
  就緒儀表尚全空。上游無任何已產出物。
- **待決 elicitation**：無（P 不跑 elicitation 迴圈）。
- **S1 開跑提醒**：用 `啟動prompt.md` B 段（標準 session）貼進乾淨新 session。
  守三角色硬防火牆——系統 agent 冷啟、**不得讀 examples/ 與 情境測試/**；作者 agent
  的答案標來源（腦／fallback:examples），fallback 記進 `fallback-log.md`（首次 fallback 時建）。

## 刻意留下的 elicitation 缺口（S1+ 會撞到，這是設計）
系統若要往下走，**必須問出**（答案在本資料夾 `作者腦內-*` 各檔）：
- 成長引擎「輪回世界」的完整規則（善功／兌換分級／任務間隔／難度遞增／會死人／
  同伴機制／開竅×丹藥鐵律）——raw 只給了「存在」的鉤子。→ `作者腦內-成長引擎.md`
- 修煉境界全貌、江湖勢力地圖（四寺三宗六派…）——raw 只有殘缺階梯與一堆問號。
- 人物對號入座（少林群僧、輪回同伴各自門派）——raw 只給影子與名字池。→ `作者腦內-人物誌.md`
- 小玉佛的來歷與作用、孟奇原身（侯府二少爺／已筑基／被送入寺之謎）。→ `作者腦內-設定與伏筆.md`
- 卷一收尾的盜經案來龍去脈、機心咬字過關。→ `作者腦內-卷一走向與主題.md`

## 累積留意（長程效應追蹤，逐 session 更新）
- **跨 arc 伏筆**：①小玉佛真相（卷一埋、不揭，全書級）②孟奇穿越身份 ③原身被送入寺
  之謎／玄藏知情 ④輪回世界的真正目的（全書級）。→ 目前皆未進正文，待 S1+ 決定如何種。
- **設定自洽風險**：目前無（尚無產出）。留意點：兌換分級 vs 現實世界兵器分級須同一套；
  開竅×丹藥「一藥一次」規則後續章節須一致。
- **角色狀態漂移**：目前無（尚無產出）。孟奇戰力起點極低，注意後續成長曲線別暴衝。

## honesty guards 狀態
- 硬防火牆：系統 agent 冷啟 prompt 已規定不得讀 `examples/`、`情境測試/`（S1 起執行）。
- fallback-log：尚未產生（首次作者 agent 走 examples fallback 時建 `fallback-log.md`）。
- purity 檢查：持續盯系統是否吐出「沒被告知的原著專屬細節」（命中＝有效性警訊、記一筆）。

## 每 session 留痕
- 每個 session 收場另寫 `SNN-<階段>.md`（格式見 `../端到端貫穿測試流程.md` §5）。
- P session 無 elicitation／無系統路由，故不另建 SNN 記錄；本台帳 P 列即為留痕。
