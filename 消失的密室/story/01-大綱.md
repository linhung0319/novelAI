# 大綱

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `organize` 依 schema 填寫；檔案存在不代表這一層已經有東西。
> **`develop` 刻意不產大綱**——raw 還亂的時候先跑 `develop` 收斂大方向，別因為看到這個檔存在就跳過粗階段直接拆幕。


（依 `結構定義/大綱.schema.md` 填寫。）

## 選用結構公式

（公式名照 `技巧知識庫/結構公式.md` 的 registry 字串寫；α 變形一條一列，只標「是什麼變形」，為什麼變形屬 `story/參照/裁決流.md`。「哪一階段對應哪幾幕」不寫在這裡——跑 `structure-project`。）

## 故事全文（連續敘述）

## 結局與題旨
- 結局：（一句話 ＋ 指標。**「結局是什麼」的權威在 `00-摘要.md`「結局方向」，本檔不複述**。）
- 題旨：因為（X），導致（Y）。
- 通向結局的情節路徑：（**大綱層獨有的東西**——分簇與匯流、揭底手法、結構公式怎麼收。）
