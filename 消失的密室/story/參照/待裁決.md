# 待裁決

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由下游 skill 在撞到問題時 append。
>
> 依 `結構定義/待裁決.schema.md`：下游流程（`beat-test`／`write`／`write-test`／`expand`／`revise`…）在細節層發現「問題根源其實在上游設定／摘要」時，先落地記錄於此，交作者裁決是否回寫**源檔**。**只記錄，不代寫。**
>
> **這支檔的合約**（`共同約定.md` 零）：AI 寫、**你可以直接改、改動會留著**（沒有東西會重生它）、格式由 **`decision-lint`** 守。它與大綱、幕綱、`story/物件/<名>.md`、`裁決流.md` 同一列：**作者利器·直改**。
>
> **這裡住「還沒裁決的」；「已經裁決的」在 `裁決流.md`。** 兩者是**兩筆內容**：這一筆的決定權在 AI（它是觀察），那一筆的決定權在人（它是裁決）。一列混兩種決定權就該拆——實測混住的下場是那個只允許一個值的 `狀態` 欄被寫成 440 字論證。
>
> **沒有 `狀態` 欄，這是刻意的。** 這張表只住待裁決的，狀態是恆真的。**離開＝刪列**，而刪列的同時要在 `裁決流.md` append 一列記下「為什麼這樣裁」。
>
> **查詢**：`uv run --project <套件根>/tools/decision_projection decision-project --book <你的書> [--target 設定/角色/<名>/]`——它**一次吐兩節**（裁決＋待裁決，兩軸共用 `標的` 選擇器），所以消化前查一次就同時看到「還沒裁決的」與「已經裁決過的」。
>
> **落檔前跑 `decision-lint --book <你的書>`**：恰四欄、日期格式、標的存在性、`發現` 欄長度。

| 日期 | 來源 | 標的 | 發現 |
|------|------|------|------|
| 2026-07-31 | character（角色需求層第 1 輪） | 設定/世界觀/血統與歸屬.md | 該檔「四格表」那一段寫著洛哈與看門人同格，作者本輪已改判 2×2、飛七獨佔「沒能力×選對」。這一段要跟著改，本輪未動 |
| 2026-07-31 | character（角色需求層第 1 輪） | 00-摘要.md | 題旨那段的「第五格是看門人」說法作者本輪已作廢（四格表不會有第五格），改採 2×2、飛七入格。這一句要刪，本輪未動 |
| 2026-07-31 | character（角色需求層第 1 輪） | 全書 | `所屬arc` 欄的原始消費者（rollup）已廢除，現在只剩 `char-lint --emit` 印它，而全書 5 支衍生檔會全是 `[]`。這欄還要不要留，請作者定 |

<!-- 欄位說明（實際使用時可刪）：
  日期＝發現當天 YYYY-MM-DD；
  來源＝哪個 skill／哪次測試（例：`beat-test 測試4（幕017–020）`、`write（ch0007 撰寫時·承重細節缺口）`）；
  標的＝**要改哪個源檔**，寫相對 story/ 的路徑（`設定/角色/凱.md`、`00-摘要.md`）。它是選擇器，不是散文——
        寫錯路徑這筆回饋就再也查不到，`decision-lint` 會報。跨檔的通則寫「全書」；
  發現＝一句話講清楚問題是什麼（≤200 字）。論證不寫這裡，裁決之後寫進 `裁決流.md` 的「理由」欄。
範例：
| 2026-07-25 | write-test 測試9（ch0046） | 設定/角色/少年/核心.md | 其「需要」中段就被滿足，弧線塌陷 |
-->
