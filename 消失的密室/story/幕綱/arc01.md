# arc01

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `beat-sheet` 依 schema 填寫；檔案存在不代表這一層已經有東西。
> 大綱就緒後才拆幕。
> （`beat-lint` 認得上面這個「尚未產出」標記並跳過本檔，覆蓋率行會印「跳過 N 支骨架」。）

（依 `結構定義/幕綱.schema.md` 填寫。**分區是硬規定**：承諾區 → 各幕 → 伏筆狀態表（選用）→ 檔尾設計註。
每一幕一個 `幕NNN · 幕名` 的二級標題，含固定八欄位；連結語法見 schema「連結規範」。
欄位格式一律以 schema 為準，**本骨架不再抄一份範例**。）

> **樣板各節 2026-07-31 清掉，理由是機械的**：原本那份 `幕001 · （幕名）` 範例標題會被
> `fact_projection/objects.py` 的 `_BEAT_HEAD_RE` 命中，於是 `unbuilt_arcs()` 把 arc01 算成
> **已拆幕**——`揭示層級` 的「揭示點待落幕」判定就少數一支 arc。
> **兩把尺不同**：`beat-lint` 認上面那個「尚未產出」標記並跳過，`unbuilt_arcs()` 不認、只看有沒有幕標題。
> 在骨架檔裡放一份帶真標題的範例，等於對第二把尺謊報進度。
