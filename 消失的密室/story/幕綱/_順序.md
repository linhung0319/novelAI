# 幕綱順序

> ⚠️ **尚未產出**——本檔是空骨架，只是先把位置佔好。實際內容由 `beat-sheet` 落檔時維護。
> **這是源檔（`設計原則.md` A1）**：arc 的故事先後是作者的創作決定，沒有任何檔算得出來，
> 所以**你改就是新的真相**——它不重生、不會被覆蓋。格式由 `beat-lint` 守。
> **三行以內。** 它只回答一個問題：這些 arc 誰先誰後。其餘一律不進本檔。

- 全書順序：arc01 → arc02 → arc03 → arc04 → arc05 → arc06 → arc07 → arc08（幕的先後以此排列為準，**非幕號大小**）
- arc09（換回＋收尾）綁在未拍板的分叉一上，尚未建大綱檔，故不在序上；建檔那天補進本行。

<!--
為什麼它是獨立的一支檔（2026-07-28 功能 12 抉擇 2 A）：

`全書順序：` 是四支工具共用的定序來源（`foreshadow-project`／`beat-metrics`／
`fact-project`／`decision-project`），而它原本住在 `_index.md`——一支被 `beat-lint`
驗成「視圖 ≡ 資料夾」的索引檔。**同一支檔裡一半是源、一半是視圖**，那是六問 Q0
的違反；而 `設計原則.md` A1 同輪補的那句正是這件事：有一支 lint 在驗某支檔 ≡ 別的
檔，那條 lint 就是一條只報錯不產生的 inbound 規則，那支檔就不是純粹的源。

已駁回「證明它 ≡ 檔名排序後刪掉，四支工具改用 sorted(arcNN)」（抉擇 2 C）：
實測 10/10 個歷史實例 spine 與檔名排序相同，但刪掉它省下的是 133 字元，賭上的是
系統宣稱了三個月的「arc 可亂序下沉」自由度。見 `docs/重構/02-待用構想.md`。

既有書的 spine 可能還住在 `_index.md`：四支工具**新落點優先、舊落點回退**，而
`beat-lint` 的覆蓋率行會印它實際讀到哪一支、走回退時多報一行——回退是可見的。
-->
