---
generated-from: 626d813351c9
generated-at: 2026-07-25
角色: 符真真
定位: 配角
弧線類型: （源檔未載，待跑 `character` 補）
所屬arc: [卷一(arc02–arc03), 卷二(arc05, arc07, arc08)]
暫定: false
伏筆: { 埋: [], 收: [] }
🧊水下: []
---
> ⚠️ **甲批機械可見度檔（2026-07-23·純機械產生）**：本檔內容**只搬既有材料**——源檔 `符真真.md` 原話、`_index.ai.md` 既落欄位（定位／一行需求／所屬卷）、以及已落定的硬事實。**未做任何四象限分析、未補任何新設定、一個字都沒有替作者發明。**
> **它為什麼存在**：`tools/derived_sync` 從**衍生檔**出發掃描，「源檔有、衍生檔根本不存在」的缺口它在定義上掃不到（成因與處置見 `參照/就緒儀表.md` 偵測盲點段）——2026-07-23 真觀升格那次就是靠人眼才發現的。本檔讓系統看得見這個角色。
> **要厚度請跑 `character`**（本角色的觸發條件見 `_index.ai.md` 文末「乙批·`character` 觸發條件表」）。**`暫定` 維持 `false`**——本檔沒有任何一項在等作者拍板（`暫定` 欄的語意就是「待拍板」）；四象限補不補**已由該表拍定**，「四象限待補」記於各格與本警語，**不佔用 `暫定` 欄**。

## 源檔既載要點（原話搬運）
- 北周**影華庵**俗家弟子。十七八歲、**鵝蛋臉、溫婉甜美**。
- 由**張遠山**帶進同伴組，兩人有**曖昧苗頭**。是隊裡較新、較嫩的一個。
- 源檔標注：次要角色，卷一自然帶出即可。

## 需求四象限
- 期盼（動力）：新入這個真會死人的圈子，**站穩、別拖累人、被這群人接納**——溫婉底下是想證明自己不只是「被帶進來的花瓶」。
- 想要（誤導／假目標）：（源檔未載，待跑 `character` 補）
- 落差（阻礙）：（源檔未載，待跑 `character` 補）
- 需要（圓滿）：（源檔未載，待跑 `character` 補）

## 預期弧線
（源檔未載，待跑 `character` 補）

## 馬斯洛層次
（源檔未載，待跑 `character` 補）

## 對衝關係
（源檔未載，待跑 `character` 補）

## 待裁決回饋
| 日期 | 來源 | 發現 | 建議回寫（改哪個源檔） | 狀態 |
|------|------|------|----------------------|------|

（本檔 0 筆待裁決。）

> **2026-07-25（`sync` 順向清查·`所屬arc` 一次補齊·機械對齊·非重新設計角色）**：`所屬arc` 由 `[卷一]` 改為 `[卷一(arc02–arc03), 卷二(arc05, arc07, arc08)]`。
> - **在場**＝卷一 arc02（幕102–106）／arc03（幕203–210）·卷二 arc05（幕404–407·`ch0033`–`ch0036`）／arc07（幕601–603·`ch0049`–`ch0050`·正文亦以「真真」帶稱）／arc08（幕702·`ch0062`）。**不在場＝設計非漏標**：arc06／arc11 刻意不進輪回世界；arc09／arc10 同伴組只排張遠山＋江芷微（`ch0070` 明寫她不在）。另 arc11 排除線④明令**符真真〔同伴組暖線〕與妙音〔江湖棋盤〕不得交叉**，她 arc11 不出場是硬約束、不是缺口。
> - 採計規則（2026-07-25 `sync` 本輪·作者拍板三條）：以 `幕綱` 各幕「角色」欄為準並依既成正文核對——列在該欄者計入；標「（心裡·不在場）」「（暗處·不出場）」「（空缺）」「（事後提起的）」或正文只作回想／比較／點名者＝**純提及，不計入 `所屬arc`**（承重的提及於本節註明）。**「在場」與「被提到」分開記**——混在一起標，這個欄位就從「他在哪幾段有戲」爛成「他的名字出現在哪幾段」。
> - **本輪只動 `所屬arc` 這一個機器事實**：四象限／弧線／馬斯洛／對衝關係／伏筆欄／🧊 一字未動，源檔未動，正文未動。
