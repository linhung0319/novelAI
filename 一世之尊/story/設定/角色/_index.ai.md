# 角色清單（衍生 rollup）

> ⚠️ **尚未產出**——本檔是 AI管·rollup 衍生檔，由 `character`／`develop`／`organize` 從本資料夾所有 `<角色名>.md` 源檔重生，重生後 `tools/derived_sync stamp _index.ai.md` 封章。**作者不手改**。
> **怎麼新增角色**：在本資料夾（`story/設定/角色/`）新增一個 `<角色名>.md`，自由手寫這個角色（無格式要求）；skill 會據此生成同名 `<角色名>.ai.md`（需求四象限／弧線／馬斯洛／🧊）並重生本清單。
> 完整格式見 `結構定義/角色.schema.md`。尚未封章時 `derived-sync check` 會報 `unstamped`——正常。

## 角色清單
| 角色 | 定位 | 一行需求（期盼／反派正當需求） | 所屬arc | 暫定 |
|------|------|------------------------------|---------|------|
| （尚無角色） |  |  |  |  |
