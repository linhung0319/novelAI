# 已知紅的測試 ＋ **登記在案的理由**（2026-07-28 功能 14，抉擇 2 A）。
#
# **雙向擋**（`.github/workflows/tools.yml`）：
#   - 紅而**不在**清單裡 → fail（那是新的回歸）；
#   - **在**清單裡卻變綠了 → 也 fail（一份過期的已知紅清單就是下一個
#     「守衛壞了而沒有人知道」——`設計原則.md` E2 第八種形態）。
#
# **一個永遠紅的套件與關掉的套件效果相同**（03 已記）。所以進這份清單的門檻是：
# 說得出**為什麼現在不修**，而不是「還沒空修」。

# ---------------------------------------------------------------- 現況：清單是空的
#
# **2026-07-30（驗證輪階段 1d）清空。** 唯一的 6 筆（`prose_metrics` 的三組語料
# 回歸）已修好並轉綠，`known_red` 現在**零筆**。
#
# **這支檔留著、不刪**：雙向擋的第一個方向（「紅而不在清單裡 → fail」）需要它，
# 而一份空清單與一支不存在的檔在 `tomllib` 底下不是同一件事。**空要看得見**——
# `meta-lint` 第 9 項照樣印「已知紅 0 支」。
#
# 那 6 筆修的是什麼（存照，別再犯同一個形狀）：
#
# - **`skipif` 守錯對象**：它檢查 `芯片巫師/` 這個**資料夾**在不在（在，只剩
#   `raw/`），而測試要的是 `芯片巫師/chapters/`（不在）。於是它不是 skip，
#   是 6 支長期紅。**守錯對象的 guard 比沒有 guard 更糟**——沒有 guard 至少壞得
#   像壞的；守錯對象的 guard 讓「這一格沒東西可測」長得像「這一格壞了」。
# - **舊清單反對過的修法，反對得有道理**：它說「改成檢查 `chapters/` 之後這 6 支
#   會變 skip，而那正是 E2 第七形態的鏡像——**測試是綠的，射程是空的**」。
#   答案不是不改 guard，是**改對 guard ＋ 補一支說「射程空了」的測試**：
#   `test_rhythm.py::test_the_regression_corpora_scope_is_not_silently_empty` 與
#   `test_exposition.py::test_the_control_group_scope_is_not_silently_empty`。
# - **真正的死因是語料沒了，不是 guard**：對照組 `芯片巫師` 的章節在 commit
#   `a19bf0d`（作者「只保留他們的 raw」）被刪除。它撐的那個獨立論點——**另一本書、
#   另一份風格檔、另一個文類，節奏簽名一模一樣，所以病因不在風格檔**——從此沒有
#   語料。依賴它的兩支測試移除，並立一支墓碑
#   `test_rhythm.py::test_the_second_generated_book_is_gone`：`芯片巫師/chapters/`
#   哪天回來，那支會紅，而紅字說的是「對照組回來了，把那兩支接回去」。
# - **舊清單指的「三組回歸語料該住哪」交功能 02 沒接**，而其實不必等：
#   known-good 的六本一直都在 `examples/`（測試也一直讀得到），缺的只有對照組
#   那一本，而它是**被刪掉的**，不是**放錯地方的**。

known_red = []
