# 結構定義：共同約定（所有 skill 共用）

> **這是什麼**：所有寫作 skill（develop／organize／outline-test／beat-sheet／beat-test／write／write-test／revise／sync）共用的基本約定。各 SKILL.md 以一行引用本檔，不再各自複述——避免七八份逐字重複的條文各自漂移。
> **修改本檔＝修改所有 skill 的行為**，變更前先確認影響面。

---

## 一、資源定位（不得寫死絕對路徑）

- **技巧知識庫／結構定義**：讀「本寫作系統 package 根目錄」下的 `技巧知識庫/` 與 `結構定義/`（與 `.claude/` 同層）。不確定根目錄時，從當前工作目錄向上尋找「同時含這兩個資料夾的目錄」。
- **書內容**：`raw/`、`story/`、`chapters/` 一律相對於「作者當前所在的那本書資料夾」。作者未指明是哪本書時，先確認。

## 二、AI 角色 ＝ 共創 ＋ 審稿（非門檻）

- **所有創作決定權在作者。** AI 提草稿、抓重點、攤開選項與後果，但承重的創作抉擇（主角、結局、題旨、排幕、分章、任何一段文字）必須由作者拍板，絕不代決。
- **審稿員不是門檻。** 所有測試／診斷輸出「可疑點報告」交作者判斷，不做自動 pass/fail、不設數值門檻；嚴重度標示僅供排序參考。
- AI 產出的一切（骨架或散文）都是**可編輯、非受保護、非定案**的草稿。

## 三、下游回饋記錄（動尾改頭的落地）

- 下游發現問題根源在上游時：`世界觀.md`／`00-摘要.md`／`角色需求.md` 三檔有「下游回饋記錄」區塊，依 `結構定義/回饋記錄.schema.md` **追加一列、不改上游正文**。
- **幕綱沒有回饋記錄區塊**——它定位為「作者可直接修改的利器」；發現幕綱本身的問題只在對話中指出，建議作者直接修改或重跑 `beat-test`，不代寫。
- 更新 `story/參照/就緒儀表.md` 時，順手發現上游檔有「待裁決」列，於對應列備註欄加註「⚠️N 筆待裁決回饋（見 X.md）」（被動偵測，不另開掃描流程）。
