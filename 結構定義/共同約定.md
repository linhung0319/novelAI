# 結構定義：共同約定（所有 skill 共用）

> **這是什麼**：所有寫作 skill（develop／organize／outline-test／beat-sheet／beat-test／write／write-test／revise／sync／worldbuild／character／expand）共用的基本約定。各 SKILL.md 以一行引用本檔，不再各自複述——避免十來份逐字重複的條文各自漂移。
> **修改本檔＝修改所有 skill 的行為**，變更前先確認影響面。

---

## 零、兩類檔案（本系統的分類公理）

**動任何檔前，先問它屬哪一類。** 這條先於第二節的所有權——所有權講「誰能寫」，本節講「怎麼被讀，因而該不該限制大小」。

**副檔名只扛一個位元**：你的手改會不會被覆蓋。

| | **作者手改會不會被覆蓋** | 副檔名 |
|---|---|---|
| **源** | ❌ 你的編輯就是新的真相 | `.md` |
| **衍生** | ✅ 下次重生即覆蓋 | `.ai.md` |

**第二個位元是「程式會不會解析它」，它由「有沒有檢查器」承擔，不由副檔名承擔**（`設計原則.md` **A4**）。所以源檔底下還要再分一次——不是分成兩種副檔名，是分成**有沒有閘門**：

| 源檔 | 程式怎麼讀它 | 誰保證格式 | 例 |
|---|---|---|---|
| **自由源** | **只能整檔讀** | 無（怎麼改都不會弄壞任何工具） | `raw/`、設定層 `<實體>.md`、`00-摘要.md`、正文 `chNNNN.md` |
| **強格式源**（作者利器·直改） | **逐行／逐欄解析** | **一支 lint**，落檔後跑 | 大綱（`outline-lint`）、幕綱（`beat-lint`）、`story/物件/<名>.md`（`object-lint`）、`story/參照/裁決流.md`＋`待裁決.md`（`decision-lint`） |

> **為什麼不是第三個副檔名。** 2026-07-27 前有一個 `.co.md` 字位專扛第二個位元，理由是：實測作者把一個全形括號打成半形，**整本書的事實投影 raise**，而副檔名正告訴他「這檔不保證格式，隨便改」。那個危險是真的——**但副檔名沒有解決它**：警示是被動的、可以被略過，而檢查器會當場報錯並指出第幾行第幾欄。而且幕綱與物件檔形式上與 `.co.md` 同一格（可解析、不被覆蓋）卻一直叫 `.md`，差別只在**它們有 lint**——判準其實一直是檢查器，不是字位。**功能 04 補上 `decision-lint` 之後，`.co.md` 的最後一個用戶消失，該檔類廢除。** 完整的理由與已駁回的方案見 `裁決流.schema.md`「為什麼它不再叫 `.co.md`」。
> **既有書相容**：2026-07-27 前建立的書可能仍是 `裁決流.co.md`，工具兩種命名都吃，不必為此改書內檔。
> **大綱那一格 2026-07-28（功能 09）才兌現。** 在此之前它是**這張表上唯一一個列進「一支 lint」那一格、而那支 lint 不存在**的項——同表下方的 schema 欄還寫著「嚴格（**有 lint**）」，`CLAUDE.md` 的分類公理逐字複製了同一句。實測 `grep -rn "大綱" tools/ --include=*.py` 命中的**全部是 `.venv` 裡 pygments 的 Gherkin 關鍵字表**（0 支工具解析它），而 4 支 SKILL.md 逐節取值、`大綱.schema.md` 的 7 條檢查點 0 條有實作。**這與 04 抓到 `.co.md` 的病完全同形：同一張表上的同一個假宣稱，換了一個主詞，活到 09 才被兌現。**
> **`story/參照/約束.co.md`／`約束.md` 這個落點已廢除**（約束遷進 `story/物件/<名>.md`）：檔還在的話 `fact-lint` 會報落點錯，因為留著它沒有任何工具會讀——那些排除線等於不存在，而作者以為它們還在生效。

| 類別 | 讀取方式 | 檔案大小 | 單行長度 | 控制手段 |
|------|---------|---------|---------|---------|
| **自由源** `.md` | **必須整檔讀入** | **要限制** | 不限 | `結構定義/` 的資料夾與檔案骨架**引導作者分檔**；檔名即選擇器 |
| **強格式源** `.md`（有 lint） | 程式按需切片，作者可直改 | **不限** | **要限制** | 格式固定 ＋ 投影工具 ＋ **lint** |
| **衍生** `.ai.md` | 程式按需切片 | **不限**（有切片工具才算） | 見下 | 格式固定 ＋ 對應的投影／選取工具 ＋ `derived-sync validate` |

> **「有投影工具」不豁免行長。** 投影的粒度**就是行**——一行不可再切。2026-07-27 前成長哨兵整支檔豁免 append log 的行長，於是實測某本書的單一事件行從 76 字長到 194 字（最長 645）、括號註解佔比 36%→52%，長了 11 個 arc 一聲沒吭。見 `事實流.schema.md`「為什麼再改一次」。

### 第二問：可不可重生

上表分的是「怎麼被讀」。**還要再問一次：這份內容能不能從別的檔重新算出來？** 這一問決定它該住衍生檔還是源檔，答錯會爛在兩種相反的方式上：

| | 可重生（源一動就能重算） | 不可重生（此處即唯一副本） |
|---|---|---|
| **住哪** | `.ai.md` 衍生檔，走 hash 偵測 | **源檔**（自由源或強格式源），不重生 |
| **住錯的下場** | 放進源檔 → 源改了沒人發現，孤兒事實永遠躺著 | 放進 `.ai.md` → 下次重生直接消失 |

**可執行的判準**：把那支 `.ai.md` 全部刪掉、只拿源檔重跑一次，這條會不會回來？會 → 它是源的**新表述**，放衍生檔；不會 → 它是**新資訊**，要嘛回寫源檔，要嘛進不重生的檔（`story/物件/<名>.md`／`story/參照/裁決流.md`／`待裁決.md`）。

實例：事實流的三類型正是按這一問拆的——`狀態`／`錨` 是該章正文的函數（可重生，住 `chNNNN.ai.md`），`約束` 是作者拍板的意圖（不可重生，住 `story/物件/<實體>.md` 的「## 不得寫成什麼」）。見 `事實流.schema.md`「為什麼拆軸」。

**答錯這一問的兩個實測案例**（2026-07-27 修掉）：**約束與錨**寄生在設定層 `.ai.md` 的枚舉外節（13 支檔 70 行，含「不可翻案」「守死」這種明說要保命的東西）；**揭示層級 🧊**被 schema 明文規定寫進 `<主題>.ai.md`／`<名>.ai.md`。兩者都是「不可重生的內容住在會被重生的容器裡」，下場都是重生直接消失，而且**沒有任何東西會報**——哨兵看得見那些節，但它報的是「檔太大」。兩者現在都住 `story/物件/<名>.md`（見 `物件.schema.md`）。

### 第三問：是「事件流」還是「登記表」

不重生的檔底下還有一分：內容是**一連串發生過的事**，還是**一組會被就地修改的規則**？

| | append-only 事件流 | 就地編輯的登記表 |
|---|---|---|
| **判準** | 歷史本身就是內容（要能回查判例） | 只查「當下有效的那一組」；欄位會被改 |
| **例** | `story/參照/裁決流.md`（為什麼這樣定） | `story/物件/<名>.md` 的「## 不得寫成什麼」（下游不得寫成 X）、`story/參照/待裁決.md`（還沒裁決的觀察） |

判錯的代價是**死行累積**：把登記表做成 log，「延長射程」就只能再 append 一行同名的去覆蓋，舊行從此不再被讀到卻永遠不會消失。判準見 `物件.schema.md`「## 不得寫成什麼」。

> **登記表一律不設「狀態」欄。** 狀態要嘛可由別的欄導出（約束表的 `解除於`），要嘛因為表裡只住一種狀態而恆真（`待裁決.md`）。實測兩次教訓：`裁決流` 那個必須人手動維護的狀態欄，是「`射程` 欄程式從未讀、所謂『不必有人負責刪』不成立」的失效來源；而舊回饋表那個**只允許一個值**的狀態欄，實測 7 列平均 171 字元、最長 440、佔整列 61%——**一個窄欄位 ＋ 沒有別的地方可放 ＝ 欄位溢位**。

### 資格條款（本節的執行力所在）

> **衍生檔要享有「可以很大」的豁免，必須有對應的投影／切片工具。沒有工具能切它，它就跟源檔同等受限**——大小照源檔的門檻管，由成長哨兵計量。

這條堵的是一個具體的失效模式：`.ai.md` 因為掛著「AI管·嚴格 schema」而被默許成長，但實際上沒有任何工具切得動它（`settings-select` 的粒度是「哪幾支檔」不是「檔裡哪一段」），於是同時具備「會長大」與「必須整檔讀」——**最壞的組合**。判準見 `docs/superpowers/specs/2026-07-26-設定層事實與裁決分軸-design.md`。

> **這條與 A4 是同一件事的兩面**：有 lint 才配被解析（A4），有切片工具才配變大（本條）。兩者都拒絕「宣告了就算數」。

> **行長那一半 2026-07-27（功能 05）才真的覆蓋設定層。** 上表寫「自由源·單行不限」，而成長哨兵的 `long_lines` 目標集當時只含 rollup（`_*.ai.md`）與 `story/參照/`——於是設定層**非 rollup** 的檔（`<實體>.ai.md` 與源 `<實體>.md`、`<名>/<切面>.md`）行長完全無人守，實測 1,155 字元的單行（rollup 門檻的 2.9×）長了八次重生一聲沒吭，而同一個資料夾裡的 `_總覽.ai.md` 一直在報。現在**衍生 800／源 600**（門檻取自 n=4 的實測空隙，是暫定值，見 `世界觀.schema.md`「大小與單行長度」）。
> 「自由源單行不限」那句講的是**格式自由**（自由源沒有欄位，所以沒有欄位溢位），不是「行可以無限長」——一個 759 字元的 bullet 內含三個日期的翻案沿革，那是登記表被當 log 用（本節第三問），沿革屬 `story/參照/裁決流.md`。**檔案大小仍由 `SOURCE_BYTES`／`DERIVED_BYTES` 管，兩件事分開計。**

### 推論：內容放錯類別怎麼辦

某份內容若屬「只由AI與程式產生」卻被寫成自由 prose、或住進了源檔（例：裁決理由、正文釘死的事實），**正解是把它移進格式固定、有投影工具的軸，不是定期刪它**。「回收／壓縮」只在無處可搬時才是解法。

## 一、資源定位（不得寫死絕對路徑）

- **技巧知識庫／結構定義**：讀「本寫作系統 package 根目錄」下的 `技巧知識庫/` 與 `結構定義/`（與 `.claude/` 同層）。不確定根目錄時，從當前工作目錄向上尋找「同時含這兩個資料夾的目錄」。
- **書內容**：`raw/`、`story/`、`chapters/` 一律相對於「作者當前所在的那本書資料夾」。作者未指明是哪本書時，先確認。

## 二、四種所有權（本系統的骨架）

**動任何檔前先確認它的所有權**（以及第零節的類別）。本表的主軸是**作者能不能改**——那是作者唯一需要一眼看出的事；「誰寫」只是它的成因。

> ### `.ai.md` ＝「別改，改了會被覆蓋」的唯一訊號
> **副檔名就是那個訊號**：帶 `.ai.md` 的檔一律是 AI 依源重生的建構產物，作者的手改會在下次重生時消失。**其餘都可以改**——差別只在「這支檔有沒有 lint」：沒有的隨便改（`raw/`、設定層源），有的要守語法（大綱→`outline-lint`、幕綱→`beat-lint`、物件檔、裁決流、待裁決），而**那個差別由檢查器告訴你，不由副檔名告訴你**（`設計原則.md` A4）。

| 作者能不能改 | 改了會怎樣 | 檔名 | 誰寫 | schema | 例 |
|------------|-----------|------|------|--------|-----|
| **改就是了**（它就是真相） | 你的編輯**就是**新的真相 | `.md` | 作者自由寫 | 無 | `raw/`；設定層源 `<實體>.md` 或 `<實體>/<切面>.md`；`00-摘要.md`；正文 `chapters/chNNNN.md` |
| **改就是了**（作者利器·直改） | 同上，但要守語法；**改動會留著**（沒有東西會重生它），所以你改壞了也沒人幫你修——故它有 lint | `.md` | 作者直接改；`裁決流`／`待裁決` 平常由 AI 在既有時機寫、作者校訂 | 嚴格（**有 lint**） | `大綱`（`outline-lint`）、`幕綱`（`beat-lint`）、**`story/物件/<名>.md`**（`object-lint`）、**`story/參照/裁決流.md`**（拍板紀錄·append-only）與 **`待裁決.md`**（`decision-lint`）、**`story/參照/就緒.md`**（作者的成熟度判斷·`readiness-lint`） |
| **別改** | 下次重生即覆蓋 | **`.ai.md`** | AI 依源重生 | 嚴格 | 設定層 `<實體>.ai.md`、`_index.ai.md`、`_總覽.ai.md`；`00-摘要.ai.md`；`chNNNN.ai.md`（含 `## 本章事實` 的狀態／錨）、`chapters/_index.ai.md`；**宣告式綜合檔** `結構.ai.md`（**剩最後一支** → 功能 11） |

### 兩個容易誤解的位置

- **`裁決流.md`／`待裁決.md` 平常是 AI 在寫，為什麼不叫 `.ai.md`**：因為它們的合約與衍生檔**相反**——不重生，所以**作者可審可改、改了不會被吃掉**（審事件正是它的兜底機制）。給它們 `.ai.md` 會叫作者別碰一個本來就設計給他校訂的檔。
  > 2026-07-27 前它們（當時只有裁決流）用第三個字位 `.co.md`，理由是「給它素 `.md` 等於騙作者這檔沒有格式，而它其實會被逐行解析」。**那個顧慮由 `decision-lint` 接手**——格式保證由檢查器兌現，不由副檔名兌現（`設計原則.md` A4）。見 `裁決流.schema.md`「為什麼它不再叫 `.co.md`」。
- **`.ai.md` 也不是完全不能碰，但碰的方式不同**：它保證格式，所以作者能在**既有格式內**改一格的內容（改表格某欄、改一句分析）；但**不該在裡面新增結構**（開新節、加新欄）——那是 schema 的事。而且任何手改都只是「回報一筆修正」，重生就沒了；要留住它，得回頭改源檔。格式由 `derived-sync validate` 守。
- **宣告式綜合檔只剩 `結構.ai.md` 一支，而「宣告式」這個格子本身已被判定是錯的**（2026-07-28 功能 10）：`設計原則.md` **A6** 明訂「不許人改 ＋ 沒有 inbound 重算規則」是一個**必須解掉的衝突，不是第三種身分**——「別改」的正當性完全來自「改了會被下次重生覆蓋」，沒有重算規則那個訊號就是空的。`結構.ai.md` 的處置**交功能 11**（A6 對它同樣適用，別重新推導）；在那之前它照舊走 `derived_sync` 的 `_is_declarative`：`check` 報 `declarative`、不計入需處理數、不必也不能 `stamp`。
  > **`就緒儀表` 已於 2026-07-28 廢除。** 實測它是全書最大的一支檔（292,591 B、六天 254.7×、53.8% 是拍板日誌），而**在任何命名下都拿不到格式守衛**：舊名 `.md` 讓 `check_book` 的 `rglob("*.ai.md")` 掃不到，新名 `.ai.md` 讓 `_is_declarative` 在 `validate_file` 早退三次。它拆成了 **A1 源檔 `story/參照/就緒.md`**（作者的成熟度判斷與就緒清單，`readiness-lint` 守）＋ **`readiness` 投影**（機械現況、局部下沉、待裁決筆數，不落檔）。日誌的家是 `story/參照/裁決流.md`（04 蓋好的）。**還帶著舊檔的書由 `readiness-lint` 第 7 項報**，搬移條文見 `就緒.schema.md`。
  > **「工具兩種命名都吃」這句話 2026-07-28 撤掉**：實測它**只對 `long_lines` 成立**（那一支用 `_base_stem` ＋ `glob("*.md")`），而 `check`／`validate` 的入口是 `rglob("*.ai.md")`。**一句半真的相容承諾比沒有承諾更糟**——讀 schema 的人會以為兩邊都被守著。`結構` 那一支既有書仍可能叫 `結構.md`，那個狀態現在是**已登記的欠債**（功能 11），不是保證。

### 源 → 衍生（本次重設計的核心）

- **源是唯一真實來源**；`.ai.md` 是「重生自源的建構產物」，如同 `.c`→`.o`。衍生檔**只會「新鮮／過期」，不會與源漂移**——因此拆兩檔不違反單一真實來源。
- **作者只碰源檔。** 衍生檔即使被誤改，下次重生即覆蓋；整個衍生層可隨時由源整批 rebuild。
- 衍生檔**照樣 commit 進 git**（本 repo 刻意版控書內容；衍生檔小、且 skill 讀它當 context）。

### 衍生檔 `.ai.md` 格式（混合：MD 殼＋YAML front-matter）

```markdown
---
generated-from: 3f9a1c…      # 對應源檔（正規化後）內容的 hash，見第三節
generated-at: 2026-07-19
定位: 主角                     # 以下為「機器事實」：對映／旗標／禁用詞表／維度狀態…
所屬arc: [arc01, arc03]        #   供 tools/ 快而準地讀（YAML，可程式解析）
暫定: false
伏筆: { 埋: [怕水], 收: [] }
---
## 需求四象限                  # 以下為「prose 分析」：供 LLM skill 讀
- 期盼：…
- 想要（誤導）：…
## 限制與代價                  # （世界觀衍生檔的節；2026-07-27 起第三節叫 `## 自洽`）
…
```

- **機器事實放 front-matter**（YAML），**prose 分析放 MD 本體**。哪些欄位是機器事實由各產物 schema 規定。
  > ⚠️ **「機器事實」是一個承諾，不是一個標籤**（`設計原則.md` A4／E1，2026-07-27 功能 05 補）：一個欄位要算機器事實，得指得出**讀它的那一行**（哪支工具的哪個函式、或哪支 SKILL.md 的第幾步）**與守它的那支 lint**。填不出來就別把它放進 front-matter——把它寫成本體的散文。
  > 實測教訓：世界觀衍生檔曾有五個「機器事實」欄（`影響力`／`主從`／`維度`／`升格哨兵`／`伏筆`），`tools/` 底下**零解析器**、`validate.py` 只驗 `generated-*` 兩個鍵，而 schema 還寫著「供 beat-test/sync 盯」（那兩支全文零次提及它）。三處已漂移且**壞了永遠不會發現**。四欄 2026-07-27 刪除，`伏筆` 留下來的條件是它有 `world-lint` 守、且只引用既有 registry。**這個縫對每一支 `.ai.md` 都成立，不只世界觀。**
  > **角色軸 2026-07-27（功能 06）用同一把尺量過**：七欄裡刪掉 `角色`（檔名的複本）／`弧線類型`（**連假的消費者都沒宣稱過**——比世界觀那四欄的證據更硬）／`影響力`；留下的 `定位`／`所屬arc`／`暫定`／`伏筆` **每一欄都指得出 `char-lint` 裡讀它的那一行**（見 `角色.schema.md`「留下來的四欄」表）。**留下的欄若 lint 沒有真的讀它，就不准留**——那是留欄的條件，不是加分項。
- `_index.ai.md`／`_總覽.ai.md` 是 rollup 衍生檔：其「源」＝同層所有源 `*.md` 的集合（見第三節 digest 規則）。

## 三、變動偵測與同步

- **機械偵測（`tools/derived_sync`）**：對每個 `<實體>.ai.md`，重算其源檔正規化 hash，與 front-matter 的 `generated-from` 比對；不符即 **stale**。這給「像 git 那樣偵測人動了設定」但零外部 DB——hash 隨檔走。
  - 正規化＝統一換行、去每行尾空白、去檔尾多餘空行（見 `tools/derived_sync`），使無語意差異不誤判。
  - rollup（`_*.ai.md`）的源 digest＝同層各源 `<檔名>:<內容hash>` 排序後再 hash——**新增／刪除／改動任一同層源檔都會令 rollup stale**。
  - **目錄形態的源**（角色的 `<名>/<切面>.md`）也算一個源：它自己的 digest＝其下所有 `*.md` 的 `<檔名>:<內容hash>` 排序後再 hash，而它在 rollup 的 digest 裡佔一個 entry（鍵是 `<名>/`）。所以 `<名>.ai.md` 與 `_index.ai.md` 都會因為動了任一切面而 stale。
    > **2026-07-27（功能 06）補上實作。** 在此之前 `角色.schema.md` 就寫著這條規則，而 `derived_sync` 對非 rollup 一律找同層 `<名>.md`、rollup 只 `glob("*.md")`（不遞迴）——**目錄形態整個是壞的**（`check` 報 `orphan`、`stamp` raise、改切面不會讓 rollup stale）。0/24 實測所以從沒撞到。**一條零實作的格式承諾，形式上完全合法**（E1）。
  - skill 重生某 `.ai.md` 後，呼叫 `derived-sync stamp` 把新 hash 蓋回 front-matter（別手算）。
- **stale 的處置**：偵測器只報「哪些衍生檔過期」；**重生內容是 skill（LLM）的工作**（worldbuild／character／write…），不是偵測器能做的。整批重生＝把偵測器列出的 stale 清單逐一交對應 skill。
- **深層語意過期**（結局→伏筆回收、題旨→立場鋪陳幕、世界觀規則→幕可行性）**仍走 `sync` 的語意比對**；機械偵測只管「人動了某源檔」這一類，兩者互補。

## 四、下游回饋（動尾改頭的落地）

- 下游發現問題根源在上游的「設定／摘要」時：**不改源、不改衍生的分析區**，而是在 **`story/參照/待裁決.md`** 依 `結構定義/待裁決.schema.md` **追加一列**（四欄 `日期｜來源｜標的｜發現`，**無狀態欄**；`標的` 欄寫「要改哪個源檔」的相對路徑，它是選擇器不是散文）。
- 由 `worldbuild`／`character`／`develop` 回頭消化：**消化前先跑 `decision-project --target <該源檔>`**——它一次吐兩節，同時給你「這個標的還有哪些待裁決」與「過去已經裁決過什麼」。作者拍板後提請修改**源檔**、再重生 `.ai.md`；**結論落源檔／事實軸，理由 append 進 `story/參照/裁決流.md`，然後把那一列刪掉**。
- **`.ai.md` 一律不得有 `## 待裁決回饋` 節，也不得出現自由 blockquote 形式的裁決日誌**（`derived-sync validate` 兩者都守，後者整本聚合成一行）。
  > **2026-07-27 為什麼搬出 `.ai.md`**：那一節沒有 inbound 重算規則＝依 A1 它是源，卻住在依 A2 必被覆蓋的容器裡（A3 說這種衝突必須解掉）。實測 30 支檔帶這個節、其中 **0 列真的待裁決、7 列是結案未刪的**，而「重生把它吃掉」與「它被正常消化」在檔案系統上留下的痕跡**完全相同**。理由與已駁回的方案見 `待裁決.schema.md`「為什麼搬出來」。
- **幕綱沒有回饋列**——它是「作者可直接修改的利器」；發現幕綱問題只在對話中指出，建議作者直接改或重跑 `beat-test`，不代寫。
- **「⚠️N 筆待裁決」這條手抄規則 2026-07-28（功能 10）廢除**：它現在是 `readiness` 投影的一行，**由 `decision-project` 當場算，沒有人再手抄**。
  > **為什麼廢除而不是再改一次規則**：04 已經把它從「被動偵測、順手加註」改成「N 從 `decision-project` 的覆蓋率行讀」，而**沒有配守衛**——實測 04 量到 14 處字樣 vs 實際 0 筆，10 複量是 **26 處 vs 0 筆（病徵翻倍）**。**一條改過而沒有人驗證照做的規則，會繼續照舊被違反**；正解是讓那個數字不再有手抄的落點。

## 五、AI 角色 ＝ 共創 ＋ 審稿（非門檻）

- **所有創作決定權在作者。** AI 提草稿、抓重點、攤開選項與後果，但承重的創作抉擇（主角、結局、題旨、排幕、分章、任何一段文字）必須由作者拍板，絕不代決。
- **審稿員不是門檻。** 所有測試／診斷輸出「可疑點報告」交作者判斷，不做自動 pass/fail、不設數值門檻；嚴重度標示僅供排序參考。
- AI 產出的一切（骨架或散文）都是**可編輯、非受保護、非定案**的草稿。

## 六、揭示層級（後段才向讀者揭示的條目）

有些設定與角色動機是**後段才向讀者揭示**的（母愛護盾的機制、反派的真動機）。它何時揭，記在**該物件的物件檔** `story/物件/<名>.md` 的 front-matter `揭示層級` 欄。**完整定義見 `物件.schema.md`**，這裡只留三件下游要知道的事。

### 唯一語法（三種值）

```
揭示層級: 公開                                  ← 預設，可整行省略
揭示層級: 水下｜揭示於 收[[伏筆:母愛護盾]]        ← 指向幕綱裡的回收點
揭示層級: 水下｜跨集留白                         ← 本書內不揭示
```

**水下必須二選一**：指向一個伏筆名（須與幕綱的 `收[[伏筆:x]]` 完全一致），或明標跨集留白。

> **設定層 `.ai.md` 一律不再出現 🧊**（2026-07-27 作者拍板）。原本有三種 schema 授權的寫法——本節的行內 `（🧊 水下｜…）`、`世界觀.schema.md` 的 front-matter 鍵 `🧊水下:`、`角色.schema.md` 的 `## 🧊 水下` 整節——**而工具只認第一種**：實測 92 次出現、解析出 1 條，`foreshadow-project` 印「0 條為可疑點」exit 0。三種全刪，理由（含代價與已駁回的選項）寫在 `物件.schema.md`「`揭示層級`」。
> 還留在設定層 `.ai.md` 裡的 🧊 會被 `foreshadow-project` 報成**落點已廢除**——它在那裡活不過下一次重生。

### 為什麼是指標，不是描述

**揭示時機的唯一真實來源是幕綱的 `收[[伏筆:x]]`。** 這裡只放一個指向它的連結，不用散文複述「此條於結局引爆」——那等於在幕綱之外另立第二本揭示帳，正是 `幕綱.schema.md`「伏筆不另立登記簿」刻意避開的壞味道。要知道某條何時揭示，去幕綱查它的 `收` 在哪一幕。

### 解析規則

- **判先後不能比幕號大小。** 幕號是穩定 ID、不代表全書順序（見 `幕綱.schema.md`「幕號配號規則」）。要判斷某個揭示點排在某章之前或之後：先查該幕屬於哪個 arc，再對照 `story/幕綱/_index.md`「全書順序」比較兩個 arc 的先後；同一 arc 內才以幕號序為準。
- **揭示點還不存在是合法狀態。** 指向的伏筆名在幕綱找不到 `收`，但該伏筆已埋、或還有 arc 尚未拆幕 → 標「揭示點待落幕」列為**資訊、非可疑點**。只有「找不到、且沒有未拆 arc 可解釋」才是可疑點（疑似手誤或那條伏筆已被刪）。

### 分工

- **寫**：`worldbuild`（世界觀）、`character`（角色）、`develop`／`organize`（冷啟動時發現該留水下的條目）——皆作者確認後，寫進 `story/物件/<名>.md`（**不是**寫進衍生 `.ai.md`：那是作者的裁決、不可重生，寫在會被重生的檔裡下次重生就沒了）。
- **驗**：`object-lint`（語法唯一、指向的收點存在）；`foreshadow-project` 出報表，一律印「掃到 N 處／解析 N 處／待落幕 N 處／無法解析 N 處」，`beat-test` 讀它。
- 它是**給下游看的資訊**，不是門檻——讓下游知道「這條在某章是不是還在水下」，不阻擋任何撰寫決定。
- **比它更強一級的是 `角色/水下.md`**：那是存取控制，`write` 在揭底前**拿不到**那份檔，不只是被要求別用（實作＝`settings_select` 的 `UNDERWATER_FACET`，預設不給，要 `--include-underwater`）。
  > **它與角色大小無關**（2026-07-27 功能 06 抉擇 3 B）：**有秘密就開，不必等角色變大**。一個 800 B 的配角也可以有秘密——要開就把它升成目錄形態，**只建用得到的切面**（最小＝`核心.md`＋`水下.md`）。「因為大而升目錄」與「因為有秘密而升目錄」是兩條獨立的規則，前者的觸發者是成長哨兵。**`<名>.md` 與 `<名>/` 不得並存**（並存會讓 selector 產生兩個同名 Entity，目錄形態的切面從此選不到），由 `char-lint` 守。見 `角色.schema.md`「兩種形態怎麼選」。

## 七、承諾與演出（下游能自由發明什麼）

上游永遠不可能前置窮盡所有細節——幕綱寫不完一個房間裡有什麼、誰在場、說了哪句話。本節定義下游（尤其 `write`／`revise`）**哪些必須遵守、哪些可以自由發明**。

### 兩類，一條線

| | 定義 | 下游怎麼對待 |
|---|------|------------|
| **承諾** | 上游已經釘死、下游不可牴觸的事實 | 遵守；要動它得回上游改 |
| **演出** | 其餘一切——在場的人、房裡的物件、天氣、一句寒暄、一個動作、對白的措辭 | **自由發明** |

**承諾的範圍是可列舉的三處**，不多不少：

1. `story/設定/**`（世界觀規則、角色事實、風格約束）
2. `story/幕綱/`（因果、伏筆埋收、賭注、結構階段、arc 承諾區）
3. **事實軸**：各章 `chNNNN.ai.md` 的 `## 本章事實`（當下狀態＋正文釘死的錨）＋ `story/物件/<名>.md` 的「## 不得寫成什麼」（生效中的約束）——查詢一律走 `tools/fact_projection` 投影，別整檔讀（也別整個 `物件/` 目錄讀）

### 邊界規則

- **動筆前只回答一個「查詢」問題**：**上游有沒有講過這件事？** 有 → 遵守；沒有 → 自由。
- **不要回答「預測」問題**（「這個細節算不算承重／後面會不會被依賴」）。預測不可判定，查詢可判定——**把不可判定的預測換成可判定的查詢，是本節的全部用意**。要求下游預先分類每個細節，實測結果是它乾脆什麼都不寫。
- **沉默＝授權。** 上游沒寫的細節就是下游的。幕綱**不必寫「留 write 現場長」**這類標記——那是預設，不是例外；要限制某個細節，把它寫成承諾。
- **判不準時，先把場面寫出來，再把那條可能承重的斷言報出來。** 兩件事不衝突，不要為了避免標記而放棄演出。

### 升格走既有機制，且永遠在事後

某條演出事後若判定該升格為承諾：

- **狀態型**（某實體某維度的變化）／**錨型**（正文釘死的數字·專名·道具語彙）→ `write` 章末寫進該章 `chNNNN.ai.md` 的 `## 本章事實`；**約束型**（下游不得寫成 X）→ 新增一列進 `story/物件/<實體>.md` 的「## 不得寫成什麼」（該物件檔還不存在就順手開一支，見 `物件.schema.md`）。
- **新規則／新設定事實** → 在 `story/參照/待裁決.md` 追加一列（見第四節），交作者裁決是否改源。

**這是事後回報，永不作為動筆前的閘門。** 一致性由三件既有的事承擔：下游讀前一章正文對腔、狀態投影、事後的促升偵測（`write-test`）。**本節改的是順序（先演出、後回報），不是取消回報，也不新增第二本帳。**

## 八、context 取用契約（skill 該把什麼讀進來）

書愈長，產物愈大——這是**正確的**，一本 500 章的書本來就比 30 章的書記得多。問題不在檔案大，在於 skill 預設**整檔讀**：於是單次執行的 context 載入量隨書長度線性成長，最後擠掉真正該用來寫作的注意力。

### 硬規則

> **skill 不得整檔讀取「成長型產物」，一律經投影／選取工具取用。**
> 需要新的取用切面時，**加投影，不要在 SKILL.md 裡寫「請只讀相關部分」這種規勸**——規勸不會被執行，工具會。

### 成長型產物與各自的查詢入口

| 產物 | 為何成長 | 查詢入口 |
|------|---------|---------|
| `story/設定/角色/*`、`story/設定/世界觀/*` | 角色／主題愈來愈多 | `settings-select --arc arcNN [--beats 幕N-幕M] [--facets 核心,能力]`。**它一律印覆蓋率行**（2026-07-27 功能 05 起，0 也印）：「掃 N 幕；角色命中 X 筆，**其中 H 筆是空殼**；世界觀命中 N 筆，**其中 M 筆僅因檔名被引用**」。<br>⚠️ **「命中」不等於「可用」**（功能 06 補的 H）：**空殼＝衍生檔缺失，或 `需求四象限`／`預期弧線` 整節是佔位**。實測 arc02 印「角色命中 7 筆」而其中 **4 筆是空殼**——依據正確、數字正確，而 `beat-test` 測試4 就拿那兩節當弧線基準。修法不在這支工具（跑 `character` 補實、`char-lint` 會逐項列出），這一格只負責讓它可見。<br>⚠️ **兩側的命中依據不同，而世界觀那一側比較弱**：角色以幕的「角色」欄為準（幕綱的正式宣告）；**世界觀無對應欄位，是拿檔名字串去掃全幕文字**。實測一世之尊 4 個主題有 **3 個裸提及率為 0**——它們全靠幕綱散文裡的 `見 \`X.ai.md\`` 註腳被命中。02 要把那些註腳從八欄清掉，清完這幾筆就會靜默消失，所以那個 M 必須印。選擇器怎麼修＝`功能報告/05-設定層-世界觀.md` 抉擇 2（本輪只量，A／C 刻意延後）。<br>⚠️ **它看不見 `_` 開頭的檔**（`select.py` 明文跳過），所以 5 支 SKILL.md 宣稱的「先讀 `_總覽.ai.md` 索引挑實體」與本表是**兩套互不相通的入口** → 已登記為功能 15 的疑點 |
| `chapters/*.ai.md` 的 `## 本章事實`（狀態／錨） | 每章一筆筆累加 | **`fact-project --for-beat 幕NNN`**（由幕綱的角色欄自動導出實體，別讓 LLM 自己填）；要看某個 slot 的演變走 `--history <實體>/<維度>` |
| `story/物件/*.md` 的「## 不得寫成什麼」 | 每次拍板多一條負向排除線 | 同上（`--kinds 約束`；射程由「生效自／解除於」兩欄決定）。改／解除一條之前先跑 `fact-refs --constraint <約束名>`。**含 `物件/全書.md` 的書級方針（型別 `方針`，射程＝全書、不綁實體）——它另立 `### 全書方針` 一節印，不受實體過濾，0 條也印** |
| `story/幕綱/arcNN.md` 承諾區的**排除線**（`不得發生`） | 每個 arc 幾條 | 同上——`fact-project` 會把目標幕所屬 arc 的排除線當獨立一節印出（**0 條也印**）。落點留 arc 檔（射程天然綁 arc），但**與物件檔約束在查詢層合流**，`write` 跑一次投影就同時看到兩種負向約束 |
| `story/參照/裁決流.md`（為什麼這樣定）＋ `story/參照/待裁決.md`（還沒裁決的觀察） | 每輪 skill append 拍板紀錄／下游 append 觀察 | `decision-project [--target <源檔路徑>] [--active-only] [--as-of arcNN]`——**一次吐兩節**（兩軸共用 `標的` 選擇器），`待裁決` 那節 **0 列也印** |
| `story/幕綱/*.md`（跨 arc 的伏筆帳） | 每個 arc 一檔 | `foreshadow-project [--arc arcNN] [--open-only] [--suspect-only]` |
| `story/幕綱/*.md`（幕的可演性＋母題自我複製） | 每個 arc 一檔 | `beat-metrics [--arc arcNN] [--detail]` |
| `chapters/*.md`（全書正文的結構統計＋解說段掃描） | 每章一檔 | `prose-metrics [--per-chapter] [--exposition-list --range chNNNN-chNNNN]` |
| **`story/00-摘要.{md,ai.md}`（摘要軸兩支檔）** | **不隨書長成長，但也不有界**——成長軸是**裁決輪次**（實測源 1,483→15,656 B ＝ **10.6×**、衍生 679→10,530 B ＝ **15.5×**，全部發生在 `sync→develop` 收斂與 arc 收官盤點那幾輪） | **無選擇器，而且不該有**（一本書一份、無 rollup、無切面、無 arc 分檔，`settings-select` 明文不碰它）。取用契約是**兩支都讀**：源整檔讀（承重決定的散文與理由）＋衍生 front-matter 的**六個語意欄**逐欄取值（`主線`／`題旨`／`基調`／`視角結構`／`取向定位`／`貫穿大懸念`）。<br>⚠️ **不得在 SKILL.md 裡寫「讀 `00-摘要.md` 的『XX』欄」**——源是自由源、沒有欄。權威在衍生那六欄（有 `summary-lint`），源檔給的是理由（`設計原則.md` **A4 第三條路**，2026-07-27 功能 08）。<br>⚠️ **它是全系統讀寫比最懸殊的產物**（12 支 skill 中 10 支是純讀者、2 支是寫者），也是**唯一每支 skill 都無條件整檔讀的源檔**——所以它的成本函數該量「被無條件載入多少」，不是「檔多大」。**代價由三道守衛壓住**：`summary-lint`（欄語意＋源檔提示）＋成長哨兵的 `SOURCE_BYTES`／`DERIVED_BYTES`／行長 600·800 ＋ `derived-sync validate` 的三節枚舉 |
| **`story/01-大綱.md`＋`story/大綱/arcNN.md`（大綱層）** | **成長軸是「已下沉的 arc 數」與「每支檔累積的拍板輪次」**（實測某本書 arcNN 4,848 → 44,307 B ＝ **9.1×**／11 個 arc，約 +4 KB/arc；`01-大綱.md` 531 → 28,500 B ＝ **53.7×**，成長軸是併入的卷數、每卷 ≈+13 KB） | **選擇器是檔名 `arcNN`（C1 正例，覆蓋率 100%），一次只讀一支**——`beat-sheet` 拆某個 arc 先找 `story/大綱/arcNN.md`，找不到才讀 `01-大綱.md`（**2026-07-28 路由反轉**：兩者按卷分工，見 `大綱.schema.md`「收斂規則」）。<br>⚠️ **這是第一個「選擇器完全健康、覆蓋率 100%、而成本仍線性」的軸**：成長發生在**被選中的那一份裡面**（後面的 arc 天生要背前面所有 arc 的伏筆帳、排除線與卷級方向）。「不隨**書**長成長」不等於「有界」→ D2 的成本函數重新表述交功能 14。<br>⚠️ **既成的伏筆埋收不要從大綱檔讀**，跑 `foreshadow-project --arc arcNN`（大綱的 `## 本 arc 伏筆狀態` 只寫**意圖**）。**代價由兩道守衛壓住**：`outline-lint`（11 項＋覆蓋率行）＋成長哨兵的 `SOURCE_BYTES`／行長 600·400 |
| **`story/大綱/_index.md`（大綱索引）** | 每個已下沉的 arc 一列（實測某本書 1,411 → 19,959 B ＝ **14.1×**／5 天，而 arc 數只長 2.75×——**每列從 ≈350 B 長到 ≈1,700 B**） | **尚無投影入口——已登記的欠債，不是漏網。** 2026-07-28 功能 09 刻意不裁它的形態（與 §10 就緒儀表、§12 rollup 是同一個 D3 判斷），暫仍整檔讀。**§10 那一支 2026-07-28 已裁＝「小源檔 ＋ 投影」**（可重生的走投影、不可重生的留源檔），**形態隨 12 一起裁時那是現成的判準，不是新提案**。<br>⚠️ 它是**唯一一支不帶 `.ai.md` 的 rollup**（有「視圖 ≡ 資料夾」的形狀，卻無 hash、不能 stamp、`_is_declarative` 也不適用）——05 立的「用 hash 機制的有無分類」那把尺在它身上兩邊都不像 → 功能 12 的第六支語料。**代價由三道守衛壓住**：`outline-lint` 第 3 項（視圖 ≡ 資料夾＋列序遞增）＋`ROLLUP_LINE_CHARS`（400）＋schema 明訂「一列只寫 `arcNN：名稱 —— 狀態`」。形態隨 10／12 一起裁 |
| **`story/參照/就緒.md`（作者的成熟度判斷）＋就緒現況** | **不隨書長成長，因為它現在只裝不可重生的兩筆**（5×4 狀態表＋6 條清單，目標 < 1,500 B）。它的前身 `就緒儀表.ai.md` 的成長軸是 **skill 輪次**，實測 1,149 → **292,591 B ＝ 254.7×／6 天／45 個版本**（比摘要 10.6×／風格 20.04× 快一個數量級，全書最大的一支檔） | **兩邊都讀，形狀同風格軸**：源 `story/參照/就緒.md` 整檔讀（它 < 1,500 B）＋ **`readiness --book <書>`** 印機械現況。三節 **0 也印**：① 產物軸 × 深度 20 格逐格印「機械來源 ｜ 機械結果 ｜ 作者判斷」，**兩邊不一致印一行**；② 就緒清單六條逐條印機械前哨與**覆蓋度（完整／部分／未接）**；③ 局部下沉逐 arc（大綱／幕綱／正文章數／`beat-test` 紀錄），由檔案系統與 `所屬arc` 重算。<br>⚠️ **成熟度以外的東西一律不寫進源檔**：沿革與「為什麼判就緒」屬 `story/參照/裁決流.md`；「幾支檔·封章沒·lint 幾筆」「哪個 arc 下沉到哪一層」「⚠️N 筆待裁決」跑投影，**不手抄**（實測手抄的下場是 26 處「筆待裁決」字樣 vs 實際 0 筆，以及與 `outline-lint` 資訊行直接矛盾的「已併入」）。<br>⚠️ **跨套件的機械來源印「未接」＋該跑的指令**（`outline-lint`／`beat-lint`／`ch-lint`／`decision-project`），**不 subprocess、不複製**——零相依政策要不要改是功能 14 的題目。**「未接」0 也印**：那是給 14 看的儀表，不是隱藏的欠債。**代價由兩道守衛壓住**：`readiness-lint`（七項＋覆蓋率行）＋成長哨兵的 `SOURCE_BYTES`／行長 2,000 |
| **`chapters/_index.ai.md`（章序表）** | 每章一列（實測一世之尊 196 B/列 → 781 B/列，**4.0×**；500 章依末段斜率外推 ≈381 KB） | **尚無投影入口——已登記的欠債，不是漏網。** 2026-07-27 功能 03 刻意不裁它的形態（那與 §10 就緒儀表、§12 rollup 是同一個 D3 判斷，先在 03 拍板等於替後面三個功能定調），暫仍整檔讀。**§10 那一支 2026-07-28 已裁＝「小源檔 ＋ 投影」**（逐格問「這一格的機械來源是哪一支檔的哪一欄」，答不出來的才留在源檔——`設計原則.md` D3 的新推論），12 裁形態時那是現成的判準。**代價由三道守衛壓住**：成長哨兵的 `DERIVED_BYTES`（檔案大小）＋ `ROLLUP_LINE_CHARS`（單列 400 字元）＋ `ch-lint` 的備註欄兩條（不得含伏筆標記／≤100 字）。形態隨 10／12 一起裁 |

工具一律以 `uv run --project <套件根>/tools/<工具> <指令> --book <書資料夾>` 呼叫，**唯讀、零 LLM、可覆算**。

另有幾支**格式閘門**，與新鮮度分開跑、分開計（`check` 問「產出了沒、過期沒」，它們問「產出的東西形狀對不對」）：

- `derived-sync validate --book <書>`——驗每支 `.ai.md` 的**結構**：front-matter 必填鍵、`##` 節是否在該產物 schema 的封閉枚舉內、**檔內不得有裁決日誌 blockquote**（只算第一個 `##` 之後、>40 字元的行；檔頭說明不計；**整本聚合成一行**）。這是「`.ai.md` 保證格式」這條承諾**唯一在守的東西**；在它之前那句話只是口頭承諾。
  > 覆蓋率行會分開印「**N 支套節枚舉／M 支只驗 front-matter**」——後者代表該產物**根本沒有節枚舉**，它 schema 上宣告的節是空頭承諾。**2026-07-27 功能 07 補上 `風格`（＝空 tuple：不得有任何 `##` 節）、功能 08 補上 `摘要`（＝`壓縮`／`高概念`／`取向定位分析` 三節），實測一世之尊 `fm_only` 2→1→0。**
  > **`fm_only` 從此該恆為 0**——那一格現在是「**有沒有新產物又沒登記節枚舉**」的哨兵，不是一個已知缺口的計數器。**它印 0 才代表沒有第三支漏網的產物**，所以照樣要印。
  > **摘要那一支漏了整段開發期的原因是掃描起點**（E2）：`classify()` 只認 `chapters/` 與 `story/設定/<kind>/`，而摘要衍生檔住 `story/` 根目錄——於是 04 從五處枚舉移除的 `## 待裁決回饋` 在它裡面完好無損地活著（818 字元／19.2%），而覆蓋率行只把它印成 `fm_only` 裡一個**匿名的數字**。這是 `sentinel.py` 那個「手寫路徑清單漏檔」根因的**第七次**，只是換一支檔 → 功能 14。
  > **空 tuple 是一個合法的枚舉，不是「沒有枚舉」**：判斷要用 `is not None` 而非 truthiness——寫成 `if allowed:` 的話那支檔會被算進 `fm_only`、stray 檢查整個跳過，**而輸出看起來完全正常**（實作時實測到，見 `validate.py` 該行註解）。
- `decision-lint --book <書>`——驗**裁決軸與待裁決軸**（`tools/decision_projection`）：裁決流的七欄與欄序、日期、狀態枚舉、**射程寫法**、標的存在性、`裁決` 欄 ≤200 字（「一項裁決一列」）、`已升為通則` 的理由欄指向的檔存在；待裁決的**恰四欄（不得有狀態欄）**、日期、標的存在性、`發現` ≤200 字、以及「有待裁決列卻沒有裁決流」。由七支 append 型 skill 落檔後跑。
  > 裁決流曾是「宣告七欄固定格式、三種射程寫法、三種狀態值，而**零 lint**」的檔，同時 `共同約定.md` 宣稱它「嚴格（有檢查器）」——那是口頭承諾。它也是 `.co.md` 這個字位得以廢除的條件（`設計原則.md` A4）。
- `fact-lint --book <書>`——驗**事實信封行與物件檔**（`tools/fact_projection`）：格式、落點、集合維度的操作串與命題名、delta 純化三條（內容欄字數／括號佔比／夾帶語彙）、事實行的幕號是否落在該章 `對應幕` 區間內、以及物件檔的檔名·型別·節·約束表·揭示層級。`fact-project` 執行前會自己先跑一次。
- `object-lint --book <書>`——**物件軸的聚焦入口**，跑的是 `fact-lint` 的同一組檢查、只印物件檔那幾類（給只動物件檔的 `character`／`worldbuild` 用）。要一次看全部就跑 `fact-lint`。
- `world-lint --book <書>`——驗**世界觀軸**（`tools/derived_sync`，與 `validate` 同套件：`validate` 管所有 `.ai.md` 共通的結構，這支管世界觀專屬的**欄語意**）：① `<主題>.ai.md` front-matter 三鍵齊全且 `主題` ≡ 檔名；② **不得再有 `影響力`／`主從`／`維度`／`升格哨兵`／`🧊水下`**（2026-07-27 功能 05 刪除，見下）；③ `伏筆` 每個名字命中既有 registry（幕綱 `埋|收[[伏筆:x]]` 標記 ∪ `story/物件/<名>.md` 檔名）；④ `_總覽.ai.md` 核心規則索引的 `主題`／`檔` 兩欄 ≡ 資料夾裡的源檔（雙向）；⑤ 背景維度盤點 ≡ 封閉七維；⑥ **幕綱裡的 `<名>.ai.md` 檔名引用不懸空**（拆主題時舊引用漏改的守衛）。由 `worldbuild`／`develop` 落檔後跑。
  > 那四個欄曾**寫成機讀格式而零解析器、零 lint、零消費者**——`升格哨兵` 的註解甚至寫著「供 beat-test/sync 盯」，而那兩支 skill 全文零次提及它。實測三處漂移（旗子與自己的彙總節相反／七維少宣告一維／8 個伏筆名 2 個幕綱查無），全部「壞了永遠不會發現」。依 A4 刪欄而非補 lint（已駁回補 lint，見 `docs/重構/02-待用構想.md`）。**留下來的 `伏筆` 欄不是第三份名冊，是對 01／02 registry 的引用**，第 ③ 項就是那個歸一的閘門（提示不是門檻——沒有物件檔的 ID 是合法的）。
  > **第 ④／⑤ 項只驗機械可驗的部分**：`主從`／`影響力`／`帶升格哨兵` 三欄與背景維度的 `內容`／`狀態` 在四欄刪掉之後**沒有機械來源**（它們是 AI 重生時的判斷），依 E1 **schema 明說不宣稱它們與任何檔一致**——驗不到的就不要宣稱。
- `char-lint --book <書>`——驗**角色軸**（`tools/derived_sync`，與 `validate`／`world-lint` 同套件同理由）：① `<名>.ai.md` 的 `需求四象限`／`預期弧線` 存在且**不是佔位**；② **每支源檔（`<名>.md` 或目錄形態 `<名>/`）都有對應的 `<名>.ai.md`——從源側掃**；③ front-matter **不得再有 `角色`／`弧線類型`／`影響力`／`🧊水下`**（2026-07-27 功能 06 刪除），且 `定位`·`暫定` 是枚舉、`所屬arc` 可解析；④ `伏筆` 每個名字命中既有 registry（幕綱標記 ∪ 物件檔）；⑤ `_index.ai.md` 的角色清單 ≡ 資料夾（雙向，**列名 ≡ 檔名、不得加括號別名**）且 `定位`／`所屬arc`／`暫定` 三欄 ≡ 各檔 front-matter；⑥ `一行需求` 不含 `🧊`／`【表面】`／章號；⑦ **`<名>.md` 與 `<名>/` 不得並存**；⑧ 幕綱角色欄裡 registry 查無且出現 ≥3 次的 token（集合名／別名，提示不擋路）。由 `character`／`develop`／`organize`／`expand` 落檔後跑。
  > **第 ② 項是 `設計原則.md` E1 新推論的實作**（「凡宣稱『每一支 X 都有一支 Y』就要從 X 側掃」）：`derived-sync check` 從 `rglob("*.ai.md")` 出發，「源有衍生無」**在定義上掃不到**，而系統的代償是手工建 13 支空殼衍生檔去餵它。**空殼檔是症狀，掃描方向才是病。**
- `style-lint --book <書>`——驗**風格軸**（`tools/derived_sync`，與 `validate`／`world-lint`／`char-lint` 同套件同理由）：① `風格.ai.md` front-matter 七鍵齊全（`generated-*`＋五個語意欄），空值也算缺；② `禁用詞表` 的 entry **不得含括號**（含括號＝一類詞的描述，不是可字面掃的詞）；③ `稱謂系統` 是 `{ 分組: [詞…] }` 的形狀（**分組名自由**），`禁` 組的 entry 同 ②；④ **`可用句式` 三小條**——顆粒度 token（`分句`／`句`／`段`）出現在**值的開頭或 `碎在` 之後**、值裡有**阿拉伯數字**、**不得所有檔位都落在最粗的一級**（未宣告的檔位一律算最粗一級）；⑤ `語域` 非空且不得有半形括號；⑥ **`基調參照` ≡ `00-摘要.ai.md` 的 `基調`**（跨產物比對）。另**順帶掃正文的字面禁用詞**，只印覆蓋率與候選、**不做 pass/fail、不計入問題數**。由 `develop` 落檔前跑（**第 ④ 項有命中就停下回問作者、不得封章**）、`write-test` 開頭跑（取禁用詞掃描結果）。
  > **這一軸的病與 05／06 不同，所以處置也不同**：那兩軸的宣告欄是「零解析器＋**零消費者**」故依 A4 刪欄；風格五欄的**消費者存在且指得出行號**（`write`／`write-test`／`revise` 逐欄指名），只是消費者是 **LLM 不是程式**——`設計原則.md` A4 2026-07-27 據此改寫（位元改成「有沒有人逐欄取值，程式或 LLM 都算」，並補上「**LLM 取值者比程式取值者更需要 lint**：程式讀壞格式會當場炸，LLM 會安靜地讀錯，而錯的那一格會直接進正文」）。實測：`可用句式` 4/4 檔位是讀感形容詞、0/4 宣告顆粒度、0/4 給數字，而它已封章、`check` 報 fresh、五輪重生零守衛。
  > **判準一律是結構或位置判準，不得用詞表**——「這是不是讀感形容詞」用詞表判，是 `prose-metrics` 關鍵詞分類／世界觀主題別名表／幕綱集合名對照表**三次被駁回的同一個形狀**。第 ④ 項要求 token 出現在正規位置就是為此：實作時實測，只驗「值裡有沒有『句』字」會把 `短促碎句動詞驅動` 判成「已宣告句層」——**閘門對著它誕生要抓的那一格報乾淨**。
- `summary-lint --book <書>`——驗**摘要軸**（`tools/derived_sync`，與 `validate`／`world-lint`／`char-lint`／`style-lint` 同套件同理由）：① **源檔在而 `00-摘要.ai.md` 不在**（從源側掃，`check` 在定義上看不到這一格）；② front-matter **八鍵齊全且非空**（`generated-*` ＋六個語意欄）；③ **不得再有 `基調主從`／`終局`／`節奏檔位`**（2026-07-27 功能 08 刪除，見下）；④ **`視角結構.人稱` ∈ 系統支援的封閉二值枚舉**（結構判準：以 `第三人稱` 開頭 ＋ 含 `有限` 或 `全知`）；⑤ `POV` 抽得出來（`beat_metrics.load_pov` 的**同一把尺**）；⑥ **`基調` ≡ 源 `00-摘要.md` 基調節首句**（正規化後比字面：剝粗體／括號註記／尾端標點）；⑦ front-matter 值裡的 `幕NNN`／`arcNN` 引用不懸空；⑧ **基調宣告寫得出單位 token ＋阿拉伯數字**（要落在同一行；**摘要基調節或源 `風格.md` 兩邊至少一邊有**）。另印兩個**只印、不擋**的源檔提示（駁回語彙處數／「臨場拍板」類節帶 `已定案`·`已鎖定`·`已寫定` 字樣的條數）。由 `develop`／`organize` 落檔後跑——**第 ④ 項或第 ⑧ 項有命中就停下回問作者、不得封章**。
  > **第 ④ 項是「規格寫對了、閘門沒接上」的第二個實例**（07 的 `可用句式` 是第一個）：`摘要.schema.md` 早就有那張封閉二值表 ＋「填了非支援值，停下回問作者、**不得直接封章**」，而在 2026-07-27 之前那是純自律——唯一存在的那本書填 `第一人稱貼身`、順利封章、寫了 93 章，事實鏈是 `raw/` 一句形容詞 →`develop` 原樣抄 → 成為 `write` 的 POV 權威，**全程沒有一處問過**。整套系統的視角規則都是照第三人稱有限視角寫的：**閘門是小改動，支援第一人稱是讓每一層各自長出第二套判準**。回問時要把這件事講給作者聽，**不要只說「不支援」**。
  > **第 ⑥ 項補的是本輪唯一一條有真實傷害路徑的縫**：基調字串實測住在四個地方，而守衛只覆蓋 `.ai.md` ↔ `.ai.md` 那一條邊（`style-lint` 第 6 項）。`風格.md` 那一份與 `00-摘要.md` **正規化後逐字相同、零守衛**，而 07 把 `write`／`write-test`／`revise` 改成「源散文＋衍生五欄兩邊都讀」之後，**它是會被直接讀進落筆 context 的字**。抉擇 4 A 因此把 `風格.md` 那一份改成指標（**四份砍到三份**），剩下兩條邊各有一支 lint。`風格.md` 還帶不帶複本由覆蓋率行印。
  > **判準一律是結構或位置判準，不得用詞表**——第 ④ 項問「開頭是不是 `第三人稱`」、第 ⑧ 項問「單位 token 與阿拉伯數字在不在同一行」。**整檔比對沒有意義**（任何散文檔裡都有日期與 arc 編號，那會讓每本書無條件通過）；**中文數字不算**（認了的話「短句為主、一律放慢」也會過關，同 `style-lint` 第 4 項的紀律），所以達標線的範例句一律寫「每章至少 **1** 次…」。
  > **刪掉的三個欄**：`基調主從`（零取值者，內容本來就在源基調節的 `（主）`／`（從）` 裡）／`終局`（零取值者 **＋ schema 從沒定義過它**——自生欄、534 字元，而 `validate` 只驗缺不驗多 → 功能 14）／`節奏檔位`（**權威已外移**到 `story/幕綱/arcNN.md` 檔頭，這一份實測覆蓋 1/11 arc ＝ **9.1%**，值合法、有消費者、**而不可用**）。已駁回「為它們補 lint」（第三次同形狀，見 `docs/重構/02-待用構想.md`）。
- `readiness-lint --book <書>`——驗**就緒軸**（`tools/derived_sync`，與 `validate` 及其他四支軸 lint 同套件）。它與那四支不同的是**它守的是一支源檔**（`story/參照/就緒.md`，A1）：① **源檔存在**（從源側掃，`check` 從 `rglob("*.ai.md")` 出發看不到這一格）；② **恰兩節、不得有第三節**（第三節就是日誌回來的入口）；③ 狀態表**恰五欄、欄名 ≡ `產物軸`／`粗`／`大綱`／`幕綱`／`正文`、無備註欄**；④ **恰五列 ≡ 五個產物軸**（雙向）；⑤ **每格 ∈ `空白`／`草擬`／`就緒`／`已鎖定`／`—` 五 token 封閉枚舉**；⑥ 就緒清單恰六條、checkbox 形狀、條目 ≡ schema；⑦ **舊 `就緒儀表.{md,ai.md}` 仍在就報**（`設計原則.md` A5），且若同時沒有 `裁決流.md` 一併報目的地不存在。由八支寫者 skill（`develop`／`organize`／`outline-test`／`beat-test`／`sync`／`character`／`expand`／`worldbuild`）落檔後跑。
  > **第 ⑤ 項是「門檻量錯單位」的解藥**：舊儀表 20 個狀態格中 **7 格違反四值枚舉、最長 3,647 字元**，而 `long_lines` 一筆都沒報——它量的是**行**，病在**格**（832 字元的那一行含 107／92 字元的違枚舉格）。**已駁回「留一個 ≤N 字的備註欄」**：分佈 1 → 3,647 連續無空隙，任何長度門檻都是任意值（同 06 抉擇 5 A）。正解是病徵本身的機械特徵——**格值不是五個 token 之一**。
  > **`readiness` 是它的投影對半**（見上面查詢入口表）：這一支管「作者填的那兩筆形狀對不對」，那一支管「機械現況是什麼、與作者判斷合不合」。
- `beat-lint --book <書>`——驗**幕綱**（`tools/beat_metrics`）：幕號唯一與預配號段、**`前因 [[幕NNN]]` 的目標存在**、八欄齊全、`幕綱/_index.md` 的 `全書順序：` 可解析且涵蓋所有 arc、每支 arc 有 `## 本 arc 承諾`、伏筆近似名、設計註的搬移目的地存在、**檔頭 `beat-test:` 宣告行的形狀**（2026-07-28 功能 10 補；**有寫才驗、缺席合法不計入問題數**）。由 `beat-sheet` 落檔後與 `beat-test` 開頭跑。
  > 幕綱曾是全系統唯一「被四支工具逐行解析、卻零檢查器」的強格式產物（2026-07-27 功能 02 補上）。它守的是**人破結構**那一側（`設計原則.md` B1，同 `derived-sync validate`）；`fact-lint` 的純化三條守的是另一側（AI 破格的克制）。**內容好壞不歸它**：因果、伏筆品質、可演性交 `beat-test`，漂移統計交 `beat-metrics`。
- `outline-lint --book <書>`——驗**大綱層**（`tools/beat_metrics`，與 `beat-lint`／`ch-lint` 同套件：**第 4 項要的幕號 registry 就在 `structure.parse_book()`、第 5 項要的伏筆 regex `beat-lint` 已經在用，另開套件＝新增第 7 份幕號集合實作**）：① 節枚舉（全書版／scoped 各一組封閉枚舉，**一支檔一行**）；② 檔頭狀態恰一個（`暫定` ／`已併入`）且與它住的目錄一致；③ `大綱/_index.md` 視圖 ≡ 資料夾（雙向，含 `_已併入/`）＋列序遞增；④ **`幕NNN` 對幕綱 registry、`chNNNN` 對 `chapters/` 不懸空**；⑤ `[[伏筆:x]]` 命中既有 registry（幕綱標記 ∪ 物件檔名，**提示不是門檻**）；⑥ **`結局：` 非空且不含 `待定`／`未定`／`大概`、`題旨：` 是「因為…導致」句型**；⑦ `本段收束與鉤子` 三格齊全；⑧ **同一個 arc 不得兩邊都有內容**（並存本身是常態，見 `大綱.schema.md`「收斂規則」）；⑨ 檔內指名的書內路徑存在；⑩ **`本 arc 伏筆狀態` 恰三欄且不得出現 `幕NNN`／`chNNNN`**；⑪ **`卷級方向（卷X·本卷權威）` 的卷 token 唯一、`_已併入/` 不得有、同卷其餘檔只准指路**。由 `organize`／`expand` 落檔後跑、`outline-test` 開頭跑。
  > 大綱曾是**這份文件自己列進「強格式源·一支 lint」那一格、而那支 lint 不存在**的產物（2026-07-28 功能 09 補上）——**同一張表上的同一個假宣稱，04 抓過 `.co.md` 一次，換了主詞活到 09**。實測代價是一條乾淨的曲線：`本段全文` 佔全檔 **54.0% → 11.1%**，同期 schema 枚舉外的節 **0% → 64%**，11 個 arc、5 天、零警報。**它的覆蓋率行第 2 行是本輪量到的最強訊號**（連續敘述佔全檔的百分比＝「這一軸還在不在做它該做的事」的單一數字）。
  > **⑥ 是「規格寫對了、閘門沒接上」的第三個實例**（前兩個：07 的 `可用句式`、08 的人稱）：`outline-test/SKILL.md` 測試 1／2 明文寫著「檢查欄位非空（**機械**）」「**正則檢查句型**是否含『因為…導致』（機械）」，而執行者一直是 LLM。
  > **裸 `arcNN` token 刻意沒有守衛**：尚未下沉的未來 arc／規劃中的中間段／全書模式直接寫的卷，三種合法狀態與打錯字在檔案系統上長得一模一樣——**依 E1，驗不到就不宣稱**；寫成路徑的那一半由 ⑨ 守。覆蓋率行誠實印「N 筆指得到既有 arc 檔·**裸 token 無守衛**」。
- `ch-lint --book <書>`——驗**正文層**（`tools/beat_metrics`，與 `beat-lint` 同套件）：`<!-- 幕NNN -->` 錨點的幕號存在於幕綱、章內單調遞增、一幕不跨章、過渡錨點 `幕A→幕B` 兩端存在且遞增、`--- 幕NNN ---` 人性寫法的正規化候選（**報，不代改**）、**`對應幕` ≡ 正文錨點集合**、章衍生檔 front-matter 六鍵齊全（`狀態` 枚舉、`POV` 三子欄、`對應幕` 寫法）、`chapters/_index.ai.md` 六欄 ≡ 各章 front-matter、備註欄不得含伏筆標記且 ≤100 字、**`風格` 欄指向的檔存在**（2026-07-27 功能 07 補，聚合成一行）、**`write-test` 欄的形狀**（2026-07-28 功能 10 補；**有寫才驗、缺席合法不計入問題數**）。由 `write`／`revise` 落檔後與 `write-test` 開頭跑。
  > `<!-- 幕NNN -->` 曾是全 repo 唯一「有機讀標記、零解析器」的地方（2026-07-27 功能 03 補上）：`章節.schema.md` 對它下了五條格式承諾，而唯一碰到它的程式（`prose_metrics`）作用是**把它剝掉當雜訊**。`write-test` 測試1 更明文寫著「這是最高優先級的**機械事實，不需 LLM 判斷**」——而執行者一直是 LLM。**與 `beat-lint` 分成兩個指令是刻意的**：解析層只有一份（幕號 registry 在 `structure.parse_book`），但 `write`／`revise` 要驗的是正文層，不該同時吃到幕綱那邊與它無關的問題（同 `object-lint` 是 `fact-lint` 聚焦入口的先例）。

> **這幾支一律先印「我檢查了幾筆」**（`設計原則.md` E2）：幾支章 delta／幾筆事實行（新舊格式各幾筆）／幾支物件檔／幾條約束／幾個揭示層級；幕綱那支印幾支 arc／幾幕／幾條前因／幾個伏筆標記／幾列狀態表（**其中幾列的伏筆名全書無標記·不入機械 diff**）／幾條排除線／**幾支 arc 有 `beat-test` 紀錄（幾支格式不合）**；正文那支印幾章正文源／**幾個幕錨點**／幾個過渡錨點／幾支章衍生／幾列章序，以及比對了幾章的對應幕、幾個幕號對不到 registry、幾列章序不一致、**幾個 `風格` 欄指向的檔（幾個不存在）**、幾筆正規化候選、**幾支章衍生有 `write-test` 紀錄（幾支格式不合）**；`derived-sync validate` 印幾支 `.ai.md`（**套枚舉的與只驗 front-matter 的分開數**）／幾個 `##` 節／幾支檔有裁決 blockquote；`world-lint` 印幾支主題檔（**幾支仍帶已廢除的欄**）／幾個伏筆名（**幾個 registry 查無**）／registry 兩側各幾筆／索引幾列 vs 資料夾幾支／背景維度幾列／幕綱檔名引用幾處（**幾個懸空**）；`char-lint` 印幾支源（幾支目錄形態·幾支並存）／幾支衍生（**幾支源沒有衍生檔**·幾支骨架）／幾支帶已廢除的欄·共幾個／四節幾個 slot（**幾個是佔位**）與必填兩節幾個（**幾個是佔位**·幾節整節是佔位）／幾個伏筆名（**幾個 registry 查無**）／rollup 幾列 vs 資料夾幾支／幕綱角色欄幾個·幾個 token（**幾個查無·其中幾個 ≥3 次**）；`style-lint` 印幾支風格衍生檔（幾支骨架）／幾個語意欄（**幾個是空的**）／`可用句式` 幾個檔位（**幾個無顆粒度 token·幾個無數字**）／`禁用詞表` 幾個 entry（**幾個含括號·不可字面掃**）／稱謂系統幾組幾個詞／`基調參照` 比對狀態（相符·不符·**未比對**）／**掃了幾章正文·幾個字面 entry·命中幾次**；`summary-lint` 印幾支摘要源／幾支衍生（**幾支源沒有衍生檔**·幾支骨架）／幾個語意欄（**幾個是空的**）·幾個已廢除的欄／**人稱狀態**（合法·非支援值·**未比對**）／**基調兩方比對狀態**（相符·不符·**未比對**）／**`風格.md` 還帶不帶基調複本**／**達標線落在哪一側**（摘要·風格·**兩邊都沒有**）／幾個幕號·arc 引用（**幾個懸空**）·registry 兩側各幾筆／**幾處駁回語彙·「臨場拍板」幾條（其中幾條帶已定案字樣）**；`outline-lint` 印幾支大綱檔（全書版·scoped·`_已併入/` 分開數·幾支骨架）／**連續敘述佔全檔百分比與最低的那一支**／枚舉外節幾個（最大一節幾字元·在哪）／幕引用·章引用各幾筆（**幾個懸空**）·arc 引用幾筆（幾筆指得到既有 arc 檔·**裸 token 無守衛**）／伏筆幾處幾名（**幾個 registry 查無**）／卷級方向幾節幾個卷 token／索引幾列 vs 資料夾幾支（**幾筆不一致·列序非遞增幾處**）／檔內路徑引用幾個（**幾個不存在**）／`01-大綱.md` 涵蓋幾個 arc·未併入的 scoped 幾支（**兩邊都有內容幾支**）／**`01-大綱.md` 還帶不帶結局複本**；`settings-select` 印掃了幾幕／角色命中幾筆（**其中幾筆是空殼**）／世界觀命中幾筆（**其中幾筆僅因檔名被引用**）；裁決那兩支印**掃描了幾列裁決／命中幾列／待裁決幾列／命中幾列／標的幾個（幾個找不到）**；`readiness-lint` 印**源檔在不在**／幾個 `##` 節／狀態表幾格（**幾格不是五 token 之一**）／清單幾條（幾條已勾）／**舊 `就緒儀表` 在不在**／**`裁決流.md` 在不在**；`readiness` 印**源檔在不在**／20 格中**幾格有機械前哨·幾格未接·幾格無機械來源·幾格不適用**／**幾格作者判斷與前哨不一致**／清單六條中**幾條完整·幾條部分·幾條未接**／掃了幾個 arc。**乾淨的時候也印、0 也印。** 只回答「發現幾個問題」的檢查器，在它自己被關掉時會印「乾淨」——實測就是這樣讓 206 個問題報 0 的，而 `decision-project` 對空表與無命中的輸出曾**逐字相同**。
> **`ch-lint` 最需要這一行的是「幾個幕錨點」**：在它上線之前那 108 個錨點被 **0 支工具**讀過，而 schema 對它們下了五條承諾。剛寫完一章而它印 0，那不是乾淨，是它沒讀到你的檔。

改了上游事實之後，另有 `fact-refs --book <書> {--entity <名> [--after 幕NNN（arcAA）] | --anchor 〔名〕 | --constraint <約束名>}`——**反向**列出誰依賴了這條事實。整條事實軸原本只有正向（事實→下游），`derived-sync` 看不到「下游正文已經按舊事實寫下去了」這一半。`--constraint` 是給「改／解除一條約束」用的：約束不會自癒（它只被讀、不被再寫），所以動它之前得先看射程內已經寫成哪些章。

分屬不同工具是刻意的：各自擁有自己那份格式的唯一真相，不建立跨工具相依。

另有 `derived-sync check --book <書>`：除了衍生檔新鮮度，會附帶印出**成長哨兵**。哨兵量的是**「必須整檔讀的東西有多大」**（第零節），不是「檔多大」——四項：幕綱每幕位元組、源檔（單檔或目錄總和）上限、**衍生檔不可切片**（超過門檻且含 schema 封閉枚舉外的節＝違反第零節資格條款）、單行長度（狀態格被當日誌用；門檻分四級——綜合檔 2,000／rollup 400／**設定層非 rollup 衍生 800／設定層源 600**）。**後兩項的目標集 2026-07-27 補了兩次**：功能 03 補上 `chapters/`（在那之前 50,782 B 的 `chapters/_index.ai.md` 與 2,235 字元的單行完全靜音，而 `check` 印「0 個需處理」）；**功能 05 補上設定層非 rollup 的衍生檔與源檔**（在那之前 1,155 字元的單行也是靜音的）。**功能 08 補上 `story/00-摘要.{md,ai.md}`**（在那之前**四支目標集 0/4 含它**：源 15,656 B ＝門檻的 62.6%、衍生 10,530 B ＝門檻的 **87.8%**、源最長行 594、衍生最長行 626，全部靜音——而摘要的成長軸是**裁決輪次**，源 10.6×／衍生 15.5×）。**功能 09 補上 `story/01-大綱.md` 與 `story/大綱/`**（在那之前**四支目標集 0/4 含整個大綱軸**：6/12 支超過 25,000 B（最大 44,307＝門檻的 **1.77×**）、7 支有超過 600 字元的單行（最長 1,324）全部靜音，而同一次 `check` 卻報了 32,650 B 的 `血刀頭陀.md`）。**功能 10 補上 `story/參照/` 底下非 append-log 的檔**（在那之前**三支體積哨兵目標集 0/3 含它**：`就緒儀表.md` **292,591 B ＝門檻的 11.7×、全書最大的一支檔、佔全書 11.3%**，`結構.md` 58,455 B，全部靜音——而 `long_lines` 早就在掃同一個資料夾的**行**；門檻沿用 `SOURCE_BYTES`，不新造第五級）。**五次都不是刻意豁免，是各支函式各自手寫路徑清單時漏了目標**——這已經是第八個同根因的案例（前幾次漏的是資料夾或 `story/` 根目錄下的兩支檔，**這次漏的是一整個產物軸**，而且是 repo 裡體積第三大的軸 302,669 B），**根因不在功能 09 修**，要不要有一份統一的「這本書有哪些受管檔」交功能 14。
> **大綱層的門檻也全部沿用現成值，不新造**：大小 `SOURCE_BYTES`（實測 11 支 arcNN 的分佈 21,836 → 33,468 之間有空隙，25,000 落在裡面）／行長沿用設定層源那一級 **600**（最長行分佈的空隙在 409 → 723）／`大綱/_index.md` 套 `ROLLUP_LINE_CHARS` **400**。樣本 **n=11**（比設定層那兩個門檻的 n=4 好，仍是單書樣本）。**`story/大綱/_已併入/` 底下的退役源不受管**（`設計原則.md` A5：它已經不是權威、也不該再長，報它只是雜訊）。
> **摘要兩支檔沿用設定層的行長門檻**（衍生 800／源 600），**不新造第五級**：衍生是分析段落、源是自由散文，性質與設定層那兩類相同；而 `n=1` 的樣本不足以另立門檻（06 抉擇 5 A 已駁回「沒有空隙時硬定數字」）。實測 626／594 **都只差門檻一點點**——那是要它們進目標集的理由，不是放行的理由。哨兵是 advisory——**不計入需處理數、不影響 exit code**，但它是「檔案形態開始走樣」唯一的自動訊號，別忽略它。

### 不受此限的

- **人管·源**（`raw/`、設定層 `<實體>.md` 或 `<實體>/<切面>.md`、`00-摘要.md`、`chapters/chNNNN.md`）：系統用「一實體一檔／一實體一目錄、檔名即選擇器」控制單檔大小，整檔讀是安全的。**代價是單一源檔不該無限長**——超過門檻時**先搬後拆**：先檢查有沒有寄居內容（共創逐題問答與拍板紀錄 → `story/參照/裁決流.md`；正文釘死的事實 → 該章 `## 本章事實`；硬約束 → `story/物件/<名>.md`），搬完仍超標才**升級成目錄形態**（角色見 `角色.schema.md` 的五切面；世界觀則拆主題）。不要改成「只讀一部分」，那會把自由格式的源檔逼成有 schema 的東西。
  > **順序不能顛倒**（2026-07-27 功能 06 抉擇 4 B）：實測 `血刀頭陀.md` 32,650 B 觸頂，而 **61.2% 是寄居內容**（逐題問答 35.9%＋帶日期的拍板紀錄節），搬走後剩 5,164 字元、根本不必拆。**先拆等於把要搬走的東西分裝到五個切面檔。** 成長哨兵的單檔 hint 現在就是先問這件事。
- 該 arc 自己的幕綱：本來就是單檔且有界。
  > **~~風格檔、摘要~~ 這兩項 2026-07-27 從本清單移除**——**「不隨書長成長」不等於「有界」**，而它們兩支都被實測推翻了：
  > - `風格.ai.md`（功能 07）：五次重生從 611 B 長到 **12,247 B（20.04×）**、越過 `DERIVED_BYTES=12000`。處置是**廢除衍生檔的本體**（那四節實測是源檔的同長度改寫，1.02×）：衍生檔只留 front-matter 五欄約 1 KB，腔調散文的唯一落點是源 `風格.md`，下游**兩支都讀**。
  > - **摘要（功能 08）**：源 1,483 → **15,656 B（10.6×）**、衍生 679 → **10,530 B（15.5×，＝門檻的 87.8%）**，**而四支成長哨兵目標集 0/4 含它**——本條無條件豁免的字面正是它靜音的理由之一。成長軸是**裁決輪次**（三次跳全在同一天的 `sync→develop` 收斂輪），**它沒有 rollup、沒有 arc 分檔、沒有任何「因為書變長所以它變長」的機制**——這是這個成長軸最乾淨的一份證據。處置是**把兩支檔補進哨兵目標集**（源 25,000／衍生 12,000／行長 600·800）＋在 `摘要.schema.md` 加兩條**帶觸發者**的搬移條文（裁決理由與已駁回 → `story/參照/裁決流.md`；「臨場拍板」已定案的歸位、仍未定的 → `raw/`）——實測源檔 51.4% 是前者、16.7% 是後者。
  >
  > 這個成長軸與 05 的 `_總覽.ai.md`、06 的角色衍生檔是**同一個軸的四個實例** → 功能 14（**D2 若重新表述成本函數，這是第四票**）。

### 為什麼是工具而不是規勸

每個測試 skill 都已宣告「**機械可抽取部分**（確定性）vs **LLM 判語意部分**」。長期以來「機械」那一半是**靠 LLM 讀整份檔案**執行的——既貴又會漏。凡確定性的工作（掃標記、配對、計數、選檔、算 as-of、統計）一律進程式，LLM 只做判斷。

## 九、宣告型約束（書級宣告要能被核對）

書級宣告——**基調**、**取向定位**、**節奏檔位**（住 `story/幕綱/arcNN.md` 檔頭，2026-07-27 起不在摘要）、各種**配額**——共有一個失效模式：宣告寫下來了，然後沒有任何一層對它負責，成品與宣告脫節而無人察覺（診斷01 P9／P11 就是同一條）。

### 三件事，缺一不成立

一條書級宣告落檔時，必須連帶寫出：

| | 問什麼 | 反例（等於沒宣告） |
|---|---|---|
| **(i) 單位** | 這條在**哪個尺度**上成立：每章／每 arc／全書？ | 沒寫——於是單章跑測試時判不了，逐 arc 跑時也沒人跑 |
| **(ii) 達標線** | 怎樣算做到了？寫成**可數或可指認**的形式 | 「貼身喜劇」「節奏偏快」「資訊密度偏豐富」——讀感形容詞，不可證偽 |
| **(iii) 誰核對** | 哪個 skill、在哪一層、對照什麼判？ | 沒指定——每個 skill 都以為別人管 |

> **寫不出這三件的不是宣告，是願望。** `develop`／`organize` 落檔時發現某條宣告缺這三件，**停下回問作者，不得直接封章**——比照 `風格.schema.md`「`可用句式` 不得所有檔位都碎在同一個最粗的一級」的處置。
> **那條比照對象 2026-07-27（功能 07）才有實作**：`style-lint` 第 4 項（顆粒度 token 的位置／阿拉伯數字／不得全落最粗一級），`develop` 落檔前必跑。在此之前它是純自律，實測某本書 4/4 檔位違反且順利封章、五輪重生零守衛——**「停下回問作者」寫在兩支 schema 加一支 SKILL.md，而沒有任何東西在守它**。依 `設計原則.md` E1 第四推論（同輪新增）：宣稱某欄「供 X 核對」時，要一併定義**什麼樣的值算可核對**且機械可判。

**已經是本節實例的既有規則**（不在此重述，改到那邊看）：`風格.schema.md` 的 `可用句式`「只宣告顆粒度、值要可數」與「不得全碎在同一級就停下回問」（**兩條皆由 `style-lint` 第 4 項實作**，2026-07-27）、`摘要.schema.md` 的「常態段宣告靠長線大謎供能時，`貫穿大懸念` 要可指認」（**`summary-lint` 第 1／6 項實作**：欄非空 ＋ 欄裡的 `幕NNN`／`arcNN` 引用不懸空）、以及**基調宣告本身的三件事**（**`summary-lint` 第 7 項實作**，2026-07-27 功能 08）。

> **基調那一條的達標線寫在哪都算**（抉擇 4／第 7 項）：摘要「基調」節，**或**源 `story/設定/風格/風格.md`——後者是「這個基調在句子層怎麼落」的執行細則，而達標線正是執行細則。`summary-lint` 驗「兩邊至少一邊有」並在覆蓋率行印它落在哪一側，**不逼作者把一句執行細則複製回摘要**（那正是本輪在砍的複本）。
> **達標線的範例句一律寫阿拉伯數字**：「每章至少 **1** 次讓讀者鼻子出一口氣」。判準不認中文數字——認了的話「短句為主、一律放慢」也會過關（同 `style-lint` 第 4 項的紀律）。

### 「可核對」不等於「機械可測」

達標線要可核對，但**不強求有機械量**。有些宣告實測就是量不出來——這時 (iii) 指定的核對層是 **LLM 逐 arc 判**，而不是放棄核對。

**基調就是這一類**（2026-07-25 實測）：拿一份喜劇簽名詞表掃一世之尊逐 arc，arc10 掉到 0.00 次/千字、arc11 又彈回 1.02，arc03/06/07/09 全在中段——**任何門檻都會過度回報**。故基調**不設機械判達標的門檻欄**，核對走 `write-test` 測試5 的**逐 arc、相對本書前段**語意判斷（判準紀律同 `prose-metrics` 漂移：拿這本書自己的前段當尺）。

> **兩種斷言的分界（2026-07-27 功能 08 抉擇 8 A 補；2026-07-22 那四個理由一個字都不改）**——本條的射程是**前者**，不是後者：
>
> | 斷言 | 合不合法 | 誰在做 |
> |---|---|---|
> | 拿詞表／門檻判基調**達標** | ❌ **已駁回**（2026-07-22 作者裁決＋2026-07-25 實測，四理由不變） | 無人。核對層是 `write-test` 測試5 的語意判斷 |
> | 比對兩份抄本是否**一致** | ✅ 合法 | `style-lint` 第 6 項（衍生 `基調參照` ≡ `00-摘要.ai.md` 的 `基調`）、`summary-lint` 第 5 項（衍生 `基調` ≡ 源 `00-摘要.md` 基調節首句） |
> | 檢查達標線**寫了沒**（單位 token ＋阿拉伯數字在同一行） | ✅ 合法 | `summary-lint` 第 7 項——它驗的是**宣告的形狀**，不是成品達不達標 |
>
> **為什麼要寫這一段**：本節原文寫的是「故基調**不設機讀欄、不進 front-matter**」，而 `摘要.schema.md` 定義了 `基調` 欄、`style-lint` 第 6 項（07 新增）就建在它上面——**規則的字面會讓下一個 session 依它刪掉一個有程式取值者的欄**。實質從來沒有衝突：被駁回的是「拿門檻判基調**達標**」，兩支 lint 做的是「兩份抄本是否**一致**」，那是完全不同的斷言。
> 這條路徑是 2026-07-22 作者裁決（否決「加基調機讀欄」，四理由存檔）指定的：「**未來要機械化的正確順序＝先驗證、再泛化**……動的也應是 schema 的『檢查點』清單加一行文字，而非加機讀欄。」本節就是那個泛化。**不要再提「幫基調加一個供詞表掃描判達標的 front-matter 欄」**——已測過、不成立。

### 與「地板／天花板／配額」的關係

`技巧知識庫/_index.md`「技法的三種效力」分的是**技法**的效力；本節管的是**書級宣告**怎麼寫才能被核對。配額型技法的密度上限是跨書恆定的（歸知識庫）；配額型**宣告**（這本書的某個東西每 arc 至多幾次）才歸本節。判斷歸誰，問 `風格.schema.md` 那句：**換一本書，這條會不會不一樣？**
