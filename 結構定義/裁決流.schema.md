# 結構定義：裁決流（`story/參照/裁決流.md`）

> **這是什麼**：定義「**為什麼這樣定**」怎麼記。作者拍板的每一項承重決定——判例、護欄、降回閥、母題論證、本輪拍板記錄——的**單一落點**。append-only 表格，**永不人工回收**：過期的由程式按射程過濾掉，不必有人負責刪。
> **方法依據**：`結構定義/共同約定.md`（第零節兩類檔案）、`結構定義/回饋記錄.schema.md`（待裁決回饋結案後流向本檔）、`docs/superpowers/specs/2026-07-26-設定層事實與裁決分軸-design.md`。
> **所有權**：**AI管·append log**（見 `共同約定.md` 二）——由各 skill 在作者拍板後**只增不重生**地追加；作者可審可改，投影程式（`tools/decision_projection`）唯讀。

---

## 為什麼需要這條軸

裁決理由屬「**只由AI與程式產生**」（`共同約定.md` 零）——它從來不是作者手寫的，是每輪 skill 執行完自己寫下的；作者在對話裡拍板，落檔的是 AI。它的欄位天然固定。

在本檔存在之前，它無處可去，只能寄生在三個「必須整檔讀」的地方：

- 設定層 `.ai.md` 檔末的自由 blockquote（一檔動輒 2–3k 字，且因為是拍板紀錄，重生時沒人敢刪 → 永久累積）；
- 幕綱檔尾的「設計註」（`幕綱.schema.md`）；
- `就緒儀表.ai.md`／`結構.ai.md` 的表格 cell（實測最長單一 cell 逾 10,000 字）。

> **本檔取代的是「回收」這個動作本身。** 設計理由之所以曾經只能靠人工刪，是因為它住在自由 prose 檔裡、沒有第二條路。移進格式固定、程式可切片的軸之後，**它永遠不必刪**——`--active-only` 不會把過期的送進 context。

## 檔案配置

- **每本書一檔**：`story/參照/裁決流.md`，append-only。
- 由 `character`／`worldbuild`／`develop`／`organize`／`expand`／`beat-sheet`／`revise` 在**作者拍板後**追加；由 `sync` 與上列各 skill 在**消化前**查詢（避免重新爭論已經定案的事）。
- **`write` 不讀本檔**——它讀的是 `事實流.md` 的錨與約束（結論），不是理由。這是本檔與事實流分兩檔的理由。

## 格式（固定七欄表格）

```markdown
# 裁決流

> 依 `結構定義/裁決流.schema.md`：作者拍板的承重決定，append-only、永不人工回收。
> 查詢走 `tools/decision_projection`。「為什麼」在這裡；「結論」在 `事實流.md` 或對應源檔。

| 日期 | 來源 | 標的 | 裁決 | 理由 | 射程 | 狀態 |
|------|------|------|------|------|------|------|
| 2026-07-22 | write-test 測試9（ch0046） | 設定/角色/少年/核心.md | 年齡由區間收窄成定點 | 專名／數字登記防分裂；年齡是讀者最會記的數字之一 | 全書 | 生效中 |
| 2026-07-23 | beat-sheet arc07 | 幕綱/arc07.md | 本 arc 母題＝付現 | （三段論證留在本列，不進幕的欄位） | 至arc07 | 已過射程 |
```

### 逐欄定義

- **日期**：拍板當天（`YYYY-MM-DD`）。
- **來源**：哪個 skill／哪次測試（同 `回饋記錄.schema.md` 的寫法，例：`write-test 測試9（ch0046）`、`character（血刀深來歷輪）`、`beat-sheet 排 arc03 時`）。
- **標的**：這項裁決管的是哪個檔或哪個範圍，**寫相對 `story/` 的路徑**（`設定/角色/少年/核心.md`、`幕綱/arc07.md`、`00-摘要.md`）。跨檔的通則寫 `全書`。`decision-project --target` 就是比對這一欄。
- **裁決**：一句話講清楚定了什麼。**結論本身若是下游必須守的事實，另外落進 `事實流.md` 的錨／約束**——本欄只留一句摘要，不重抄。
- **理由**：為什麼。這是本檔存在的目的，**可以寫長**（它不會進 `write` 的 context）。
- **射程**：`全書` ／ `至arcNN` ／ `本輪`。判定「已過射程」靠 `story/幕綱/_index.md` 的全書順序。
- **狀態**：
  - `生效中` — 仍在管事。
  - `已過射程` — 射程已過，程式在 `--active-only` 時濾掉；**不刪列**。
  - `已升為通則` — 這條臨時規定後來被寫進 `結構定義/` 或 `技巧知識庫/` 了；**理由欄改成一句指過去**（如「已升格為 `幕綱.schema.md` 八欄只寫故事事實」），不留第二份。

## 規則

- **append-only，永不人工回收。** 不刪列、不改舊列的裁決與理由；狀態欄可以改（`生效中` → `已過射程`／`已升為通則`），那是唯一允許的回頭修改。
- **只記「為什麼」，不記「是什麼」。** 下游必須遵守的結論屬 `事實流.md`（錨／約束）或對應設定源檔；本檔重抄結論會製造副本漂移。
- **一項裁決一列。** 不把一輪 session 的全部決定併成一列長文——那會讓 `--target` 過濾失效。
- **不另立登記簿**：判例、護欄、降回閥、母題論證、本輪拍板記錄全部住這裡，不在 `.ai.md`、幕綱檔尾或狀態表 cell 另開一份。
- **消化前先查**：`character`／`worldbuild`／`develop`／`organize` 精修某源檔前，先跑 `decision-project --target <該源檔>`，避免重新爭論已定案的事、或做出與既有裁決相牴觸的提案。

## 與待裁決回饋的分工

| | `.ai.md` 的「## 待裁決回饋」 | 本檔 |
|---|---|---|
| 住什麼 | **還沒裁決的**提案 | **已經裁決的**決定 |
| 生命週期 | 短——消化即離開 | 長——永久保存 |
| 誰讀 | 消化型 skill（就在它要改的那個 `.ai.md` 裡） | 消化型 skill＋`sync`，經投影查詢 |

流程：下游發現問題 → 在對應 `.ai.md` 追加一列（`回饋記錄.schema.md`）→ `character`／`worldbuild` 消化、作者拍板 → **結論落源檔或 `事實流.md`，理由落本檔，回饋列刪除**。

> **`.ai.md` 一律不得出現自由 blockquote 形式的裁決日誌。** 那是本檔存在之前的權宜寫法，現在是違規。

## 投影程式契約

- `tools/decision_projection`（Python／uv）：
  `uv run --project <套件根>/tools/decision_projection decision-project --book <書資料夾> [--target 設定/角色/少年/核心.md] [--active-only] [--since YYYY-MM-DD]`
- `--target` 比對「標的」欄：**前綴雙向相符即命中**——給目錄 `設定/角色/少年/` 命中其下所有切面的裁決；給切面 `設定/角色/少年/核心.md` 也命中管整個角色（標的寫目錄）的裁決。標的為 `全書` 的列一律命中。
- `--active-only`：只回 `生效中`。
- **唯讀、零 LLM、可覆算**；壞行（欄數不符、日期格式錯、未知狀態值）報錯不靜默丟。
